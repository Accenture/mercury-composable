# Session (2026-07-29T19:03:28.000Z)

**Agent:** Claude Code

## What happened

**Suspend/resume production-polish round, part 2 — Eric's IDE review + SonarQube scan
PASSED; branch `chore/suspend-resume-production-polish` pushed for PR.** The round grew
far beyond the first two findings (see 2026-07-29-162652 for part 1) through an
interactive review loop: Eric reviewed class by class in the IDE, I ran a four-lens
adversarially-verified review sweep in parallel, and each finding was fixed on the spot.

**Eric's own touch-ups (verified + gated each time):** GraphResume `record`→`restored`
(restricted identifier), per-request `log.info`→`debug` ×2, javadoc language polish,
`HashMap` diamond operator; CompileGraph `hasText`→`withoutText` inversion + javadoc;
GraphSuspend unused import; properties comment "(optional)"→"(required)".

**My sweep findings applied (15 confirmed by adversarial verification, 1 refuted):**
wrong config key in two error messages (GraphExecutor/GraphCommandService said
`location.graph.temp` while validating `location.graph.deployed`); GraphSuspend log-level
consistency + cid; stale CompileGraph javadoc; S1192 `"node "` ×8 → NODE_NAME constant;
S2589 unreachable null-branch in `getNext`; dead `CONTINUE` constant; GraphTraveler
per-run reset missing `hits.clear()` (drift); atomic `putIfAbsent`/`computeIfAbsent` in
both walkers' `walk`/`checkFrequency`; **reserved-key strip on restore** (HIGH: the store
is pluggable so a record is external input — a forged record could overwrite `model.cid`,
a capability; `NON_PERSISTED_MODEL_KEYS` hoisted to GraphStateSkill, stripped before the
merge, forged-record regression test); `recordFailure` always-500 parameter removed
(Eric's IDE hit).

**Eric's four ratified rulings implemented:** (1) a `mapping`/`for_each`/`output` entry
missing `->` rejects the graph at compile time — but **property-aware**: bare `input`
entries are fetcher skill vocabulary (`person_id`, `exception:false`) in 12 shipped
graphs, so they pass silently (the old blanket `log.error` was a false alarm on every
startup, now gone); (2) the suspend node requires an outgoing connection (err6 fixture);
(3) ttl overflow guard — `GraphSuspend.getValidTtlSeconds` mirrors
`Utility.getDurationInSeconds` in long math (int wraps for absurd day counts), shared by
skill + gate; (4) duplicate constants consolidated (`OUTPUT_BODY_NAMESPACE`→`OUTPUT_BODY`,
`INPUT_BODY_NAMESPACE`→`INPUT_BODY`).

**The big architecture decision (Eric's, ratified stepwise; proposed as ADR-0011):
CompileGraph is the MANDATORY deployment gate — CompileFlows parity.** A deployed graph
is executable only when manifest-listed AND gate-passing; failed or unlisted = HTTP-404
as if it does not exist; lazy per-request loading DELETED (the interim rejected-graph
registry was superseded by this simplification — "compiled or 404" is the whole rule).
The manifest carries its own `location` (default `classpath:/graph`, the flows.yaml
convention) replacing `location.graph.deployed` (removed from 4 properties files;
obsolete-key startup warning; GraphCommandService reads the location from
CompiledGraphs, resolved by CompileGraph — @BeforeApplication ordering guarantees it).
Backward compatible via the default; migration note in CHANGELOG.

**Two-lane validation architecture (Eric's framing, verified by a 3-reader inventory):**
production = models → CompileGraph → deployed → GraphExecutor (trusts the gate — dropped
beginTraversal root/end checks and walkToSuspendNode's three guards; keeps data-driven
guards: store-record checks, dynamic jump targets, loop breaker, po.exists); dry-run =
/tmp/graph drafts → UI CLI validation at create/update → GraphTraveler with FULL runtime
validation (untouched — the inventory proved the CLI validates ZERO suspend/resume
semantics, so the traveler guards are that lane's only enforcement). Gate strengthened
first with what the executor used to carry: mandatory `end` node + continuation edge on
every suspension point (err7 + unit-test-no-end fixtures).

**Modularization (Eric's suggestion):** `GraphModelValidator` (common) now owns the
whole-graph suspend/resume contract; CompileGraph delegates; the playground `run` command
invokes it as a pre-run quality check — partial drafts stay allowed at authoring time,
but `run` rejects with "Unable to run - <reason>" + the uniform "Graph traversal aborted"
terminal (drain-safe for the sync companion). New CompanionSyncTest e2e proves it.
This class is the landing pad for [[thread-compilegraph-syntax-validation]] (imported
drafts skip mapping checks today).

**Docs catch-up (Eric's screenshot):** skills-reference + knowledge-graph index still
said "eight skills" — both now say ten with graph.suspend/graph.resume rows; frontmatter
summary/keywords updated. Plus: build-your-first-graph Step 4 (manifest + location),
help tutorial 2, help run.md, ai-developer-guide, architecture.md, annotations-reference,
minigraph-commands.json (run note, invariants), CHANGELOG (Changed #1 = the gate, with
migration note). vision.md current-state skill count refreshed 7→10 (maintenance, not a
Vision change).

**Verification:** every round gated (engine suite ×8 runs, app e2e ×5, grammar check,
mkdocs --strict, full reactor ×4 — final reactor at HEAD green). Engine 88 tests
(net +2 vs main: forged-record, ttl parser, pre-run e2e, validator/location/404 tests
replacing the 4 retired lazy-load runtime-guard e2e tests). SonarQube scan by Eric:
PASSED ("The rest are all good").

**P5 Rust lock-step additions from this round:** the final surface (no `missing`,
`model.run`), mandatory gate + compiled-or-404 + manifest-carried location, executor
streamline vs traveler-keeps-guards, GraphModelValidator + pre-run check, the sweep-class
fixes (atomicity, reserved-key strip on restore, ttl long-math), ten-skill docs.

**Also (post-push follow-up, commit `291c1fa9`): `model.run` reserved as flow metadata
(Eric's ruling — "group it with model.flow/instance/trace/cid/ttl").** Added to
CompileFlows.RESERVED_MODEL_KEYS — ONE list feeds both the compile-time drop and the
runtime dynamic-target guard (TaskExecutor delegates to
CompileFlows.reservedModelKeyViolation); notably NON_PERSISTED_MODEL_KEYS already
matched RESERVED_MODEL_KEYS + run, so the code groupings now agree. parser-test-32
fixture + regression; documented in the flow-schema-reference and syntax.md metadata
tables (which previously lacked model.run — the "did you add it to docs/guides?"
answer was only partial) and the workflow-suspension reserved-keys rule. Reading
model.run as a mapping SOURCE stays legal. P5 note: the Rust engine's reserved-key
list must gain model.run too.

**MERGED 2026-07-29 as PR #240 (squash `4348b0da`, CI green: Build & Unit Tests
Java 21 + memory + verify) — ADR-0011 thereby ACCEPTED. The production-polish round is
closed; next arc = P5 Rust lock-step.**

**Also: post-merge docs follow-up MERGED as PR #241 (squash `ff2635fd`).** Eric's manual
regression of tutorial-14 PASSED on all three surfaces (REST four-run purchase workflow,
playground dry-run, business-cid + span lineage in the app-log context) and surfaced two
doc gaps, both fixed: (1) the playground UI's Tutorials tab bundles the engine help pages
at WEBAPP BUILD time (Vite import.meta.glob over resources/help) — the shipped bundle
predated the tutorial-14 rework and still showed the retired missing=check-fresh draft;
refreshed via `npm run release` in system/minigraph-playground-engine/webapp (124 webapp
tests; REMEMBER: help-page edits need a bundle rebuild to reach the UI). (2) `help
tutorial 14` gained a "Dry-run the workflow interactively" section (import draft → four
instantiate+run rounds sharing one model.cid → inspect output.body; seen proves
no-re-execution; 404 rejection probe), and the workflow-suspension guide now shows all
four curl runs explicitly (runs 2-3 were prose).

**Also: Java-side pin from the P5 consistency review (branch
test/pin-restore-putall-immunity, commit local).** The review's blocker #0 (the Rust
restore merge's composite-path bypass of the reserved-key strip) prompted the agent's
suggestion to pin Java's putAll immunity: the forged-record regression now also injects
"cid.x"/"ttl[0]" as LITERAL record keys — a future refactor toward path-based writes
cannot silently reopen the guarantee. Engine 88 green.

## Memory References

- referenced: thread-graph-suspend-resume, graph-suspend-resume-design,
  bp-graph-workflow-suspension, conv-telemetry-presentation-parity,
  event-script-over-code, thread-compilegraph-syntax-validation (landing pad noted),
  vision-mercury-composable (current-state count refreshed)
- created: compilegraph-mandatory-gate (tier: working, ADR-0011 proposed — the PR merge
  is the ledger gate, the ADR-0010 precedent)
- updated: thread-graph-suspend-resume (polish round complete, Sonar passed, pushed)
