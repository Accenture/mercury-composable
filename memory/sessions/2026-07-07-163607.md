# Session (2026-07-07T16:36:07.000Z)

**Agent:** Claude Code

Same conversation as 160546 (PR #140 finalization). Continues
[[thread-sonar-quality-gate-4.6.2]].

## x.y.z placeholder extended to example/helper READMEs — commit `57ccb9ee`
Per Eric: Mercury jar versions in run commands across 7 READMEs (kafka-demo,
scheduler-example, schema-registry-demo, sync-over-async-demo, kafka-standalone,
redis-standalone, schema-registry-standalone) now use `x.y.z` + the same annotation
note as docs/guides. Also corrected stale literals found in the sweep (4.3.30 in
scheduler-example, 4.5.0 ×2 in schema-registry-demo). NOT touched: third-party
versions (kafka-protobuf-provider:8.3.0 in sync-over-async README) and the
kafka-standalone Dockerfile (executable build code — needs literal jar name).

## RELEASE-PROCESS EFFECT (for the v4.6.2 bump and future releases)
Docs/guides AND example/helper READMEs no longer need version bumps. Remaining
version-bump surface: 31 poms, kafka-standalone Dockerfile, root README + CLAUDE.md /
GEMINI.md version refs, CHANGELOG (4.6.2 entry already in PR #140).

## PR #140 MERGED (squash `a3d42363` on main) 🎉

## Release bump DONE — branch chore/release-4.6.2, commit `186c3940`
- 30 poms 4.6.1→4.6.2 (verified every changed version line is a Mercury artifact).
- OtelForwarderContext.VERSION "4.6.2" (comment mandates keeping in step with release).
- CLAUDE.md/GEMINI.md header, kafka-standalone Dockerfile (4.6.2-exec), CHANGELOG 4.6.2
  entry's jar example.
- 3 stragglers the earlier x.y.z sweep missed (docs/guides/event-driven/ subdirectory +
  extensions/opentelemetry-forwarder/README + system/mini-scheduler/README) → x.y.z+note.
  LESSON: sweep docs/guides RECURSIVELY and include ALL module READMEs.
- Verified: full offline reactor `mvn clean test` at 4.6.2 — all modules pass.

## TAGGING PRECEDENT (confirmed from v4.6.1)
v4.6.1 tag points at `5804c1fc` = the SQUASH-MERGE commit of PR #139 on main. So tag
v4.6.2 on the merge commit of the release PR AFTER Eric merges — not on the branch.

## 🚀 v4.6.2 PUBLISHED
PR #141 merged (merge commit `56ac1067`); CI green (Build & Unit Tests Java 21 4m29s,
memory ×2, verify — 0 failures). Eric published the GitHub release:
https://github.com/Accenture/mercury-composable/releases/tag/v4.6.2
Release title/notes provided by me (⚠ callout: kafka-standalone -exec jar rename).
**The entire quality-gate arc is closed**: failing gate (1 blocker/24 critical/83 major)
→ clean scan → two new test suites → released.

## Memory review ritual (post-release, 14 sessions since last)
- refresh-metadata: re-tiered elastic-queue-file-fifo-plan + snyk-oss-dependency-update
  to archive-candidate; guava thread uses 1→2.
- Archived 7: thread-sonar-quality-gate-4.6.2 (completed — v4.6.2 released),
  elastic-queue-file-fifo-plan (shipped 4.6.0), kafka-csfle-delegation (shipped),
  jackson-databind-alert28-not-applicable (dormant — Dependabot re-flag on Jackson
  2.22.1 release is the reminder), minimalist-kafka-schema-registry (shipped PR #129),
  snyk-oss-dependency-update-2026-07 + thread-guava-failureaccess-field-fix
  (superseded-by: release-4-6-2-shipped).
- New Key Decision fact `release-4-6-2-shipped` carries the durable caveats forward:
  failureaccess pins must stay; -exec jar rename; x.y.z docs convention; never start a
  second EmbeddedKafka in one JVM. NB: fact ids must be dot-free (footer regex
  `[a-z0-9-]+`) — `release-4.6.2-shipped` dangled until renamed `release-4-6-2-shipped`.
- memory-lint: 0 errors. continuity.md back under budget (28 facts).
- Stamped last_review 2026-07-07 through 2026-07-07-163607.md.

## Memory References
- Referenced: [[release-4-6-2-shipped]]

## Memory References
- Referenced: [[thread-sonar-quality-gate-4.6.2]]
