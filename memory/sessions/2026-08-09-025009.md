# Session (2026-08-09T02:50:09.000Z)

**Agent:** Claude Code

## What happened

**graph.task gains `model.*` input-mapping staging (Event Script parity) + tutorial-13
remodeled onto the AsyncHttpClient — Java half committed on branch
`fix/graph-task-model-staging` (`059c686f`, amended to fold Eric's X-TTL round),
awaiting Eric's PR gate.**

Eric's debugging request: in tutorial-13 he mapped `text(100) -> model.id` then
`text(/api/mdm/profile/{model.id}) -> url` on a graph.task node (the exact idiom of
event-script's http-client-by-config.yml) and the target received `/api/mdm/profile/null`.

**Diagnosis (code-grounded, both engines):** graph.task's input mapping recognized only
three RHS shapes — `*`, `header.{name}`, body composite path — so a `model.*` RHS
silently landed in the request body under a literal `model` key; the state machine never
saw it and `substituteVarIfAny` rendered the missing variable as the string "null"
(matching the symptom exactly). The staging half of the dynamic-variable idiom was
missing; the substitution half already worked. graph.task was the fetcher-family
outlier — `fillFetcherApiParameters` (graph.api.fetcher AND graph.extension input
mappings) already supported `model.*` staging under the shared immutability guard, and
`help graph-task.md` line 28 already promised "the same syntax of Event Script".
The Rust port mirrors the same gap (skills.rs `stage_task_parameter`).

**Fix (ratified by Eric with the tutorial direction):** in `GraphTask.buildTaskRequest`,
an RHS starting with `model.` → `assertMutableModelTarget` + state-machine write/remove
(null semantics matching the family precedent) instead of the request body; later
entries' `{model.x}` substitutions see it because entries process in order against the
live state machine. The gate ALREADY rejects reserved-metadata targets in `input[]`
(discovered when the new negative fixture answered 404 — "compiled or 404"); the runtime
guard covers single-node execution of drafts.

**Env-var substitution parity (Eric's question 2) — answered by investigation, no code
change needed:** `${name:default}` resolution happens when the model is LOADED in both
lanes — CompileGraph loads the deployed model through ConfigReader (which eagerly
resolves references at load; `getMap()` is substituted after that), and the dry-run's
`instantiate graph` deliberately round-trips the authored session graph through a temp
file + ConfigReader ("use config reader to resolve environment variables" comment). The
authored session model and any export keep the raw `${...}` placeholder (portability),
only the run instance gets resolved values; `update node` edits are picked up at the
next instantiate. `{model.x}` survives loading untouched (ConfigReader only reacts to
the `${` signature).

**tutorial-13 remodel (Eric's spec verbatim):** hello-task now calls
`task=async.http.request` (platform-core's AsyncHttpClient — no API fetcher involved)
with `input.body.person_id -> model.person_id`,
`text(http://127.0.0.1:${rest.server.port:8080}) -> host`,
`text(/api/mdm/profile/{model.person_id}) -> url`, `text(GET) -> method` — an HTTP
client by configuration against the existing mock.mdm.profile service. The input-mapping
map is rendered into the HTTP request by AsyncHttpRequest.fromMap at the function
boundary (keys host/url/method/headers.{name}/body/parameters.query|path — Eric's hint).
**HelloTask (v1.hello.task) retired.**

**X-TTL round (Eric's follow-up ruling — a ttl-propagation inconsistency):** the graph's
regular ttl propagation (node `ttl` else model.ttl) bounds only the EVENT call to the
composable function — it cannot reach inside a generic function, so the AsyncHttpClient
would run on its own 30s default, decoupled from the graph deadline. The sanctioned fix
is explicit in the request contract: `text(5000) -> headers.x-ttl` (X-TTL in
MILLISECONDS; AsyncHttpRequest.getTimeoutSeconds ceilings ms→s min 1; wire response
timeout = value+1s; absent → -1 → client's DEFAULT_TTL_SECONDS=30). The header also
rides the wire for end-to-end deadline propagation (ingress honors inbound x-ttl —
the v4.11.1 feature). Added to the tutorial node, taught in the tutorial help +
graph-task help Timeout section + the "Using AsyncHttpClient by configuration" guide
section (new X-TTL paragraph + parameter-table row + example line) + both worked
examples (skills-reference, AI catalog). Wire-echo pinned in the e2e (mock echoes
observed_ttl "5000" — the fetcher arc's proof pattern).

Docs as one story: help graph-task.md (RHS rule 3 = model.* staging + example),
help tutorial 13.md (full rewrite teaching staging, dynamic variables, env-var
substitution semantics incl. export-keeps-placeholder), skills-reference.md graph.task
section (new worked example), AI catalog graph.task notes + example (async.http.request
demo), CHANGELOG Unreleased (Fixed + Changed). Webapp bundle regenerated
(index-C4WpyX93.js; help baked at build time).

Tests: GraphTaskTest — tutorial-13 deployed-lane e2e (profile fetch via dynamic
variable), unknown-profile error propagation, gate rejection of `-> model.ttl` in
input[] (unit-test-task-6, deliberately-invalid fixture answering 404);
unit-test-task-1 gained the in-process staging pin (`text(alpha) -> model.token` →
`text(Bearer {model.token}) -> nested.auth`, also pinning dotted body paths);
GraphTests.tutorial13 remodeled; CompanionSyncTest gained the dry-run-lane e2e via the
sync companion (import → instantiate with `int(100) -> input.body.person_id` → run),
proving instantiate-time env-var resolution (test port 8090, NOT the 8080 default) and
that the traversal's JSON payload arrives in the sync response's `result` field (console
narration is `output` — learned when the first assertion looked in the wrong field).
Module 105/105; webapp 212/212. Eric's cosmetic rest.yaml comment touch-up folded into
the same branch (PR #265 precedent).

**Java half MERGED as [PR #267](https://github.com/Accenture/mercury-composable/pull/267),
squash `e16f4b40`, CI green (Build & Unit Tests 6m29s).**

**Accept-header round (found while mirroring on Rust):** the remodeled tutorial worked on
Java WITHOUT an Accept mapping only because reactor-netty sends an implicit `Accept: */*`
(the REST layer negotiates it to application/json); the Rust client sends NO default
Accept, both engines' REST servers omit content-type absent Accept (verified: Java
updateContentType accept==null → "?" → no header; Rust accept_fallback_type → None), so
the same model decoded JSON on Java and returned raw bytes on Rust. **Durable fact: never
rely on an HTTP library's implicit default Accept — the reference http-client-by-config
flow always declared it.** Fix on both engines: tutorial-13 adds
`text(application/json) -> headers.accept` (fixture byte-identical; tutorial help teaches
"declare what you accept"; catalog + skills-ref examples updated). Java follow-up branch
`fix/tutorial-13-accept-header` (`0a492fc3` — amended to fold Eric's javadoc cosmetics on
GraphTask/GraphTaskTest/CompanionSyncTest/GraphTests, awaiting gate). **Eric's ruling on
the client-default divergence: the Rust async HTTP client now sends a default
`Accept: */*` when the caller gives none** (Java reactor-netty parity; explicit accept
never overridden; wire-echo pinned both ways in rest_automation via the /api/echo/chain
harness, which gained an optional ?accept= for the inner hop).

**Rust lock-step half implemented on the mercury repo's `fix/graph-task-model-staging`:**
skills.rs stage_model_variable branch (MODEL_NAMESPACE made pub; guard from common),
fixtures/help byte-identical, hello-task mock retired, unit-test-task-6 in the manifest +
both compiler invalid-lists, tutorial-13 runtime remodel + staging pin + negatives,
playground dry-run twin via the sync companion (the ephemeral-port harness points the
rest.server.port override at the actual bound port before instantiate — config-chain
resolution proven), Increment 83 + CHANGELOG Unreleased, webapp bundle regenerated.

**INCREMENTS.md repair (Eric's catch):** sections 78 (minigraph node-ttl + metadata
immutability, v4.11.1 R3) and 79 (dry-run watcher + drain + fetcher x-ttl + docs mirror,
R4) were never written — reconstructed from session 2026-08-01-233448 ("Increments
76-80" = the six lock-step commits); tail re-ordered to strict ascending 76→83 (82 had
been inserted before 81; my 83 initially anchored on the misplaced 82); the Overview
table extended with rows 76-83 + a note that from 76 the gate is green suites (58), not
a cumulative test count.

**ARC CLOSED — all merged, CI green on both:** Java accept follow-up merged as
[PR #268](https://github.com/Accenture/mercury-composable/pull/268) squash `7ab9c771`;
Rust lock-step (incl. the default-Accept ruling + INCREMENTS repair) merged as mercury
PR #197 merge `79212bc0` carrying `0530bd13`. The graph.task staging fix, the remodeled
tutorial-13 and the default-Accept client behavior are COMPLETE ON BOTH ENGINES and ride
the next release.

## Memory References

- created: thread-graph-task-model-staging
- referenced: conv-telemetry-presentation-parity (built-in surface must ship lock-step
  on both engines), compilegraph-mandatory-gate ("compiled or 404" shaped the negative
  test), thread-tutorial-14-decision (webapp bakes help at build time — regen ritual),
  typed-io-map-or-pojo (fromMap at the function boundary is the same Layer-1 contract)
- Superseded: (none)
