# Session (2026-07-14T17:55:01.000Z)

**Agent:** Claude Code

## What happened

Executed the env-leakage hardening follow-up flagged in 2026-07-14-173816 (branch
`fix/test-config-env-leakage`, commit `0a7fcc9f`, independent of the twin-kafka PR branch).
Converted the two genuinely leak-prone test configs to plain values and verified the two
suspected-but-actually-safe ones:

1. **sync-over-async** — `redis.password=` plain blank (was `${REDIS_PASSWORD:}`). `RedisConfig`
   maps null and blank to the same no-auth behavior (`getProperty(key, "")`), so behavior-preserving;
   the production idiom stays documented in RedisConfig's javadoc. 32 tests green.
2. **opentelemetry-forwarder** — endpoint / service name / headers are plain values (were
   `${OTEL_*}` references). A leaked `OTEL_EXPORTER_OTLP_ENDPOINT` (common on instrumented CI
   agents) would have silently redirected the test away from MockOtlpCollector. `parseHeaders`
   treats blank and "null" alike → blank headers preserve the no-auth path. 22 tests green.
3. **reactive-postgres + pg-example — CORRECTION to the earlier suspicion: NOT leak-prone.**
   The `${PG_USER}`/`${PG_PASSWORD}` references are **load-bearing and leak-immune**: each pom pins
   `PG_USER=postgres` via surefire `<environmentVariables>` (surefire overrides any inherited
   agent-level value, and env vars exist from JVM start → no test-class-order freeze either);
   `PG_PASSWORD` resolves null by design and zonky embedded PostgreSQL trust auth ignores passwords.
   Documented in place, no functional change. 19 + 7 tests green.

platform-core `looping.test.*` / `${PATH}` fixtures untouched (deliberate substitution-mechanism
tests). PR text handed to Eric; branch pushed.

## Memory References

- referenced: test-config-injection-exact-key (this is its follow-up; the fact's "leakage" list is
  now refined — postgres entries were false positives, surefire-pinned)
- referenced: release-4.6.1-security-patch (otel-forwarder test architecture context)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

> also: MERGED as PR #181 (squash commit `bcaeb38e`, 2026-07-14; CI green - Build & Unit Tests
> Java 21 + memory checks). Test-resources-only, no runtime surface. This closes the env-var
> sweep opened by the twin-kafka field failure: both follow-ups (#180, #181) are now on main.
