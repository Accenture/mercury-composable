# Session (2026-09-01T03:11:12.000Z)

**Agent:** Claude Code
**Round:** Onboarding-efficiency follow-through — the fresh-agent context-efficiency
exercise (the pending measure from [[thread-onboarding-efficiency]], due after two
post-adoption reviews) plus its punch-list fixes. Same working session as
2026-09-01-022524 (that log was committed before this round started).

## What happened

### The re-measure (vs the 2026-08-21 baseline: 64% closed-thread narrative)

- **Quantitative:** continuity.md 972 → 637 lines. Closed-`[x]`-block share 64% → 17%
  by block count; a fresh agent's fairer count (adding done-narrative embedded inside
  nominally-open threads, ~86 lines — mostly `thread-polyglot-initiative`) puts
  shipped-history at ~31%. Contributor session-start read set measured: ~23.7k tokens
  total (continuity ~13k; PROTOCOL ~2.7k; instructions ~1.9k; vision ~1.5k; 3 newest
  logs ~4k).
- **Fresh-agent smoke test** (clean-context subagent, memory files only, per
  memory/smoke-test.md): **12/12**, no dangling `[[refs]]` (every absent id resolves in
  archive/INDEX.md — the 5 threads archived at today's review all clean). Verdict:
  **"acceptable, trending lean."** Result-log row appended to smoke-test.md, plus a
  methodology note: a Claude Code agent cannot be perfectly blind (CLAUDE.md auto-imports
  part of the read set); honor "answer only from the prescribed set" and disclose.

### Punch-list fixes applied (all from the fresh agent's findings)

1. **`thread-polyglot-initiative` condensed** ~65 → ~19 lines (the single largest noise
   block — P1–P4 completion narrative moved to pointers; only the live P5 content and
   the one durable lesson kept).
2. **instructions.md:** a "the field" glossary line (client production installations
   consuming releases through governed Snyk/Sonar pipelines; client specifics stay out
   of the repo — compartmentalization-safe); the retired placeholder-module note
   (rest-spring-3 paths, ADR-0017/PR #305); `draft-design-specs/` added to the structure
   sketch (committed, public since 2026-08); the minigraph-playground run command.
3. **continuity Project State:** new `latest_release` line (v4.12.0, 2026-08-30; pom.xml
   stays the live version source).
4. **vision.md current-state context:** stale "v4.11.0" refreshed to v4.12.0 +
   language-pack mention (factual maintenance of the "inferred — confirm or replace"
   section only; the Vision statement itself untouched — flagged for Eric's eyes).
5. **Stale wording fixed:** `thread-elastic-queue-bdb-to-file` still said "gitignored
   draft-design-specs" (predates the specs-go-public round).
6. Known lag left for the next review to fix mechanically: `thread-onboarding-efficiency`'s
   footer (`last_used`/`uses`) trails its substance edits — this log's Memory References
   entry feeds the next refresh-metadata run.

### The remaining decision (Eric's)

`continuity_max_lines` 1000 → ? — the originally queued "lower to ~600" is now provably
too low: healthy post-review size is ~640 lines, so a 600 cap would fire permanently on
a lean file. The sharper re-bloat guards that actually caught this round are
`continuity_max_facts` 35 and `closed_narrative_max_lines` 150 (both active). Options
put to Eric: keep 1000 (recommended — the line cap is a coarse backstop; the sharp
advisories do the work) or tighten to ~800 as a tripwire.

## Memory References

- Referenced: thread-onboarding-efficiency, thread-polyglot-initiative,
  thread-elastic-queue-bdb-to-file, ot-agent-orchestration-e0,
  snyk-retired-manifest-placeholders, stack-integration-spring-boot4,
  bp-agent-orchestration, eric-release-rhythm
- Created: (none)
- Reactivated: (none)
- Superseded: (none)

## Decision (post-exercise, same round)

**Eric decided (2026-09-01): `continuity_max_lines` stays at 1000.** Rationale recorded
in decay-policy.md next to the value: healthy lean size is ~600 lines, so the
once-floated 600 cap would fire permanently; the sharp re-bloat guards are
`continuity_max_facts` 35 and `closed_narrative_max_lines` 150. With that,
[[thread-onboarding-efficiency]] is CLOSED — every item done (fork, conventions,
upstream handoff, backlog condensed over two reviews, fresh-agent re-measure, cap
decision). Close record condensed in continuity per the convention the thread itself
established.
