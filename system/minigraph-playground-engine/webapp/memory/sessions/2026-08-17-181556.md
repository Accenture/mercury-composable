# Session (2026-08-17T18:15:56.853Z)

**Agent:** Codex

## Summary

Completed manager feedback under `ui-loop-engineer` run
`ui-graph-toolbar-run-controls-20260817-11`. Removed the MiniGraph header split Run control and added
two separate compact `Instantiate` and `Run` actions to the Graph toolbar immediately before the raw
graph JSON Copy button, sharing the toolbar button styling. `Run` is disabled until the backend has
acknowledged instantiation and, for graphs with derived `input.body` references, the existing JSON
input upload has succeeded. Instantiate never auto-runs; the optional input modal, lifecycle
invalidation, primary-session permissions, manual console compatibility, and JSON-Path isolation are
preserved. The production bundle was rebuilt and deployed to the Java-served public resources.

Also completed visual follow-up `ui-graph-toolbar-icons-20260817-12`: the old Graph-toolbar Copy
`📑` and Run `▶` were Unicode glyphs rendered by the platform font, not an icon library. Replaced
that mixed presentation with one dependency-free local SVG family: a cube for Instantiate, an
outline play icon for Run, and overlapping pages for Copy. All three are true 16×16 `currentColor`
icons with matching 1.5px rounded strokes and decorative accessibility semantics; button behavior,
labels, ordering, and acknowledgement gating are unchanged.

Also completed tooltip follow-ups `ui-graph-toolbar-tooltips-20260817-13` and
`ui-graph-toolbar-hover-only-tooltips-20260817-14`: Instantiate and Run now show compact dark purpose
hints only while their own wrapper is pointer-hovered, including disabled Run. Button focus or click
alone never opens or latches a hint, so leaving the action closes it and stale hints cannot overlap.
Each button still references its state-aware `role="tooltip"` description with `aria-describedby`;
native browser titles were removed to avoid duplicate hints. `pointer-events: none` keeps the
presentation layer from intercepting graph-surface input.

## Verification and review

- Test-first focused verification passed: 5 files, 22 tests covering toolbar ordering/style reuse,
  idle/Ready/busy button states, explicit command ordering, optional input gating, and prop wiring.
- Final full webapp Vitest passed: 35 files, 270 tests.
- `npm run typecheck`, `npm run release`, and `git diff --check` passed. Vite emitted only its existing
  advisory for the main chunk exceeding 500 kB.
- The deployed hashed JavaScript contains the new Graph-toolbar controls and two-step modal labels.
- A fresh-context read-only reviewer found no actionable correctness, regression, accessibility, or
  test-coverage issues.
- The icon follow-up's first implementation exposed unreliable `?react` handling under the current
  Vite/Vitest combination, so the bounded retry used the app's established inline-SVG pattern. Its
  fresh reviewer found two Low consistency gaps (14px rendered size and under-specified SVG tests);
  explicit 16px dimensions and complete shared-attribute assertions resolved both, and the same
  reviewer confirmed no new issue. Final focused tests, typecheck, release, dist/public parity, and
  `git diff --check` all passed; release emitted `index-DRFa7b0P.js` and `index-BqlWKJYb.css`.
- The tooltip follow-up added direct disabled-hover, click/focus non-latching, mouse-leave,
  cross-action exclusivity, and disabled-callback tests.
  A fresh reviewer first identified the missing interaction coverage, then caught an intermediate
  pointer-interception regression; both were fixed within the bounded loop. Final full Vitest passed
  at 35 files / 271 tests, with typecheck, release, dist/public parity, bundle marker inspection, and
  `git diff --check` green. A user-reported click-focus persistence regression was then reproduced
  red-first and corrected by making wrapper hover the sole visibility state. Fresh review then found
  the old close transition left a 120ms visual linger; the closed state is now transition-free and
  CSS-contract tested, while only entry retains its delayed fade. Targeted recheck closed the finding,
  all gates stayed green, and release emitted `index-DB8QnVmP.js` and `index-2akXhli_.css`.

## Memory References

- referenced: webapp-stable-transport-boundary
- Superseded: webapp-acknowledged-graph-run-control → webapp-graph-toolbar-run-controls
- created: webapp-graph-toolbar-run-controls
