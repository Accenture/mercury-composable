# Session (2026-08-22T18:53:19.000Z)

**Agent:** Claude Code

## Summary

**P2 of the polyglot initiative DONE on both engines, same day the wrapper repos came back
AI-enabled: `graph.task` reaches declarative Event-over-HTTP targets (ratified D5 — the
initiative's ONLY engine change).**

1. **Wrapper repos returned agent-memory-enabled** (v4.38.0; visions ratified, first
   blueprint gap "publish behind the interop gate", smoke tests 11/12 with a wiring gap
   fixed). Local clones: hooks activated via `.githooks/init.sh` per protocol step 2.
   The earlier verification confusion resolved: Eric's polyglot memory commit is
   `21bb3c60` with the agent-memory smoke test `5bda5cee` on top.
2. **Java half** (branch `feature/graph-task-event-over-http`, commit `10c53ca3`):
   GraphTask's existence guard now also accepts a route resolvable via the declarative
   map — `!po.exists(route) && po.getEventHttpTarget(route) == null` — with a new
   `PostOffice.getEventHttpTarget(route)` passthrough (EventEmitter's accessor was already
   public). The teaching error for genuinely unknown routes is unchanged
   (missingTaskRouteFailsFast pin holds). **Pin proven against unfixed code first**: new
   `unit-test-task-7` fixture + `foreignRouteViaEventOverHttp` e2e — the map loaded but
   graph.task rejected with "task 'polyglot.stub.function' does not exist"; green after
   the fix. The stub peer is a com.sun.net.httpserver `/api/event` endpoint decoding the
   relayed envelope and replying standard-format — the same wire contract the
   python/node wrappers speak. Test config: `yaml.event.over.http` +
   `stub.peer.port=8391` in the module's test application.properties.
   Gates: minigraph-playground-engine **119/119**; platform-core **426 green** (3
   pre-existing skips). CHANGELOG Unreleased added.
3. **Rust half** (mercury repo, same branch name, commit `83b12c36`): identical guard
   relaxation in skills.rs via the public `event_api::get_event_http_target` (no
   PostOffice passthrough needed — documented divergence); fixture **byte-identical** to
   Java's; stub peer = raw tokio TCP; proven failing first with the identical signature;
   the compiled-set parity pin caught the new fixture immediately (49→50 — the guard
   working as designed). Gates: workspace 63 suites + clippy 0 + fmt clean (the recurring
   hand-written-test rustfmt lesson). CHANGELOG + INCREMENTS 90. Rust-side log:
   mercury memory 2026-08-22-185217.
4. Deliberately NOT touched: `graph.suspend`'s store-task guard stays local-only (state
   stores over HTTP are not this feature, per the ratified design); help-doc/docs-chapter
   wording rides P4 (no contradicting claim found in help graph-task.md).

5. **Eric's IDE cosmetics folded** (commit `a31c5316`: comment quoting + a
   @SuppressWarnings relocation in PostOfficeTest; re-gated 10/10 + 78/78) and **both PRs
   MERGED same day**: Java [PR #292](https://github.com/Accenture/mercury-composable/pull/292)
   squash `fad24f11` == gated `a31c5316` (tree verified, squash title clean), CI green
   (Build & Unit Tests 7m34s + memory + verify). Rust
   [PR #212](https://github.com/Accenture/mercury/pull/212) merge `c49d6cd7` carrying
   `83b12c36` (tree verified), CI green (test 2m26s + build + memory — the enablement's
   agent-memory checks passed their first feature-PR outing on both repos). Branches
   deleted both ends. Both ride the next release via CHANGELOG Unreleased.

**Remaining:** P4 docs chapter + examples demo +
interop-report extension + ADR-0016 proposal + fresh wrapper-repo CI; P5 publishing gates.

## Memory References

- referenced: polyglot-event-over-http-design (D5 executed), thread-polyglot-initiative
  (P2 item), conv-telemetry-presentation-parity (lock-step, identical fixture + messages),
  compilegraph-mandatory-gate (compiled-or-404 context for the new fixture),
  eric-release-rhythm
- updated: thread-polyglot-initiative (P2 done pending gates; pause lifted)
