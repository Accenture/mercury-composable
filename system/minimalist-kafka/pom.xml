<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.platformlambda</groupId>
    <artifactId>minimalist-kafka</artifactId>
    <version>4.6.3</version>
    <packaging>jar</packaging>
    <name>Minimalist Kafka (notification function + flow adapter)</name>
    <description>
        A minimalist bridge between Kafka and Event Script: a drop-n-forget notification function
        (simple.kafka.notification) that publishes Post Office events to Kafka, and a Kafka flow adapter
        that routes each topic into a configured Event Script flow (the Kafka counterpart of rest.yaml),
        with W3C trace propagation across the Kafka boundary. byte[] headers/body, no custom serdes.
    </description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
    </parent>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>21</java.version>
        <netty.version>4.2.15.Final</netty.version>
        <jackson-2-bom.version>2.22.0</jackson-2-bom.version>
        <!-- Pin the Kafka client to the same version as the embedded broker used in tests
             (kafka_2.13) to avoid client/broker protocol skew. -->
        <kafka.version>4.2.0</kafka.version>
        <!-- Provides the out-of-the-box Schema Registry serializers (interop with existing client
             projects). Confluent Platform 8.3.x's own tested pairing is Apache Kafka 4.3.x, but we
             deliberately pin our own kafka-clients (above) rather than follow that pairing - the
             serializers implement the stable Serializer/Deserializer contract, so they are tolerant of a
             different Kafka client version. Bumping kafka.version to 4.3.x is a separate, deferred
             decision (broader blast radius: 24 modules, embedded broker behavior, no CVE driver) -
             see the project backlog. -->
        <confluent.version>8.3.0</confluent.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.projectreactor</groupId>
                <artifactId>reactor-bom</artifactId>
                <version>2025.0.5</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Converge Guava to platform-core's version: Confluent's serdes otherwise drag in an
                 older 32.0.1, leaving this module on a different Guava (and failureaccess) than the
                 rest of the reactor. Pinning here keeps one Guava + one failureaccess companion. -->
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>33.5.0-jre</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo1.maven.org/maven2/</url>
        </repository>
        <!-- Confluent's Schema Registry serializers are published here, not on Maven Central. -->
        <repository>
            <id>confluent</id>
            <url>https://packages.confluent.io/maven/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- Event Script engine brings the flow runtime + REST automation, and fetches platform-core
             transitively. The flow adapter routes Kafka messages into Event Script flows. -->
        <dependency>
            <groupId>org.platformlambda</groupId>
            <artifactId>event-script-engine</artifactId>
            <version>4.6.3</version>
        </dependency>

        <!-- Guava's failureaccess companion is required at runtime by Confluent's serdes (Guava
             LocalCache -> InternalFutureFailureAccess). Declared explicitly so it is a first-class
             classpath entry and cannot be dropped by strict/curated dependency resolution in the
             field, where it otherwise arrives only as a transitive-of-a-transitive of Guava. -->
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>failureaccess</artifactId>
            <version>1.0.3</version>
        </dependency>

        <!-- Kafka client (key=String, value=byte[]). -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
            <version>${kafka.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.lz4</groupId>
                    <artifactId>lz4-java</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Confluent JSON Schema serializer/deserializer (used as a library to (de)serialize the
             Confluent wire format - magic byte + global schema id + payload). We keep our own pinned
             kafka-clients, so exclude the transitive one to avoid version skew. -->
        <dependency>
            <groupId>io.confluent</groupId>
            <artifactId>kafka-json-schema-serializer</artifactId>
            <version>${confluent.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.kafka</groupId>
                    <artifactId>kafka-clients</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Confluent Avro serializer/deserializer (same role as the JSON one, for the Avro wire format).
             It brings org.apache.avro:avro transitively (used to build/read GenericRecord). Exclude the
             transitive kafka-clients to keep our own pinned version. -->
        <dependency>
            <groupId>io.confluent</groupId>
            <artifactId>kafka-avro-serializer</artifactId>
            <version>${confluent.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.kafka</groupId>
                    <artifactId>kafka-clients</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Confluent Protobuf serializer/deserializer is deliberately NOT a dependency. It pulls
             com.squareup.wire:wire-runtime-jvm, a discontinued artifact with an unpatched DoS CVE
             (CVE-2026-45799 / GHSA-7xpr-hc2w-34m9) that fails our security gate. Confluent has not moved to
             the renamed wire-runtime coordinate as of kafka-protobuf-provider:8.3.0. See SchemaType.PROTOBUF's
             Javadoc for the full rationale and the reintroduction path. -->

        <!-- CSFLE (Client-Side Field Level Encryption). The base artifact carries the FieldEncryptionExecutor
             (registered as a Confluent RuleExecutor via ServiceLoader) and the local KMS driver used for tests.
             CSFLE is delegated entirely to the Confluent serdes: with these on the classpath and rule.executors
             + KMS configured, encryption/decryption runs on the existing serialize/deserialize path. Absent the
             config, the serdes behave exactly as before (plaintext). -->
        <dependency>
            <groupId>io.confluent</groupId>
            <artifactId>kafka-schema-registry-client-encryption</artifactId>
            <version>${confluent.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.kafka</groupId>
                    <artifactId>kafka-clients</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Cloud KMS driver. A field installation configures exactly ONE KMS vendor - each registers its
             KmsDriver via ServiceLoader and pulls that cloud's SDK, and the field-encryption executor above
             works with whichever single driver is present. AWS is enabled here as the default/template; for
             Azure Key Vault or GCP KMS instead, replace the AWS artifactId below with
             kafka-schema-registry-client-encryption-azure or kafka-schema-registry-client-encryption-gcp.
             NOTE: optional=true means this driver is NOT inherited transitively - a downstream app depending
             on the published minimalist-kafka artifact does NOT receive a KMS driver from here and must
             declare exactly one itself (this keeps non-CSFLE and other-vendor consumers from being forced to
             resolve a cloud SDK they don't use). The field-encryption executor above is a normal compile
             dependency, so consumers DO inherit it - only the vendor KMS driver is the app's own choice. -->
        <dependency>
            <groupId>io.confluent</groupId>
            <artifactId>kafka-schema-registry-client-encryption-aws</artifactId>
            <version>${confluent.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Local (Tink) KMS driver: a static in-JVM keyset for repeatable CSFLE round-trip tests, so
             encryption is exercised in CI without cloud credentials. Test scope only. -->
        <dependency>
            <groupId>io.confluent</groupId>
            <artifactId>kafka-schema-registry-client-encryption-tink</artifactId>
            <version>${confluent.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Embedded Kafka (real KRaft broker) for integration tests. -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka_2.13</artifactId>
            <version>${kafka.version}</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.lz4</groupId>
                    <artifactId>lz4-java</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-collections</groupId>
                    <artifactId>commons-collections</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-io</groupId>
                    <artifactId>commons-io</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-beanutils</groupId>
                    <artifactId>commons-beanutils</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-digester</groupId>
                    <artifactId>commons-digester</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.junit</groupId>
            <artifactId>junit-bom</artifactId>
            <version>6.0.3</version>
            <type>pom</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
                <executions>
                    <execution>
                        <id>default-compile</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>default-testCompile</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>java-compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <compilerArgs>
                                <arg>-proc:full</arg>
                                <arg>-Xlint:deprecation</arg>
                            </compilerArgs>
                        </configuration>
                    </execution>
                    <execution>
                        <id>java-test-compile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                        <configuration>
                            <compilerArgs>
                                <arg>-proc:full</arg>
                                <arg>-Xlint:deprecation</arg>
                            </compilerArgs>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-install-plugin</artifactId>
                <version>3.1.4</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.4</version>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.14</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check-coverage</id>
                        <phase>test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.85</minimum>
                                        </limit>
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.85</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
