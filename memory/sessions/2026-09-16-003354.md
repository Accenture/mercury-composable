# Session (2026-09-16T00:33:54.000Z)

**Agent:** Claude Code
**Summary:** Prepared the **v4.12.9** release after Eric confirmed the field CI pipeline passed —
CHANGELOG cut, version sweep across 43 build files, full-reactor and Gradle-template verification.
Branch `release/v4.12.9`; PR-open, tag and publish remain Eric's gates ([[eric-release-rhythm]]).

## What happened

### Release scope — 34 PRs, not nine

My own earlier framing of v4.12.9 was wrong and worth correcting in the record: I had described the
release as "PRs #389–#397", which is only the distributed-cache arc. `git log v4.12.8..main` shows
**34 merged PRs, #364–#397**. v4.12.8 (tag `e7379c5b`) shipped on 2026-09-12 and everything merged
since belongs to this release — including the entire sync-over-async streaming return route
(D1–D8 / E1–E4, #365–#374), the clustered-Redis support (#385), and the three field Snyk fixes
(#380, #381, #382). Lesson: derive release scope from `git log <last-tag>..main`, never from the
arc that happens to be top-of-mind.

### CHANGELOG

The `## Unreleased` block already carried #364–#391. Cut it to `## Version 4.12.9, 9/15/2026` and
added the **ten missing PRs**, each read from its commit body rather than its title:

- Added: `Platform.onShutdown` lifecycle (#390); general purpose MsgPack
  `packMapOrList`/`unpackMapOrList` (#396); the three-layer distributed-cache worked example
  (#392, #395, #397); dev mode pre-wired in the Layer 3 starter template (#397).
- Changed: a Layer 3 app needs one graph endpoint, not one per graph, plus the
  declare-the-engine-alone packaging note (#397).
- Fixed: the MiniGraph async-callback guard and the reworded mapping errors (#393, #394); the
  mini-scheduler job-list 500 on a state file caught mid-write (#384); folded the second
  sync-over-async Sonar round (#386) into the existing quality item.
- Security: no credential literals in test fixtures, CWE-798 (#381).

### Version sweep — 43 build files

Per [[conv-template-version-sweep]], BUILD FILES ONLY. Confirmed the surface before editing: a
repo-wide grep for `4.12.8` outside `pom.xml`/`build.gradle` returned only CHANGELOG history and
memory files (both correctly historical), so prose really is all on the `x.y.z` placeholder. 43
files (40 at v4.12.8 + redis-connection, distributed-cache, distributed-cache-example), covering
both Snyk placeholder poms ([[snyk-retired-manifest-placeholders]]) and all three templates'
`pom.xml` **and** `build.gradle`. Inspected the multi-hit files first — every occurrence was a
version literal, so the bulk rewrite was safe. 97 occurrences now read 4.12.9; zero `4.12.8`
remain in any build file.

### Verification

- Full reactor `mvn clean install` at 4.12.9 — **BUILD SUCCESS, 35/35 modules, zero test failures
  or errors** (checked by grepping for any non-zero Failures/Errors, not just the BUILD line).
- All three starter templates built with **Gradle** too, each producing `<name>-4.12.9.jar` — the
  lane that resolves engine artifacts from mavenLocal, so it only passes if the sweep and the
  reactor install agree.
- `check-doc-canon.py` OK.

### Deliberately NOT done

`latest_release` in continuity stays at v4.12.8. Checked the precedent: `git show
e7379c5b:memory/continuity.md` shows the v4.12.8 release PR still reading v4.12.7 — the bump lands
in a later memory commit, once the squash hash and GitHub release time exist and can be recorded
rather than invented.

### A v4.12.10 follow-up is expected

A field logistics detail worth knowing, because it explains why another release follows this one
closely: the field's CI pipeline published the pre-release JARs of this content **stamped with pom
version 4.12.8**, so those coordinates in the artifact repository no longer unambiguously identify
the 4.12.8 release. Eric briefly considered skipping 4.12.9 to sidestep it, then ruled for the
straightforward path — **release v4.12.9 as planned, then v4.12.10 as a follow-up** — so the
version sequence stays gapless and the record stays legible. This branch is v4.12.9, prepared
clean off `main` with no trace of the short-lived renumber.

Operating note for the next release: a passing field CI pipeline can publish artifacts before the
version sweep lands, stamping them with the OUTGOING version. Worth confirming what the pipeline
published before assuming the previous release's coordinates are still clean.

### Merged

**PR #398 MERGED** 2026-09-16T01:23:21Z as squash `9274cf92`. CI green on all four checks
(Build & Unit Tests Java 21, Starter Templates Gradle, memory x2). Gated close-out: verified
`state == MERGED`, confirmed origin/main moved (`ec4fbb06` → `9274cf92`) and carries the release by
content — root pom at 4.12.9, CHANGELOG top section `## Version 4.12.9, 9/15/2026`, **zero** build
files still holding 4.12.8, template `build.gradle` at 4.12.9 — then fast-forwarded local main and
deleted the branch (remote auto-deleted).

**Tag and GitHub release remain ungated work — NOT done.** `latest_release` in continuity therefore
still reads v4.12.8, by design (see above).

### Queued for v4.12.10

Opened [[ot-redis-client-separation]]: Eric's ruling that sync-over-async and distributed-cache
*should* instantiate separate `RedisClient`s, and that the `redis.*` fallback exists for
backward compatibility (sync-over-async used alone) rather than as an equal option. Scoped against
the live code and the two guides during this session; the write-up waits for v4.12.10.

### Released

**v4.12.9 tagged and released** by Eric — tag `v4.12.9` → `9274cf92`, GitHub release published
2026-09-16T01:29:22Z (not a draft). Verified both before touching continuity, then bumped
`latest_release` from v4.12.8 with the real squash hash and publish time rather than placeholders.

Eric also stated the cross-engine versioning convention explicitly: **every port adopts the Java
release number on catch-up and never runs its own sequence**, because Java is the reference
implementation. Recorded as [[conv-ports-adopt-java-release-number]] — the useful corollary being
that a port sitting below Java (Rust at v4.12.7, python/node at 4.12.1, Java at v4.12.9) is *lag
awaiting catch-up, not divergence*, so no intermediate version should be invented for partial
catch-up.

I first wrote this fact as Rust-only, explicitly excluding the python/node packs as "a separate,
slower cadence". Eric corrected it in the same session: the packs follow the **same** convention and
will catch up to the Java number on their next update. Fact rewritten and re-keyed
`conv-rust-adopts-java-release-number` → `conv-ports-adopt-java-release-number` in place (created
minutes earlier, never committed, so a supersession record would have been ceremony rather than
history).

## Memory References
- referenced: eric-release-rhythm (prepare only; PR-open and tag/publish separately gated),
  conv-template-version-sweep (build-files-only surface incl. build.gradle),
  snyk-retired-manifest-placeholders (the two non-reactor placeholder poms),
  conv-squash-title-prefill-check (release PR title on its own line),
  minigraph-dev-mode-app-shape + minigraph-guarded-async-completion + platform-onshutdown-lifecycle
  + cache-separate-from-soa + redis-connection-foundation (sourced the missing CHANGELOG entries)
- created: ot-redis-client-separation, conv-ports-adopt-java-release-number (tier: working)
