# Session (2026-08-21T17:39:02.000Z)

**Agent:** Claude Code

## Summary

**PR #284 review → rejection → ratified re-design → both replacement PRs code-complete,
awaiting Eric's push gates.**

**1. PR #284 review (a colleague's first PR: "runtime-served Mercury operational contract",
64 files, +3986).** Full review delivered in chat (EMU constraint). Verdict: impressive
security engineering and a REAL motivating find, but one live-proven functional defect, one
cross-engine design leak, and house-convention gaps. Evidence highlights:
- **Live-proven defect:** the PR's `SkillResources` read packaged resources via
  `CodeSource` + a hard `file`-protocol check — works in unit tests (classes dir) and its
  own plain-`java -cp` IT, but the REAL playground app is spring-boot packaged (nested
  jars, protocol `jar:nested`). Booted the built app + drove the companion endpoint:
  `help mercury` → `ERROR EXPORT_INTEGRITY_FAILED` while `list contracts` /
  `describe contract` worked. Durable lesson: **test the packaging you actually ship;
  read classpath resources via getResourceAsStream, never CodeSource protocol checks.**
- **Cross-engine leak:** it added Java-only commands to the playground webapp bundle +
  minigraph-commands.json + help pages — all surfaces synced wholesale to the Rust engine
  ([[conv-telemetry-presentation-parity]]) — so the Rust playground would advertise
  commands its engine cannot serve.
- **Real find at the core (verified against the parser):** the docs taught a rest.yaml flow
  binding `flow:` WITHOUT `service:` (one guide said "instead of"), but
  `RoutingEntry` (lines 403-411) skips any entry lacking `service` — the documented form
  NEVER worked. A flow binding requires BOTH `service: 'http.flow.adapter'` AND `flow:`.
- Convention gaps: ADR self-marked Accepted pre-gate; an unexplained external reference
  ("the Orca precedent" — sole occurrence in the repo); no CHANGELOG entry; new committed
  docs/specs convention; contracts pom missing the house build section; two new
  release-sweep sites (a version constant + a hard-coded IT version); a docs-authoring tax
  (files.list closure gates docs CI). Full reactor on the PR branch was green — the defects
  were in exactly the places CI could not see.

**2. Eric's ruling:** PR not approved; DO NOT use it — extract the essence, re-implement
properly on the Composable pattern as a standalone **AiContractProvider** app under
`system/` (CLI or REST on 8999; "AiContactProvider" confirmed as a typo). Plan first.
Plan written (gitignored `draft-design-specs/ai-contract-provider-plan.md`) and RATIFIED:
D1-D5 + D7 as recommended; D6 = we implement (Eric discussed with the colleague; both PR
bodies credit their find).

**3. PR A — the docs fix (branch `fix/rest-flow-binding-docs`, commit `d784ff34`):**
corrected the broken flow-binding teaching in ai-developer-guide + event-script
ai-agent-guide; the worked example is now ONE canonical fixture
(`system/platform-core/src/test/resources/guide-fixtures/rest-bindings.yaml` — flow
binding with cors/headers configs, function with path parameter, HTTP relay with
url_rewrite) embedded into the guides by the existing mkdocs snippet include
(`base_path: ["."]` + `check_paths: true` + `--strict` were already configured) AND loaded
through the production `RoutingEntry` parser in `RoutingEntryGuideFixtureTest`.
Discovered: **RoutingEntry.load() is ADDITIVE** — the test's finally re-loads the app
config so app routes stay authoritative; fixture paths are collision-free by design.
Gates: platform-core 426/426 exit 0; mkdocs strict green (rendered pages verified to embed
the fixture, zero raw markers); all 4 python doc checks green; CHANGELOG Unreleased Fixed.

**4. PR B — the app (branch `feature/ai-contract-provider`, stacked on PR A):**
`system/ai-contract-provider`, a standalone composable app that is itself a reference
implementation: 6 `@PreLoad` private functions, 7 Event Script flows, rest.yaml on 8999
(discovery/contracts/contract-detail/skill/references?path=/manifest — all GET, all
flow-bound), `--export <dir>` one-shot CLI through the export-skill flow (FlowExecutor +
callback rendezvous — the blessed blocking-in-code shape). Key design vs the rejected PR:
- dependency arrow INVERTED (app depends on platform-core + event-script-engine;
  minigraph at TEST scope only — a runtime dep would auto-preload the playground's
  functions into this app); zero diffs to framework modules;
- no ServiceLoader — one `contracts.yaml` catalog (configuration over code), anchors
  verified by Class.forName in tests;
- `mercury_version` read from platform-core's own `pom.properties` via classloader —
  ZERO new release-sweep sites (the new pom rides the existing sed sweep; count 33→34);
  startup refuses a mixed platform-core/event-script assembly;
- resources via getResourceAsStream only; exporter keeps the essence (refuse existing
  target, CREATE_NEW, verify-then-manifest-LAST, cleanup only manifest-less partials,
  determinism) in ~120 lines instead of 658.
Gates: module 18/18 (5 classes) exit 0; no-overwrite pin MUTATION-PROVEN (guard removed →
refusal test fails); **live on the REAL packaging** — booted the -exec jar
(`jar:nested:...BOOT-INF/...` in the log = the exact environment that broke the PR), all
six endpoints correct incl. 404s and traversal rejection; CLI export from the exec jar
wrote 55 files whose snapshot hash EQUALS the served /api/manifest hash
(`07c8bd61...` — runtime serves what the export writes, proven end-to-end); manifest
independently re-verified in python (54 hashes + snapshot hash, no app code); mkdocs
strict + doc checks green after ADR-0015 (Proposed) + guide pointer + CHANGELOG Added.
Full reactor gate exit 0.

**5. Late fold (Eric + the colleague's ask): scoped `system/AGENTS.md`** — their AI tool
discovers `system/AGENTS.md` for the plugin-consumer starting point. Two-audience split:
contributors routed to the root AGENTS.md shim (memory protocol untouched — nearest-wins
shadowing considered), consumers to `GET :8999/api/discovery` / `--export`, module map,
machine-catalog paths, the flow-binding gotcha. The snapshot-level AGENTS.md variant
(inside the export) noted as a possible follow-up if their tool later consumes exports.

**6. Ship: both PRs MERGED same day.**
[PR #285](https://github.com/Accenture/mercury-composable/pull/285) squash `89a1ea91`
(tree == gated `d784ff34`), CI 7m10s. PR B rebased onto merged main
(`git rebase --onto main d784ff34`, tree byte-identical, 0-line diff) →
[PR #286](https://github.com/Accenture/mercury-composable/pull/286) squash `c5b05c1d`
(tree == gated `4cc48855`; post-merge diff vs main = only PR #287's files), CI 7m39s;
ADR-0015 accepted via the merge; branches deleted both ends. On the way: #286's first CI
run hit the pre-existing ObjectStreamTest expiry flake — diagnosed here (100ms margin;
markdown-only commit, identical tree green minutes earlier), handed to a parallel session
via task chip, fixed same-day as PR #287 (the parallel session refined the mechanism:
per-publisher one-shot Vert.x timer cancelled by publish()'s doFinally — see
2026-08-21-195149). Its continuity landed while my edit was uncommitted → clean two-way
merge (both threads kept, later last_session wins), memory-lint 0 errors. Superseded
PR #284 now OPEN+CONFLICTING against #285's guide rewrites (by design) — to be closed
unmerged by Eric/Eu Gene. Durable lessons: (a) a squash-merge makes `git branch -d`
refuse (commit not an ancestor) — tree-verify then `-D`; (b) `mvn` builds on a
version-identical branch POLLUTE ~/.m2 for sibling checkouts — reinstall clean artifacts
before module-level gating; (c) `${maven.multiModuleProjectDirectory}` resolves to the
MODULE dir on `-f` module runs — validate the property before trusting it in tests.

## Memory References

- referenced: conv-telemetry-presentation-parity, functions-decoupled-routes,
  event-script-over-code, conv-add-capability, kafka-mesh-opt-in (lean-core rationale),
  eric-release-rhythm, thread-docs-improvement-backlog, conv-squash-title-prefill-check,
  thread-objectstream-expiry-test-determinism (parallel session's fix folded into this arc)
- created: thread-ai-contract-provider (tier: working)
