# Session (2026-08-22T00:30:07.000Z)

**Agent:** Claude Code

## Summary

**Onboarding-efficiency round: evaluated a fresh-agent assessment of the AI onboarding docs
(Eric's ask: "have we over-engineered?"), verified its claims, and executed the ratified
fixes.** Verdict delivered: the consumer/product side is NOT over-engineered (the assessment
itself scored it 9+/10 and traced the architecture in minutes); the contributor side is
over-RETAINED, not over-engineered — measured: continuity.md 972 lines, **621 (64%) completed
`[x]` ship narratives** vs 144 open-thread lines, ~10k tokens of already-shipped history per
session per agent, all duplicated from origin logs. The decay system operates as configured
(custom max_lines 1000, archive_window 20) — the cause is close-record style + a hot repo
filling the window faster than it drains.

**Claim verification highlights:** the vendor identity "collision" (assessment P3) is an
assessment artifact, NOT real — CLAUDE.md/GEMINI.md/copilot-instructions.md are addressed
envelopes, each runtime's official surface carries exactly one identity; the assessor read all
three while exploring. The fork-earlier gap (P1/finding 5) IS real — the root shim routed every
agent into contributor memory activation. agent-memory UPGRADE.md 4.37.0 preserves customized
root shims, so the local fix is upgrade-safe. The P5 recipe backlog independently re-derived
our own [[thread-docs-improvement-backlog]] item-for-item (good sign the backlog aims right).

**Executed (Eric ratified: handoff + in-repo items):**
1. **Root AGENTS.md** → contributor/consumer fork (contributors → memory/PROTOCOL.md;
   consumers → system/AGENTS.md, skipping the memory protocol) **plus the ratified
   role-resolution ladder** (design discussion with Eric, refined by his "this assumes a
   fresh context agent" push): memory-carrying session → contributor, never re-ask;
   arrived via a consumer surface → consumer by construction; **fresh + interactive →
   ask ONE question ("contributing to Mercury itself, or building an app that uses it?")
   whenever the first instruction does not itself RESOLVE the path — not only when no
   task is stated (many first questions are valid on both paths); "the answer creates
   your path" (Eric) and every subsequent interaction lands on it**; headless with no
   signals → default contributor (over-informed read-only = safe failure). Rationale worth keeping:
   a zero-context agent's inference is least trustworthy at activation (the same argument
   that rejected P1 task self-classification), and the role question is the one safe
   ask — binary, trivially known to the human, once per human-repo relationship.
   **MERGED 2026-08-22 as [PR #290](https://github.com/Accenture/mercury-composable/pull/290)
   squash `f85aa02a` (tree verified identical to the gated `14ae11f5`), CI green (Build &
   Unit Tests 7m50s + authoritative recheck), branches deleted both ends.**
2. **memory/instructions.md** → new "Session Working Conventions": (a) terse close records —
   `[x]` thread records are 3–6 lines (outcome, refs, one durable lesson, origin pointer),
   full narrative stays in the origin log; (b) ready-to-work checkpoint template emitted
   before substantive implementation work.
3. **Handoff note to the agent-memory team** → `~/Desktop/agent-memory-onboarding-handoff.md`
   (companion to Eric's `mercury-composable-ai-guidance-assessment.md` there): proposals for
   shim-level role routing (P1), close-record economy in DECAY/REVIEW/schema (P2, with our
   964-line measurement as evidence), optional readiness checkpoint in PROTOCOL (P4), and a
   recommend-no-change verdict on P3; notes our local customizations should converge on
   upstream forms at the upgrade that ships them.

**Declined (with Eric's agreement):** P3 vendor isolation (non-problem); building the full P1
routing-tree document in-repo (the 2-line fork + system/AGENTS.md + llms.txt already implement
the consumer branch). Deferred to the next review: condensing the existing `[x]` backlog to
stubs and possibly lowering the custom `continuity_max_lines` 1000 → nearer the 600 default
once drained.

## Memory References

- referenced: thread-docs-improvement-backlog (re-derived by the assessor; P5 stays there),
  thread-ai-contract-provider (its discovery surface + system/AGENTS.md carried the consumer
  path the assessment praised), ot-reverify-invariants-20260821 (invariant freshness backed
  the correctness score)
- created: thread-onboarding-efficiency (tier: working)
