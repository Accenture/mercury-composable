# Session (2026-07-13T00:10:09.000Z)

**Agent:** GitHub Copilot
**Lightweight:** corrected twin-kafka Event Script/header coverage so the bridge faithfully demonstrates correlation-id impedance matching: on-prem uses its own Kafka correlation header, the cloud emulator uses a distinct one, and bridge flows translate through `model.cid` instead of leaking the default header. Added wire-level regression coverage for secondary headers and kept demo docs/config aligned.

Also: ran the `examples/twin-kafka-demo` README multi-terminal flow end-to-end (dual Kafka helper, seeded schema registry, topics, SOR/bridge/REST apps, node listener, four curl requests). Validated the listener response headers, cloud-side `X-Cloud-Correlation-Id` probe, and app telemetry span chain (`span_id`/`parent_span_id`) for `demo-cid-001`; documented the trace/span validation example in the demo README.

Also: fixed the SOR response leg loose end. `profile-store.yml` now publishes `C_PROFILE_RESPONSE` via
`secondary.kafka.notification` so the cloud response stays on cloud Kafka until the bridge consumes it;
the earlier `simple.kafka.notification` path could make the response leg ambiguous/shortcut-prone. Re-ran
the multi-terminal demo with `sorfix-cid-001`; verified listener output on `OP_PROFILE_RESPONSE`, cloud
topic headers on both `C_PROFILE_REQUEST`/`C_PROFILE_RESPONSE`, and telemetry showing:
`SOR secondary.kafka.notification -> Bridge bridge-response -> Bridge simple.kafka.notification`.
Kept the SOR role's auto-created minimalist-kafka primary publisher pointed at the cloud broker too, so the
SOR process opens only cloud Kafka connections even though the profile-store response uses the secondary
publisher.

Also: investigated network isolation for the three Spring-profile personalities. Finding: the demo's single
fat jar means profiles are logical personalities, not security boundaries; both Kafka notification functions
remain on the classpath. The profile configs now keep each role on its intended broker (REST on-prem,
Bridge both, SOR cloud-only), and the README documents that real isolation needs separate deployment,
credentials, and network policy rather than relying on Spring profiles.

## Memory Review (2026-07-13, through 2026-07-13-001009.md)
- Metadata refresh: 2 footer(s) updated from the reference log (`release-4-8-1-shipped`, `release-4-8-0-shipped` last_used).
- Archived: 1 fact -> memory/archive/2026-Q3.md (faded after archive_window).
- Swept threads: 0.
- Archive-verify: pass (memory-lint: 0 errors after archive; no id in both continuity and archive).
- Invariants: not re-raised; `thread-reverify-invariants-2026q2` remains open/pending Eric.
- Promoted core: 0 (auto-core off).

## Memory References
- Referenced: `release-4-8-0-shipped` (twin-kafka header-name precedence and model.cid convention)
- Referenced: `release-4-8-1-shipped` (twin-kafka-demo and flow-authoring conventions)
