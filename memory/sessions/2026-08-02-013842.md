# Session (2026-08-02T01:38:42.000Z)

**Agent:** Claude Code

## What happened

**The Rust lock-step halves SHIPPED — both engines now in lock-step at v4.11.1.**
This closes the two open threads that were waiting on the Rust side. Work done in the
Rust repo (github.com/Accenture/mercury): feature PR #191 merged (`5db06a8f`, six
commits, Increments 76-80), release PR #192 merged (`7358f1a2`), tag `v4.11.1` on the
verified merge commit. Full detail lives in the Rust repo's session log
2026-08-01-233448 and its `thread-v4-11-1-lockstep`.

**Closed here:**
- [[thread-redis-getdel-compat]] — the Rust store now consumes version-aware (GETDEL
  6.2+ / atomic MULTI/EXEC below); the fallback proven ON THE WIRE by a
  separate-process suite against a 5.0.14-reporting RESP double (one contiguous
  MULTI/GET/DEL/EXEC window in the command journal, GETDEL never sent).
- [[thread-task-ttl-override]] — the full arc ported: per-task/per-node ttl, delay
  deferral + teardown cancellation, metadata immutability (the Rust graph layer's
  model.* RHS was previously UNGUARDED — same hole our review found in Java), the
  dry-run watcher with the CAS/owner-token constraints by construction, honest
  companion drain, fetcher x-ttl stamp, end-to-end deadline propagation.

**Durable cross-engine facts from the Rust round's adversarial review (4 lenses incl.
a dedicated Java-parity lens; 14 confirmed, all fixed):**
- **The x-ttl flow-budget derivation is a wire contract:** ceil to whole seconds via a
  32-bit parse (AsyncHttpRequest.getTimeoutSeconds) — x-ttl 700 = a 1000 ms budget and
  "Flow timeout for 1000 ms" on BOTH engines. A raw-ms reading would have made the
  engines report different values for the same request.
- The Rust round found the propagation HALF-ported by a docs-vs-code audit (header
  precedence without the flow-budget read) — the "document it" step caught a real bug.
- Two shared Java-parity residuals recorded as future lock-step follow-ups (never
  one-engine fixes): the watcher owner-token prefix window; the schedule-then-register
  deferred-dispatch window at teardown.

## Memory References

- referenced: thread-redis-getdel-compat (closed), thread-task-ttl-override (closed),
  conv-telemetry-presentation-parity (the Java-parity review lens is its enforcement
  arm), release-4-11-1 thread (the Java half this pairs with),
  graphjs-phase-out-direction (three-skill validator divergence applied as ruled)
- Superseded: (none)
