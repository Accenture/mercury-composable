# Session (2026-07-05T02:13:52.000Z)

**Agent:** Claude Code
**Lightweight:** in-place agent-memory upgrade (tool files only); no `continuity.md` facts created or changed.

## Summary

Mode B in-place upgrade of the agent-memory layer: **v4.28.0 → v4.28.2** (two rungs, both
additive/non-destructive). Ran `ENABLE.md` → Mode B; installed = 4.28.0, current tool = 4.28.2.
"Warn before you clobber" guard ran first — the working tree was clean and the target's
`.githooks/post-commit` + `memory-lint` scripts were the stock pre-upgrade copies (no local
customization; mercury *reported* the continuity-bloat bug rather than patching it locally), so the
overwrite was safe.

- **Rung 4.28.0 → 4.28.1** — re-copied `.githooks/post-commit` (adds the primary uncommitted-session-log
  guard ahead of the time-window check; `git status --porcelain -- memory/sessions/` → skip the stub
  when an in-flight log already covers the session). Kept mode `100755`.
- **Rung 4.28.1 → 4.28.2** — re-copied the four `memory-lint` scripts (`memory-lint.py`, `.mjs`, and both
  mirror test files). This is **the fix mercury's own 2026-07-05-012640 review surfaced**:
  `[continuity-bloat]`'s fact count now excludes `tier: core` invariants + pinned `- [ ]` open threads
  (decay-eligible only), matching `decay-policy.md`'s documented intent.
- Stamped `.agent/version.md` → `version: 4.28.2`, `last_upgraded: 2026-07-05` (preserved
  `enabled_with: 4.14.1`, `mode: A`).

**Verification (`memory-lint --root`, both runtimes byte-identical):** 41 footers = 14 core + 11 pinned
threads + 16 decay-eligible. The fact-count `[continuity-bloat]` warning that stayed red after the clean
012640 review is now **gone** (16 ≤ 30). One advisory remains — `continuity.md 703 lines > 600`
(the separate line-count check, not addressed by this fix; a genuine "lean it down" signal). 0 errors.

So the 012640 review's "consider a decay-policy tune for the fact cap — a human call" note is now moot on
the **fact-count** axis: the tooling, not the cap, was the problem. The line-count axis is unchanged and
still advisory.

Neither rung touches `AGENTS.md`, adapters (skill descriptions unchanged → no re-sync), or source.

## Memory References
(none — no `continuity.md` facts referenced or created. Relates to the observation in
`2026-07-05-012640.md` / the field report that drove agent-memory v4.28.2.)
