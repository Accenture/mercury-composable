# Session (2026-07-14T22:54:13.000Z)

**Agent:** Claude Code

## What happened

**Read-only state-machine metadata: docs impact check → engine enforcement** (branch
`feature/enforce-reserved-model-keys`, commit `ac8b4c0d`, pushed; PR text handed to Eric).
Eric edited syntax.md to declare the five flow-metadata keys READ only and asked for a docs
impact check (llms.txt etc.). Findings → actions:

1. **Docs impact:** llms.txt links-only (clean); event-script-flow.json doesn't cover metadata
   (clean); flow-schema-reference.md had THREE errors — model.flow described as "flow instance ID"
   and model.instance as its "alias" (they are DISTINCT: flow config ID vs flow instance ID),
   missing model.cid/model.ttl rows, and an output-mapping example that itself wrote to
   model.trace. All fixed.
2. **Engine gap found during the check:** CompileFlows' RESERVED_MODEL_KEYS enforced only
   cid/instance/flow/ttl (+parent/root) — model.trace was writable despite the docs. Eric agreed
   to enforce; **also agreed to enforce model.none** (his observation: the null constant was pure
   convention — a write would silently turn every later 'model.none -> X' clear into a copy).
3. **Dynamic-target bypass closed:** 'x -> model.{model.pointer}' resolves at RUNTIME and skipped
   compile-time validation entirely. TaskExecutor.substituteDynamicIndex now re-checks the
   resolved RHS (only when substitution changed the statement) via the shared
   CompileFlows.reservedModelKeyViolation (made package-private static) and throws — the task
   aborts to the flow's exception handler (precedent: the negative-index guard in the same method).
4. **Wording nuance (Eric agreed):** metadata is a legal mapping SOURCE in both input AND output
   mappings (the impedance-matching 'model.cid -> header.X' depends on it) — syntax.md now says so.

Tests: parser-test-30/31 static rejections + a runtime dynamic-pointer test. event-script-engine
136 green, composable-example 3 green. Repo-wide grep confirmed no existing flow writes any of
the newly-reserved keys.

## Memory References

- referenced: event-script-over-code, functions-decoupled-routes (the state machine IS the
  flow-scoped data plane — why reserved-key integrity matters)
- referenced: release-4-8-0-shipped (origin of the reserved-model-keys validation + model.cid rule)
- referenced: thread-docs-improvement-backlog (dual-audience docs: this pass fixed the
  machine-greppable reference tables' factual errors)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>

> also: MERGED as PR #184 (squash commit `daf779df`, CI green - full reactor validated the
> enforcement against every module's flows). The read-only state-machine contract is now fully
> enforced: compile-time for static targets, runtime for dynamic model.{pointer} targets.
> CHANGELOG note: #183 + #184 belong to the NEXT release bump's entry (post-4.8.5).
