# Session (2026-09-24T23:47:13.000Z)

**Agent:** Claude Code

## Summary

**v4.12.17 release prep, Java + Rust in lock-step (Eric's ask once both syncs — twin-kafka #460 and mercury #327 — were on
their mains).** Java branch `release/4.12.17`, commit `fcf44c83`: sweep BUILD FILES ONLY 43 files / 98 occurrences
(re-derived from `--include=pom.xml --include=build.gradle`, unchanged from 4.12.16 — [[conv-template-version-sweep]],
[[snyk-retired-manifest-placeholders]]; `docs/guides/claims-registry.json` keeps its `since` markers by design); CHANGELOG
from `git log v4.12.16..HEAD` ([[conv-changelog-from-tag-range]]): Added 1 (#458, the consumer-side Schema Registry identity
with its two READ notes), Added 2 (#460, the same opt-in on twin-kafka via the shared `SchemaCodec.forConsumer`),
Documentation 3 (#459 sample + guides); memory-only commits excluded. Readiness `mvn clean install` (full reactor): BUILD
SUCCESS, 34 modules, 220 suites, **1514 tests, 0 failures, 0 errors, 3 skipped** — counts from the surefire XML, up from
218 / 1506 at 4.12.16 by exactly the two new suites (`KafkaFlowAutoStartTest` 5, `SecondaryConsumerCodecTest` 3). Rust
branch `release/4.12.17` (mercury, cut after the #327 memory close landed on its main): 13 manifests / 24 occurrences +
`Cargo.lock` via `cargo update --workspace` (24 entries at 4.12.17, 0 left); its CHANGELOG entry lists the WHOLE tag range,
which the convention caught — not only Increment 139 but the two Rust-only increments merged after v4.12.16 and never
released (137 `yaml_serde`, 138 `cargo audit` + the rustls/event-listener lock refresh); light gates green (fmt-check, claims
28/28, links 37/37, mkdocs strict); clippy / `cargo test --workspace` / `cargo audit` started only after the Java readiness
build finished (shared ports). Release rhythm unchanged ([[eric-release-rhythm]], [[conv-ports-adopt-java-release-number]]):
Eric opens the PRs, merges, tags one memory commit past each merge, publishes the GitHub releases with the CHANGELOG entries
as bodies and the crates from the tag; `latest_release` updates at the seam close. Tracks [[kafka-consumer-identity-release]].

## Advisory sweep at the seam

`memory-lint` raised two `[overdue]` advisories (sslu 21 > `archive_window` 20) on `conv-thread-id-names-the-thing` and
`conv-schema-example-not-a-fact` — both conventions whose rules shipped upstream the day they were written (agent-memory
v4.41.1 / v4.41.2), so the facts carried history, not a live rule. Archived both with `archive-fact` (retrievable via
`memory/archive/INDEX.md`); facts 34 → 32. Not a cadence review (4 sessions since the last; `review_every` 10; lines 894 ≤
1000; invariants not due).

## Memory References

- kafka-consumer-identity-release (updated: release branches cut, readiness recorded)
- eric-release-rhythm (consulted)
- conv-changelog-from-tag-range (consulted — it is what widened the Rust entry to the full tag range)
- conv-template-version-sweep (consulted)
- snyk-retired-manifest-placeholders (consulted)
- conv-ports-adopt-java-release-number (consulted)
