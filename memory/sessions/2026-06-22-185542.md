# Session (2026-06-22T18:55:42.000Z)

**Agent:** Claude Code
**User:** Continue content polishing — fix a routing bug, drop a leftover line, then standardize the
three layers' structure. (Eric, the creator, collaborating.)

## What We Did (this log = the two contained fixes; the layer reorg is pending approval)

**Fix 1 — `composable-design` (and 7 others) broke in local preview.** Root cause: 8 `mkdocs-redirects`
entries were pure **case** renames (`ARCHITECTURE.md` -> `architecture.md`, etc.). On a case-insensitive
filesystem (macOS, and `mkdocs serve`) the redirect stub is written to the *same* path as the real page
and overwrites it with a self-redirect. Proved production is unaffected by rebuilding on a case-sensitive
APFS volume (real page 17.5 KB + stub coexist) — it is a **macOS-local artifact only**.
- **Decision (Eric):** drop the 8 case-only redirects. Old UPPERCASE reference-doc URLs will 404 after
  merge, but README/llms.txt already use lowercase and the high-value CHAPTER-N / APPENDIX redirects
  (non-colliding) stay.
- Added a guard to `scripts/check-doc-canon.py`: rejects any `mkdocs.yml` redirect whose source==target
  ignoring case (negative-tested). All 8 pages now render locally; canon check + `--strict` green.

**Fix 2 — leftover attribution line.** Removed `> *Source*: This article was summarized ... using AI in
January 2025` from `composable-design.md` (pure metadata, no content to keep). Its essence overlaps
`methodology.md` and will be absorbed in the layer reorg.

## Decisions Made

- Drop case-only redirects (production keeps only non-colliding ones); guard added to the canon check.
- **Layer standardization approach (Eric chose): "Shared Foundations + lean parallel layers"** — move
  framework-wide `architecture`/`methodology` (and absorb `composable-design`) into a shared *Foundations*
  section; give each of the 3 layers the same shape: Overview -> Tutorial -> Grammar -> Reference ->
  AI guide -> Integration. (Execution pending a target-nav approval — next.)

## Context for Next Session

- **Next: the layer reorg** (the big one). Present/confirm the concrete target nav (Foundations part +
  lean per-layer page sets, new layer Overviews, retire/absorb composable-design), then drill in layer by
  layer starting with Layer 1 (Event-driven). Part IV (Knowledge Graph) is already the template.
- Build gate unchanged + now the case-only-redirect guard. PR still on hold until the full rewrite lands.

## Memory References

- Referenced: `docs-content-canon`, `docs-style-conventions`, `docs-rewrite-architecture`,
  `stack-docs-mkdocs`, `vision-mercury-composable`
- No new facts (the redirect convention is self-documented in `mkdocs.yml` + `check-doc-canon.py`).
