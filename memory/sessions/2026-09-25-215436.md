# Session (2026-09-25T21:54:36.539Z)

**Agent:** Claude Code
**Title:** docs — the rapid-prototyping deploy recipe (external manifest, no rebuild) for AI agents

## Summary (lightweight)

Docs-only change on branch `docs/deploy-without-rebuild`. A live leadership demo deployed an exported graph into the
running `minigraph-playground` example without a rebuild — the export copied to `/tmp/graph/deploy/`, a manifest
there, and `-Dgraph.model.automation=file:/tmp/graph/deploy/graphs.yaml` on the restart — and Eric asked for the
recipe in the AI docs, framed as the **rapid-prototyping lane** (production keeps the manifest inside the artifact).
Added: `ai-agent-guide.md` §"Rapid prototyping — deploy without a rebuild" (`#deploy-without-rebuild`), the
broker's new-session-id choreography in `#hosting` and a pointer from recipe step 7; a Step-4 block in
`build-your-first-graph.md`; the restart callout in `playground-and-companion.md`; the property's runtime override
in `configuration-reference.md`; one clause in `llms.txt`. Gates: `check-doc-canon` OK; `mkdocs build --strict`
clean (scratch venv); ai-contract-provider 19/0/0 (the packaged-link check). No engine change; no new registry claim
(no test pins a `file:/` manifest location). The Rust docs twin is a follow-up that needs its own verification — the
Rust engine has no JVM system property, so its override mechanism differs.

## Incidental sweep

memory-lint flagged two `[overdue]` facts once this log entered the window (sslu 21 > `archive_window` 20):
`l1-caller-checks-reply-status` and `exception-status-from-cause-chain`. Archived with `archive-fact` — both rules
shipped in v4.12.13 (PRs #426, #427) and live in the code and `event-envelope-reference.md`. Not memory references
of this session.

## Memory References

(none)
