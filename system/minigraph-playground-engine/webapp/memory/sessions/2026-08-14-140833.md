# Session (2026-08-14T14:08:38.051Z)

**Agent:** Codex

## Summary

Added the MiniGraph header Run / Instantiate split control under `ui-loop-engineer` run
`ui-graph-run-controls-20260814-09`. Main Run now instantiates the current graph and waits for the
backend acknowledgement before running. If executable graph properties reference `input.body`, the
workflow requests the existing session-specific mock-upload endpoint, opens the JSON modal with derived
path hints, and runs only after upload succeeds. The dropdown performs the same preparation without
running and shows Ready when the backend instance is prepared; a later main click sends only `run`.

The implementation extends the existing typed ProtocolBus and preserves the command console and manual
upload flow. Controls are MiniGraph-only and limited to a connected primary/host session. Delayed text
acknowledgements are causally quarantined instead of being attributed to a retry, graph/export/session
lifecycle changes invalidate stale instances, and workflow modal closure is scoped to its exact upload
path. No backend or persistence contract changed. The Vite production bundle was released into the
Java-served public directory.

Also completed follow-up `ui-loop-engineer` run `ui-file-picker-cancel-20260814-10`: cancelling the
native file chooser now returns to the still-open upload modal instead of bubbling through the dialog
and cancelling the graph-run workflow. Explicit dialog Escape/backdrop/close/Cancel behavior is
unchanged.

## Verification and review

- Focused graph-run workflow/component/modal tests passed after test-first implementation and bounded
  review fixes.
- Final full webapp Vitest passed after the follow-up fix: 35 files, 268/268 tests.
- `tsc --noEmit` passed as part of the production release.
- `npm run release` passed; Vite transformed 649 modules and deployed `index-BHtCgBzG.js` and
  `index-ChdCP1yV.css`. The existing >500 kB main-chunk warning remains non-blocking.
- `git diff --check` passed and the deployed bundle contains the new typed workflow and UI markers.
- Two independent read-only reviewers found stale-ack correlation, modal ownership, Save/export
  invalidation, redundant instantiate, and ARIA menu-focus issues in r1. Bounded r2/r3 fixes and
  targeted rechecks closed every supported finding; no unresolved review finding remains.
- The file-picker cancellation fix was red/green regression tested and received a fresh-context
  read-only review with no actionable findings.

## Memory References

- referenced: webapp-stable-transport-boundary
- created: webapp-acknowledged-graph-run-control
