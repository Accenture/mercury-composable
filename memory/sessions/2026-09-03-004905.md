# Session (2026-09-03T00:49:05.000Z)

**Agent:** Claude Code

## What happened

Closed the evidence gap behind the "off-the-shelf SSE client" claim in the progressive
rendering positioning (Eric noted the existing tests were curl + a hand-rolled .mjs —
which prove the zero-dependency wire, but not library compatibility). Live verification
against lambda-example 4.12.2's `GET /api/hello/sse?delay=400&count=3` (port 8085):

1. **`eventsource` npm package v5.1.1** (the standard Node EventSource implementation):
   4 default `message` events via onmessage + the named `done` event via
   addEventListener, arrivals 33/434/836/1240ms — progressive, zero hand parsing. PASS.
2. **Browser-native `EventSource` (Chromium)**, run in-page on the app's origin:
   identical dispatch, arrivals 9/408/813/1221ms. PASS.

CORS note: `cors_1` defaults `Access-Control-Allow-Origin: ${api.origin:*}`, so
cross-origin browser consumption works out of the box. Evidence appended to
conv-no-streamable-http-claim (its approved wording now stands on verified library
compatibility, not assertion). No engine code touched; test scripts in session
scratchpad only.

## Memory References

- conv-no-streamable-http-claim (evidence appended)
