# Session (2026-08-22T16:49:36.000Z)

**Agent:** Claude Code

## Summary

**The polyglot initiative: designed, ratified (D0–D8), and both wrapper repos rebooted and
proven live — all in one session.** Eric's ask: bring python/node.js into Composable and
MiniGraph without porting the composable foundation.

1. **Design investigation.** My v1 concept (a composable function spawning python/node as
   child processes over stdio) was fully investigated — viable, but it needed
   `@KernelThreadRunner` (pipe I/O and `Process.waitFor` pin VT carriers on JDK 21, the
   BdbElasticStore lesson), a kernel thread per in-flight script, process-tree kill
   engineering, and Sonar command-injection surface. **Eric redirected mid-investigation:
   wrap the existing "Event over HTTP" protocol instead — non-blocking (the relay is
   interceptor-based on the event loop, zero JVM threads per in-flight call) and no
   subprocess, so no stability surface.** The code sweep showed the foundation is
   essentially finished: declarative `yaml.event.over.http` target maps resolved inside
   EventEmitter at all three dispatch paths; the composable-example ↔ lambda-example
   zero-code flow demo ships today; the wire format reference is implementable standalone;
   golden conformance vectors exist (`system/platform-core/src/test/resources/
   envelope-vectors/vectors.json`, shared with Rust); trace context rides the envelope;
   per-route security headers + `/api/event` auth exist. **The ONE engine gap: graph.task's
   `po.exists(route)` guard (GraphTask.java:74; Rust knowledge-graph skills.rs:362) does
   not consult the event-over-http map** — flows work (no pre-check), graphs need a
   surgical relaxation. Spec: `draft-design-specs/polyglot-script-runner.md` (gitignored;
   options A–H compared; stdio-subprocess design preserved as the shelved alternative —
   its niche is single-artifact embedded scripting).
2. **Ratification.** D0–D8 accepted; D7 resolved to non-blocking async design/libs. Two
   in-flight refinements from Eric: wrappers carry the **minimalist utilities**
   (Configuration Management, Logging, Telemetry) in engine-consistent style so
   foundational things are done one way; configuration uses the engines' **`resources/`
   location convention and `-Dkey=value` runtime override syntax**. D2 answered by
   repurposing the legacy repos; D3: the node wrapper IS the sanctioned "fresh node.js
   re-port" answer.
3. **Execution — both repos rebooted on `feature/polyglot-event-over-http`, legacy cleared
   (git history keeps it), org governance files retained.**
   mercury-python: commits `ad7e104` (clear) + `2c92a29` (scaffold), **30/30 tests** —
   **MERGED same day as PR #15, true merge `99ae249`, tree verified identical to the gated
   commit; branches deleted both ends.**
   mercury-nodejs (legacy npm `mercury-composable` v4.3.28): `0ae7a17` + `5f56d1d`,
   **31/31 tests** — **MERGED same day as PR #86, true merge `05e66a5`, tree verified
   identical; branches deleted both ends.** (TypeScript ESM; deps only @msgpack/msgpack + yaml; int64 > 2^53 kept
   exact as BigInt). Both packages: standard-format codec verified against the golden
   vectors (compact detected + rejected with a teaching error); Event API host mirroring
   EventApiService exactly (x-ttl floor 1000, x-async 202 ack `{type,delivered,time}`,
   transport errors 400/403/404/408 set HTTP status, handler errors ride HTTP 200 with
   envelope status, engine-identical messages incl. "Timeout for N ms" / "Route x not
   found"; x-event-api + my_* stripped from handler view; my_cid tag → my_correlation_id
   injected); `preload` registry (instances = semaphore concurrency); PostOffice client
   with the relay's exact HTTP contract (octet-stream, x-ttl, x-no-stream, X-Trace-Id +
   W3C traceparent when 32-hex, +100ms cushion); utilities (AppConfig resources/ + -D +
   `${ENV:default}`; log4j2-pattern logging text|json with LOG_LEVEL; trace contextvar /
   AsyncLocalStorage with reply annotations); `mercury-serve` dev runner.
4. **Proofs (live, this session):** python⇄node cross-wrapper both directions with trace
   context and annotations intact; **the flagship — composable-example 4.11.10's shipped
   `event-over-http-declarative` Event Script flow executed the PYTHON `hello.declarative`
   function unchanged** (zero engine change, zero engine config change): reply carried
   `"language": "python"`, `my_correlation_id` injected from the my_cid tag, x-flow-id +
   x-ttl=15000 delivered, and the python log line showed the ENGINE's trace id
   (`Handled hello.declarative status=200 exec_time=0.019ms trace_id=f894c339…`) in the
   engine-consistent log format.

**Remaining (next phases):** P2 = the graph.task exists-guard relaxation (Java + Rust
lock-step — the only engine change); P4 = docs chapter + demo + interop-report extension on
both engine repos + ADR-0016 proposal; P5 = publishing (npm version strategy vs legacy
4.3.28 and PyPI name availability are Eric's calls). Note: both wrapper repos' legacy CI
configs went with the reboot — fresh workflows ride P4.

## Memory References

- referenced: vision-mercury-composable (blueprint trace), graphjs-phase-out-direction
  (synergy: graph.task → polyglot beats in-engine injection), conv-telemetry-presentation-parity
  (drove the utilities' engine-format consistency), kafka-mesh-opt-in (docs caution to
  reuse), functions-decoupled-routes (extends across languages verbatim),
  trace-thread-keyed-mono-gotcha (background for EventApiService span capture),
  conv-serialization-gotchas (wrapper README notes), thread-docs-improvement-backlog
  (P4 docs), eric-release-rhythm
- created: polyglot-event-over-http-design (tier: working), thread-polyglot-initiative
  (tier: working), bp-polyglot-functions (tier: working)
