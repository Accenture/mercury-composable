# Session (2026-08-11T22:06:00.000Z)

**Agent:** Claude Code

## Summary

**Eric simplified the dry-run graph-id resolution; I reviewed it, agreed, and applied the four
follow-ups. MERGED same session: Java
[PR #280](https://github.com/Accenture/mercury-composable/pull/280) squash `68cd9d28`,
Rust PR #206 merge `3bdcd3b3`, both trees verified identical to the gated commits, both CI
green. Rides the next release via CHANGELOG Unreleased.**

**Eric's patch (reviewed and agreed):** replace the v4.11.8 rejection guard + ephemeral
`playground-{uuid}` fallback with a single line — graphId = root's non-blank `name`, else the
CONSTANT `untitled`. Helpers (stableGraphIdentity/rootName/usesSuspension), the teaching message
and the rejection test deleted; graphId passed as a parameter into the extracted
`instantiateGraph`.

**Why it is right (the durable insight):** the store contract requires the dry-run identity to be
**STABLE ACROSS INSTANTIATIONS**, not *derived from the model name*. A constant satisfies that, so
the v4.11.8 guard was only ever defending against my own unstable fallback — remove the ephemeral
fallback and the guard has nothing left to defend. This is not a reversal of Eric's morning ruling
("stableGraphIdentity will silently fail the resume mechanism"): that objection was correct *given
a uuid*, and his patch removes the premise instead of the capability. Also durable: a *global*
constant beats anything session-derived — the session handle comes from the WebSocket route
(GraphLambdaFunction.java:273-276) so it changes on reconnect, which would have reintroduced the
silent fresh-restart across the realistic leave-and-come-back flow.

**Review method:** 4-lens adversarial workflow (mechanism / collision / coverage / parity+docs),
each finding refuted by an independent skeptic — 11 findings raised, 4 survived. Refuted findings
worth remembering: the `untitled` shared bucket is NOT a new risk class (two developers dry-running
the same NAMED graph with the same literal cid already collide today), and `GraphResume.java:111-113`
rejects a restored record whose node alias is absent from the current graph, so structurally
different drafts fail loudly rather than cross-feeding silently.

**The four follow-ups applied:**
1. **Rust lock-step** — collapsed `stable_graph_identity` to the same constant, deleted the guard,
   `root_name`, `uses_suspension` and the now-orphaned mirror test (whose doc-comment named the
   deleted Java test); added `const UNTITLED`.
2. **The missing pin (the one I pushed back on)** — the reviewers judged a test for the `untitled`
   branch optional; I judged otherwise because that is precisely the shape of the original bug (an
   unpinned "just the fallback" identity), and the ONLY branch with no coverage.
   `unnamedDraftResumesAcrossInstantiations` + Rust twin sketch a nameless suspend/resume draft
   through companion commands (edge-mode: drawn edge into suspend + continuation edge, mirroring
   unit-test-suspend-2) and assert suspend → resume across two instantiations.
   **Both mutation-proven:** re-introducing a per-instantiation handle fails them on the store-key
   assertion; restored after.
3. **CHANGELOG** — new `## Unreleased` → `### Changed` on both repos. The v4.11.8 section (published
   hours earlier, stating the rejection) was deliberately NOT rewritten.
4. **Stale comment** — CompanionSyncTest.java:214 named a guard that now greps to zero hits.

**Sonar round (Eric, mid-session):** the new test tripped S5961 at 28 assertions — Sonar counts
assertions reached through the private `syncCommand` helper, so each of the 12 graph-building calls
counted. Fixed by collapsing the build into ONE loop over a `List.of(...)` of commands (Sonar counts
static call SITES, so 12 → 1): now 22 vs the sibling's 24, both under the 25 limit. Mirrored in the
Rust twin for structural symmetry even though Rust has no such lint.

**Gates:** Java engine module 118/118 exit 0; Rust workspace 58 suites / 305 tests exit 0 +
clippy 0 + `cargo fmt --check` clean.

Also: **v4.11.9 release PREPARED on both repos (lock-step), with a live field-deployment
regression drive.** Java `release/v4.11.9` commit `b134e1ca` (33-pom sweep 4.11.8→4.11.9, zero
hazards/remnants, CHANGELOG "## Version 4.11.9, 8/11/2026", full reactor exit 0); Rust
`release/v4.11.9` commit `40f99dc8` (Cargo.toml + lock + CHANGELOG; 58 suites / 305 tests exit 0,
clippy 0, fmt clean). **Live drive against the built v4.11.9 artifacts** (redis-standalone +
minigraph-playground, real WS sessions): (A) NAMED path — tutorial-14's four runs all resumed
(fresh → resume → resume → resume, through to "shipped"); (B) UNNAMED path (the release's actual
content) — a nameless draft sketched via companion commands suspended to
**`graph:untitled:rel-untitled-9`** in Redis, then a second instantiation with the same cid
resumed (`run: resume`, restored `first: step-one-done`) and the record was consumed. Drive
lesson: my first attempt at (B) failed on MY script, not the product — I invented a
`v1.hello.world` task route, and a `reset` mid-session left the previously imported tutorial-14
nodes in the model (the traversal log showed the mix); redone on a FRESH session with
graph.data.mapper steps. App log clean — the only WARNs are my own harness (WS holder exiting)
plus the pre-existing late-callback `companion.sync.*` consumer-unregistered noise. **SHIPPED:** Java release
[PR #281](https://github.com/Accenture/mercury-composable/pull/281) squash `eff46c5f` (tree
verified vs `b134e1ca`), CI green (7m52s); Rust release PR #207 merge `27fa527e` (tree verified
vs `40f99dc8`), CI green (2m20s); tags `v4.11.9` on both, dereference-verified. Remaining: Eric
publishes both GitHub releases. Both apps left RUNNING on v4.11.9.

## Memory References

- referenced: thread-dry-run-graph-scope-fix (the v4.11.8 fix this supersedes),
  graph-suspend-resume-design (store contract), thread-release-4-11-8,
  conv-telemetry-presentation-parity (why the Rust half is mandatory)
- created: thread-untitled-dry-run-identity (tier: working), thread-release-4-11-9 (tier: working)
