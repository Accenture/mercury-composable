# Session (2026-09-16T04:15:00.000Z)

**Agent:** Claude Code
**Summary:** v4.12.10 tagged and released (`92e94f2b`, 04:01:46Z) — `latest_release` bumped with the
real hash and publish time. Then ran the **memory review**, overdue by 5 sessions, which also tripped
the invariant re-verification cadence at 44 sessions.

## What happened

### v4.12.10 released

Verified tag → `92e94f2b` (the release squash) and the GitHub release published 2026-09-16T04:01:46Z,
not a draft, **before** editing continuity — the reason `latest_release` is held back until the tag
exists is precisely so the hash and time are recorded rather than invented. The entry records the
2-PR scope, the retirement plus ADR-0024, the Redis-separation guidance, the benchmark log, and that
no lockstep is needed for this release's content (platform-core internals + docs, no wire contract);
the outstanding lockstep remains v4.12.9's distributed cache.

### Memory review

Window: 15 sessions since `2026-09-14-193941.md` — 5 past `review_every` 10. Steps 2–3 were a no-op:
`refresh-metadata` reported every footer already matching, because it has been run after each
session's log this cycle. The judgement work was steps 4–5 and 7.

**Archived 1 fact.** `conv-kafka-transitive-pin-placement` at sslu 21 > `archive_window` 20. Worth
naming the tension: this is *episodic-recurrence* knowledge — where to place a pin vs an exclusion
across the six Kafka declaration sites — and a session-count window punishes knowledge whose triggers
arrive on their own schedule rather than the project's. Archived anyway, because archiving is a move
and retrieval is lexical: the next Kafka CVE session should find it via `archive/INDEX.md`. Flagging
the pattern rather than special-casing the fact.

**Swept 1 completed thread.** `ot-streaming-return-route`, `[x]` and sslu 25. The other three closed
threads (`ot-redis-client-separation`, both elastic-queue threads) completed this cycle and stay as
stubs until they age out.

**Condensed the closed-thread stubs.** I had written 10–29-line close records this cycle; the protocol
specifies 3–6 lines, with the narrative living in the origin logs. Trimmed all three — 67 → 25
narrative lines. My own drift, corrected by the ritual that exists to catch it.

**Invariant re-verification is DUE** — 44 sessions since 2026-09-11, threshold 40. Raised
`ot-reverify-invariants-20260916` listing all 18 core facts grouped by section plus the Vision. The
review never auto-invalidates; Eric confirms or supersedes. I flagged two for a closer look without
ruling on them: `stack-language-java21` carries a *field-state* claim ("the toolchain STAYS on 21
until the majority of field installations run Java 25") that ages on its own, and
`conv-reentrantlock-not-synchronized` is explicitly written to decay when that moves — so they
resolve together.

**Contradiction / altitude scan:** no conflicts found. The newest facts
([[conv-ports-adopt-java-release-number]], [[elastic-queue-file-store]]) are consistent with the
older ones they touch — the latter complements `conv-reentrantlock-not-synchronized` and
`virtual-threads-rpc` on carrier pinning rather than competing with them. Every `(blueprint)` thread
still names `serves: vision-mercury-composable`.

**Size triggers:** all clear — continuity 537 lines (max 1000), decaying facts 34 → **32** after the
moves (max 35, so the review bought headroom that was nearly gone), closed-thread narrative 25 (max
150).

## Memory Review (2026-09-16, through 2026-09-16-032012.md)
- Reactivated:    0
- Superseded:     0
- Archived:       1  fact → memory/archive/2026-Q3.md (faded — conv-kafka-transitive-pin-placement, sslu 21)
- Swept threads:  1  completed thread → archive (ot-streaming-return-route, sslu 25); 3 recent ones condensed to stubs
- Archive-verify: pass (memory-lint: no archived id referenced within archive_window; no id in both places)
- Tier changes:   0  (refresh-metadata found every footer already current)
- Invariants:     **prompted** — 44 sessions ≥ 40; 18 core facts + the Vision up for re-confirmation (ot-reverify-invariants-20260916)
- Promoted core:  0  (auto-core off; core is human-set)

## Memory References
- referenced: eric-release-rhythm (tag/publish gated; release close-out),
  conv-ports-adopt-java-release-number (the no-lockstep-needed line in latest_release),
  elastic-queue-file-store + minigraph-dev-mode-app-shape (the v4.12.10 release content),
  ot-distributed-cache (named as the outstanding lockstep)
- created: ot-reverify-invariants-20260916 (tier: working)
