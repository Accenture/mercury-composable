# Session (2026-09-03T17:41:00.000Z)

**Agent:** Claude Code

## What happened

Follow-up to the session-broker contribution (previous log `2026-09-03-172753.md`), at Eric's
direction: implemented the AI-docs improvements that the `ai-enabled-repo-demo` feedback items
9–10 proposed, directly in the guide sources under `docs/guides/` (bundled into the
ai-contract-provider at build time via the pom's `guides/**` resource include).

1. **`knowledge-graph/ai-agent-guide.md`** — three additions:
   - Rules of engagement reworded: "single operator" → turn-taking (co-editing with humans is
     the design intent); new explicit statement that **session sync is symmetric** (every
     command except `session` topology commands propagates to primary and all subscribers —
     equal co-authorship).
   - New section **"Hosting the session yourself" {#hosting}**: the agent-as-host topology, the
     4-step WS handshake (`/ws/graph/playground`, welcome frame, plain-text
     `session ws-NNNNNN-N started`, 20s ping/pong), restart caveat, and the
     `scripts/playground-session-broker.mjs` reference.
   - New section **"Scaffolding a project from the template" {#scaffolding}**: a boilerplate
     manifest (file-by-file keep/trim table grounded in the template) and **two named
     `rest.yaml` profiles** — `headless-minimal` (`http.flow.adapter`,
     `post.companion.command.sync`, `get.live.graph`) and `playground-enabled` (adds
     `get.index.html`, `get.ws.html`, `show.graph.model`, `upload.json.content`,
     `upload.mock.content`, `inspect.state.machine`), with `llm.stream.relay` and the mock
     routes marked example-specific. This gives an agent a contract to trim against instead of
     the diff-the-template workaround (the `layer-3-agent-independent` over-trim incident).
     At Eric's follow-up request the two profiles are also rendered as a **copy-paste
     `rest.yaml` boilerplate block** (full entries with per-route comments; delete the
     `[playground-enabled]`-marked routes to get `headless-minimal`), with the operator-facing
     upload dialogs called out: `upload.json.content` = the console `upload` command's
     paste-a-JSON dialog, `upload.mock.content` = the `upload mock data` dialog.
2. **`event-driven/ai-agent-guide.md`** and **`event-script/ai-agent-guide.md`** — new
   "Scaffolding a project from the template" sections grounded in `lambda-example` and
   `composable-example` (file manifests; minimal `rest.yaml` = your own routes only /
   one `http.flow.adapter` entry per endpoint; the `snake.case.serialization=true` test-property
   trap called out).
3. **`playground-and-companion.md`** — `session subscribe` wording fixed: table comment now
   "join that primary session as an equal co-author"; paragraph now states commands mirror
   **both ways** (the old "mirror another session's commands into yours" read one-directional
   and misled an agent in the field).

Parallel edits landed in the Rust repo's guides (its own session log); the Rust
`playground-and-companion.md` already said "mirror both ways" and needed no fix.

## Memory References

- Referenced: playground-session-broker
