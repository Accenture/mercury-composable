# Session (2026-07-05T04:24:00.000Z)

**Agent:** Claude Code

## Summary

P2 (harden) of the ElasticQueue file-FIFO effort (branch `feature/elastic-queue-file-fifo`; commit
`2885d9e3`). See [[thread-elastic-queue-bdb-to-file]] and [[elastic-queue-file-fifo-plan]].

### Root-caused the 582 ms `file` write outlier — it is NOT GC
Re-ran the worse-case `file` benchmark with `-Xlog:gc`. The stall reproduced (**582.6 ms write at
t=161.8s**), but the GC log shows the **max GC pause in the whole 180 s run was 2.0 ms, with zero pauses
> 100 ms** (healthy young collections, 333M→27M, no full GC). So the stall is an **OS dirty-page-flush
throttle**: writes only, late in the run (dirty pages accumulate), high-throughput unsync'd (we don't fsync).
**Decision (Eric):** no in-store background flusher (adds a thread, against the simplicity goal); instead
**elevate P5** (per-route VT off-loading — makes any on-loop spill stall harmless, the principled fix) and
**document tmpfs** as an immediate ops mitigation (transient store on RAM ⇒ no disk flush). Design spec §5.11,
P5 elevated to RECOMMENDED, P2 notes updated.

### Hardening tests — `FileElasticStoreTest` (5, all green)
FIFO across many segment boundaries; **O(1) reclamation / bounded disk** under sustained overflow (live
segment peak stays < 20, does not grow with op count — proves consumed segments are deleted); **no leftover
files across 50 reuse cycles**; clean reuse-after-close (no stale data); degenerate inputs (null/empty
ignored, peek/read consistency).

### Small enabling refactor
`FileElasticStore.segmentBytes` is now a **per-instance** config read
(`elastic.queue.segment.size.bytes`, default 16 MB, min 512 B) instead of static-once — testable segment
rolling + less static state; `baseDir` made package-private for test assertions.

**Verification:** full platform-core **381 tests green** (parity + facade + 5 new hardening), coverage gate met.

**Status: P2 largely done** (hardening + root-cause + mitigation decided). Remaining P2: external-IO fault
injection needs a mockable seam (deferred — current behaviour wraps IOException as unchecked, parity with
BDB). **Next: P3 — A/B benchmark `file` vs `bdb` against P0 targets + canary; then P4 retire BDB; P5
(recommended) VT off-loading.**

## Memory References
- Referenced: elastic-queue-file-fifo-plan, thread-elastic-queue-bdb-to-file
