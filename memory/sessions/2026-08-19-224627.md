# Session (2026-08-19T22:46:27.000Z)

**Agent:** Claude Code
**Lightweight:** agent-memory Mode B upgrade v4.34.1 → v4.34.2 (one rung); no
mercury-composable memory facts touched.

Ran the `4.34.1 → 4.34.2` rung — the fix for **this team's own field issue** (the
`[secret-material]` self-knob false positive reported 2026-08-19): re-copied the
tool-managed `memory-lint` built-in (both runtimes, both test suites, SKILL.md —
description unchanged, adapters untouched) and stamped `.agent/version.md` → 4.34.2.
The rung's drop-obsolete-waivers step was a **no-op**: this repo's session logs had
dodged the FP by rewording to prose ("the advisory setting of the … env knob"), so no
waiver existed; documenting the knob in assignment form (`AGENT_MEMORY_SECRET_GUARD=advisory`,
either spelling) is now safe. Verified here: mirror suites **50/50 both runtimes**;
full `memory-lint` 0 errors (the repo's own review-cadence advisories remain its next
working session's business); scratch probe — documented knob value passes, an opaque
value under the same key still flags.

## Memory References
(none — tooling upgrade only; no continuity facts referenced or created.)
