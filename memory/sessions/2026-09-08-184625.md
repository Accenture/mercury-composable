# Session (2026-09-08T18:46:25.000Z)

**Agent:** Claude Code
**Lightweight:** MiniGraph Playground connect-UX round on `feat/playground-connect-ux`
(PR #330 — the eight-round UI arc — merged as squash `74a8b1d9`, clean title; branch
retired). Eric's direction: connecting nodes was too hard (invisible hover anchors), a
dialog for connecting is a bad idea, and the Neo4j graph browser is the gold standard to
borrow. Shipped three pieces, verified end-to-end on the dev server (tutorial-12):
**halo-ring connect** (body = move, perimeter ring = connect; evenodd clip-path donut;
full-body drop targets light up mid-drag), **click-to-connect** (context-menu "Connect
to…" + pick-target banner), and an **anchored ConnectionPopover** at the drop point
(18-relation colored chips = one-click create, free text for custom; Esc cancels) —
`ConnectionDialog` and `GraphAuthoringModals` deleted, zero modals left. 237 vitest tests
green (4 new popover tests). Live proof: ring drag from clear-exception onto person-name →
chip `details` → edge created + auto-refresh; Esc path created nothing; hit-testing
verified via elementFromPoint; test edge deleted (`delete connection {a} and {b}`).
Bundle + jars rebuilt; 8085 restarted for Eric. Webapp memory: fact
`webapp-connect-ux-neo4j-halo` + session-log round 9 in
`system/minigraph-playground-engine/webapp/memory/sessions/2026-09-08-164102.md`.

**Second round (Eric's follow-up): connections are clickable + Delete removes them.**
Click an edge → blue selected stroke; Delete/Backspace fires the backend
`delete connection {a} and {b}` (new `buildDeleteConnectionCommand` +
`validateDeleteConnectionAliases`) through React Flow's delete pipeline with local removal
ALWAYS blocked — the graph redraws from the backend confirmation; selected nodes are
deliberately ignored (node deletion keeps its confirmed context-menu flow). Also fixes the
latent default-Backspace client-side-only deletion. 239 tests green (2 new builder tests);
live proof: selected the dictionary→person-name edge, Delete → 9→8 edges via backend +
auto-refresh, then restored the tutorial edge. Bundle + jars rebuilt; 8085 restarted.

**Third round: UNDO (Eric ratified frontend-only compensating commands — "we want the
backend to be lightweight - our minimalist design principle").** Every UI mutation
captures a pre-mutation snapshot and pushes inverse console commands (`undoCommands.ts`
through the command-builder boundary): connection delete ⇄ re-connect both directions;
node edit ⇄ re-submit the full-replace snapshot; create node/connection and node delete
covered too. `useGraphUndo` (depth 20) + Ctrl/Cmd+Z + Undo buttons in mutation toasts
(new ToastAction support); invalidation = disconnect + import only; no redo; console
commands untracked; batch delete not undoable; no Rust lock-step obligation (webapp-only).
246 tests green (7 new inverse-builder tests). Live proof: edge delete undone via BOTH the
toast button and Ctrl+Z; node edit reverted to the exact prior value; import cleared the
stack. Found live: window keydown `event.target` can be a non-Element — `instanceof
Element` guard before `.closest`. **Eric's cascade question caught a real race:** the
backend can process a later single-line `connect` before an earlier multi-line
`create node` completes ("node fetcher not found") — the undo executor now PACES inverse
commands (await each `graph.mutation` confirmation, 2.5s timeout, concurrent-undo guard);
node-delete undo then verified live: fetcher + all 4 touching relations restored.

## Memory References
(none — webapp-local facts recorded in the webapp memory bank per its scope split)
