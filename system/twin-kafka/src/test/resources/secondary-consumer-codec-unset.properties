#
# SecondaryConsumerCodecTest fixture: secondary.schema.registry.consumer.properties is deliberately ABSENT, so
# the secondary flow adapter shares the secondary producer's codec - the unchanged default. Also the config the
# test's secondary producer codec is built from (the bundled secondary template, all commented out).
#
secondary.schema.registry.cache.ttl=1h
