# Session (2026-09-03T19:02:45.000Z)

**Agent:** Claude Code

## What happened

Guide refinements to `docs/guides/knowledge-graph/ai-agent-guide.md`, distilled from two
fresh-agent live builds run today in `ai-enabled-repo-demo` (branches `layer-3-live-java` and
`layer-3-live-rust` there — Java engine: 33/33 companion commands first-try, 8/8 conformance;
Rust engine: 42/42, 8/8; both with a human subscribed to the broker-hosted session throughout):

1. **Export step (recipe step 7) rewritten:** export is file-based to `/tmp/graph/{name}.json`;
   delete any stale file first, verify fresh content after — the success line alone cannot be
   trusted (this engine's 4.12.x export reports `ok:true` while writing nothing when refusing an
   overwrite; the Rust engine refuses loudly — divergence filed as feedback item 11 in the demo
   repo's `docs/feedback/mercury-composable-docs-feedback.md`, which Eric relays).
2. **New best practice (recipe step 2 + pre-send checklist), from Eric:** stamp the root node
   with `name={graph-id}` and `created={current timestamp}` at creation — `name=` authorizes
   overwrite on export, `created=` makes each build content-unique so re-exports always save;
   also neutralizes the mirrored-export subscriber symptom (feedback item 12: symmetric session
   sync executes `export` in subscriber sessions too, where the missing `name=` success-stamp
   shows the watching human an overwrite refusal while the primary succeeded).

## Memory References

- Referenced: playground-session-broker
