# Session (2026-09-14T19:12:15.000Z)

**Agent:** Claude Code

## sync-over-async Redis config: soa. namespace + redis.* fallback + two-key cluster select (field compat)

Continuation of the clustered-Redis work ([[soa-redis-cluster-support]], origin
2026-09-14-181948) — made it drop-in compatible with the field's L2 distributed-cache
client after reviewing the field's design extract (kept out of the repo per
compartmentalization; the cache repo/secrets-manager names are client-identifying).

- **Auth compatibility, verified against the field design.** The field's cache client auths
  two ways and both map to what I already built, identical Lettuce calls / wire AUTH, no AWS
  SDK: `token` → `withPassword(token)` = my username-blank + password path; `username-password`
  → `withAuthentication(user, pass)` = my username-set path. I infer the mode from whether
  `soa.redis.username` is set, so no explicit `auth.type` key is needed. The field's token is a
  static secret (ElastiCache AUTH token / vault password), NOT IAM/SigV4 — confirming the
  earlier call to keep IAM's `RedisCredentialsProvider` as an unused future seam.
- **Credential timing, verified.** Field credential loader = `@MainApplication(sequence=9)`;
  sync-over-async autostart = `@MainApplication` default sequence **10** ("smaller runs first"
  in the engine) → loader runs first. Contract: the loader must publish resolved creds to the
  property names sync-over-async reads. The @PreLoad health check re-resolves lazily so it
  heals once creds land ([[preload-before-mainapp-lazy-config]]).
- **soa.redis.* namespace + redis.* fallback (Eric).** All the module's Redis keys moved to
  `soa.redis.*` so sync-over-async no longer collides with a co-resident `redis.*` consumer
  (the field cache lib, or minigraph-state-redis) in the same app. Each key falls back to the
  un-prefixed `redis.*` via the config reader's nested default
  `getProperty(soaKey, getProperty(redisKey, default))` — so **no breaking change** (existing
  redis.* deployments keep working). The `soa.redis.health` ROUTE rename (#377) has no fallback
  and stands. Updated the health check's now-false "minigraph shares the same keys" claim.
- **Two-key cluster selection (Eric).** Replaced the 3-value `RedisClusterMode` enum with two
  keys so a cache-style boolean stays intact: `soa.redis.cluster.detect=auto` (default) probes
  `INFO`; anything else defers to the boolean `soa.redis.cluster.mode` (true/false), which is
  ALSO the inconclusive-probe fallback (robust when INFO is restricted). RedisConfig now carries
  `autoDetectCluster` + `clusterEnabled` booleans; the enum file was deleted.
- Files: RedisConfig (namespace + fallback + two-key + auth from earlier), RedisBackendFactory
  (two-boolean logic, inconclusive→boolean fallback), RedisHealthCheck (soa. keys + fallback +
  reworded doc), SyncOverAsyncAutoStart (log line), demo + extension test resources, guide +
  configuration-reference, CHANGELOG (reframed as non-breaking). Tests: RedisConfigTest
  (fallback, soa-wins, two-key resolution), RedisBackendFactoryTest (booleans).
- Verified: sync-over-async **85/85 green**.

## Memory References
- soa-redis-cluster-support — updated (soa.redis.* namespace + redis.* fallback; two-key
  detect/mode selection replacing the enum; auth-compat-with-field conclusion; sequence-9<10
  credential-timing verification)
- preload-before-mainapp-lazy-config — consulted (credential bootstrap sequencing + the health
  check's lazy re-resolution)
- soa-transport-neutral-cid — consulted (the module's config/credential surface)
- eric-release-rhythm, conv-squash-title-prefill-check — consulted (branch/commit/PR prep)
