# Session (2026-07-13T13:58:41.000Z)

**Agent:** Claude Code

The field Snyk gate REJECTED v4.8.2 (screenshots in /tmp/snyk-issues). Root-caused and
remediated all three transitive findings on branch `fix/snyk-4.8.2-remediation`
(commit `1e9a0e0a`, 30 poms):

1. **httpcore5/httpcore5-h2 5.4.2 → 5.4.3 (HIGH, CWE-770)** — flagged in all six
   Confluent-chain modules. KEY LESSON: Spring Boot's `dependencyManagement` pins
   `httpcore5.version=5.4.2` (SB 4.1.0), and **managed versions beat nearest-wins** —
   direct dependency pins in minimalist-kafka fixed only that module and did NOT
   propagate to consumers (unlike the scram-client 3.3 precedent, where no management
   was in play). This is also why Snyk reported "no supported fix". Correct idiom:
   override the SB **property** (`<httpcore5.version>5.4.3</httpcore5.version>`) in
   every module resolving the chain — minimalist-kafka, twin-kafka, sync-over-async,
   kafka-demo, twin-kafka-demo, sync-over-async-demo.
2. **log4j-api 2.25.4 → 2.26.1 (MEDIUM, CWE-116)** — four poms (minimalist-kafka,
   twin-kafka, sync-over-async, opentelemetry-forwarder) never declared
   `log4j2.version`, so SB's default applied. SWEEP LESSON: the 4.8.1 dependency
   refresh updated only poms that already HAD the property — a version sweep must also
   check for poms *missing* the property when the parent supplies a vulnerable default.
3. **reactor-netty-http 1.3.5 → 1.3.6 (MEDIUM, CWE-319)** — reactor-bom
   2025.0.5 → 2025.0.6 in all 28 poms importing it (incl. non-reactor api-playground +
   pg-example).

Verified: reactor-wide `dependency:tree` audit = ZERO vulnerable versions remaining
(benchmark-reporter also checked clean); full reactor build green with all tests;
pg-example 7/7 standalone. Branch pushed; PR text handed to Eric. Next: Eric merges,
then likely a 4.8.3 bump for field redelivery.

## Memory References
- Referenced: [[release-4-8-2-shipped]] (the rejected release; this remediation follows it)
- Referenced: [[release-4-8-1-shipped]] (the dependency sweep whose property-gap this exposed)
