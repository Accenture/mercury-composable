# Session (2026-07-01T23:02:46Z)

**Agent:** Claude Code

## Summary

Follow-on to the Protobuf removal session: Eric asked whether `confluent.version` (8.2.0) should bump to
Confluent's current 8.3.0, and separately whether the Kafka client (`kafka.version=4.2.0`) should follow
Confluent's compatibility table pairing 8.3.x with Kafka 4.3.x.

**`confluent.version` → 8.3.0: recommended and applied.** Verified before applying: diffed the resolved
dependency tree (8.2.0 vs 8.3.0) — clean, just Confluent's own version bumps plus a
`swagger-annotations`→`swagger-annotations-jakarta` swap and two split-out artifacts
(`kafka-avro-types`/`kafka-schema-types`); checked OSV for every new/changed artifact — zero vulnerabilities;
ran `minimalist-kafka`'s full test suite — 49/49 green, including the stock-Confluent-serializer interop
tests. Now that Protobuf is gone, `confluent.version` only touches `kafka-json-schema-serializer`/
`kafka-avro-serializer`, so the wire-runtime-jvm CVE that drove the Protobuf removal is unaffected either way.

**`kafka.version` → 4.3.x: deferred, not rejected.** Talked through the reasoning rather than just doing it:
Confluent's 8.3.x↔Kafka-4.3.x pairing describes what they test their *full platform* against, not a hard
requirement for us — this repo already deliberately pins its own Kafka client independent of Confluent's
suggestion (excludes their transitively-suggested `kafka-clients`), precisely because the Confluent
serializers implement the stable `Serializer`/`Deserializer` contract and tolerate client-version drift by
design. No CVE drives this bump either direction (checked: 0 vulns at both 4.2.0 and 4.3.1). The blast
radius is much larger than a serializer-library bump, though: `kafka.version` is pinned in **24 pom.xml
files**, including the embedded KRaft broker used across many modules' integration tests — a broker version
carries real behavioral risk (config defaults, controller behavior) that a pure library bump doesn't. Eric
agreed to defer it as a separate, dedicated maintenance item.

Fixed the stale pom.xml comment ("Confluent Platform 8.2.x aligns with Apache Kafka 4.2.x") to reflect the
new state and record why the Kafka-version pairing isn't being followed here.

### Verification
Final full-reactor `mvn test` run with `confluent.version=8.3.0` in place (see Memory References for status).

### Next
Eric will commit + push this to `feature/deprecate-simple-type-matching` (PR #130), then resume code review
on the event-script-engine and minigraph-playground-engine improvements that are the actual subject of that PR.

## Memory References
- Referenced: [[minimalist-kafka-protobuf-removed]]
- New: [[minimalist-kafka-confluent-8-3-0]], [[thread-kafka-client-version-upgrade]]
