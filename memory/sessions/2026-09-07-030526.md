# Session (2026-09-07T03:05:26.000Z)

**Agent:** Claude Code

The claims-fixture gate (the coverage study's directional recommendation, designed and
implemented) plus the study's full "next" phase — Eric's direction: "design the
claims-fixture test to incorporate the learnings. Proceed with next and directional steps."
Branch `feat/claims-fixture-gate`: `a25c255a` (the gate) + `567924f2` (next-phase docs).
**Proposed ADR-0023** (drafted in `draft-design-specs/claims-fixture-gate.md`) awaits Eric's
approval at the PR gate.

## What happened

- **The gate**: `docs/guides/claims-registry.json` (11 claims, each = normative sentence +
  pages + engine-test pin; a claim states EXACTLY what its test pins); check-doc-canon
  **check 8** (quote presence, whitespace-normalized case-insensitive + test-pin existence as
  a method DEFINITION, hardened against malformed registries and call-site false matches);
  7 new `Claim*Test` classes + 4 existing pins (test code only — no main-code changes).
  The registry ships in the packaged contract (files.list) and is listed in llms.txt — it is
  itself a grammar asset.
- **Pin quality**: the error.code pin was mutation-tested (renaming the engine constant
  failed exactly that test); kafka threshold boundary-pinned from both sides (399 vs 400);
  reserved-key set fails on additions AND removals; config yml-wins guarded against vacuous
  passes. All 11 pins green in a consolidated per-module run (5 modules exit 0, incl.
  ai-contract-provider packaging).
- **Empirical corrections the pinning produced** (the gate improving the docs before it even
  ran in CI): (1) envInstances — a raw `${...}` literal is FATAL at boot (uncaught
  path-compiler exception escapes preload's catch list), NOT the silent no-op the study's
  verifier concluded; docs and the merged study report corrected, and the
  annotations-reference parameter TABLE (line 84) that still taught the fatal syntax was
  rewritten. (2) decision values — 0/negative integers clamp to branch 1; only
  above-branch-count aborts; my first draft of the syntax.md paragraph was wrong and was
  fixed pre-commit.
- **Next phase shipped**: behavioral notes on the verifier-named pages (decision semantics,
  graph.extension edge behaviors incl. the corrected graph-id-vs-flow:// routability split,
  tracing worker-return deadline, event-over-http loaded-once + the
  [[event-api-local-routes-only]] invariant now stated ON the page, default error body,
  ttl abort semantics, kafka body-type/cid/timestamp/auto.offset.reset/CSFLE-auth notes,
  direct-binding path-param example, PoJo shape, Playground URL, suspended-reply 200);
  config-reference completeness (new Knowledge Graph section, schema.registry.serde.*,
  graceful degradation); 11 llms.txt annotation touch-ups (map ~17.5KB ≈ 4.4k tokens).
- **Verification discipline**: 3 max-effort adversarial refuters audited every new statement,
  the registry, check-8 code (found two crash shapes + the call-site regex hole — fixed), and
  the spec (composition/count corrected; "verbatim" claim honesty-adjusted). Four registry
  claims narrowed to pinned scope. Workflow runs: pins `wf_74d8d878-674`, verify
  `wf_c27a0b42-107`.

**Post-push scan reconciliation** (`11966cef`): Eric's local scan (Ruff FURB188 in the
pre-existing check-7 slug code; IntelliJ always-constant parameter + assertEquals arg order)
reconciled with my preemptive Sonar-lens pass (7 missing assertion messages, NIO file ops,
/tmp-path rationale) — one hygiene commit, no logic changes, all touched tests re-run green.

## Memory References

- referenced: event-api-local-routes-only (its rule is now documented on event-over-http.md —
  the invariant consulted to write the note), compilegraph-mandatory-gate (the
  compiled-or-404 map annotation + config-reference entry draw on it), graphjs-phase-out-direction
  (the skills-reference map annotation names the deprecation), conv-telemetry-presentation-parity
  (gate design made engine-neutral for the held Rust sweep), eric-release-rhythm (branch
  prepared/pushed; PR-open and ADR approval gated), conv-declare-consulted-references (this
  list follows it)
- created: (none yet — a claims-fixture continuity fact lands when Eric accepts ADR-0023)
- Superseded: (none)
