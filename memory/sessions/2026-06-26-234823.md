# Session (2026-06-26T23:48:23.000Z)

**Agent:** Claude Code
**Lightweight:** memory-only — record the DLQ enforcement-boundary rationale.

Eric articulated why the DLQ logic belongs *in* `minimalist-kafka` rather than as a doc recommendation: a
recommendation is not enforceable, but baking retry → per-topic DLQ → commit-gating into the library makes
"no silent loss" **structural/enforced** for every consumer. The library's responsibility ends at **durable
capture** (message lands in its per-topic DLQ); **reprocessing** (read `<topic>.dlq` → fix → replay) is
**per-business-domain logic, deliberately out of scope** (schemas/remediation differ per topic, not
enforceable). Responsibility line: library = durable capture (enforced); domain = reprocessing (not enforced).
Folded into the `kafka-flow-failure-dlq` fact. No code change.

## Memory References
- Updated: `kafka-flow-failure-dlq` (added the enforcement-boundary rationale)
