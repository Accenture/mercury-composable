# Session (2026-09-16T02:41:48.000Z)

**Agent:** Claude Code
**Summary:** ADR-0024 accepted by Eric — status Proposed → Accepted in the ledger, and the
pointer aligned on the four surfaces that had named it pending (continuity now carries the bare
`(ADR-0024)` tag like every other formalized fact; the CHANGELOG entry and both thread close-records
drop "(proposed)"). Commit `1b4311f7`, pushed to PR #399. No new facts or decisions — the decision
itself is recorded in [[elastic-queue-file-store]] and its session log, `2026-09-16-020051.md`.

### PR #399 merged, and the first milestone benchmark

PR #399 merged 02:52:48Z as squash `9aa40089`; CI was green on the true head (`4f30ba75`) - checked
per-commit, since the earlier run had started against `6989e4e7`. Gated close-out verified
`state == MERGED` and that main carries the change **by content** (BdbElasticStore gone, 0 sleepycat
in the platform-core pom, ADR-0024 reading `Accepted`, the contract test present), then
fast-forwarded and deleted the branch. The remote branch did **not** auto-delete this time, unlike
every prior PR this session - removed explicitly after confirming the content was in main.

Then ran `benchmark-reporter` against merged main and recorded the finding as a timestamped pair,
`analysis/v4.12.10-20260916-025610Z.{html,md}`, indexed from a new milestone table in
`analysis/README.md`. Commit `63fca62a` on `chore/v4.12.10-benchmark-baseline` (PR-open gated).

**Result: no regression from collapsing ServiceQueue's dispatch.** The scenario that matters - the
latency probe measured while a *different* route's spill runs - improved at every percentile against
the July baseline (p99.9 1.54 → 1.141 ms, max 1.62 → 1.386 ms). Callback-flood p99.9 rose ~8%, which
the finding explains rather than waves off: that scenario is queue-bounded by Little's law and its
throughput was flat, so the delta is queue-residency drift.

Two process points worth carrying forward:

- **Verify what you benchmarked.** I unpacked the harness jar to confirm zero `BdbElasticStore`
  entries and zero `je-*` jars before trusting the numbers. The artifact still reads `4.12.9` because
  the v4.12.10 sweep has not run, so the version string alone would not have proved it.
- **`benchmark-reporter` is deliberately outside the reactor** (commented out in the root `pom.xml`),
  so the README's documented `mvn -pl … -am package` fails outright. Fixed to the standalone `-f`
  form, plus the timestamped-report convention. Found only by running it - the kind of stale
  instruction that survives indefinitely in a module nobody builds routinely.

### Merged

**PR #399** merged as squash `9aa40089` (verified by content before close-out). **PR #400**
(benchmark baseline + `BENCHMARK-LOG.md`) merged 03:09:16Z as squash `99d5b245` — Eric merged it
directly as docs-only. Close-out verified both by content, fast-forwarded main to `99d5b245`, and
removed the branches.

**v4.12.10 content so far:** the Redis client-separation docs, the BDB retirement (P4) with ADR-0024
accepted, and the benchmark baseline. The release itself is not cut — the version sweep still has to
run, and the v4.12.9 sweep machinery (43 build files) is fresh enough to reuse.

## Memory References
- referenced: elastic-queue-file-store (the fact ADR-0024 formalizes; the benchmark is its standing
  regression signal), thread-elastic-queue-bdb-to-file, thread-elastic-queue-docs-adr,
  eric-release-rhythm (PR-open gated), merge-verification-is-a-gate
