# Session (2026-09-21T23:39:28.889Z)

**Agent:** Claude Code (Fable 5.1)
**Title:** v4.12.14 release branch prepared — the lock-step release with the Rust port (K5 + its lock-step round); the Java demo drove the two-engine Kafka + Schema Registry interop unchanged

## What happened

- **Eric's K5 rulings (Rust side, recorded here for the release):** port the Schema Registry if viable (it
  was — JSON Schema and Avro; CSFLE refused, never degraded), mirror the Java `sync-over-async-demo`, and
  release **v4.12.14 on both engines** once the Rust lock-step round completes.
- **This repository's part: `release/v4.12.14`** (branch from `7def2bb4`): the version sweep re-derived —
  **43 build files / 98 occurrences**, unchanged from 4.12.13 (pom.xml + build.gradle only, the Snyk
  placeholder poms included) — and the CHANGELOG entry derived from `git log v4.12.13..HEAD`: one
  non-memory commit, #436 (`group.protocol=auto` as the bundled consumer template's default), with the
  lock-step paragraph naming what the Rust port catches up with at this number. Full build at the new
  version: (filled at persist time). PR-open, merge and tag are Eric's gates — **PR #437 opened by Eric** (release/v4.12.14 → main, `08182a5c`); CI pending at the time of writing (Build & Unit Tests, Starter Templates Gradle; the memory checks green).
- **The Java demo as the interop counterpart (unchanged, 4.12.13 jar):** the Rust `sync-over-async-demo`
  mirror was driven against it on `kafka-standalone` + `redis-standalone` + `schema-registry-standalone`
  4.12.13 — Java backend + Rust facade and Java facade + Rust backend with the Confluent JSON Schema and
  Avro frames decoded in both directions (Confluent's serializers ⇄ the Rust codec), a mixed
  `system-of-record-group` (20 requests, 11 Java / 9 Rust), and the Java facade delivering 8 of 20 replies
  to the waiting Rust facade through the Redis return route — the route is a wire contract both engines
  honour. Record: the Rust repository's `docs/test-reports/minimalist-kafka-interop.md`, K5 addendum.
  Observed on the Java side, not a defect: `AssociatedNameStrategy: Associations endpoint not found (404),
  using fallback strategy` per schema call against the mock registry (it lacks that endpoint; Confluent
  falls back).
- **Doc gap noted, not fixed here:** neither engine's guides stated the CompileGraph task↔skill gate
  (4.12.12 #406) — the Rust skills reference now does; the Java `skills-reference.md` graph.task section
  could carry the same sentence in a follow-up.

- **Later in the same working session (memory-neutral commits recorded here rather than in a new
  log):** PR #437 MERGED 2026-09-22T00:05:52Z (squash `dbc26f31`; main reads 4.12.14; the release branch
  cleaned after the merge-verification gate); tag `v4.12.14` and the GitHub release remain Eric's next
  steps. Eric's follow-up ruling for the Rust side — "Open-Telemetry forwarder feature is missing in Rust"
  — moved the Rust 4.12.14 cut behind the forwarder port, so the Rust CHANGELOG at this number carries it;
  the Java engine needs no change (its module has shipped since 4.12.11). Eric also asked about the
  closed Rust PR #305: nothing to reactivate — its head `0da85068` is on the Rust main through #303
  (`753eb9fe`) and its branch is deleted.

- **RELEASED 2026-09-22:** tag `v4.12.14` → `e8a8d8e5` (pom verified at the tag), GitHub release published 02:11:55Z;
  the Rust port released the same number at 02:12:43Z (mercury #308 → `b83c493f`, tag → `2c88fe2b`). Verified from the
  repositories (tags, versions at the tags, `gh release view`); main has no commits past either tag. crates.io
  publication of the 12 Rust crates is Eric's next step (commands handed over, dependency-first).

- **The Rust crates.io publication completed 2026-09-22 ~02:30Z** — 12 of 12 mercury crates at 4.12.14 (five for the
  first time), verified from the registry; the lock-step release is complete on both engines.

- **CHANGELOG follow-up (Eric: "please update the CHANGELOG follow-ups on both repos"):** the 4.12.14 entry now names all
  five first-time crates, the forwarder port on the Rust side and the completed registry state — branch
  `docs/changelog-4.12.14-crates` `6fe31620`, docs only, PR pending (Eric); the Rust twin `aad0690b` adds the post-tag
  keyword fix behind the sync-over-async publication. Lesson folded into [[conv-changelog-from-tag-range]]'s spirit: an
  entry written before a decision it describes must be re-read against the final state before the tag.

## Memory References
- kafka-group-protocol-auto-default (consulted — the one 4.12.14 item; its release note reused)
- conv-changelog-from-tag-range (applied — the entry derived from `v4.12.13..HEAD`)
- conv-template-version-sweep (applied — build files only, count re-derived)
- snyk-retired-manifest-placeholders (consulted — the placeholder poms are in the sweep)
- conv-ports-adopt-java-release-number (applied — the Rust port adopts 4.12.14 in one step)
- eric-release-rhythm, eric-code-changes-via-pr (consulted — branch + PR text; no merge, tag or publish)
