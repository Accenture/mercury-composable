# Session (2026-07-30T17:33:41.000Z)

**Agent:** Claude Code

## What happened

**v4.11.0 RELEASED AND PUBLISHED on both repos in lock-step — the suspend/resume
feature release.** Eric ratified the release in his California morning (timezone fact
recorded in personal memory: he is US Pacific; UTC stays in the ledger, PT in
conversation).

**Java (mercury-composable):** release-prep branch `chore/release-4.11.0` → PR #245 →
squash `3a870951` → tag `v4.11.0` on the VERIFIED merge commit (pom + CHANGELOG checked
at the exact commit before tagging; squash-merge means the branch commit is not an
ancestor — content checks are the real verification) → published. Contents beyond the
feature arc: **the skipTests fix** (Eric's pre-release request) — every module pom
carried a hardcoded `<skipTests>false</skipTests>` in its surefire config, which
SILENTLY OVERRIDES the command-line `-DskipTests` (proven empirically: tests ran with
the flag before, "Tests are skipped" after); removed from all 26 poms (surefire 3.5.3
pin kept; the 2 poms with environmentVariables kept their siblings; CI's `mvn verify`
unaffected; full 29-module quick build now ~34s vs ~5-6 min). Version sweep: 33 poms
(the 32 historical + minigraph-state-redis; zero substring collisions). CHANGELOG dated
`Version 4.11.0, 7/30/2026` and completed with the trim + cross-engine-validation
entries.

**Rust (mercury):** agent-driven prep → PR #189 → merge `cc529071` → tag placed →
**then Eric's docs rulings arrived** (the Reference section was missing the Release
Notes link; the home footer was inconsistent with the Java model) → docs-parity fix
PR #190 (Release Notes nav link; "Where to go next" restructured into the Java-style
"Explore the docs" clusters; Project block with the mirrored "Java version:"
cross-engine line) → **tag MOVED pre-publication** (deleted + re-tagged on the
docs-inclusive merge `167484bd`, verified before and after; the 4.10.2-round
precedent) → published. **Eric's ruling recorded: release tags must include the
updated docs — and a tag never moves after publication.**

**Release evidence stack:** engine suites both repos (Java reactor with tests at
4.11.0; Rust 297/clippy 0), the scripted 50/50 cross-engine drive, Eric's manual
interop regression ("everything works as advertised"), and the permanent report on
both doc sites.

**Field significance (Eric):** the suspend/resume feature is CORE FUNCTIONALITY for a
few installations — regression sensitivity on this surface is now field-critical on
both engines; treat suspend/resume behavior changes with release-level care.

**Docs-nav rulings this release day (both repos):** the Java tab row exceeded screen
real estate → Code/Documentation Conventions folded into Reference (#244); Rust folded
Background into Reference (#188) + gained the Release Notes link and Java-consistent
footer (#190). Cross-check both sites after nav changes — Eric caught both gaps from
the live sites.

## Memory References

- referenced: thread-release-4-11-0 (completed this session),
  thread-graph-suspend-resume, compilegraph-mandatory-gate,
  graph-suspend-resume-design, conv-helpers-docker-less,
  conv-telemetry-presentation-parity, bp-graph-workflow-suspension (closure proposed
  to Eric at the closeout — human gate), vision-mercury-composable (current-state
  version line refreshed to v4.11.0 during the sweep)
- updated: thread-release-4-11-0 → [x] SHIPPED AND PUBLISHED
