# Session (2026-09-23T01:46:50.000Z)

**Agent:** Claude Code

## Summary

**Release v4.12.15 shipped on all four runtimes** — the seam close. Eric opened, merged and tagged the four release PRs
within the hour: Java #451 (squash `aafeff04`, 01:27Z; PR CI and main CI green — build and unit tests, Gradle templates,
memory), mercury #322 (merge `87ee371f`, 01:21Z; the `rust` workflow green on main), mercury-python #37 (`95101575`,
01:19Z), mercury-nodejs #105 (`13426732`, 01:19Z). Tags `v4.12.15` and GitHub releases on all four — Java → `b705e9ff`
(released 01:35:26Z), Rust → `28cd3328` (01:36:57Z), Python → `7bf6991` (01:37:51Z), Node → `4c43ffe` (01:39:01Z) — each
tag one memory-only commit past its merge, the version verified at the tag, each release body the CHANGELOG entry. Every
merge was gate-checked (state MERGED + ancestor of `origin/main`) before its release branch went; Eric's parked stash
untouched. The registry publications (crates.io, PyPI, npm) had not started at
persist time — Eric's step; the commands were handed over.

## Seam close

- `latest_release` → v4.12.15; the release chain condensed to pointers (history belongs in the CHANGELOG and the origin
  logs), the review and invariant stamps and the [[connected-edge-spans]] merge chronicle condensed the same way —
  the `[continuity-bloat]` lines advisory asked for exactly that.
- [[v4-12-15-release-prep]] CLOSED; its carry-overs moved to the new [[post-4-12-15-carry-overs]] thread (Eric: the AI
  SDLC/MCP backlog resumes first).
- [[connected-edge-spans]]: SHIPPED in v4.12.15.
- Memory review (size trigger) — the block below.

## Method notes

- A count that comes out of a `tail` is not a count: the release queue filtered each gate's output inline and truncated
  both the Maven module summaries and the Rust result lines. The Java count came from this build's surefire reports
  (218 suites, 1505 tests, 0 failures), the Rust count from a full re-run captured whole (603 passed, 9 ignored — the
  earlier "467" was the truncation). Capture whole, filter afterwards ([[conv-changelog-from-tag-range]]'s re-verify rule).
- Release notes = the CHANGELOG entry, verbatim, on all four repos (Eric asked for them; the convention held).
- Eric's "tagged and published" meant the GitHub releases; the registry publications are a separate step with their own
  commands (handed over: `cargo publish --workspace` from the tag; `uv build` + `uv publish`; `npm publish`).

## Memory Review (2026-09-23, through 2026-09-23-014650)
- Reactivated:   0
- Superseded:    0
- Archived:      0  facts (nothing past `archive_window` — no `[overdue]` after the refresh)
- Swept threads: 0  (the eight completed threads are all within `archive_window`; they sweep from later sessions on)
- Archive-verify: pass (memory-lint 0 errors; no id in both places)
- Tier changes:  6  (3 working→active — connected-edge-spans, redis-connection-reset-on-timeout, v4-12-15-release-prep;
  1 archive-candidate→active — otel-optional-service-and-negative-control; 2 active→archive-candidate —
  ot-distributed-cache, ot-subgraph-for-each-suspend) + 16 uses-only bumps (`refresh-metadata`, 22 footers)
- Lines: 1032 → 1000 (the release chain, the review and invariant stamps, the connected-edge-spans chronicle, the P10 and
  forwarder addenda condensed — narrative lives in the origin logs); the facts advisory persists by design at 40 > 35
- Invariants:    not due (30 sessions since 2026-09-16, cadence 40)
- Stalled threads: none (thread-doc-improvement-feedback-loop last used 2026-09-21, window 40)
- Promoted core: 0

## Memory References

- referenced: connected-edge-spans (SHIPPED — condensed), minigraph-dev-mode-app-shape (P10 addendum condensed),
  otel-optional-service-and-negative-control (addenda condensed), platform-onshutdown-lifecycle,
  kafka-group-protocol-auto-default, eric-release-rhythm, eric-code-changes-via-pr, conv-changelog-from-tag-range,
  conv-ports-adopt-java-release-number, conv-template-version-sweep, snyk-retired-manifest-placeholders,
  conv-squash-title-prefill-check, conv-declare-consulted-references, bp-agent-orchestration
- created: post-4-12-15-carry-overs (tier: working)
- closed: v4-12-15-release-prep

## Next

- Eric: the crates.io / PyPI / npm publications — recorded here when visible (the `latest_release` lock-step line; the
  packs' `otel-forwarder-lockstep` threads close). Then the AI SDLC/MCP backlog ([[bp-agent-orchestration]]).

Commit: `git add memory/ && git commit -m "session 2026-09-23 [Claude Code]"`
