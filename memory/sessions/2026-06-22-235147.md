# Session (2026-06-22T23:51:47.000Z)

**Agent:** Claude Code
**User:** Eric Law — upgrade agent-memory in this repo to pull in the new ADR upkeep trigger (validate cross-repo propagation).

## What We Did

Upgraded this repo's agent-memory layer **4.14.1 → 4.15.0** by executing the tool's new
`4.14.1 → 4.15.0` rung (the rung itself was authored upstream this session — see the
`agent-memory` tool repo, commit `cd69701`). The rung re-syncs the generic ADR-protocol
guidance and stamps the version; nothing in `docs/arch-decisions/ADR.md` was created or
rewritten (the log was already adopted).

Applied:
- **`AGENTS.md`** — **merged** the new "keep it alive" maintenance trigger into this repo's
  *customized* ADR paragraph (preserving the adopted-at-`docs/arch-decisions/ADR.md` text + path),
  rather than overwriting from `templates/AGENTS.md`. Trigger: a new durable architecture decision,
  or supersession/invalidation of an `(ADR-NNNN)`-tagged fact, → **propose** a human-gated ledger
  update.
- **`.agent/schema.md`** — added the generic "When to maintain it" paragraph to the `docs/ADR.md`
  section.
- **`DECAY.md` §12** — the *Design* primitive now notes the ADR lifecycle is kept in sync with
  fact supersession.
- **`.agent/version.md`** — stamped `version: 4.15.0`, `last_upgraded: 2026-06-22`; preserved
  `enabled_with: 4.14.1`, `mode: A`.
- **`continuity.md`** — `adr-pattern-adopted` fact extended with the v4.15.0 upkeep note.

Skill-adapter sync (a Mode B step) was **not** run: it only (re)writes gitignored vendor adapters
(no tracked change) and is orthogonal to this ADR-propagation validation.

## Decisions Made

- **Merge, not overwrite, on `AGENTS.md`** — this repo's ADR note is repo-customized (adopted at a
  non-default path); the rung's customized-target clause says to merge the trigger and keep the
  repo-specific text.

## Context for Next Session

- The ADR pattern here is now **self-honoring**: future sessions reading `AGENTS.md` + the
  `adr-pattern-adopted` fact are told to propose ADR-ledger updates on supersession / new durable
  decisions (human-gated). This closed the trigger gap identified earlier today.
- **Validation result:** a protocol change made in the agent-memory tool (v4.15.0) propagated into
  this target via the upgrade ladder — confirming future targets inherit it on their own upgrade.

## Memory References

- Referenced: adr-pattern-adopted
