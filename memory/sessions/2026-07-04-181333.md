# Session (2026-07-04T18:13:33.000Z)

**Agent:** Claude Code

## Summary

Three reviewer-driven `minimalist-kafka` **test-infrastructure** improvements on
`feature/kafka-regex-dlq-commit-mode` (PR #133). All test-only — no production code touched. Full module
green (78 tests, coverage gate met) after each.

### 1. topic-pattern end-to-end test (reviewer proposal `/tmp/minimalist-kafka-topic-pattern-e2e-proposal.md`)

Real gap: `topic-pattern` only had mock-based unit coverage (`KafkaFlowConsumerTest#subscribesWithPattern...`)
+ config-validation (`KafkaFlowAdapterConfigTest`). Nothing exercised it through a real embedded Kafka +
`subscribe(Pattern)` + Event Script pipeline. Verified the proposal's technical claims against the code
before implementing (metadata header type-coercion in TaskExecutor; explicit-partition publish path in
SimpleKafkaNotification/KafkaRequestPublisher; no test collisions). Added:
- `KafkaTestSupport.createTopic(bootstrap, topic, partitions)` overload (2-arg delegates to it).
- Activated the previously-commented `topic-pattern: 'events\.[a-z]{2}'` binding in `kafka-flow-adapter.yaml`.
- New `kafka-pattern-sink-flow.yml` (maps `input.metadata.topic/partition/offset -> header.*`) + registered
  in `flows.yaml`; new `KafkaPatternSinkTask` (own `RECEIVED` queue).
- `KafkaFlowAdapterTest#topicPatternRoutesConcreteTopicMetadataThroughFlow`: publishes to `events.de`
  (2 partitions, pinned partition 1), asserts the sink sees the record's **actual** topic/partition/offset
  (not the binding's regex) end-to-end. `boot()` creates `events.de` before `AutoStart.main()`.
- Trimmed the proposal's unused second topic (`events.fr` was created but never published to).

### 2. JSON/Avro naming clarification (reviewer suggestion)

The JSON path was named with the generic word "schema" (`SCHEMA_TOPIC`/`schema-test-topic`/
`schemaFramedMessageDecodedIntoFlow`), hiding that JSON Schema and Avro are two *parallel* Confluent-wire
cases. Confirmed both already had real embedded e2e coverage (not just codec-level). Renamed the
JSON-specific identifiers to `JSON_TOPIC`/`json-test-topic`/`jsonSchemaFramedMessageDecodedIntoFlow`
(+ group `json-test-group`, tracePath `TEST /json`); clarified comments. Deliberately kept the shared
`schema-sink-flow`/`SchemaSinkTask` name at that point (both bindings still routed there).

### 3. Split the shared schema sink into two flows + two sinks (Eric's suggestion)

Eric identified the shared `schema-sink-flow`/`SchemaSinkTask` as an agile-iteration artifact (JSON added
first, Avro bolted onto the same flow/sink) and asked to split for readability. This also removes the
latent shared-`RECEIVED`-queue coupling (the earlier race-condition discussion: safe under Maven's
sequential defaults and class-level forking, only JUnit *method*-level parallelism would expose it — now
moot). Before splitting, confirmed the "one decoder handles both wire formats by embedded schema id"
capability is fully proven at the codec level in `SchemaCodecTest` (JSON + Avro round-trips + stock-Confluent
decode), so the shared-flow e2e demonstration was redundant — splitting loses no real coverage. Changes:
- New `json-sink-flow.yml` -> `JsonSinkTask` (route `json.test.sink`, own `RECEIVED`) and `avro-sink-flow.yml`
  -> `AvroSinkTask` (route `avro.test.sink`, own `RECEIVED`).
- Rewired `flows.yaml`, `kafka-flow-adapter.yaml` (each binding -> its own flow), `KafkaFlowAdapterTest`
  (JSON test polls `JsonSinkTask.RECEIVED`, Avro polls `AvroSinkTask.RECEIVED`).
- Deleted `schema-sink-flow.yml` + `SchemaSinkTask.java`.
- Cross-referenced `SchemaCodecTest` in both new flow comments + the test class comment so the next reader
  knows why there are two flows but the codec is shared.

### Race-condition analysis (asked mid-session, no code change)

Confirmed no race under current Maven config: surefire 3.5.4 with no `<configuration>` (sequential,
forkCount 1), no `junit-platform.properties`, no `@Execution`/`@ResourceLock`, JUnit 6.0.3 defaults to
sequential. Class-level forking (surefire forkCount / a future Gradle `maxParallelForks`) splits by class,
not method, so same-class methods never parallelize. Only JUnit method-level parallelism would have exposed
the shared queue — and split #3 removed that coupling anyway. No Gradle build exists yet
(`thread-add-gradle-build` still open).

**Status: three test improvements done, full module green, committed & pushed.**

## Memory References
- Referenced: kafka-flow-consumer-cid-header, business-vs-internal-correlation-id-terminology
