# Session (2026-06-22T19:08:07.000Z)

**Agent:** Claude Code
**User:** Proceed with the layer reorg ("Shared Foundations + lean parallel layers"). Pass 1 = Foundations + Layer 1.

## What We Did — Reorg Pass 1 of ~3 (Foundations + Layer 1)

Began the approved IA reorg (`docs-content-canon` conformance order step toward standard per-layer shape).
The target nav was approved by Eric. This pass:

- **New Part II · Foundations** — lifted framework-wide `architecture` + `methodology` out of the old
  "Layer 1" grouping (they describe the whole framework, not just the event-driven layer).
- **New Layer-1 Overview** — `docs/guides/event-driven-foundation.md` (functions/route names/PostOffice/
  EventEnvelope/event bus/virtual threads; code-true, canon-compliant). It's the lean "Overview" the
  layer lacked (architecture was framework-wide).
- **Absorbed `composable-design`** — its essence (the 6-benefit payoff list) folded into `methodology`
  ("What composable design buys you"); the standalone essay retired (`git rm`) with a redirect →
  `methodology`. Inbound links (index.md, methodology See-also) repointed.
- **Nav restructured to 7 Parts** with explicit "Layer N —" labels: I Orientation · II Foundations ·
  III Layer 1 Event-driven · IV Layer 2 Composable · V Layer 3 Knowledge Graph · VI Operate · VII Reference.
  `build-test-deploy` moved to Operate (it's cross-cutting, not Layer-2-specific).

Verified: canon check + 3 grammar checks pass; `mkdocs --strict` 0 warnings; new overview renders;
`composable-design` redirects to `methodology` (non-colliding).

## Context for Next Session

- **Reorg Pass 2 = Layer 2 (Composable orchestration):** add a lean Overview (the big `event-script/index`
  currently mixes overview + tutorial + syntax — split or front a short overview); confirm the layer's
  spine (Overview → Tutorial → Grammar → Reference → AI guide). Then Pass 3 polish + Layer 3 is already the template.
- PR still on hold until the full rewrite lands. Pushed through the redirect-fix commit; Pass 1 committed locally.

## Memory References

- Referenced: `docs-content-canon`, `docs-rewrite-architecture`, `functions-decoupled-routes`,
  `typed-io-map-or-pojo`, `virtual-threads-rpc`, `stack-docs-mkdocs`, `vision-mercury-composable`
- Open thread added: `thread-layer-reorg` (in progress)
