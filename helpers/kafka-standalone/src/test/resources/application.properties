application.name=kafka-standalone

#
# Start both standalone brokers (9092 via server.properties, 8092 via server2.properties)
# so the MainApp end-to-end test exercises the two-server startup path.
#
dual.servers=true

#
# websocket configuration
#
#websocket.idle.timeout=60

#
# cloud.connector= none
#
cloud.connector=none
