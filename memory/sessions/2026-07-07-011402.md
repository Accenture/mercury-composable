# Session (2026-07-07T01:14:02.000Z)

**Agent:** Claude Code

Second field Sonar round on branch `chore/sonar-quality-gate-4.6.2` (screenshots in `/tmp/sonar2`).
Continues [[2026-07-06-235842]] / [[thread-sonar-quality-gate-4.6.2]].

## Result of the field re-scan
Snyk **passed**. Sonar coverage now **passes at 85.4%** (≥80% gate on 2.5k new lines) — the coverage work
landed. Gate failed only on **issues**: 3 blocker + 1 critical + 8 major (21 new issues total). All fixed
in one commit (`fb8dcaf3`).

## Fixes (12 issues, 7 files)
- **S2699** (blocker, add assertion) — the `bench.run`-gated benchmark tests in platform-core test sources
  (`DispatchBenchmark`, `ElasticQueueBenchmark`) had no assertions. Added meaningful ones: processed
  counters == dispatched counts; `ops > 0`. (These are NOT excluded in the field — `sonar.exclusions`
  props are ignored by the sonar-scanner CLI, per [[2026-07-06-223151]].)
- **S1192** (critical) — `GetVersionFunction`: extracted `NOT_FOUND_SUFFIX` constant (literal `"' not
  found"` used 3×).
- **S5778** (major, assertThrows lambda with >1 throwing call) — extracted the `List.of(...)` setup out of
  the assertThrows lambdas in `ReactiveDbTest` (batch guard) and `SqlStatementTypesTest` (2 spots). These
  were in the coverage tests I added last session.
- **S3824** (major, Map.get+condition) — `SchemaStore.register` now uses `containsKey` (for the first-time
  flag) + `computeIfAbsent` instead of `get()==null` then `put()`. Behavior-preserving (verified by the
  15 schema-registry tests).
- **S125** (major ×2, comment looks like code) — reworded the two Confluent JSON-shape comments in
  `SchemaRegistryTest` to plain prose (the `{...}` braces tripped the code detector).
- **S2925** (major, Thread.sleep) — `DispatchBenchmark.runBatch` now uses `Utility.sleep()` (the
  blocking-queue helper); dropped the now-unneeded `throws InterruptedException`.
- **Restricted identifier** (major) — renamed `ElasticQueueBenchmark.Histogram.record()` → `observe()`
  (`record` is restricted since Java records).

Verified: platform-core test-compiles (benchmarks are `@EnabledIfSystemProperty(bench.run)`, so Sonar
static-analyzes but never runs them); schema-registry 15 tests + reactive-postgres 19 tests green.

Follow-up (`ef1958f1`): removed a leftover `System.out.println(result)` debug line (**S106**) from both
copies of `GraphTests` — `system/minigraph-playground-engine` (the scanned one) and the
`examples/minigraph-playground` copy (cleanup; examples aren't scanned).

Note: many of these (SchemaStore, GetVersionFunction, benchmarks) are flagged as **new code vs main** —
the schema-registry module + the benchmark harnesses are recent, so their whole surface is "new" to the
gate. Pushed for the next field re-scan.

## Round 3 touch-ups (`a3ed6ecf`) — mostly IDE/SonarQube-for-IDE findings

Eric ran the IDE (SonarLint + IntelliJ inspections); `/tmp/sonar3`. Fixed:
- **S3577** (test-class naming) — renamed the two benchmark harnesses to the `*Test` convention
  (`DispatchBenchmark`→`DispatchBenchmarkTest`, `ElasticQueueBenchmark`→`ElasticQueueBenchmarkTest`). They
  use `@Test`, so Sonar treats them as test classes; `@EnabledIfSystemProperty(bench.run)` still keeps them
  skipped in a normal `mvn test` (nothing runs unless the flag is set). Dropped the now-stale "class name
  doesn't match *Test pattern" javadoc note; updated the `-Dtest=` examples.
- **S1130** — removed the unused `throws Exception` on `DispatchBenchmarkTest.dispatchThroughput` (a
  consequence of round 2 dropping `runBatch`'s `throws`; confirms `Platform.registerPrivate` throws nothing
  checked).
- **Month enum** — `SqlStatementTypesTest` now uses `LocalDate/LocalDateTime.of(2026, Month.JANUARY, ...)`.
- Redundant `(String) null` cast removed in `ReactiveDbTest` (query's 2nd param is already String).
- Cleanups: removed the dead `overflow` histogram field in `ElasticQueueBenchmarkTest`; reworded two
  comments whose stray `"` tripped an unpaired-symbol inspection.
- Skipped pure IDE noise (`vthread`/`checkpointer` spellcheck, javadoc blank-line hints).
- Verified: platform-core test-compiles; reactive-postgres 19 tests green.

## Memory References
- Referenced: [[2026-07-06-223151]], [[2026-07-06-235842]], [[thread-sonar-quality-gate-4.6.2]]
