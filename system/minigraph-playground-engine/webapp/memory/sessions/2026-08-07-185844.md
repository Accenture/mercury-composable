# Session (2026-08-07T18:58:58.473Z)

**Agent:** Codex

## Summary

Added the requested Help-style onboarding hint to the compact bottom-right minimap control under
`ui-loop-engineer` run `ui-minimap-hint-20260807-04`. On the first usable Graph canvas, a dark
popover points at the 28px logo and reads `Ctrl + M to toggle minimap` with a keyboard-token chip;
the logo pulses while the hint is fully visible. The popover is canvas-safe, associated with the
toggle through `aria-describedby`, announced as status content, and contains a keyboard-semantic
dismiss action with visible focus treatment.

The one-shot lifecycle lives in `useMinimapHint`, called by `GraphView` above its keyed render-error
boundary. It accumulates exactly three seconds of eligible on-screen time and then fades for 400ms.
Inactive Graph tabs, maximized Help that clips the canvas, and focus inside the hint pause the
remaining time. Clicking the hint returns focus to the minimap logo before fading; opening the
minimap removes it immediately and consumes onboarding even when the hint had not yet become
eligible. Existing Ctrl+M guards, click toggling, default collapse, 28px/14px sizing, graph-remount
state persistence, pannable minimap navigation, node colours, mask, background, and disabled wheel
zoom remain intact. Technical documentation and the checked-in Java-served assets were updated.

## Verification and review

- Tests were written before each implementation/fix and failed for the missing hook, Help-maximized
  eligibility, exact remaining-time behavior, pre-eligibility opening, and focus safety. Final focused
  coverage passed 15/15.
- Full webapp Vitest passed: 24 files, 228/228 tests.
- `npm run release` passed, including `tsc --noEmit`; Vite transformed 643 modules and emitted only
  the existing >500 kB main-chunk warning before deploying to Java public resources.
- `dist/` and `../src/main/resources/public/` have exact directory parity; the built bundle contains
  the Ctrl+M hint and accessible dismiss label. `git diff --check` passed.
- The package has no lint script, so no lint command was available.
- Fresh correctness and delivery reviews found four lifecycle/accessibility edge cases: paused time
  had reset, the clickable status lacked keyboard semantics, maximized Help could consume an unseen
  hint, and opening could overlap the fade. Targeted fixes closed all four. A bounded recheck then
  found and closed pre-eligibility one-shot consumption and focus-removal regressions; the third and
  final recheck reported no remaining findings.
- Residual manual boundary: no browser automation is installed, so pixel-level appearance and a
  physical-browser keyboard/mouse smoke test remain manual checks.

## Memory References

- referenced and updated: webapp-toggleable-minimap
