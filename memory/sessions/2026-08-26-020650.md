# Session (2026-08-26T02:06:50.000Z)

**Agent:** Claude Code

**Field re-scan follow-up: the new-code coverage gate — one test covers the S2139
rethrow.** The field's re-scan of the remediation drop cleared all six Sonar issues from
[PR #296](https://github.com/Accenture/mercury-composable/pull/296) and **the Snyk
secret scan PASSED** (the GUIDANCE rename worked) — but the quality gate failed again on
a new condition: **new-code coverage 0.0% < 80%, on exactly 1 new line** —
`OfflineSkillWriter.java:75`, the S2139 rethrow itself. Root cause: the export's catch
path was never exercised (all three existing tests fail BEFORE the try block: 409
pre-existing snapshot, 400 invalid root), so the old log-and-rethrow lines were equally
uncovered — but old code doesn't count against the new-code ratio and the replacement
line does. **Durable shape: an S2139-style rewrite of an uncovered catch block converts
invisible legacy uncoverage into a gate-failing new-code condition — pair such fixes
with a catch-path test in the same PR.**

Fix (branch `fix/offline-export-catch-coverage`, gated commit `e6a73945`):
`wrapsAnExportFailureWithContextAndCause` — forces a real I/O failure inside the try
(POSIX read-only export root → `Files.createDirectory` throws `AccessDeniedException`)
and pins the #296 failure contract: status 500, message names the export target, the
`IOException` survives as the cause. Assumption-guarded (`assumeTrue` posix view;
`assumeFalse(Files.isWritable(...))` catches permission-ignoring environments like
root), permissions restored in `finally` so `@TempDir` cleanup works; the assertThrows
lambda stays single-invocation (S5778-safe) and `assertTrue` returns as a USED import.
Gates: module 19/19 (the new test ran for real locally, not skipped); jacoco L75 `fc`
(fully covered) → the gate condition computes 1/1 = 100%; **mutation-proven** — cause
dropped from the rethrow → the test fails on "the underlying cause must survive"
(expected IOException, was null); reverted green.

Also this working session, before the re-scan: Eric's post-merge IDE polish (redundant
`@SuppressWarnings` removal) folded straight to main as `fdf82808` — recorded in the
2026-08-25-231243 log.

**MERGED same day.** [PR #297](https://github.com/Accenture/mercury-composable/pull/297)
squash `a6db32a2` == gated `e6a73945` (tree verified, title clean per
[[conv-squash-title-prefill-check]]); CI full reactor green 6m37s (the ubuntu runner is
POSIX + non-root, so the new test RAN there rather than skipping); branches deleted both
ends. **Upstream loop also closed:** agent-memory 4.38.1 shipped the `GUIDANCE` rename
and this repo upgraded (parallel session log 2026-08-26-002631) — zero `SECRET_GUIDANCE`
anywhere; the interim local edit converged into upstream's own edition as designed.
Remaining external: the field's final gate re-check.

## Memory References
- Referenced: thread-field-scan-remediation-20260825 (updated),
  thread-sonar-4-11-x-field-round-3, thread-ai-contract-provider,
  eric-release-rhythm, conv-squash-title-prefill-check
