# Session (2026-09-18T22:10:24.871Z)

**Agent:** Claude Code

Lightweight: Mode B upgrade 4.41.0 → 4.41.1 (PATCH, guidance only — an id names the thing, never its kind:
no `ot-`/`thread-`/`bp-` prefix, since the section, the `(blueprint)` tag, the `serves:` trace and the
`thread-<id>.md` filename already carry the kind; an existing id is never renamed, because immutable session
logs are refresh-metadata's only input; new stalled-thread gates are named `close-stalled-threads-<YYYYMMDD>`
and a gate already open under the earlier `ot-` name keeps it; from this team's 2026-09-18 note). Reconcile
re-copied DECAY.md, REVIEW.md and .agent/schema.md. Protocol — unchanged (byte-identical to the template; no protocol text changed in 4.41.1). Rung step 2 — nothing renamed — 14 of 14 thread files carry a kind-prefixed id and stay as they are.
Adapters re-synced; stamped 4.41.1; reconcile converged. memory-lint: 0 errors, 1 warning(s). Upstream
release: https://github.com/Accenture/mercury-go/releases/tag/v4.41.1 .

## Memory References

(none)
