# Session (2026-09-23T22:18:23.000Z)

**Agent:** Claude Code

## Summary

Eric asked for a review of two field issues and ruled both: **#453** (an unresolvable mapping source deletes the
target, contradicting the old "skips the entry" doc) is a genuine Layer 2 / Layer 3 inconsistency — Layer 3 adopts
Event Script's rule; **#454** (a simple plugin over an absent source aborts the traversal, and the abort names
nothing) is by design for the plugin half — plugin null handling is plugin-specific — and is answered in the docs.

**What the code did (verified, 4.12.15 main):** `GraphLambdaFunction.handleDataMappingEntry`,
`fillFetcherApiParameters` and `getForEachMapping` removed ANY target on a null source (indexed target → null),
while `setOutputMappingEntry` skipped every null, `model.*` included; Event Script (`TaskExecutor
.doInputDataMappingEntry` / `setInputDataMappingModelVar`) clears only a `model.*` target — removed when the source
key is absent, set to null when the key exists with a null value — and for any other target ignores an absent source
and propagates an existing null. The reporter read 4.12.7 docs; PR #432 (v4.12.13) had already moved the doc to the
code ("A null source removes the target"), so at 4.12.15 doc and code agreed on the behaviour Eric now changed.

**Fix (commit `7068678f`, branch `fix/l3-null-source-mapping-parity`, PR to follow):** one helper,
`applyNullSource(stateMachine, lhs, target)` — `model.*` target: `keyExists(lhs)` or indexed → set null, else
remove; any other target: set null only when the source key exists — applied to the mapper/math/js MAPPING path,
`for_each`, the `model.*` half of fetcher/extension parameters (a non-model parameter from a null source stays "not
supplied", the staging area is fresh per execution) and the graph.task/extension/fetcher output mapping, which now
clears a `model.*` target on a null result (FLAGGED to Eric for veto — Event Script's output mapping clears too).
`unit-test-lookup-1` gained the second half of the rule (`text(kept) -> output.body.kept` survives an absent
overlay; `model.probe` is still removed). Module: 21 suites, 134 tests, 0 failures (counts from surefire XML; the
first `-o` run died resolving jacoco 0.8.15 from PR #452 offline — run online once). Docs: command-reference
Namespaces paragraph rewritten (rule, scope, defaults from the source side); in-Playground `help graph-data-mapper`
gained a "Null source" section; the Event Script guide now states its own rule in "Input/Output data mapping" (it
was only ever stated as a parenthetical in the L3 doc) and its plugin section answers #454 (null arguments are
plugin-specific; `f:defaultValue`/`f:isNull` accept null, the arithmetic family throws; a plain mapping of the same
absent source is skipped; guard in a preceding entry because plugin calls do not nest — `getValueFromSimplePlugin`
passes a nested `f:` argument as null on purpose). Claim `null-source-removes-target`: text, quote and `since`
(4.12.16+) updated, id kept. Docs gates green (four anti-drift scripts, `mkdocs build --strict`). Tutorial 7's
"clear idiom" (`model.none -> model.address`) targets a model variable, so it stays valid on both engines.

**Live evidence on the rebuilt Playground (broker-hosted dry-run session, then torn down):** the reporter's
#453 repro on `model.threshold` still aborts — `Unknown identifier: null` — because a model target is cleared in
BOTH layers now; the doc says so and points at `f:defaultValue`. An `output.body.kept` overlay returns
`{kept: preset, probe: removed}`. The #454 repro at 4.12.15: console shows only `Graph traversal aborted`, `inspect
error` is `{}`, `inspect output` keeps the first entry's `rounded_present: 100.46`, and the app log records NO
exception — the arithmetic plugin's `promoteNumber(null)` hits a pattern `switch` with no `case null`, i.e. an NPE
without a message, so the traveler's 400 body is empty. Two follow-ups left for Eric's ruling, NOT done: (1) name the
cause and node on the abort line (or give `promoteNumber` a `case null` with a message) — the reporter's (b);
(2) `Unknown identifier: null` should name the unresolved variable — graph.math substitutes `{model.x}` as the text
"null" before evaluation. Rust twin (lockstep, [[conv-ports-adopt-java-release-number]]): the same shape lives in
`crates/knowledge-graph/src/common.rs` (mapping entry, `for_each`, `set_fetcher_output_entry`) and `skills.rs`
(`stage_model_variable`; `stage_task_parameter` already treats a null body parameter as not supplied), with the rule
stated in `help graph-data-mapper.md`, the `graph_runtime.rs` probe and the Rust CHANGELOG — to follow as a separate PR.

## Later the same session

- **Command JSON (`50c9b64b`):** `docs/guides/knowledge-graph/minigraph-commands.json` still stated the old rule in its
  `mapping_operator` description — my doc grep had covered Markdown only; now it states the shared rule (both repos carry
  the file).
- **graph.math names the unresolved variable (`82530652`, refined in `16d1da80`) — the reporter's secondary suggestion,
  accepted by Eric mid-turn** ("throwing an exception without reference makes debug more difficult"). A `{selector}` is
  rendered as the text `null` by `replaceWithParameter` before the expression reaches the math package, so the evaluator
  can only ever say `Unknown identifier: null` — Eric reached the same conclusion when he asked whether the math package
  could pinpoint the index. The pinpointing therefore sits in the engine, on both sides of the call:
  `GraphLambdaFunction.assertVariablesResolved` checks a COMPUTE expression and an IF condition before substitution and
  names EVERY unresolved selector joined by "or" (Eric's second refinement — `Unknown identifier: model.threshold or
  model.factor (unresolved variable in '{model.threshold} * {model.factor}')`; a single unresolved one is named alone);
  `nameNullIdentifier` handles the evaluator's own `null` (a variable holding the TEXT "null" passes the pre-check and
  renders as the identifier) by re-rendering the selectors and naming the ones that render as `null`, the whole list only
  when none can be told apart. RESET, DELAY, jump targets and MAPPING keep the documented `null` rendering — tutorial 14's
  null-safe idiom lives inside a `text()` constant. `unit-test-math-1` (new fixture, two selectors) pins the three shapes;
  a first assertion of mine failed because the quoted expression naturally contains the other selector's name — assert
  on the named prefix, not on the absence of a substring. Module: 135 tests, 0 failures; `help graph-math` states it.
- **Rust twin PUSHED:** mercury branch `fix/l3-null-source-mapping-parity`, code `f779e46a`, memory `77bfa349`
  (Increment 136). Same helper (`common::apply_null_source`) at the mapping entry, `for_each`, the `model.*` half of
  `fill_fetcher_api_parameters`, `stage_model_variable` (now taking the source selector) and `set_fetcher_output_entry`;
  same graph.math pair (`assert_variables_resolved`, `name_null_identifier`); fixtures byte-aligned; the manifest compile
  count moved 53 → 54 (its first run stopped there — a hard-coded count, not a compile failure). Gates: fmt clean, clippy
  clean, `cargo test --workspace` 603 passed / 0 failed / 9 ignored, check-doc-claims 28, check-llms-links 37, mkdocs strict.
  Its memory-lint reports `[review-overdue]` (10 sessions since the Rust repo's last review) — due on main after the merge.
- **Still for Eric's ruling (not done):** the bare `Graph traversal aborted` of #454 — the arithmetic plugin's null hits a
  pattern `switch` with no `case null` (an NPE without a message), so the traveler's 400 body is empty and `inspect
  error` stays `{}`.

## Memory References

- static-decision-table-is-graph-data (edited: its null-source statement is superseded by the shared rule)
- clean-knowledge-design-over-engine-coverage (consulted: one rule across every mapping path, no per-path special case)
- eric-code-changes-via-pr (consulted: branch + PR)
- conv-ports-adopt-java-release-number (consulted: the Rust twin follows in lockstep)
- playground-session-broker (consulted: hosted the dry-run session for the live repros)
