# Session (2026-09-09T16:57:09.000Z)

**Agent:** Claude Code

## What happened

**v4.12.5 released** (execution record; prep detail in `2026-09-09-163915.md`). Eric
merged release PR #343 (squash `447663d7`, clean title) and published the GitHub release
`v4.12.5` at 2026-09-09T16:55:34Z. Release-gate results: local full reactor
`mvn clean install` BUILD SUCCESS across all 29 modules (clean log capture this time),
PR CI fully green (Build & Unit Tests 8m38s + both memory gates).

The Rust v4.12.5 released one minute later (its own execution log). One gate anomaly
resolved during the round: the LOCAL Rust workspace suite had one TLS test time out
(408 after 10s in `https_strict_mode_rejects_a_self_signed_certificate`) — it ran
concurrently with the Java 29-module build on the same laptop; rerun idle it passed
4/4 in 7.9s, and the PR's CI had already proven the identical code green. Machine
contention, not the branch.

Continuity `latest_release` updated to v4.12.5, initially with the crates.io PENDING
flag (the registry showed 4.12.4 right after both tags). Eric ran the publish shortly
after; **all seven mercury-* crates API-verified at 4.12.5** and the flag is cleared —
v4.12.5 is fully out on both engines and the registry.

## Memory References

- eric-release-rhythm (consulted — merge/tag/publish were Eric's gated steps; this log
  records the execution)
