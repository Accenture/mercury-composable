# Session (2026-07-07T16:05:46.000Z)

**Agent:** Claude Code

Same conversation as the 145132 log (smell-fixing phase after scan #3 showed 0 majors).
Continues [[thread-sonar-quality-gate-4.6.2]].

## S1104 review + fix — commit `cc9fe225`
Eric asked for a review of Sonar's recommendation on HttpRequestEvent (platform-core
automation/models): "make parentSpanId / businessCorrelationId static final or
non-public". VERDICT: adopt the non-public+accessors half, scoped to the two NEW
mutable public fields only. Rationale:
- HttpRouter is the ONLY consumer (2 writes + ~12 reads, one file) — cheap, mechanical.
- The 9 pre-existing public fields are OLD code (outside New Code window) — churning
  them adds HTTP-hot-path regression risk for zero gate value.
- TraceInfo's public fields are all `final` → EXEMPT from S1104 (that's why only these
  two were flagged); FlowInstance.businessCorrelationId also public FINAL → exempt.
Implemented: private + getters/setters in HttpRequestEvent; HttpRouter uses accessors.
Also committed Eric's IDE tidy-ups (redundant throws removed in SpanPropagationTest,
KafkaFlowConsumerTest).
Verified: platform-core RestEndpointTest 34/AdminEndpointTest 12/PostOfficeTest 74,
event-script-engine 132 (SpanPropagationTest 4 = end-to-end parent-span assertions),
minimalist-kafka 79 — all pass.

## Context
Eric is regression-testing core event system + kafka connector + presence monitor and
fixing some of the 80 new-code smells (57 minor/23 info/1 bug) in his IDE. He will send
the REMAINING smells + the 1 Bug for me to fix. Holding for his batch.

## Memory References
- Referenced: [[thread-sonar-quality-gate-4.6.2]]
