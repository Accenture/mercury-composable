# Session (2026-09-25T22:36:33.720Z)

**Agent:** Claude Code
**Title:** graph.model.automation accepts a list of manifests — the later manifest wins; the rapid-prototyping deploy lane completed

## Summary

The leadership demo (no tracked change: a Playground session hosted through the broker, the `us-orientation`
decision-table graph built live with `f:lookup` under a `graph.island`, dry-run, exported and deployed into the running
example with `-Dgraph.model.automation=file:/tmp/graph/deploy/graphs.yaml`) exposed a hole in the deploy-without-rebuild
recipe: a single external manifest REPLACED the bundled set, so a prototype delegating through `graph.extension` to a
bundled graph could not run. Eric proposed a comma-separated list of manifests and RULED **later manifest wins**, with the
rationale that the prototyping loop is: `import graph from` a deployed graph → correct → dry-run → export → stage in the temp
deploy folder with its manifest → restart with BOTH manifests → curl the deployed behaviour → bundle. Refinement (Eric
agreed): a REJECTED later copy leaves the id not executable (404) — never a silent fallback to the copy the operator meant
to replace. Recorded as [[graph-manifest-list-later-wins]].

Branch `feat/graph-manifest-list` (stacked on `docs/deploy-without-rebuild` `46edccee`, the recipe docs):
- `CompileGraph`: `util.split(manifests, ", ")` (the CompileFlows / `yaml.flow.automation` precedent), `compileManifest`
  per entry with its own `location`, a manifest that fails to load is skipped with a warning; `compileOneGraph` removes an
  earlier copy from another location (`WARN Graph X from B replaces the copy from A`) before compiling or rejecting the later one.
- `CompiledGraphs`: per-graph source location (`addGraph(id, model, location)`, `removeGraph`, `getGraphLocation`) and the
  ordered `getDeployedLocations()`; `getDeployedLocation()` stays as the primary (first) location.
- `GraphCommandService`: `list graphs` enumerates every location; the `import graph from` fallback searches the compiled-from
  location first, then all (`findDeployedGraph`), and names the location it found.
- Tests: `CompileGraphTest` +3 (a second manifest's own location; later wins on `unit-test-manifest-dup` version 1 → 2; the
  rejected later copy of `unit-test-manifest-reject` leaves the id out) plus the deployed-locations assertion; fixtures under
  `graph/` and the new `graph-extra/`, `graphs-extra.yaml`; the test `application.properties` lists both manifests.
- Docs: the recipe (`ai-agent-guide.md#deploy-without-rebuild`) rewritten for the two-manifest form with the
  iterate-on-a-deployed-graph loop; `configuration-reference.md` (comma-sep manifest paths, later wins);
  `build-your-first-graph.md`; `llms.txt`; claim `graph-manifest-list-later-wins` pinned to
  `CompileGraphTest#laterManifestWinsForADuplicateGraphId`. CHANGELOG stays for the release PR (tag-range rule).

Gates: engine module 141 tests / 0 failures (138 + 3); `check-doc-canon` OK (claim pin and quote resolve); `mkdocs build
--strict` clean; ai-contract-provider 19/0/0. **Live proof** on the rebuilt 4.12.18 playground jar with
`-Dgraph.model.automation='classpath:/graphs.yaml, file:/tmp/graph/deploy/graphs.yaml'` and a modified `tutorial-1` copy in
the deploy folder: log `Graph tutorial-1 from file:/tmp/graph/deploy replaces the copy from classpath:/graph`, 15 graphs
compiled; curl `tutorial-1` → `hello world - prototype copy`, `tutorial-2` → the bundled echo, `us-orientation` PA → neutral,
ZZ → 400, `tutorial-99` → 404. The Rust twin (mercury `feat/graph-manifest-list`, Increment 142) ships in the same round.

## Incidental sweep

memory-lint flagged one `[overdue]` fact once this log entered the window (sslu 21 > `archive_window` 20):
`log-forwarding-is-infrastructure`. Archived with `archive-fact` — the ruling shipped with the v4.12.13 docs and stands in
the observability guide. Not a memory reference of this session.

## Memory References

- graph-manifest-list-later-wins (created)
- eric-code-changes-via-pr (consulted — the change goes through a PR; stacked on the open docs branch)
- static-decision-table-is-graph-data (consulted — the demo graph's `f:lookup` table on a skill-less node under an island)
