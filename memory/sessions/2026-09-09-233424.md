# Session (2026-09-09T23:34:24.000Z)

**Agent:** Claude Code

## What happened

**PR #341 merged** (squash `3aaa24b0`, clean title) after Eric's second local
regression confirmed the fix round — his DevTools screenshot showed the designed
restore signature (expired model `→ 400` from useGraphData, then session
`→ 200` from useSessionGraphRestore, graph re-rendered, no toast). CI green on
both rounds.

**Post-merge housekeeping branch `chore/webapp-housekeeping-post-341`**
(commit `3ad9ac1b`, pushed; PR is Eric's step):

1. **Packaged bundle rebuilt** from merged main — the PR was source-only, so
   the packaged app first carries the #341 UI here. Verified by booting the
   rebuilt 4.12.5 example jar: serves the new `index-CjGEBl_E.js`, console at
   the 40% default.
2. **nanoid 3.3.17 → 3.3.18** (GHSA-2v37-7h3g-55p8, dev-only vite→postcss
   chain) — `npm audit` now reports 0 vulnerabilities. Eric's working-tree
   audit fix was adopted as-is (plus npm-11 `libc` lockfile normalization).
3. **vendor-router chunk restored** — the manualChunks matcher still said
   `react-router-dom` after the router v8 single-package migration, so the
   rule matched nothing. Fixed to `react-router`: vendor-router (39.75 kB)
   exists again and the index chunk drops 625 → 587 kB (still over vite's
   500 kB advisory line, which we established is benign for a localhost tool;
   a vendor-react split remains an optional future lever).

Gates: 324/324 vitest, typecheck clean, engine + example `mvn` builds exit 0.

Still queued: the Rust webapp lock-step (this same session, next), the
engine-side model-endpoint 404 fix (Eric's ruling — bogus model id must not
answer 200; Java+Rust lock-step), and the overdue root memory review.

## Memory References

- eric-release-rhythm (consulted — branch/commit/push as prep; PR-open and
  merge remain Eric's gated steps)
