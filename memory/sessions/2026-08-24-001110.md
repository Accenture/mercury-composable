# Session (2026-08-24T00:11:10.000Z)

**Agent:** Claude Code

**v4.11.11 SHIPPED — both engines in lock-step (2026-08-23 local); Eric's explicit
directive ("tag release v4.11.11 for the field"), Rust lock-step confirmed by him.**

## Java half

- Release [PR #293](https://github.com/Accenture/mercury-composable/pull/293) squash
  `50d890bf` == gated `39441544` (tree diff empty, squash title verified clean per
  [[conv-squash-title-prefill-check]]), CI green (Build & Unit Tests 8m02s + memory
  checks), branches deleted both ends.
- 34-pom sweep 4.11.10 → 4.11.11 (77 occurrences, zero stragglers outside history);
  CHANGELOG Unreleased → `Version 4.11.11, 8/23/2026`.
- Gate: full reactor `mvn clean install` BUILD SUCCESS (30 modules + parent, 6:08).
- Tag `v4.11.11` annotated on the squash, dereference-verified (`v4.11.11^{}` ==
  `50d890bf`), pushed. **GitHub release PUBLISHED by Eric** (notes drafted here).

## Rust half (mercury repo)

- Release PR #213 true merge `3040e8e3` carrying gated `2d4baae4` (tree verified),
  CI green (test 2m15s + memory), branches deleted both ends.
- Workspace bump: Cargo.toml 4.11.10 → 4.11.11 + `cargo update --workspace` (exactly
  the 12 member entries in Cargo.lock — diff verified, no external churn).
- Gates: cargo test exit 0, clippy 0, fmt clean (exit codes captured unpiped).
- Tag `v4.11.11` annotated on the merge, dereference-verified, pushed. **GitHub release
  PUBLISHED by Eric.**

## Content and a small ops note

Sole content: the P2 graph.task declarative Event-over-HTTP guard
([#292](https://github.com/Accenture/mercury-composable/pull/292) / mercury #212) —
deployed knowledge graphs can invoke polyglot (python/node.js) function hosts and remote
engine instances as graph tasks; new `PostOffice.getEventHttpTarget(route)` API. This is
the engine half of the polyglot initiative reaching the field.

Ops note (Eric asked mid-gate whether the Rust build had stalled): `cargo test` ran
~8 min instead of the usual <5 — the version bump invalidated all 12 workspace crates
(full recompile incl. the trybuild `ui` fixture tests, which sit at 0 CPU while their
nested cargo compiles) and the run shared the machine with the Java reactor. Diagnosis
path that worked: pgrep the test binary's children + `ps -o etime` + Cargo.lock diff
extent; it completed exit 0 on its own.

## Memory References

- created: thread-release-4-11-11
- referenced: thread-polyglot-initiative, polyglot-event-over-http-design,
  conv-squash-title-prefill-check, eric-release-rhythm, bp-polyglot-functions
- updated substance: thread-polyglot-initiative (P2 shipped-in-release note)
