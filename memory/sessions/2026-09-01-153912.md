# Session (2026-09-01T15:39:12.000Z)

**Agent:** Claude Code

## Summary

Q&A session on minimalist-kafka schema configuration for `simple.kafka.notification`, followed by a
token-efficiency analysis of guide vs. source-code lookup paths. Finding: the `minimalist-kafka.md`
guide in the AI contract provider skill package was sufficient to answer the schema configuration
question; source reads were unnecessary. Surfaced that the guide-first rule was not encoded anywhere a
fresh agent would see it. Designed and recorded a three-layer doc-improvement feedback loop; added the
guide-first rule to `instructions.md` and opened `thread-doc-improvement-feedback-loop` in continuity.

## What happened

1. **Schema config question** — answered `simple.kafka.notification` schema configuration by reading
   source (`SimpleKafkaNotification.java`, `SchemaCodec.java`, `KafkaHeaders.java`,
   `schema-registry.properties`, the unit test). Used ~18,000 tokens for discovery + reads.
2. **Post-hoc sufficiency check** — confirmed `minimalist-kafka.md` §Schema covered everything
   structural; source added only: exact constant names (KafkaHeaders.java), @KernelThreadRunner
   rationale, and a test-proven body-contract edge case. Guide-first would have cost ~3,000–4,000
   tokens.
3. **Token-efficiency analysis** — guide-first is 3–5× cheaper for configuration questions; source is
   only justified for invariants the guide is genuinely silent on.
4. **Feedback loop design** — three layers: in-session `## Doc Gaps`, periodic sweep at review,
   fresh-agent verification after guide PRs. Metric: guide sufficiency rate on same cadence as
   onboarding re-measure.
5. **Immediate recording** — guide-first rule added to `instructions.md` Session Working Conventions;
   `thread-doc-improvement-feedback-loop` opened in continuity.

## Doc Gaps

- **This session** — guide was sufficient; source was unnecessary. Gap was the missing rule, now fixed.
- **CLOSED same session:** `minimalist-kafka.md` §Outbound — added `@KernelThreadRunner` / keep-instances-small
  callout (was only in source Javadoc); strengthened body-must-be-`byte[]` to name the `IllegalArgumentException`;
  fixed misleading YAML comment in §Produce that implied a `Map` body is acceptable on the schema path.

## Memory References

- Created: `thread-doc-improvement-feedback-loop` (tier: working)
- Updated: `memory/instructions.md` — added guide-first lookup rule under Session Working Conventions
- Updated: `system/AGENTS.md` — added "Efficient lookup" section for path-2 (consumer/developer) agents
