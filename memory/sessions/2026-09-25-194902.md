# Session (2026-09-25T19:49:03.000Z)

**Agent:** Claude Code

## Summary

**v4.12.18 release prep, Java + Rust in lock-step (Eric's ask before his break: "prepare release v4.12.18 for Java and Rust
repos").** Java branch `release/4.12.18` off main `494b1b86`: sweep BUILD FILES ONLY 43 files / 98 occurrences of `4.12.17`
→ `4.12.18` (re-derived from `--include=pom.xml --include=build.gradle`, unchanged from 4.12.17 — [[conv-template-version-sweep]],
[[snyk-retired-manifest-placeholders]]; 0 leftovers; prose untouched). CHANGELOG from `git log v4.12.17..HEAD`
([[conv-changelog-from-tag-range]]): Changed 1 (#462 — graph.math typed and finite, with the READ upgrade note from
[[graph-math-typed-arithmetic]]), Added 2 (#462 `CONDITION`), Documentation 3 (#462 the ruled doc items), Build 4 (#463 the
Snyk bumps — Jackson 2 BOM 2.22.3, Jackson 3 BOM 3.2.3, Netty 4.2.18.Final, MsgPack 0.9.12 best effort); memory-only commits
excluded. Readiness `mvn clean install` (full reactor): BUILD SUCCESS — 35 modules, 220 suites, 1517 tests, 0 failures, 0 errors, 3 skipped (surefire XML; 4.12.17 was 1514 — the three new `GraphTaskTest` methods of #462), artifacts at 4.12.18. Rust branch `release/4.12.18` (mercury, off its main
`0ae374a7`): 13 manifests / 24 occurrences + `Cargo.lock` via `cargo update --workspace` (24 entries, all workspace crates);
CHANGELOG entry lists the whole tag range — Increment 141 (#330, the #462 twin) AND Increment 140 (#329, test-only hardening
merged after v4.12.17); lock-step note: #463 has no Rust analogue. Rust gates: fmt-check, clippy, `cargo audit` (320 crates, no advisory), claims 29/29, links 37/37, mkdocs strict and `cargo publish --workspace --dry-run` (twelve crates packaged) all green on the swept branch; `cargo test --workspace` started after this build released the ports — its result is in the Rust repo's release-prep log. Release rhythm unchanged
([[eric-release-rhythm]], [[conv-ports-adopt-java-release-number]]): Eric opens the PRs, merges, tags one memory commit past
each merge, publishes the GitHub releases with the CHANGELOG entries as bodies and the crates from the tag; `latest_release`
updates at the seam close.

## Advisory sweep at the seam

`memory-lint` raised `[overdue]` (sslu 21 > `archive_window` 20) once this log entered the window; archived with `archive-fact` (retrievable via `memory/archive/INDEX.md`): redis-connection-foundation. Not a cadence review — no other fact moved.

## Memory References

- conv-template-version-sweep (consulted — the build-files-only sweep and its grep)
- snyk-retired-manifest-placeholders (consulted — the two non-reactor poms in the sweep)
- conv-changelog-from-tag-range (consulted — the entry derived from the tag range, counts re-derived)
- graph-math-typed-arithmetic (consulted — the READ note for item 1)
- eric-release-rhythm (consulted)
- conv-ports-adopt-java-release-number (consulted — Rust at the same number for the same content)
