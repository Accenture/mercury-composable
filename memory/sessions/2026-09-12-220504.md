# Session (2026-09-12T22:05:04.000Z)

**Agent:** Claude Code

## What happened

**E3 executed — the cross-pod dry-run, all five scenarios green on the first
complete run (branch `feat/srr-e3-cross-pod-dry-run`, commits `1c247872` +
`7065e5d0` + `3b3b0326`; merged via PR #371, squash `b1e57537`, title clean;
branch deleted local+remote).** Eric's go: "proceed with E3." Unlike E1/E2 this was a
LIVE experiment: three JVM processes on this machine — `redis-standalone`
(6379, the only shared infrastructure), the demo in a new `stream-ui` profile
(pod A, 8600, coordinator + StreamBridge facade, `-Dsync.stream.ttl.seconds=30`
to make TTL-bounded endings observable), and the demo in a new
`stream-producer` profile (pod B, 8601, StreamResponder only — no coordinator,
Kafka inert on both pods via the `kafka.*.enabled=false` switches).

**Demo additions** (`examples/sync-over-async-demo`): `StreamNotifyFacade`
(announces the session cid as the first SSE event; idle via
`x-stream-idle-seconds`) + `StreamProducer` (`POST /api/produce`, modes
chat/notify/close/stall + the `lost` chaos mode that appends via
ReturnRouteStore while suppressing the wake-up); two profiles + rest yamls;
README gains the three-terminal runbook and chaos recipes. Demo unit tests
4/4 unaffected.

**The five scenarios** (evidence in the new permanent record,
`docs/test-reports/streaming-return-route-cross-pod.md`, added to mkdocs nav):

1. Ordered chat tokens cross-pod — 5 tokens + eof metadata in exact order,
   no sequence number anywhere in the pipeline.
2. Lost notification — invisible until the next real post's drain healed it,
   in list order, then a backend-side close.
3. Lost CLOSE — data + eof stored with every wake-up suppressed; the facade
   watchdog's single final drain completed the render at idle + 1 ms (D4's
   nuance observed across pods).
4. Producer `kill -9` mid-stream — in-band 408 at idle expiry, 6.01 s after
   the last segment.
5. UI pod `kill -9` — the producer's post right after the kill returned
   `live:true` (the route key is Redis state, so posts are accepted into the
   void until its TTL), and `live:false` after the 30 s route TTL — the
   TTL-bounded orphan stop. Spec §6 failure-table row tightened to state this
   timing explicitly (wording only; the design already said TTL is the crash
   backstop).

No mechanism defects found. One demo-side lesson recorded in the report: a
function addressed DIRECTLY by rest.yaml receives the whole AsyncHttpRequest
map as input (the JSON body is inside it) — unlike a flow task whose input
mapping extracts `input.body`; the producer endpoint was fixed accordingly
mid-round. Spec stamped E3 DONE (next gate E4 — the bp-agent-orchestration
payoff); guide `#streaming` links the report. Doc canon OK, mkdocs --strict
clean. Runbook mechanics worth remembering: macOS `date` has no `%N`
(python3 timestamper instead), and overwriting a jar under a running JVM is
unsafe — both pods restarted after the mid-round rebuild.

**CI episode after the PR opened (same round, two follow-up commits on the
branch):** (1) Eric confirmed the AsyncHttpRequest contract and asked for the
doc-gap closure → guide updated (`7065e5d0`, see Doc Gaps). (2) That commit's
CI run then failed in ai-contract-provider — the new test report entered the
packaged documentation closure (the sync-over-async guide links it), and a
packaged doc OUTSIDE docs/guides is a "fixed extra" needing THREE touches:
the `skill/files.list` inventory line, a pom `<include>` (only `guides/**`
is globbed automatically), and the expected-extras list in SkillSnapshotTest.
Each failure named the next missing piece; fixed as `3b3b0326` (module
19/19), and the module README now documents the three-touch rule so the next
linked non-guide doc doesn't stumble. Durable lesson: linking a NEW doc from
any packaged guide grows the AI-contract closure — budget the contract-provider
registration into any change that adds a doc reachable from docs/guides.

## Doc Gaps

- Question: how does a directly-addressed (non-flow) REST service receive the
  HTTP body? Guide page: rest-automation/index.md. Missing detail: the guide
  showed only the typed shape (`TypedLambdaFunction<AsyncHttpRequest, ...>`),
  not the interceptor/untyped variant — the endpoint ALWAYS delivers the
  AsyncHttpRequest object (as the event body for an @EventInterceptor/Map
  function; reconstruct with `new AsyncHttpRequest(request.getBody())`); only
  the flow adapter extracts `input.body`. **CLOSED same round**: Eric
  confirmed the contract ("the input contract is the AsyncHttpRequest object
  ... Your instinct ... is correct") and the guide now states it explicitly
  (commit `7065e5d0` on the E3 branch, folded into PR #371); the test
  report's round note records the closure.

## Memory References

- ot-streaming-return-route (thread updated - E3 executed, PR pending)
- soa-transport-neutral-cid (consulted - the dry-run's broker-free topology is
  the design's point; Kafka switched off on both pods)
- kafka-mesh-opt-in (consulted - the report frames the rendezvous as the
  deliberately lighter alternative; no mesh anywhere in the run)
- preload-before-mainapp-lazy-config (consulted - StreamProducer builds its
  responder and chaos connection lazily, never in the @PreLoad constructor)
- eric-release-rhythm (consulted - branch/commit/push prepared; PR creation
  and merge are Eric's gates)
- conv-squash-title-prefill-check (consulted - PR title handed off on its own
  line in a code block)
