# Session (2026-07-29T00:35:28.000Z)

**Agent:** Claude Code

## Summary

**Graph suspend/resume — concept reviewed, design RATIFIED, implementation plan compiled
(P1 pending Eric's go).** Eric's concept (`~/Desktop/graph-suspend-resume-concept.md`):
workflow suspension for the Active Knowledge Graph — a graph run persists its model
key-values + suspension node at a human checkpoint (approval, intervention, inbox), the
run completes normally, and a later request with the same business correlation ID resumes
from the recorded node without re-executing it.

**Grounding:** five parallel code readers (engine core, state machine, entry paths,
Redis/TTL precedents, Rust parity) — first workflow run stalled (runner died silently,
all transcripts frozen same second; relaunched clean). Full plan with file:line anchors:
`draft-design-specs/graph-suspend-resume-implementation-plan.md` (gitignored — the
rulings live here).

**Verdict: sound + viable.** Jump-to-node already exists (skill reply body = node alias,
GraphExecutor.nextOrJump); suspension converts long workflows into short runs (no parked
instances — GraphInstance lifecycle untouched); sync-over-async's ReturnRouteStore is the
exact persistence precedent. Concept-doc corrections found: CompileGraph does NOT reject
`result -> model` (runtime check only, null-value gap); `model -> *` input is already
legal; `ttl` node property currently leaks into the state machine; Kafka-initiated graphs
need a wrapper flow (no path_parameter.graph_id in the adapter dataset).

**Ratified rulings (Eric, 2026-07-28):**
- R1: dedicated skills `graph.suspend`/`graph.resume` encapsulate persistence I/O (no node
  data mapping; superset of graph.task invoking the attached `task=` function).
- R2: skill defines behavior, TYPE is visual only (UI colors). Types
  Suspend/Resume/Suspensible = convention. Suspensibility = reserved node property
  `suspend=true` (resolves the concept's own type-vs-property inconsistency).
- R3: **`suspend` is a reserved node ALIAS** — the root/end pattern (special alias,
  resolved by name) because traversal JUMPS to it by name via the existing
  `"next"|node-alias` directive vocabulary; alias ⇔ skill enforced bidirectionally;
  exactly one suspend node per graph; drawn edge suspensible→suspend still required
  (graph = documentation). `resume` NAME stays convention (no mechanism rides it).
- R4: Redis store = self-contained module/crate imported by the PLAYGROUND EXAMPLE app,
  never the engine (both repos); engine unit tests use a temp-file mock store under
  `/tmp/suspend-resume` (same contract; file-per-cid, MsgPack, delete-on-read).
- D1 consume-on-retrieve (Redis GETDEL, at-most-once resume); D2 persisted envelope =
  model + node + nodeSeen/skillRun snapshots (joins after resume); D3 not-found = normal
  first-run case → `next` (optional `missing=<alias>` jump); D4 default suspended response
  `{"type":"suspended","cid":…}` when output.body empty; D5 sole-active-branch constraint
  (documented + runtime WARN; no suspension between fan-out and join); D6 playground
  dry-run persists for real, model.cid via init mapping.

**Key design facts for implementation:** new reserved properties `suspend`/`ttl`/`missing`
join RESERVED_PARAMETERS; new directive `resume:<alias>` (`:` outside alias charset — no
collision); resume marks the suspension node nodeSeen+skillRun then walks its forward
links excluding the `suspend` alias; `<node>.result` scratch does NOT survive suspension —
"model is the workflow's durable memory" is the documented design rule; store contract =
put {cid,node,ttl,model,seen,run} / get {cid}, synchronous ack (deliberately NOT the
fire-and-forget ext: contract — durability needs the ack); ttl format =
Utility.getDurationInSeconds (2d/5m/20s, no rest.yaml 1s-5min clamp); cid = resume
capability (docs: authentication on resume endpoints; cid does not propagate across
graph.extension → resumable workflows are top-level graphs).

**Plan phases:** P1 engine core (executor + both skills + temp-file store tests);
P2 CompileGraph checks + Playground surface; P3 extensions/minigraph-state-redis +
state-store contract page; P4 tutorial graph + e2e + docs + proposed ADR; P5 Rust
lock-step arc (its "session persistence out-of-scope" design line gets superseded;
Redis crate choice = Rust session's call).

**Proposed, human-gated, NOT yet ratified:** the `(blueprint)` thread (workflow
suspension serves vision-mercury-composable) and the ADR — awaiting Eric.

## Memory References

- referenced: vision-mercury-composable (feature serves the workflow/collaboration
  pillar), event-script-over-code (skill-encapsulation rationale), kafka-mesh-opt-in
  (opt-in extension placement precedent), thread-compilegraph-syntax-validation (the
  CompileGraph validation gap this feature's checks extend),
  conv-telemetry-presentation-parity (graph JSON engine-portability → lock-step),
  registration-metadata-contract (contract-page + conformance method for the store
  contract)
- created: thread-graph-suspend-resume
