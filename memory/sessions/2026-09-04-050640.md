# Session (2026-09-04T05:06:40.000Z)

**Agent:** Claude Code

## What happened

Closed the three smoke-test items Eric ruled on.

1. **Promoted two facts to `core`** — `eric-release-rhythm` (uses 29, the sole User Preferences
   fact) and `conv-telemetry-presentation-parity` (uses 42, the most-used fact in the layer,
   defining the cross-engine contract). Both had fallen to `archive-candidate` in the same day's
   review, purely because the recent window was docs/config work. `core` is the right tier: neither
   is a decision that fades — one is a standing operating preference, the other a standing field
   requirement. Verified the promotion holds: `refresh-metadata --dry-run` no longer proposes to
   re-tier either.
2. **Added `conv-declare-consulted-references`** (`tier: core` — a guard against decay should not
   itself decay). Twin added in the Rust repo.
3. Ran `refresh-metadata`, which also corrected a footer I had hand-written
   (`ot-reverify-invariants-20260904` `uses: 2` → `1`): the reference log is the source of truth
   for usage metadata, not my arithmetic. A small instance of the same lesson as item 2.

**Cascade handled:** each session log added during this work pushes older ids past
`archive_window`. Five crossed at sslu 21 after the smoke-test log landed
(`route-pool-registration-design` + four completed `[x]` threads) and were swept, the same way
`thread-sse-client-twin` was in the Rust repo earlier. Continuity 327 → 321 lines, threads 19 → 16,
lint back to 0/0. Worth knowing for future reviews: the review's own logs move the window, so a
final lint pass after the last commit is part of the ritual, not optional.

## Memory Review

**Archived — faded, past `archive_window` (5):** `route-pool-registration-design` (fact),
`ot-route-pool-api`, `thread-http-response-streaming`, `thread-spring-round-20260827`,
`thread-sse-consumption-streaming` (all completed `[x]` threads) → `2026-Q3.md`.
`thread-onboarding-efficiency` crossed as this log itself landed and was swept in the same
way (6 total).
**Re-tiered:** `eric-release-rhythm` → core, `conv-telemetry-presentation-parity` → core,
`conv-no-streamable-http-claim` → archive-candidate (normal decay).

## Memory References

- Referenced: eric-release-rhythm, conv-telemetry-presentation-parity
- Created: conv-declare-consulted-references
