# Session (2026-06-28T00:56:32.000Z)

**Agent:** Claude Code

Built the **sync-over-async worked example** (`examples/sync-over-async-demo`) — the second worked example
(after kafka-demo), demonstrating the cross-pod synchronous-REST-over-async-Kafka pattern (ADR-0006). Eric
chose: two pods via **one jar + Spring profile** (`-Dspring.profiles.active=facade|backend`), no Docker,
simplicity first.

## Changed
- **Promoted `soa.reply`** (reply → `coordinator.deliver`) from the sync-over-async test mock to the shipped
  extension (`org.platformlambda.tasks`, instances=250), alongside `sync.prepare`/`sync.await`. It is generic
  boilerplate every sync-over-async app needs. Tracked as a git rename; RestFlowMvpTest still green (32 tests,
  coverage met).

## Added — `examples/sync-over-async-demo`
- One jar, two roles via Mercury's native profile mechanism (`application-facade.properties` /
  `application-backend.properties` override the base). Facade = REST (`/api/sync-to-async`, port 8400,
  `-Drest.server.port` overridable for multiple instances) + sync-to-async flow + soa-reply flow + Redis
  coordinator. Backend = system-of-record flow; no REST, no Redis.
- Only app-specific code: `SystemOfRecord` (backend logic, uses `Instant.now()`) and `SyncErrorHandler`
  (408 pass-through / 5xx→503). Everything else reused from the extension + minimalist-kafka.
- Topics `soa.request` / `soa.response` (auto-created). README has the run sequence + a multi-facade demo
  and a 408 timeout demo.

## Validation (live, two pods, no Docker)
- Synchronous `POST /api/sync-to-async` returned the backend reply; `processedAt` an ISO-8601 Instant.
- Trace-id continuous across facade + backend pods (cross-Kafka-hop).
- Backend stopped → `HTTP 408` via `sync.await` timeout → `sync.error.handler`.
- All modules build; extension 32 tests pass.

## Memory References
- Updated: [[helpers-and-worked-examples]] (sync-over-async example done)
- Referenced: `event-script-over-code`, `instant-serialization`, `minimalist-examples`, `kafka-mesh-opt-in`
  (ADR-0006), `code-conventions-home`
