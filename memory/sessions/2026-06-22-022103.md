# Session (2026-06-22T02:21:03.000Z)

**Agent:** Claude Code
**User:** Move from structural/style consistency to **content** consistency — polish the docs for two
objectives: human storytelling + AI-efficient (deterministic) context discovery. Eric (the creator) collaborated.

## What We Did

After the structural rewrite (slugs/frontmatter/At-a-glance) was finished, the docs still mixed old and
new *content* — 7+ years, many human + AI contributors, code is the source of truth. We diagnosed the
content drift, locked a **Documentation Canon** with Eric, and conformed the docs to it.

**Canon (locked with Eric, code-verified) — see fact `docs-content-canon`:**
1. "layers" = the **3 paradigm layers** only; the runtime flow is the **request pipeline** (stages).
2. Layer-3 vocab: **Active Knowledge Graph** = thing; **Knowledge Graph as Application** = paradigm;
   **MiniGraph** = engine; **semantic** = adjective.
3. **Origin story told:** Scala/Akka actor model → Eclipse Vert.x → Java 21 virtual threads.
4. Human–AI collaboration = **cross-cutting capability**, not a 4th layer.
5. **"One atom, four roles":** the **function** is the only building block (`@PreLoad` +
   Lambda/TypedLambdaFunction, route-addressed); named by wiring — **function** / **service** (rest.yaml
   `service:`) / **task** (flow step) / **skill** (AKG node). Verified against `RoutingEntry.java:44`,
   `CompileFlows.EXECUTION_TYPES`, `GraphLambdaFunction.java:116`.

**Conformance executed (all 5 steps; `mkdocs --strict` green throughout):**
- **Step 1** — `index.md` + `llms.txt`: origin nod in the lede; layer-3 → "Semantic (Active Knowledge
  Graph)"; dropped the stale "rewrite in progress / legacy" note.
- **Step 2** — `architecture.md`: "five distinct layers" → **request pipeline** (stages); added the
  **"Where it came from"** origin section; removed the "generate from this page alone" overclaim (→ DSL agent guides).
- **Step 3** — `methodology.md`: re-voiced the opener (complexity/coupling *why*; Gartner/Accenture history
  demoted to one line); **task/function reconciled** to the canon; `fork-n-join`→`fork`; dropped an infra "layers".
- **Step 4** — terminology sweep: 3 residual "layer" misuses reworded (`api-overview`, `service-mesh`,
  `event-over-http`). Scans confirmed the rest was already canon-clean.
- **Step 5** — persisted the canon as a **published page** (`docs/guides/documentation-conventions.md`,
  in nav) + built **`scripts/check-doc-canon.py`** (slugs / frontmatter / At-a-glance / BOM / retired
  terms), wired into `.github/workflows/docs.yml`. Negative-tested: it flags all violation types (rc=1);
  real docs pass (rc=0).

## Decisions Made

- `docs-content-canon` (the 5 canon decisions + AI-discovery contract + conformance order).
- Canon lives as a **published site page**; a **CI drift check** guards it (Eric chose both).

## Context for Next Session

- Content polish **complete**; the canon is enforced in CI. Docs now serve human storytelling + AI
  deterministic discovery, consistently across all three layers.
- **PR is still on hold** (Eric's call). Open follow-ups unchanged: two Blueprint gaps
  (`bp-ai-companion-llm-backend`, `bp-graph-governance-lifecycle`); open the PR when ready.
- Build gate: `/tmp/mkdocs-venv/bin/mkdocs build --strict` (`DISABLE_MKDOCS_2_WARNING=true`) +
  `scripts/check-*-grammar.py` + **`scripts/check-doc-canon.py`**.

## Memory References

- Referenced/updated: `docs-content-canon` (created last write; used here), `docs-style-conventions`,
  `docs-rewrite-architecture`, `vision-mercury-composable`, `functions-decoupled-routes`,
  `typed-io-map-or-pojo`, `pom-version-source-of-truth`
- Created: `thread-docs-content-consistency` (working, closed `- [x]`)
