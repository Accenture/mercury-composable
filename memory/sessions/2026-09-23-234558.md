# Session (2026-09-23T23:45:58.000Z)

**Agent:** Claude Code

## Summary

**v4.12.16 release prep, Java + Rust in lock-step (Eric's ask after PRs #456 / mercury #323 merged; the `rust`
workflow's cancelled run on merge `da133b88` was the cancel-in-progress concurrency group superseded by my memory push
`504821c0`, whose run passed — both mains green).** Java branch `release/4.12.16`, commit `57c595b6`: sweep BUILD FILES
ONLY 43 files / 98 occurrences (re-derived from `--include=pom.xml --include=build.gradle`, unchanged from 4.12.15 —
[[conv-template-version-sweep]], [[snyk-retired-manifest-placeholders]]); CHANGELOG from `git log v4.12.15..HEAD`
([[conv-changelog-from-tag-range]]): #456 as three *Changed* items (the shared null-source rule with its READ note, graph.math
naming the variable, every abort carrying its reason with the prefix-match READ note), #455 under *Documentation*, #452
under *Build* (JaCoCo 0.8.15 — kept as a contributor-facing note; drop it if the tooling filter should apply). Readiness
`mvn clean install`: BUILD SUCCESS, 34 modules with reports, 218 suites, **1506 tests, 0 failures, 3 skipped** — counts from
the surefire XML, not a tail. Rust branch `release/4.12.16` (mercury): 13 manifests / 24 occurrences + `Cargo.lock` via
`cargo update --workspace` (24 entries at 4.12.16, 0 left); CHANGELOG twin entry with the lock-step note (Java's OpenSSL
doc and JaCoCo items have no Rust analogue); fmt/clippy/claims/links/mkdocs green; `cargo test --workspace` started only
after the Java build finished — the two suites share test ports. Release rhythm unchanged ([[eric-release-rhythm]],
[[conv-ports-adopt-java-release-number]]): Eric opens the PRs, merges, tags one memory commit past each merge, publishes the
GitHub releases with the CHANGELOG entries as bodies and the crates from the tag; `latest_release` updates at the seam close.

## Merged

Eric opened PR #457 and merged it at 23:58:16Z as squash `df605533` (title clean); PR checks all green — Build & Unit Tests
(Java 21) 10m4s, Starter Templates (Gradle), memory ×2 (the docs workflow does not run for a root-level CHANGELOG change);
gate verified (state MERGED, `df605533` an ancestor of `origin/main`), GitHub deleted the remote branch, the local branch
deleted after the gate, Eric's parked stash untouched, root pom at 4.12.16 on main. This memory-only commit is the tag
target (one past the merge, as at 4.12.15). Pending for Eric: tag `v4.12.16`, the GitHub release with the CHANGELOG entry
as its body; then the seam close updates `latest_release` and closes [[v4-12-16-release-prep]]. Rust twin: PR #324 merged
at 23:58:25Z as merge `743d4ea2`, checks green (`test` 4m11s), the local `cargo test --workspace` re-run on the idle machine
603 / 0 / 9 confirmed the three earlier failures as environmental; workspace at 4.12.16 on its main.

## Memory References

- v4-12-16-release-prep (created)
- eric-release-rhythm (consulted)
- conv-changelog-from-tag-range (consulted)
- conv-template-version-sweep (consulted)
- snyk-retired-manifest-placeholders (consulted)
- conv-ports-adopt-java-release-number (consulted)
