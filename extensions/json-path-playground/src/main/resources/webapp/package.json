{
  "name": "json-path-playground-webapp",
  "version": "1.0.0",
  "description": "A lightweight React-based JSON-Path playground for testing and debugging",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf ../public/*",
    "build:spring": "npm run build && npm run deploy",
    "deploy": "cp -r dist/* ../public/"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-json-view-lite": "^2.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
