flow:
  id: 'echo-flow'
  description: 'This is a sub-flow to echo input'
  ttl: 10s

first.task: 'echo.me'

tasks:
  - name: 'echo.me'
    input:
      - 'input.body -> *'
      - 'input.header -> header'
    process: 'no.op'
    output:
      - 'header -> output.header'
      - 'result -> output.body'
    description: 'Hello World'
    execution: end
