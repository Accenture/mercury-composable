# Session (2026-06-23T18:28:51.000Z)

**Agent:** Claude Code
**User:** Layer 1 restructure housekeeping + AI context discovery on-ramp (`thread-next-ai-context`).

## What We Did

**Context resume.** Picked up from prior session which had just committed the Layer 1 `event-driven/`
restructure (`73345afc` — moved `event-driven-foundation.md` → `event-driven/index.md`, moved
`function-execution.md` → `event-driven/function-execution.md`, created new tutorial
`write-your-first-function.md`). Two housekeeping tasks were pending.

**Housekeeping — gh-pages deploy.** Ran `mkdocs gh-deploy --force` to push the updated site (with
the new Layer 1 nav structure) to the `gh-pages` branch. Live site updated.

**Housekeeping — `site/` in git.** The `site/` mkdocs build artefact was accidentally swept into
commit `73345afc` by `git add -A`. Fixed by adding `site/` to `.gitignore` and running
`git rm -r --cached site/` (83 files removed from tracking). Committed as `a3270f06`, pushed to
main.

**AI context discovery on-ramp (`thread-next-ai-context`).** Created
`docs/guides/ai-developer-guide.md` — the cross-layer AI collaboration guide for any AI agent
working on a mercury-composable application (greenfield or brownfield). Covers:

1. **Mental model in 2 sentences** — "plain Java, framework constrains only coupling"; one atom, four
   roles (function / service / task / skill).
2. **Brownfield orientation** — table of files to read first (rest.yaml, flows.yaml, `@PreLoad` grep,
   application.properties, graph JSON); quick 5-step orientation sequence.
3. **Layer-choice decision tree** — Layer 1 for a single function, Layer 2 for multi-function
   orchestration, Layer 3 for semantic graph-driven behavior; when in doubt start at Layer 1.
4. **Adding a feature at each layer** — complete code + config snippets for Layer 1 (function +
   rest.yaml), Layer 2 (function + flow YAML + flows.yaml + rest.yaml), Layer 3 (pointer to graph
   AI guide).
5. **Testing** — unit test in isolation (no framework), integration test via FlowTest pattern.
6. **Key invariants (what not to do)** — no direct function calls, no cross-function imports, no
   business logic in YAML, Map-or-PoJo for key-by-key mapping (`*` passthrough for List), serialization
   gotchas.
7. **DSL-specific guide table** — points to the three deterministic specs (Event Script, REST, MiniGraph)
   and their AI agent guides.

Added to mkdocs nav (Foundations section) and `llms.txt` (Start here — prominently flagged for AI
agents). `mkdocs build --strict` → 0 warnings.

## Context for Next Session

- New page `docs/guides/ai-developer-guide.md` staged but not yet committed.
- `site/` gitignore fix already committed and pushed.
- `thread-next-ai-context` is now addressed (AI developer guide created).
- Next: commit the AI developer guide + deploy to gh-pages.

## Memory References

- Updated: `thread-next-ai-context` (progress: AI developer guide written)
- Updated: `thread-layer-reorg` (fully closed — `event-driven/` folder + tutorial done, housekeeping done)
