# Session (2026-06-30T05:53:33Z)

**Agent:** Claude Code

## Summary

**Upgraded agent-memory v4.27.0 → v4.28.0** (Mode B, by Claude Code from the tool checkout). Single,
additive rung. Working tree left **uncommitted** for the mercury team.

**Branch note:** the local checkout was on `chore/sonar-ide-cleanup` (PR #126, already squash-merged)
with a stale local `main`. Per the upgrade-on-main convention, I fetched, switched to `main`, and
`pull --ff-only` (brought in PR #126 `7cd8e601` + confirmed the v4.27.0 protocol commit `87f2f6b4`),
then applied the rung there. Clean tree throughout; the feature branch is untouched (pushed).

**What v4.28.0 brings — co-author convention cleanup:** refines the v4.27.0 PR convention from a
real-world finding (mercury PR #126 itself). **(1)** The `Co-Authored-By` self-identification now
specifies the **stable agent name** (e.g. `Claude Code`, `Gemini CLI`) — the actual AI collaborator,
**not** a model-version string that churns each release. **(2)** On a squash-merge, collapse to a
**single** trailer (GitHub appends a consolidated one after `---------`; trim the inline repeats).
Advisory — agent-memory can't suppress a vendor harness trailer or GitHub's squash template.

**Applied here (scoped to the repo root only):**
- **`AGENTS.md`** — additively refined the commit-trailer note (stable agent name + squash-merge
  guidance) in place; mercury's customizations preserved.
- **`.github/pull_request_template.md`** — re-copied from the tool template (footer comment now names
  the AI collaborator's stable agent name, not a model version).
- **`.agent/version.md`** → `version: 4.28.0`, `last_upgraded: 2026-06-30` (preserved `enabled_with:
  4.14.1`, `mode: A`).

No memory-shape/skill/adapter change. **Validation:** `memory-lint --root .` → **0 errors**.

**Note for the mercury team:** changes are uncommitted on `main` — review and commit at your
discretion.

## Memory References
- Created: agent-memory-upgrade-v4280
- Referenced: agent-memory-upgrade-v4270 (refines)
