# Session (2026-06-22T22:50:58.000Z)

**Agent:** Claude Code
**User:** Eric Law — "opt-in the ADR pattern to mercury-composable" using agent-memory.

## What We Did

Adopted the agent-memory **optional Architecture Decision Record** pattern. Per Eric's
direction the ledger lives at `docs/arch-decisions/ADR.md` (the repo's own path, not the
agent-memory default `docs/ADR.md`); the pre-existing `DESIGN-NOTES.md` — which is
narrative design rationale, **not** ADRs — was `git mv`'d to `docs/notes/design-notes.md`
(lowercase per the slug convention) and the `arch-decisions/` folder repurposed for the
ledger. Seeded the ledger **retrospectively** with 5 ADRs derived from the VBDI Design
altitude, each `formalizes:` an existing `continuity.md` fact and verified against source
(`platform-core`, `event-script-engine`, `minigraph-playground-engine`) + published
guides (code/guides = source of truth in ambiguity):

- ADR-0001 → `functions-decoupled-routes` (route-name coupling; orchestration as Event Script)
- ADR-0002 → `virtual-threads-rpc` (Akka→Vert.x→Java 21 VT engine; sequential RPC ≈ reactive)
- ADR-0003 → `typed-io-map-or-pojo` (Map/PoJo I/O; immutable EventEnvelope; MsgPack/Gson)
- ADR-0004 → `docs-content-canon` (three-paradigm-layer architecture; pipeline ≠ layers)
- ADR-0005 → `docs-content-canon` (one atom, four roles: function/service/task/skill)

Verified code anchors: `RoutingEntry.java` `SERVICE="service"` (vs `flow:`),
`CompileFlows.EXECUTION_TYPES` = {decision,response,end,sequential,parallel,pipeline,fork,sink},
`GraphLambdaFunction.java` `SKILL="skill"`, `@PreLoad`/`LambdaFunction`/`TypedLambdaFunction`.

Wired the ledger into the mkdocs nav as the **first entry under Part VII · Reference**;
repointed the moved Design Notes (nav + `docs/index.md` + `llms.txt`, the latter also
mislabeled design notes as "ADRs" — fixed) and added the ADR ledger to the `llms.txt` AI
map. Added `(ADR-NNNN)` human-pointer tags to the four formalized facts in `continuity.md`,
recorded the opt-in as a new Key Decision (`adr-pattern-adopted`), updated the AGENTS.md
ADR pointer to the repo's path, and corrected the `instructions.md` repo-structure note
(`arch-decisions/` = ADR ledger, `notes/` = design notes).

Verification: `check-doc-canon.py` exit 0 (ADR.md/design-notes.md sit outside `docs/guides/`
so they're exempt from frontmatter/at-a-glance/slug enforcement but pass BOM + banned-term);
all nav targets resolve; no stale `DESIGN-NOTES` references remain. `mkdocs --strict` not run
locally (mkdocs not installed) — links/targets verified by hand; CI will confirm.

## Decisions Made

- ADR pattern **adopted** at `docs/arch-decisions/ADR.md` (diverges from the agent-memory
  default `docs/ADR.md`; divergence recorded in memory + AGENTS.md).
- `DESIGN-NOTES.md` is design rationale, kept distinct from ADRs; moved to `notes/design-notes.md`.
- ADR ledger is **published** in the mkdocs site (Part VII · Reference, first entry).
- Retrospective ADR set = 5, scoped to durable *framework architecture* (not docs-process) decisions.

## Follow-up — pipeline-stage drift fixed (same session)

Eric flagged a context drift in the request-pipeline phrasing: **REST automation is not a
sequential stage** between the flow adapter and the Event Manager — the flow adapter is the
protocol entry point (HTTP via REST automation, or Kafka), and REST automation is the HTTP
boundary *behind* the adapter. Corrected the pipeline to **incoming request → flow adapter
(e.g. REST, Kafka) → Event Manager / flow engine → in-memory event bus → composable functions**
in all four places it had propagated: `docs/arch-decisions/ADR.md` (ADR-0004), the
`docs-content-canon` fact in `continuity.md` (substance correction, not a supersession),
`docs/guides/architecture.md` ("five stages" → four; REST automation reframed as the HTTP
boundary), and `docs/guides/documentation-conventions.md` (the canon page). The
`architecture.md` At-a-glance, `methodology.md`, and `getting-started.md` already described it
correctly and were left alone. Also tightened the ADR preface ("These five" → "ADR-0001 to
0005", so it stays meaningful as the ledger grows). Canon check re-run: exit 0.

## Follow-up — request-pipeline model refined (same session)

Eric supplied the authoritative pipeline, which corrected a second mistake of mine: REST
automation is the **event boundary in front of** the HTTP flow adapter (it *invokes* the
adapter), not behind it. Canonical model now: `user/calling app → event boundary (REST
automation for HTTP, a Kafka listener, or other protocol) → flow adapter → event manager/flow
engine → in-memory event bus → composable functions`. For each protocol there's a corresponding
flow adapter: for HTTP, REST automation holds the per-session request/response objects, does
endpoint rendering/serving/routing, and invokes the built-in HTTP flow adapter (`HttpToFlow`,
`http.flow.adapter`); for Kafka, the flow adapter embeds a topic listener and is fully async
(reply to another topic via a notification function). Verified against code: HTTP flow adapter =
`event-script-engine/.../HttpToFlow.java`; **no Kafka flow adapter / notification function in
this repo** — the `connectors/adapters/kafka/*` modules are the cloud connector (event mesh,
`cloud.connector=kafka`), a different concern. Re-corrected the model in `architecture.md`,
`documentation-conventions.md` (canon), ADR-0004, and the `docs-content-canon` fact. Added two
memory facts (`request-pipeline-model`, `kafka-adapter-not-in-repo`) and an Open Thread
(`thread-minimalist-kafka-adapter`) for the planned in-repo minimalist Kafka adapter +
notification function (deferred until after the docs-rewrite phase). Canon check: exit 0.

## Context for Next Session

- ADRs are read **on demand** only — never added to the per-session read path; `(ADR-NNNN)`
  tags on facts are human pointers, not a cue to open the ledger.
- New ADRs: prepend (newest-first), monotonic numbering, `Proposed→Accepted→Superseded/Deprecated`,
  never deleted. Keep `formalizes:` ↔ `(ADR-NNNN)` cross-links in sync; don't restate the
  constraint as competing truth (continuity stays authoritative).
- Open from prior session still pending Eric's call: Layer-1 overview as flat page vs `guides/event-driven/`
  folder (`thread-layer-reorg`); content-polish round 2 + AI context discovery (`thread-next-ai-context`).
- Review cadence: 6 sessions since last_review (review_every=10) — no review due.

## Memory References

- Referenced: functions-decoupled-routes, virtual-threads-rpc, typed-io-map-or-pojo, docs-content-canon, vision-mercury-composable
- Created:    adr-pattern-adopted, request-pipeline-model, kafka-adapter-not-in-repo
