# Session (2026-09-20T15:27:04.000Z)

**Agent:** Claude Code

## Summary

**A static decision table is graph DATA — the perceived `graph.task` gap is a doc gap, closed on both
engines.** Eric: "We want to configure one node to store a static decision table in a human readable
form … The feature gap that I perceive is that we cannot do input data mapping to a composable function
from a node with skill 'graph.task' and transfer all properties of the node to the composable function."
His reason: an AI agent had **hard-coded the static decision table inside a composable function and
bundled that function with a graph** — a design he wants to avoid (the table is then neither certifiable
from the graph nor reusable). His hint: a map or list can be text in a node property and the simple
plugin `f:json` converts it before data mapping. His ruling in advance: "if you figure out how to
achieve that with the current framework, it would just be a doc gap."

**Finding — no engine gap, on either engine.** `GraphLambdaFunction.initializeWithNodeProperties` (Rust
`initialize_with_node_properties`) copies every node's properties into the state machine at
instantiation: a skill node's non-reserved properties at `{node}.{key}`, a **skill-less node's whole
property map at `{node}`**. `DataMappingHelper.getLhsElement` resolves ANY selector against the state
machine (no namespace whitelist), so a `graph.task` input entry `state-rules -> table` hands the whole
table to the function in one line — exactly the ask. The property grammar already gives the human-readable
table: `keys[]=rule-a` / `rule-a[]=CA` appends into list properties (`MultiLevelMap` empty-index append),
the Playground renders a list property one row per element under its key (`MinigraphNodeBody.tsx`) and
the editor round-trips same-key rows as `[]` lines; a nested table is one `'''…'''` JSON text property
parsed by `f:json(state-rules.table)` at mapping time (plugin arguments accept any mapping source). No
compile gate or pre-run check inspects mapping SOURCES (`GraphModelValidator` and `CompileGraph` check
RHS metadata writes and the `->` shape only), and node type names are free text (`with type
DecisionTable`). The ONLY statement of this anywhere was one row of the namespaces table
(`{node-name}` — "a node's own properties") plus a half-sentence in the `graph.data.mapper` reference;
the `graph.task` reference, its in-Playground help and the AI agent guide never showed a data node feeding
a function — which is why an agent had no pattern to follow.

**Shipped (branch `docs/static-decision-table-on-a-node`, `00283800`, pushed; PR pending —
[[eric-code-changes-via-pr]], [[eric-release-rhythm]]).** No engine change.
- `docs/guides/knowledge-graph/skills-reference.md` (graph.task): the block *A static decision table is
  graph data, not function code* — `DecisionTable` node with `keys[]` (rule names in priority order) and one
  list property per rule, `select-rule` with `input[]=state-rules -> table`, the `f:json` idiom for a nested
  table, wire the node under the island, the versioned-graph rule (`v2026-08-prime-rates`).
- `help graph-task.md` (`describe skill graph.task` in the Playground): a *Static decision table* section.
- `ai-agent-guide.md`: pre-send checklist bullet *Static reference data is a node, not code* + a clause in
  recipe step 4 (the table node is wired under the island too).
- Proof pinned: `unit-test-task-9.json` (island `knowledge` → two `DecisionTable` nodes: `state-rules`
  with list properties, `state-rules-json` with one JSON text property; `select-rule` and
  `select-rule-json` each call the generic test-scope `v1.decision.table` — `DecisionTableFunction`
  reads rule names from `table.keys`, so the node's `purpose` travels harmlessly) +
  `GraphTaskTest.staticDecisionTableIsGraphData`: TX → `community-property`, NY → `separate-property`
  through both idioms; ZZ → the function's own 404 `No rule for ZZ` as the graph output. Green via
  `mvn -pl system/minigraph-playground-engine -am test -Dtest=GraphTaskTest`; `mkdocs build --strict` green.
- Twin in mercury: branch of the same name, `a0d2f4aa`, Increment 123 (byte-identical fixture, a Rust
  `DecisionTableFunction` + assertions in `graph_runtime.rs`; fmt/clippy/suite/mkdocs/links/claims green).

**Not done, deliberately.** No built-in decision-table skill or shipped lookup function — the point is that
the app's own generic function receives the table; a built-in would be Eric's call
([[clean-knowledge-design-over-engine-coverage]]: no engine feature where the design already works). PR
titles are given on their own line in the hand-off text ([[conv-squash-title-prefill-check]]).

## Doc Gaps

- Question: how does a `graph.task` node hand a node's static properties (a decision table) to its
  function? Guide pages: `skills-reference.md#task`, `help graph-task.md`, `ai-agent-guide.md`. Missing
  detail: that node properties are state-machine data (`{node}` source) and the `keys[]` / `f:json` idioms
  — source (`initializeWithNodeProperties`, `getLhsElement`, `MinigraphNodeBody.tsx`) was needed to
  confirm it. Closed by this session's branch.

## Memory References

- Created: `static-decision-table-is-graph-data` (tier: working)
- Referenced: `event-script-over-code`, `clean-knowledge-design-over-engine-coverage`,
  `eric-code-changes-via-pr`, `eric-release-rhythm`, `conv-squash-title-prefill-check`
- Superseded: (none)
