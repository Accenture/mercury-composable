# Session (2026-09-10T03:31:22.000Z)

**Agent:** Claude Code

## What happened

**The graph view's source is now the live session endpoint** (Eric's design,
2026-09-10): `useGraphData` fetches `GET /api/graph/session/{id}` — derived from
`sessionCollaboration.state.sessionId`, which arrives async via the mount-time
`session` round-trip — instead of pinned temp-model links. `useAutoGraphRefresh`
re-fetches the live endpoint on `graph.mutation` (300 ms debounce; import
immediate) instead of round-tripping `describe graph`; it no longer listens on
`graph.link` at all. `describe graph` / `export graph` stay human-operator
console commands (snapshot + link untouched); clicking a 🕸️ link row now
triggers a live refetch + Graph tab, with the URL still openable for the raw
snapshot.

**Retired:** `usePinnedGraphPath` (module-scope Map), `useSessionGraphRestore`
(the restore IS the initial-load path now), `initialFetchFailed` /
`quietInitialFetchFailure` (initial loads are always quiet — a fresh session
exports a zero-node graph, an unknown session 404s; both map to the canvas
empty state via `toRenderableGraph`, no toast, no tab yank). The Graph tab is
revealed only when a fetch delivers the FIRST content; mutation refetches ride
the `isRefreshing` overlay (no flicker — the old flow re-ran the clearing
initial-load path per mutation). `graphIdentity` for the run workflow is now
`sessionGraphPath` (mutations already invalidate via the workflow's own bus
subscription; reconnects via connectionEpoch). Collaboration needs no
graph-link forwarding dance: propagated commands execute in every member's own
session, so each member's bus emits the mutations and re-fetches its own
replica. Empty-state copy now points at `create node` / `import graph from`.

**Session collaboration hook moved above the graph state** in Playground.tsx —
its captured session id is what addresses the live endpoint. Bus-listener
insertion order stays safe (no other subscriber shares its event kinds; the
old "saved-graph after auto-refresh" ordering note died with the graph.link
listener).

Verification: typecheck + 321 vitest green (useGraphData suite rewritten for
the live contract — 8 tests incl. quiet 404, zero-node mapping, reveal-once,
refetch overlay, session-path change); live vite+engine smoke: mount fetch
200, three mutation refetches all `/api/graph/session/…`, SPA away/back
restored via ONE live fetch (the old expired-model 400 scenario is
structurally gone), describe minted its human link with NO auto-fetch, link
click = one live refetch, session reset cleared, zero browser console errors.
Bundle rebuilt (`npm run release`, index chunk 586.19 kB — slightly smaller,
two hooks deleted). Technical Documentation §§3.3–3.5, 6.4, 6.7, 6.9, 7.3,
7.4, 7.7, 7.11, 8.2, 8.3, 8.8, 9, P3/P8/P13 rewritten; stale minimap-hint and
panel-split-key lines fixed in passing.

## Memory References

- webapp-session-live-graph-restore (superseded — the fallback became the
  primary; see successor)
- webapp-live-session-graph-source (created)
- Superseded: webapp-session-live-graph-restore → webapp-live-session-graph-source
