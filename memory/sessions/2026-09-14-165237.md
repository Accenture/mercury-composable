# Session (2026-09-14T16:52:37.000Z)

**Agent:** Claude Code

## Broker-first hosting guidance for the AI companion (demo friction)

- Eric's live team demo surfaced friction in the minigraph AI companion: an agent asked to
  "host" a Playground session hand-rolled a WebSocket client, and **a hand-rolled client
  that misses the session contract's keep-alive (ping every ~20 s) appears to work, then
  dies silently at the server's idle timeout — mid-collaboration.** That failure mode is
  now named explicitly on every relevant surface, and the shipped broker is the
  recommendation everywhere.
- Root cause in the docs: `knowledge-graph/ai-agent-guide.md#hosting` LED with the raw
  5-step WebSocket contract (an invitation to hand-roll) and then claimed "the template
  ships" the broker — but `playground-session-broker.mjs` lived only in
  `examples/minigraph-playground/scripts/`; templates/starter-graph never got it.
- Changes (branch `docs/host-sessions-with-broker`):
  - ai-agent-guide #hosting: broker-first ("do not hand-roll"), keep-alive trap named
    twice (recommendation + contract step 4), wire contract demoted to "what the broker
    implements", location claim fixed to name BOTH homes, `--target` port guidance
    (starter default 8303 vs the example's 8085).
  - playground-and-companion #sessions: hosting pointer for agents.
  - templates/starter-graph: byte-identical broker copy under `scripts/` (starter-graph
    embeds minigraph-playground-engine, so hosting against it is a real workflow),
    adapted scripts/README.md (port 8303), README layout row + next-steps line, AGENTS.md
    item 3 (hosting; old 3–4 renumbered 4–5).
  - Both scripts/README.md files: the why-not-hand-roll paragraph.
- Verified: `node --check` on the copied script; ai-contract-provider tests green (the
  contract bundles these guides); starter-graph template tests green.
- Cross-engine note: the Rust repo carries an identical broker copy — its docs deserve the
  same broker-first pass in a Rust-side session (flagged to Eric, not done here).

## Memory References
- playground-session-broker — REACTIVATED (archived 2026-Q3 fact consulted for the
  broker's contract and the Rust-copy parity; substance delta to carry on reactivation:
  the broker now ALSO ships in templates/starter-graph/scripts/, and the AI docs are
  broker-first with the keep-alive failure mode named)
- eric-release-rhythm — consulted (branch/commit/PR prep; Eric opens the PR)
- conv-squash-title-prefill-check — consulted (title handed off on its own line)
