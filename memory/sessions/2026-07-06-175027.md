# Session (2026-07-06T17:50:27.000Z)

**Agent:** Claude Code
**Lightweight:** in-place agent-memory upgrade (tool files only); no `continuity.md` facts created or changed.

## Summary

Mode B in-place upgrade of the agent-memory layer: **v4.28.2 → v4.28.3** (one rung, additive/
non-destructive). Ran `ENABLE.md` → Mode B; installed = 4.28.2, current tool = 4.28.3. "Warn before
you clobber" guard ran first — the working tree was clean and the four `memory-lint` scripts were the
stock 4.28.2 copies (each carried the 4.28.2 "decay-eligible facts" marker and none of the 4.28.3
`archivable` code — no local customization), so the overwrite was safe.

- **Rung 4.28.2 → 4.28.3** — re-copied the four `memory-lint` scripts (`memory-lint.py`, `.mjs`, both
  mirror test files). This is **the fix mercury's own 2026-07-06-165910 review surfaced** (the 2nd
  continuity-bloat field report): the `continuity_max_lines` warning now branches on whether anything
  is archivable, so it stops prescribing a review that can't help.
- Re-synced the `continuity_max_lines` comment in `memory/decay-policy.md` **additively** — kept
  mercury's value (`600`) and its "raised from 300 in v4.24.0" wording, appended the note that the cap
  is meant to be raised for a legitimately large repo.
- Stamped `.agent/version.md` → `version: 4.28.3`, `last_upgraded: 2026-07-06` (preserved
  `enabled_with: 4.14.1`, `mode: A`).

**Verification (`memory-lint --root`, both runtimes byte-identical):** 37 facts / 708 lines /
**0 archivable**. The line-count `[continuity-bloat]` warning that this repo could not honestly clear
(nothing faded or superseded) now reads: *"nothing is archivable yet; the excess is active/dense
facts. Condense shipped decisions, or raise continuity_max_lines in decay-policy.md if this repo is
legitimately large."* — instead of "a review is due to lean it down." 0 errors.

So the earlier review's dilemma (a persistent lines WARN with no honest archival lever, and pressure
toward premature archival of active Key Decisions) is resolved: the message now names the real levers.
The fact-count check (v4.28.2) is unchanged; the deferred "condense shipped decisions" lever remains a
possible future feature if line-bloat-with-nothing-archivable keeps recurring.

Ran on branch `chore/release-4.6.1` (mercury's current checkout — I did not switch branches). Neither
this rung nor 4.28.3 touches `AGENTS.md`, the post-commit hook, adapters (skill descriptions unchanged
→ no re-sync), or source.

## Memory References
(none — no `continuity.md` facts referenced or created. Closes the 2nd mercury continuity-bloat arc,
after the v4.28.2 fact-count upgrade recorded in `2026-07-05-021352.md`.)
