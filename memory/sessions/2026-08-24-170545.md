# Session (2026-08-24T17:05:45.000Z)

**Agent:** Claude Code

**P4 engine-side documentation — the polyglot initiative's docs remainder, same day the
wrapper sites went live.** Context: earlier today the wrapper docs sites shipped and went
LIVE (python [PR #20](https://github.com/Accenture/mercury-python/pull/20), node
[PR #89](https://github.com/Accenture/mercury-nodejs/pull/89), both merged; sites at
accenture.github.io/mercury-python and /mercury-nodejs; the new wrapper ci.yml workflows
passed their maiden runs — the P4 wrapper-CI gap closed; node repo gotcha: its Pages
source still pointed at the legacy doc/2025-11-15 branch until Eric flipped it).

This repo's half, branch `feature/polyglot-docs`, submitted as
[PR #294](https://github.com/Accenture/mercury-composable/pull/294) (Rust twin: mercury
PR #214, commit `7e6797dc`):

- **guides/polyglot-functions.md** (Operate & integrate, after Event over HTTP) — peers
  vs subprocesses/ports rationale, wrapper family table, declarative wiring, the
  zero-code demo extended to a third language (the wrapper demos register
  `hello.declarative` and DEFAULT to port 8085 — the lambda-example's slot, which is why
  the 2026-08-22 flagship drive needed zero engine config change), graph.task v4.11.11
  floor, contract + operations sections.
- **ADR-0016 (Proposed, dated 2026-08-22 = decision day)** — "Polyglot functions are
  Event-over-HTTP peers, not subprocesses or ports"; formalizes
  polyglot-event-over-http-design. Rust ledger numbers independently → its twin is
  **ADR-0014** there.
- **Interop report: "The polyglot wrapper round (2026-08-22)"** — honest-evidence rule
  applied: claims exactly the recorded drives (golden vectors both wrappers, python⇄node
  both directions, composable-example flow → python function with my_correlation_id +
  engine trace id). NOT claimed: a live Rust-engine→wrapper drive (never run; offered to
  Eric as an optional extra gate). Intro + learnings updated: the "future language
  ports" playbook had its first execution.
- Cross-links: event-over-http.md ("Same demo, different language" + See also), home
  page (Guides line + Project family entry incl. both wrapper sites), llms.txt,
  CHANGELOG Unreleased.

**Eric's mid-flight home-page directive (Rust repo):** unify the Rust docs home on the
Java reference presentation — he screenshotted the "A layered ascent" table as the
anchor ("this section is more appealing"). Rust index.md rebuilt on the Java structure
(kept "Why a Rust port" + the port-divergence note) and the Rust site renamed
**"mercury" → "Composable for Rust"** for family naming consistency (Composable for
Java / Rust / Python / Node.js) — flagged to Eric as the one judgment call.

Gates: `mkdocs build --strict` exit 0 both engines; new cross-page anchors
(`#the-polyglot-wrapper-round-2026-08-22`) verified present in both built HTMLs; the
Rust home served locally (python http.server via launch.json) and visually verified —
table + chapter links + wrapper links render.

**CI round — both engines failed IDENTICALLY on first run, caught by the
ai-contract-provider's own fail-closed gate:** `Broken relative link in
references/guides/event-over-http.md -> polyglot-functions.md`. The snapshot's link
validator requires every relative link in a packaged reference to resolve INSIDE the
packaged set, and the set is driven by `skill/files.list` (Java: the checked inventory
SkillSnapshotTest pins against the jar's `guides/**` include set; Rust: build.rs embeds
exactly the listed files from `docs/`). Fix = one sorted files.list line per engine
(`references/guides/polyglot-functions.md`): Java `8c89b1ad`, Rust `d278d8ae`; module
gates green locally (Java module exit 0; Rust `cargo test -p ai-contract-provider`
exit 0 incl. the previously failing endpoints_e2e). All the chapter's own relative-link
targets were already packaged on both engines — verified before fixing.
**Durable lessons:** (1) a new docs page that any PACKAGED reference links to
relatively must join the ai-contract-provider `files.list` on BOTH engines — the
snapshot gate extends the docs cross-link surface beyond mkdocs strict; (2)
`gh pr checks --watch` can exit 0 BEFORE a late-registering check appears (the Rust
`test` check registered after the watch sampled) — always re-list checks after a watch
exits.

**MERGED AND LIVE same day.** [PR #294](https://github.com/Accenture/mercury-composable/pull/294)
squash `807a063c` == gated `8c89b1ad` (tree verified; squash title clean per
[[conv-squash-title-prefill-check]]); full reactor green on the PR (7m40s, incl. the
ai-contract-provider module exercising the files.list fix); branches deleted both ends;
ADR-0016 rides the merge. **Live site verified after the Pages build:** chapter at
/guides/polyglot-functions/ → 200; home page carries the wrapper family links; interop
report serves the wrapper-round anchor. **Rust twin also merged and live same day**
(PR #214 merge `c5221258`) — plus a follow-up nav round from Eric's directive (Rust
PR #215 merge `4573bd21`): the Rust site gained the Java site's "Operate & integrate"
and Orientation tabs, so the tab row is now IDENTICAL across the two engine sites (Eric
confirmed visually). **P4 is COMPLETE across all four repos** — wrapper sites + engine
chapters + ADRs + interop extension all live; remaining initiative work = P5 publishing
gates only.

Commit `47042791` — docs: polyglot functions chapter, ADR-0016, interop wrapper round

```
 CHANGELOG.md                                 |  12 ++
 docs/arch-decisions/ADR.md                   |  49 ++++++++
 docs/guides/event-over-http.md               |   7 ++
 docs/guides/polyglot-functions.md            | 180 +++++++++++++++++++++++++++
 docs/index.md                                |   6 +-
 docs/llms.txt                                |   1 +
 docs/test-reports/event-over-http-interop.md |  46 ++++++-
 mkdocs.yml                                   |   1 +
 8 files changed, 297 insertions(+), 5 deletions(-)
```

## Memory References
- Referenced: thread-polyglot-initiative (updated), polyglot-event-over-http-design,
  bp-polyglot-functions, conv-telemetry-presentation-parity, eric-release-rhythm,
  thread-docs-improvement-backlog, thread-release-4-11-11
