# Session (2026-09-14T16:39:20.000Z)

**Agent:** Claude Code

## Field snappy-java remediation upstreamed (CVE-2026-90559)

- The field's dependency scan flagged a second finding on the deployed v4.12.8 image:
  `org.xerial.snappy:snappy-java` 1.1.10.7 via kafka-clients 4.3.1 — CVE-2026-90559 /
  SNYK-JAVA-ORGXERIALSNAPPY-19778376, CWE-787 out-of-bounds write in
  `Snappy.uncompress(ByteBuffer, ByteBuffer)` (destination buffer capacity never
  validated against decompressed size), CVSS 4.0 8.7 / 3.1 7.5, no known exploit.
  **This time the field authored the remediation on their downstream copy** (their CI
  green) and Eric asked for it to be upstreamed with an independent correctness review.
  Per the compartmentalization rule the contribution is acknowledged generically as
  field-authored — no names, no tracker IDs.
- Independent verification, all claims CONFIRMED:
  - NVD: affected "through 1.1.10.8"; Maven Central metadata: 1.1.10.8 IS the newest
    release (2025-07-19) → nothing to pin to, unlike the zstd round. Exclusion is the
    only remediation.
  - Bytecode: kafka-clients 4.3.1 references `org/xerial` from exactly ONE class
    (`org.apache.kafka.common.compress.SnappyCompression`) and the kafka_2.13 broker jar
    has ZERO references of its own (it reuses the clients' compress classes) → the codec
    loads only when snappy data actually flows; the exclusion is inert otherwise.
  - Repo: no `compression.type` set anywhere; the six patched sites are exactly the six
    lz4 exclusion sites (kafka-clients in kafka-connector + minimalist-kafka; kafka_2.13
    in kafka-standalone + the minimalist-kafka/twin-kafka/sync-over-async test brokers);
    minimalist-kafka's Confluent serdes already exclude their transitive kafka-clients,
    so no seventh path reintroduces snappy.
  - The field's Maven mechanics are right: an exclusion rides its own declaration edge
    and never propagates by nearest-wins (unlike the zstd pin), hence six sites, not
    three poms.
- Change (branch `fix/snyk-exclude-snappy-codec`): snappy-java excluded at all six sites
  (comment names the CVE, the no-fix rationale and the removal condition — same shape as
  downstream: +11/+11/+11/+22/+11), CHANGELOG Unreleased → Security entry 2 with the
  consumer note (produce/consume snappy → declare snappy-java yourself, as with lz4) and
  the Rust-port note (librdkafka's snappy is its own C implementation — different
  codebase, not covered by this CVE; no Rust change).
- Verification: full reactor `mvn clean install` GREEN (32 modules, 06:40 — every Kafka
  test lane ran with snappy-java absent from the classpath); reactor-wide
  `dependency:tree -Dincludes=org.xerial.snappy,at.yawk.lz4,com.github.luben:zstd-jni`
  across all 32 modules: zero snappy-java, zero lz4-java, every zstd-jni resolution at
  1.5.7-16.
- Durable lesson folded into [[conv-kafka-transitive-pin-placement]]: the fact now has
  two arms — PIN when a fixed release exists; EXCLUDE the codec at every direct
  declaration site when none does.

## Memory References
- conv-kafka-transitive-pin-placement — updated (exclusion arm added: no-fixed-release
  case; exclusions never nearest-win → all six Kafka-artifact declaration sites, the lz4
  set; SnappyCompression single-bridge evidence)
- eric-release-rhythm — consulted (prepare branch/commits/PR text; Eric creates the PR)
- conv-squash-title-prefill-check — consulted (PR title handed off on its own line)
