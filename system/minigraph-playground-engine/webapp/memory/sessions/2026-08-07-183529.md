# Session (2026-08-07T18:35:29.442Z)

**Agent:** Codex

## Summary

Applied the user's follow-up minimap refinement under `ui-loop-engineer` run
`ui-minimap-hotkey-20260807-03`. The bottom-right visible control is now 1.75rem (28px) instead of
2rem, and its map glyph is 0.875rem (14px) instead of 1rem, making the requested reduction explicit
in both source and the served production CSS. The visibility hotkey changed from `Alt/Option + M` to
exact `Ctrl + M`; tooltip, `aria-keyshortcuts="Control+M"`, Help overview, technical documentation,
tests, and deployed assets all use the new chord.

The existing interaction safeguards remain: the listener exists only while the Graph tab is active,
matches physical `KeyM`, ignores editable targets, repeats, handled events, Alt/Meta/Shift variants,
and cleans up on disable/unmount. Click toggling, default collapse, GraphView-owned state, keyed-canvas
remount persistence, native minimap panning, node presentation, and disabled wheel zoom are unchanged.

## Verification and review

- Focused minimap/help tests failed first against the old shortcut and Help metadata; final focused
  tests passed 7/7 and cover Ctrl+M open/close, excluded combinations, editable targets, inactive
  tabs, accessibility metadata, click behavior, legacy minimap props, and keyed remounts.
- An attempted CSS-source unit assertion exposed two project-tooling constraints: Node types are not
  configured for test compilation, and this Vite CSS-module pipeline does not return source through
  `?raw`/`?inline` during Vitest. The brittle test was removed without adding dependencies; exact
  dimensions were instead verified directly in source CSS and the production CSS bundle.
- Full webapp Vitest: 23 files, 219/219 tests passed.
- `npm run release`: passed, including `tsc --noEmit`; Vite transformed 642 modules and emitted only
  the existing >500 kB main-chunk warning before deploying to Java public resources.
- `dist/` and `../src/main/resources/public/` have exact directory parity. Built CSS contains the
  1.75rem/0.875rem dimensions, and built JS contains exact Ctrl+KeyM guards plus `Control+M` ARIA and
  Help text. `git diff --check` passed.
- The package still has no lint script, so no lint command was available.
- Fresh correctness review found no actionable issue. Delivery review found one Minor stale
  `Alt/Option + M` sentence in scoped continuity; the live fact was updated and the targeted recheck
  closed it with no new findings. Historical session logs remain immutable.
- Residual manual boundary: no browser automation is installed, so final aesthetic approval of the
  28px/14px control and a physical-browser Ctrl+M smoke test remain manual checks.

## Memory References

- referenced and updated: webapp-toggleable-minimap
