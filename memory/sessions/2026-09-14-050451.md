# Session (2026-09-14T05:04:51.000Z)

**Agent:** Claude Code

## What happened

**The field's Snyk finding on v4.12.8 remediated, and the Unreleased changelog
accumulated** (Eric's priority call — the Rust Kafka port pauses at K2 with K3
parked). Branch `fix/zstd-jni-cve-2026-87823`.

**The finding** (field Snyk screenshot, verified against main):
CVE-2026-87823 / SNYK-JAVA-COMGITHUBLUBEN-19659584 — `com.github.luben:
zstd-jni@1.5.6-10`, CWE-190 integer overflow, CVSS 8.8, no known exploit,
fixed in 1.5.7-14. Path: `kafka-clients:4.3.1 → zstd-jni:1.5.6-10` (runtime).
Ten Maven projects flagged (every kafka-clients carrier, incl.
rest-spring-4-example via kafka-connector at compile scope). Verified
authoritatively: **kafka-clients 4.3.1 IS the newest release** (Central
maven-metadata), so Snyk's "no supported fix" is exact; zstd-jni's fixed line
runs to 1.5.7-16.

**The remediation and its non-obvious constraint:** the Kafka modules parent
to `spring-boot-starter-parent`, NOT `parent-mercury` — the reactor only
aggregates them — so a root-pom `dependencyManagement` override reaches
nothing. The fix pins **zstd-jni 1.5.7-16 as a DIRECT runtime dependency** in
the three client-declaring poms (minimalist-kafka, kafka-connector,
kafka-standalone — the last via the embedded broker artifacts), each with a
comment naming the CVE and the removal condition (kafka-clients shipping
≥1.5.7-14). Maven nearest-wins then fixes every downstream consumer —
verified with a reactor-wide `dependency:tree` (all ten projects resolve
1.5.7-16; per-module trees mislead by resolving stale ~/.m2 siblings). Full
reactor `mvn clean install` green. Recorded as
[[conv-kafka-transitive-pin-placement]].

**Rust port exposure: none** — `cargo tree` shows zero zstd anywhere
(librdkafka builds without the zstd codec; the feature is off).

**CHANGELOG.md gains the Unreleased section** (Eric: v4.12.9 waits for a
successful field deployment): the streaming return route (D1–D8 + E1–E4,
PRs #365–#374) as the headline Added; the `soa.redis.health` rename and the
minimalist-kafka test-scope consumer note under Changed; the RFC-3986 colon
fix, the racy closing-post test fixes and the Sonar round under Fixed; the
zstd pin under Security.

## Memory References

- conv-kafka-transitive-pin-placement (created - the pin-placement lesson)
- snyk-retired-manifest-placeholders (consulted - checked the finding did not
  touch the placeholder mechanism; it did not, all ten hits are live modules)
- field-sonar-gate-covers-overall-code (consulted - the whole-codebase gate is
  why one transitive CVE blocks the field, and why the fix sweeps every
  carrier rather than the demo that surfaced it)
- eric-release-rhythm (consulted - branch prepared and pushed; PR creation,
  merge and the v4.12.9 rollout are Eric's gates)
- conv-squash-title-prefill-check (consulted - PR title handed off on its own
  line in a code block)
