# Session (2026-09-25T19:09:51.000Z)

**Agent:** Claude Code

## Summary

**Lightweight — the field's Snyk findings on the 4.12.17 artifacts answered with four dependency bumps (branch
`chore/snyk-deps-jackson-msgpack-netty`, 37 poms, versions only).** Jackson 2 BOM 2.22.2 → 2.22.3 (CWE-770 CVE-2026-89425 on
`jackson-core`, reached through minimalist-kafka's Confluent 8.3.0 / Avro 1.12.1 path; fixed in 2.22.3), Jackson 3 BOM
3.2.2 → 3.2.3 (Eric — the current line's patch), Netty 4.2.17.Final → 4.2.18.Final (two CWE-770 findings on
`netty-codec-http`, CVSS 8.7 CVE-2026-93491 and 8.2; 4.2.18.Final lists three unbounded-resource fixes in that artifact),
`msgpack-core` 0.9.11 → 0.9.12 (CWE-674 CVE-2026-90472, MEDIUM — **best effort, Eric's call: not remediated by any release**: Snyk lists no fix, OSV says
"through 0.9.12", upstream issue #1015 open, 0.9.12 the newest tag; Mercury's `MsgPack` serializer never calls the library's
`unpackValue()`, and its own recursive descent over the internal wire format is an optional hardening). Snyk's "no supported
fix" only means the paths are transitive — our property pins move the resolved trees, proven by `dependency:tree` on
platform-core, minimalist-kafka and rest-spring-4. Field screenshots paraphrased; no client identifiers recorded. No fact,
decision or thread event; the release that carries the bumps records the version.

## Verification

`mvn clean install -DskipTests` on the new versions: whole reactor builds. `mvn clean install` (full): BUILD SUCCESS — 220 suites, 1514 tests, 0 failures, 0 errors, 3 skipped (surefire XML; identical to the v4.12.17 readiness numbers).

## Memory Note

`memory-lint` on this branch (cut from main) reports one advisory, `[overdue] ot-subgraph-for-each-suspend` — that completed
thread was already swept to the archive on the sibling branch `feat/graph-math-condition-and-typed-arithmetic` (`b8dcaff4`).
Not repeated here: two open PRs editing `memory/archive/2026-Q3.md` and `INDEX.md` for the same fact would conflict at the
second merge. Main is clean once both merge, in either order (this branch touches no memory file but this log).

## Memory References

(none)
