# Session (2026-06-24T22:27:52.000Z)

**Agent:** Claude Code
**User:** Post-upgrade integrity check, then the memory review ritual (`REVIEW.md`).

## What We Did

**Integrity check (`memory-lint`).** Ran the deterministic checker (Python) + the cross-runtime
test contract (Python 15/15, Node 15/15). Result: 0 errors, 8 advisory `[overdue]` warnings —
all bootstrap orientation facts. No id in two places, no over-archival, supersession links resolve,
`.agent/version.md` parses. The upgrade is structurally sound.

**Memory review (full-rebuild path).** Triggers: cadence (14 sessions since `last_review` ≥
`review_every` 10), on-command, and invariant-check due. Found the fact footers had never been
recomputed since creation (most stuck at `uses: 1` / bootstrap dates despite heavy use), so ran a
**full rebuild**: recomputed `uses` + `last_used` for all 34 facts (+ the Vision) deterministically
from each session's `## Memory References`, applied via `/tmp/rebuild_footers.py`.

**Archival decision (human-gated).** The 8 facts the lint flagged overdue (`sslu 23 > 20`) are the
project's bootstrap orientation — the 6 tech-stack facts + the add-capability and serialization
conventions. They went cold only because the last 23 sessions were 100% documentation work, not
because they stopped being true. Per `DECAY.md`, `core` is the human-set escape hatch for permanent
facts. **Eric chose: promote all 8 to `core`** (root-cause fix — they were mis-tiered as `active` at
birth) rather than archive. They stay in their orientation sections, never decay, and ride the
invariant-verification cadence for periodic re-confirmation.

**Invariant verification (first one, due).** `verify_invariants_every` 20 ≤ 24 session files and
`last_invariant_check` was unset → raised one Open Thread (`thread-verify-invariants-2026q2`) listing
the full never-decay set for Eric to re-confirm. The review never auto-invalidates an invariant.

**Contradiction / drift backstop.** Scanned all facts: no contradictions. The `docs-no-redirects`
vs `docs-style-conventions` / `docs-content-canon` relationship is an *aspect* supersession (only the
redirect language), already documented in prose — not a tier change. No broken supersession links
(none use footer links). VBDI altitude trace intact (Blueprint threads still `serve`
`vision-mercury-composable`).

**Verification.** Re-ran `memory-lint` after the edits — expect 0 errors, 0 overdue warnings
(the 8 are now `core`, exempt).

## Memory Review (2026-06-24, through 2026-06-24-222752)
- Reactivated:    0
- Superseded:     0  (the docs-no-redirects aspect-supersession was already recorded, no fact is false)
- Archived:       0  (the 8 overdue facts were promoted to core by human decision, not archived)
- Promoted core:  7 net  (stack-language-java21, stack-build-maven, stack-integration-spring,
                      stack-messaging-kafka, stack-ci-gha, conv-add-capability, conv-serialization-gotchas
                      — Eric-confirmed. stack-persistence-r2dbc was promoted then demoted back to active in
                      the same session — see Post-review fact corrections below.)
- Swept threads:  1  (thread-ci-docs-build → memory/archive/2026-Q2.md — completed `[x]`, CI docs-build
                      workflow shipped 2026-06-20, now >archive_window sessions old)
- Archive-verify: pass (memory-lint: no id in both continuity and archive; the one swept thread is
                      in the archive only)
- Tier changes:   19 net — 7 →core (above); 6 working→active (docs-style-conventions, docs-content-canon,
                  docs-no-redirects, adr-pattern-adopted, request-pipeline-model, kafka-adapter-not-in-repo);
                  6 active→archive-candidate (cooling docs-rewrite design facts: docs-rewrite-architecture,
                  docs-dsl-spec, docs-dir-layout, pom-version-source-of-truth, conv-canonical-example,
                  pref-github-source-of-truth)
- Metadata repair: full rebuild — uses/last_used recomputed for all 34 facts + Vision (footers were
                  stale since creation; reviews had stamped last_review without applying events)
- Invariants:     prompted — first invariant check (thread-verify-invariants-2026q2 raised); next due in 20 sessions
- Notes:          kafka-mesh-opt-in (core) absent from any session's `## Memory References` — ledger gap,
                  metadata left as-is; thread-next-ai-context / thread-minimalist-kafka-adapter likewise
                  created without a `Created` ledger entry (uses/last_used taken from where they appear)

## Post-review fact corrections (Eric, same session)

- **Build (`stack-build-maven`):** confirmed Maven 3.9.7+ is the *current* build tool; **Gradle support
  is planned** to be added alongside it. Annotated the fact + raised `thread-add-gradle-build`. Stays `core`
  (Maven is genuine bedrock); bumped uses 1→2.
- **Persistence (`stack-persistence-r2dbc`):** corrected — PostgreSQL/R2DBC is **not part of the framework
  core**; the framework is persistence-agnostic and `extensions/reactive-postgres` is just an example/optional
  add-on. Because an example is *not* never-decay bedrock, **demoted `core → active`** (it was mis-promoted in
  the review above). Rewrote the fact in place; bumped uses 1→2. This drops the core-promotion count from 8 to 7
  and the invariant-check thread was updated to match.

## Invariant verification — completed (Eric, same session)

Walked the full never-decay set with Eric and **closed `thread-verify-invariants-2026q2`** —
all 11 confirmed to hold:
- Architectural Invariants: `functions-decoupled-routes`, `typed-io-map-or-pojo`
- `core` Key Decision: `kafka-mesh-opt-in`
- core stack/conventions: `stack-language-java21`, `stack-build-maven`, `stack-integration-spring`,
  `stack-messaging-kafka`, `stack-ci-gha`, `conv-add-capability`, `conv-serialization-gotchas`
- Vision: `vision-mercury-composable`

One content correction during the walk: **`stack-language-java21`** — removed the Kotlin clause
(Kotlin is only an example module, not a framework language). `stack-persistence-r2dbc` was already
out of the set (demoted earlier this session). `last_invariant_check` now reads *confirmed*, not just
*prompted*. Verification counted as one reference this session for each confirmed fact (uses bumped;
`kafka-mesh-opt-in` thereby got its first real ledger reference, closing its earlier ledger gap).

## OpenTelemetry forwarder — built (the chosen outstanding item)

Picked the OTel "open item" off the outstanding list. It was **untracked in memory** — reconstructed from
the branch: W3C span propagation across all layers was already committed (5 commits), but the **export**
piece was missing. `distributed.trace.forwarder` is only a framework extension point; the repo shipped no
forwarder. Eric chose **reusable extension** (like `reactive-postgres`).

Built `extensions/opentelemetry-forwarder` (new reactor module):
- `OpenTelemetryForwarder` — `@PreLoad(route="distributed.trace.forwarder")` `@ZeroTracing`; auto-registers
  (under always-scanned `org.platformlambda`).
- `TraceMetricsSpanData` — builds OTel `SpanData` **directly** from the trace dataset, preserving Mercury's
  exact W3C trace/span/parent-span IDs (the `Tracer` API would regenerate them). javap'd the 1.45 SpanData
  contract first (note: `getInstrumentationLibraryInfo()` is abstract there).
- `OtelForwarderContext` — config-driven OTLP/HTTP exporter + `service.name` resource + scope; test seam
  via `install(...)`. OpenTelemetry SDK 1.45.0 (BOM).
- Verified: 7 module tests pass; `-am install` rebuilt platform-core from the branch (BUILD SUCCESS);
  event-script `SpanPropagationTest` 4/4. Docs updated (build-test-deploy, configuration-reference,
  reserved-names) + module README.

**Credentials + mock endpoint (Eric's follow-up).** The first cut had no auth path and no real HTTP test.
Added backend-credential support via the standard `OTEL_EXPORTER_OTLP_HEADERS` (+ traces-specific) env var
and an `otel.exporter.otlp.headers` property (resolution: property → env → default), wired through a new
`buildExporter(endpoint, timeout, headers)` seam. Added an embedded JDK `HttpServer` **mock-collector test**
that asserts the OTLP/HTTP POST hits `/v1/traces` with the `Authorization` header (answers "do we have a mock
endpoint?" — now yes). Module tests now 9/9. Docs + the `otel-w3c-tracing` fact updated with the credentials.

**Mock endpoint redone the composable way (Eric's follow-up).** Replaced the raw `HttpServer` mock with a
Mercury function: `MockOtlpCollector` (`@PreLoad`, route `mock.otlp.collector`, `TypedLambdaFunction<AsyncHttpRequest,
EventEnvelope>`) exposed at `POST /api/v2/otlp/v1/traces` via test `rest.yaml` + `application.properties` +
`MockOtlpAppMain` (`@MainApplication`). `OtlpComposableExportTest` boots it with `AutoStart.main` (single call —
`AppStarter.main` → preload → `startHttpServerIfAny`; no rest-spring needed, platform-core has a Vert.x server)
and drives the real OTLP exporter against it. Self-contained + IDE-runnable so a human can curl it. Validated
the approach against `docs/llms.txt` → REST-automation guide (dogfooding the docs). Still 9 tests, clean run.

**Mock now decodes the OTLP protobuf (Eric — unit-test-as-documentation).** Eric logged the request body and
saw it's a protobuf byte array (SDK-produced `ExportTraceServiceRequest`). Enhanced `MockOtlpCollector` to
decode it (`io.opentelemetry.proto:opentelemetry-proto` 1.3.2-alpha, test scope) and log the span key-values
(trace/span/parent ids as hex, name, kind, timing, status, attributes, resource service.name). The composable
test now asserts the round-tripped ids + `service.name` survived the wire — strongest reviewer evidence. Still
9 tests, green.

**End-to-end pipeline test (Eric).** Added `OtlpTracePipelineTest` + 3 demo functions (`fun.1`/`fun.2`/`fun.3`):
a trackable `new PostOffice("unit.test", util.getUuid(), path)` sends to `fun.1`, which relays via
`new PostOffice(headers, instance)` → `fun.2` → `fun.3`. That emits 3 linked spans; the **real**
`OpenTelemetryForwarder` (auto-registered, its context installed to point at the mock endpoint) carries them to
`MockOtlpCollector` automatically (Telemetry → `distributed.trace.forwarder` → OTLP). Asserts: same trace id,
and lineage root(fun.1) ← fun.2 ← fun.3 (fun.1 parent empty, fun.2 parent = fun.1.span, fun.3 parent = fun.2.span)
— all decoded off the protobuf wire. 10 tests, green.

**Level-2 (Event Script) validation (Eric).** Added `event-script-engine` (test scope) + a minimal `task.1 →
task.2` flow (`flows/otel-trace-test.yml` + `flows.yaml` + `yaml.flow.automation`) + `Task1`/`Task2` functions.
`OtlpFlowTraceTest` drives it via `FlowExecutor.request(...)` with a UUID trace id through the real forwarder →
mock. Confirmed: task spans chain exactly like Level-1 (`task.2` parent = `task.1`) **plus the synthetic
`task.executor` flow-summary span** (annotated `annotation.flow=otel-trace-test`), and the RPC round-trip record
(no `span_id`) is gracefully skipped. So the forwarder handles the flow summary. `MockOtlpCollector` now also
stashes decoded span attributes. 12 tests, green.

**SonarQube + security touch-up (Eric, in progress).** (1) CVE-2024-7254: `opentelemetry-proto` 1.3.2-alpha pulled
`protobuf-java` 3.23.4 (vulnerable); bumped proto to 1.10.0-alpha → protobuf-java 4.34.0 (fixed). (2) `TraceMetricsSpanData`
S107 (10-param private constructor) → collapsed to 4 args (`metrics, annotations, resource, scope`), computes fields
internally + a `buildAttributes` helper. (3) Renamed its `StatusData status` field → `statusData`. (4) `OpenTelemetryForwarder`:
removed redundant `@PreLoad instances=1`, removed a plain-text-link Javadoc URL, and added `noArgConstructorConfiguresFromProperties`
test so the reflective no-arg constructor isn't flagged "never used". 21 tests green; coverage gate still passes
(line/instruction 95.4%).

Round 2: `TraceMetricsSpanData` nested ternary → `spanName` helper; attribute keys reuse the `ORIGIN`/`STATUS`/
`EXCEPTION`/`FROM`/`PATH` constants (no duplicated literals); `toDouble`/`toBool` lost their always-constant `dft`
params. Round 3: `OtelForwarderContext` deprecated `InstrumentationScopeInfo.create(3-arg)` → `builder()`;
`MockOtlpCollector` `record`→`received` (restricted id), `offer`→`add`, `attribute(key)`→`serviceName(...)`;
the 3 boot tests dropped the `Thread.sleep` port-poll for a shared `TestBoot.start()` that waits on the
`async.http.response` provider (server-ready signal, no busy-wait), `SpanExporter` via try-with-resources, and
removed now-local `port` fields. Round 4: `OpenTelemetryForwarderTest` dropped unneeded `throws Exception` +
`get(0)`→`getFirst()`; `TraceMetricsSpanDataTest` class-level `@SuppressWarnings({unchecked,deprecation})` (justified
test casts + the deprecated-but-mandatory `SpanData.getInstrumentationLibraryInfo`) + `assertTrue(!x)`→`assertFalse(x)`.
Cosmetic spell-check items ("Otel"/"Otlp"/"alice") left as-is (domain terms / test data). **SonarQube/IDE-flagged
round complete** — 21 tests green, coverage gate passes (line 95.5% / instruction 95.6%).

**Test coverage to the 85% bar (Eric).** Added the JaCoCo plugin (matching reactive-postgres) + a few
robustness tests (string-typed/invalid metrics, status-only error, name fallbacks, all-zero ids, isEnabled,
forward no-op, instrumentation-info). Result: line 95.6% / instruction 95.5% / branch 84.6% (20 tests). Wired
a `jacoco:check` gate enforcing LINE + INSTRUCTION ≥ 0.85 (branch not gated — its only gap is the boot-disabled
constructor branch). Build passes the gate.

**Config-driven for production (Eric).** Replaced the manual test bootstrap
(`buildExporter` + `install`) with self-configuration: `OpenTelemetryForwarder` got a no-arg constructor that
reads `application.properties` via `AppConfigReader` (values support `${ENV_VAR:default}`); credentials via
`otel.exporter.otlp.headers=${OTEL_EXPORTER_OTLP_HEADERS}` with **no default** (static-analysis-safe — unset →
null → `"null"` → no header). Added a package-private 2nd constructor for injecting an in-memory context in unit
tests; removed the `OtelForwarderContext` static install()/getInstance()/fromConfig() singleton seam (it's now a
pure holder + static `buildExporter`/`parseHeaders`). Boot tests dropped their `@BeforeAll` bootstrap — the
forwarder self-configures (log confirmed: `service=mercury-otel-demo, endpoint=…8299…, credential headers=[]`).
12 tests, green.

**Precision fix + namespace cleanup (Eric review).** (1) Dropped the `mercury.` prefix from span attribute
keys (`route`/`from`/`origin`/`status`/`exec_time_ms`/`round_trip_ms`/`exception`; `url.path` + `annotation.*`
kept). (2) `exec_time`/`round_trip` are **float** ms in Mercury — `toDouble` was widening via
`Number.doubleValue()` and surfacing noise (`0.007 → 0.007000000216066837`); now parses via the canonical
string form so the authored 3-decimal value is preserved. Added `floatExecTimeKeepsThreeDecimalPrecision`
unit test. 11 tests, green.

**Observability docs page (Eric).** Added `docs/guides/observability.md` under **Operate & Integrate** — the
built-in distributed-tracing design (enable via rest.yaml/FlowExecutor/PostOffice, `@ZeroTracing`/`skip.rpc.tracing`,
the trace-metrics dataset, the 3-layer span tree incl. the synthetic `task.executor` flow summary) + OpenTelemetry
compliance (W3C `traceparent`, the `distributed.trace.forwarder` hook, the `opentelemetry-forwarder` extension with
config/credentials/Dynatrace+Splunk). Followed the doc canon (frontmatter + At-a-glance + See-also + slug anchors).
Wired into `mkdocs.yml` nav, `llms.txt` (Operate & integrate), `configuration-reference.md` (`{#observability}`
anchor + pointer), and a cross-ref from `build-test-deploy.md`. `check-doc-canon.py` passes; **`mkdocs build --strict`
passes with zero warnings** (via the `/tmp/mkdocs-venv` Eric provided).

**Shipped.** Staged/committed/pushed on `feature/open-telemetry` as 4 commits — agent-memory upgrade (reordered
to be first, via `reset`+`cherry-pick`+`--force-with-lease`; tree verified byte-identical), then feat (forwarder
extension), docs (Observability), chore (memory). `gh pr create` blocked for the EMU account, so Eric opened it
manually: **[PR #122](https://github.com/Accenture/mercury-composable/pull/122)** — **approved and merged to `main`**.

## Memory References
- Referenced (review recomputed metadata for all): every active fact in continuity.md
- Referenced: stack-build-maven (confirmed current build tool; Gradle planned)
- Corrected: stack-persistence-r2dbc (example extension, not framework core; demoted core→active)
- Re-verified (invariants, confirmed hold): functions-decoupled-routes, typed-io-map-or-pojo,
  kafka-mesh-opt-in, stack-language-java21, stack-integration-spring, stack-messaging-kafka,
  stack-ci-gha, conv-add-capability, conv-serialization-gotchas, vision-mercury-composable
- Corrected: stack-language-java21 (removed Kotlin clause — example only, not a framework language)
- Created: thread-verify-invariants-2026q2 (working, closed `[x]`), thread-add-gradle-build (working),
  otel-w3c-tracing (working), thread-docs-improvement-backlog (working — docs sprint backlog: human
  storytelling + AI token-efficiency, seeded from a fresh-agent doc-discovery pass this session)
