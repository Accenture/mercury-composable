# Session (2026-08-10T17:58:46.290Z)

**Agent:** Codex

## Summary

Moved the minimap toggle out of the bottom-right import-toast lane under `ui-loop-engineer` run
`ui-minimap-left-controls-20260810-05`. `GraphMinimap` now owns React Flow's native bottom-left
`Controls` and appends its toggle as the fourth/bottom `ControlButton`, after zoom in, zoom out, and
fit view. The custom button inherits the native 26px stack geometry and uses a 12px line icon; its
hint opens to the right with a left-pointing arrow. The opened colour-coded/pannable minimap remains
bottom-right without the obsolete offset that reserved space for the old standalone button.

All existing behavior remains: default collapse, click and `Ctrl + M` toggling, shortcut guards,
one-shot/pause-aware hint timing, focus restoration, keyed-remount state, ARIA metadata, canvas-safe
classes, node colours/mask/background, drag navigation, and disabled wheel zoom. Responsive review
found that the existing `≤768px` toast spans the viewport, so at widths up to 1280px the toast lane
now uses a shared 11rem bottom safe area above the four-button controls and 150px bottom-right minimap.
Technical documentation and the checked-in Java-served production assets were updated.

## Verification and review

- Placement tests failed first against the standalone bottom-right Panel. Final focused coverage
  passed 15/15 and pins one GraphView controls owner, direct native button ordering, minimap as the
  final DOM child, right-side hint containment, and preserved toggle/hint/navigation behavior.
- The first GraphView composition-test mock used unstable state setters and caused an isolated Vitest
  worker OOM; stable hook mocks removed the artificial rerender loop, and the focused test then passed
  normally without increasing the heap limit.
- Full webapp Vitest passed: 25 files, 229/229 tests.
- `npm run release` passed, including `tsc --noEmit`; Vite transformed 643 modules and emitted only
  the existing >500 kB main-chunk warning before deploying to Java public resources.
- `dist/` and `../src/main/resources/public/` have exact directory parity; production CSS contains
  the right-opening hint and responsive graph-overlay safe area. `git diff --check` passed.
- Fresh review round 1 found one regression-coverage gap and one responsive toast collision, plus
  stale live-memory wording. The first targeted recheck closed the tests but caught that the initial
  8.5rem clearance covered the controls, not React Flow's 150px opened minimap. Review round 2 ran the
  skill-mandated Step 10 from-scratch comparison (runtime ResizeObserver collision coordinator versus
  centralized CSS); the lighter CSS design remained, corrected to a shared 11rem value. The final
  targeted recheck closed all findings with no new regression. Browser automation is not installed,
  so pixel-level checks at 768/769px remain manual.

## Memory References

- referenced and updated: webapp-toggleable-minimap
