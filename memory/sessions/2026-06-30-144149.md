# Session (2026-06-30T14:41:49Z)

**Agent:** Claude Code

## Summary

**Schema Registry optimization iteration** (continues the minimalist-kafka schema work). Three changes,
all validated end-to-end by Eric (multi-terminal) and shipped on **PR #127**
(`feature/schema-registry-cache-and-store`, 3 commits off `main`):

1. **In-memory schema cache (minimalist-kafka).** Replaced the `/tmp` file-backed cache
   (`FileCachedSchemaRegistryClient`) with platform-core's `ManagedCache`
   (`ManagedCacheSchemaRegistryClient`): stores the `ParsedSchema` by id directly (drops the
   JSON/Avro/Protobuf file reconstruction + the `/tmp` dependency), bounded / TTL-expired / housekept by
   the platform. **Positive results only** — a not-found id is never cached (the lookup throws before the
   write) and Confluent's own `missing.*.cache.ttl.sec` are pinned to 0. Default TTL **24h → 30m**.
   Dropped the `schema.registry.cache.dir` config key.
2. **Producer pool `simple.kafka.notification` 10 → 5** — it runs under `@KernelThreadRunner` (scarce
   kernel threads, not virtual threads); publishing is fast + ack-bound, so a small single-flight pool
   suffices. Javadoc advises keeping the instance count small.
3. **Per-id store + on-demand pickup (schema-registry-standalone).** `SchemaStore` now persists one
   `<id>.json` per schema (not a single `schemas.json`); `get(id)` loads `<id>.json` on demand, so a file
   dropped into the store dir while the server runs is served on the next GET — no restart. Demo seed →
   `registry/{1,2,3}.json`.

**Forensic conclusion (the headline finding).** Eric reported a repeatable "schema not found persists."
Decompiled Confluent 8.2.0 (`CachedSchemaRegistryClient`) + tests: the id→schema path
(`getSchemaById` → `getSchemaBySubjectAndId`) consults/populates ONLY the positive `idToSchemaCache`; a
404 throws with no negative memo, and the `missingIdCache` (producer-side `getId`-by-content) defaults to
TTL 0 = off. So **there is no client-side negative caching** — the persistence was the standalone mock
loading `schemas.json` only at boot (no reload). Fixed by the per-id on-demand store. The client-side
hardening (positive-only + 30m TTL) remains valid for the *stale-positive* dev case (a re-seeded id whose
content changed).

**Validation (Eric, multi-terminal):** `GET /schemas/ids/2` → 404, then → 200 (AVRO) after dropping
`2.json` in with **no restart**; JSON/Avro/Protobuf sync-over-async round-trips all succeed.
minimalist-kafka **52** tests + schema-registry-standalone **8** tests green, coverage gates met.

**Also committed this session:** the agent-memory **v4.28.0** upgrade (pending from the prior session)
straight to `main` (`ddb3aa66`). All commits + the PR use `Co-Authored-By: Claude Code` per the v4.28.0
stable-agent-name convention.

## Memory References
- Referenced (updated): minimalist-kafka-schema-registry, standalone-schema-registry-mock
- Referenced: agent-memory-upgrade-v4280
