# Session (2026-06-29T22:36:51.000Z)

**Agent:** Claude Code

Follow-up after PR #124 merged to `main`: automate the docs-site publish.

## Context + fix
- Eric noticed the live docs site (https://accenture.github.io/mercury-composable/) was still 4 days
  stale after the #124 merge. Diagnosed: the `docs` workflow **did** run on the main push and passed, but
  it only **verified** the build (`mkdocs build --strict` + four anti-drift checks) — it had **no deploy
  step**, so it never pushed to `gh-pages`. Publishing had been a manual `mkdocs gh-deploy`.
- Compared with the agent-memory reference `docs.yml` (single build+deploy job, `contents: write`,
  `mkdocs gh-deploy --force --strict`). Mercury was missing exactly the deploy step.
- Fix (commit on branch `ci/docs-gh-pages-deploy`, off `main`): split into a read-only `verify` job
  (push + PR: anti-drift + strict build) and a `deploy` job (push-to-main / `workflow_dispatch` only,
  `needs: verify`, `contents: write`, `docs-deploy` concurrency) running `mkdocs gh-deploy --force --strict`.
  Added `workflow_dispatch` for on-demand refresh. Kept mercury's anti-drift checks (the reference lacks them).
- **No GitHub Pages settings change needed**: Pages is "Deploy from a branch → gh-pages", which is exactly
  what `mkdocs gh-deploy` targets. `mkdocs.yml` uses the built-in `readthedocs` theme + `search` plugin only,
  so `pip install mkdocs` suffices.

## State
- Branch pushed; **PR could not be opened via `gh`** — org is Enterprise Managed Users, which blocks
  `createPullRequest` over this API token. Eric to open the PR from the GitHub compare UI (branch is on origin).
- Once merged, docs auto-publish on the next docs change; a manual run (Actions → docs → Run workflow)
  refreshes immediately to publish the now-merged schema docs.

## Memory References
- Related: [[minimalist-kafka-schema-registry]] (the merged schema docs this will publish).
