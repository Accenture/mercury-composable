# Session (2026-06-27T00:51:08.000Z)

**Agent:** Claude Code

## Summary

Externalized the minimalist-kafka **Kafka client config into template files** so the wide variety of
enterprise Kafka installs is handled by configuration, not code (Eric's direction). Verified the existing
cloud-connector `kafka.properties` convention first and mirrored it, split into producer/consumer.

Decisions (Eric): pin **serializers + consumer commit/poll/group.id** in code; **bootstrap.servers
template-only** (drop the `kafka.bootstrap.servers` code property).

### New (main)
- `system/minimalist-kafka/src/main/resources/kafka-producer.properties` + `kafka-consumer.properties` —
  templates with `${ENV_VAR:default}` placeholders, `bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:127.0.0.1:9092}`,
  `acks=all` (producer), `auto.offset.reset=earliest` (consumer), and a commented enterprise security block
  (SASL/OAuth2/mTLS).
- `KafkaClientConfig` — loads a template via `ConfigReader` from a file→classpath fallback location
  (`kafka.producer.properties` / `kafka.consumer.properties`, default `file:/tmp/config/...,classpath:/...`),
  flattens via `getCompositeKeyValues()` (env substitution), and **pins** the contract params over the
  template: producer key/value serializers; consumer deserializers + `enable.auto.commit=false` +
  `max.poll.records=1`. (Per-topic `group.id` is added by `KafkaFlowAdapter`.)

### Changed (main)
- `KafkaFlowAutoStart` — builds producer/consumer Properties via `KafkaClientConfig`; removed the hard-coded
  `newProducer()` and the `kafka.bootstrap.servers` reading.
- `KafkaFlowAdapter` — constructor now takes the base consumer `Properties` (sets per-topic `group.id`)
  instead of a bootstrap string; dropped the hard-coded consumer props.

### Changed (test)
- `KafkaFlowAdapterTest` / `RestFlowMvpTest` — inject the embedded broker via the `KAFKA_BOOTSTRAP_SERVERS`
  system property (resolved by the template placeholder) instead of `kafka.bootstrap.servers`.
- `KafkaFlowAdapterConfigTest` — adapter constructor now takes `Properties` (empty; validation throws first).
- Both test `application.properties` — removed dead `kafka.bootstrap.servers`; documented the template
  location keys (per `pref-explicit-test-config`).
- New `KafkaClientConfigTest` (2) — proves the template loads and the pins override it.

### Validation
- `mvn clean install -f system/minimalist-kafka` → **18 tests** pass, coverage met (incl. the embedded-broker
  KafkaFlowAdapterTest through the new template path).
- `mvn clean test -f extensions/sync-over-async` → **29 tests** pass, coverage met (RestFlowMvpTest full
  REST→Kafka→Redis round-trip via templates packaged in the minimalist-kafka jar).
- `memory-lint` run after the continuity edits.

## Memory References
- Created: `kafka-client-config-templates` (born `tier: working`)
- Referenced: `pref-explicit-test-config`, `kafka-flow-failure-dlq`, `soa-config-driven-init`
