# Session (2026-09-16T03:20:12.000Z)

**Agent:** Claude Code
**Summary:** Prepared the **v4.12.10** release — CHANGELOG cut, version sweep across 43 build files,
full-reactor + Gradle-template + standalone-module verification. Branch `release/v4.12.10`; PR-open,
tag and publish remain Eric's gates ([[eric-release-rhythm]]).

## What happened

### Scope — 2 PRs, #399 and #400

`git log v4.12.9..main`, not memory: **#399** (Redis client-separation docs + the Berkeley DB
retirement, with ADR-0024 accepted) and **#400** (the v4.12.10 benchmark baseline and
`BENCHMARK-LOG.md`). A small release by count, but #399 carries a hot-path change — `ServiceQueue`
collapsed to one dispatch mode — which is why the benchmark baseline went in before the cut rather
than after.

### CHANGELOG

`## Unreleased` → `## Version 4.12.10, 9/15/2026`. Local date, matching the CHANGELOG convention and
v4.12.9's own heading — this is the **second release dated 9/15**, which is accurate for a same-day
loop (the run clock was 20:11 PDT / 03:11 UTC the next day).

The Unreleased block already covered #399. Added an **Added** entry for #400 — the benchmark log,
the no-regression result, and the preserved BDB-vs-file comparison — then reordered the sections to
**Added / Changed / Removed** so the release reads like v4.12.9's and like Keep a Changelog; they had
accumulated as Changed-then-Removed.

### Version sweep — 43 build files, 97 literals

Same surface as v4.12.9 (no modules added or removed). Verified before editing rather than assuming:
a repo-wide grep for `4.12.9` outside `pom.xml`/`build.gradle` returned CHANGELOG history, memory
logs, **and two live files worth checking individually** —
`draft-design-specs/distributed-cache.md` ("shipped for v4.12.9") and the open threads
("JAVA SHIPPED for v4.12.9", "for v4.12.10, after v4.12.9 ships"). All three are accurate statements
about where the cache shipped, so they stay; only build files moved.

### Verification

- Full reactor `mvn clean install` at 4.12.10 — **BUILD SUCCESS, zero test failures or errors**
  (checked by grepping for any non-zero Failures/Errors, not the BUILD line alone).
- All three starter templates built with **Gradle**, each producing a `4.12.10` jar.
- **`benchmark-reporter` built standalone** — it is deliberately outside the reactor, so a reactor
  build proves nothing about it; its pom carries a version literal and is in the sweep, so it needs
  its own check. New to this release's verification, and the reason is the stale-build-command bug
  found while running the benchmark.
- `check-doc-canon.py` OK.

### Deliberately NOT done

`latest_release` stays at v4.12.9 until the tag exists, so the squash hash and release time are
recorded rather than invented — same as the last two releases.

## Memory References
- referenced: eric-release-rhythm (prepare only; PR-open and tag/publish separately gated),
  conv-template-version-sweep (build-files-only surface incl. build.gradle),
  snyk-retired-manifest-placeholders (the two non-reactor placeholder poms),
  elastic-queue-file-store + minigraph-dev-mode-app-shape (the release content),
  conv-squash-title-prefill-check, conv-declare-consulted-references
- created: (none — the release is a state change, not a new durable fact)
