# Session (2026-08-30T05:00:40.000Z)

**Agent:** Claude Code

## Summary

**The progressive-rendering program COMPLETE across all four repos, shipped as the
v4.12.0 milestone** (Eric: "useful by itself and yet laying the foundation for AI
SDLC"). This repo's PR:
[PR #302](https://github.com/Accenture/mercury-composable/pull/302) squash
`6e034293` == gated `ee23d8fc` (tree verified, title clean, branches deleted).
Sibling merges same day: Rust
[PR #219](https://github.com/Accenture/mercury/pull/219) merge `bc1a9fd2`;
mercury-python [PR #21](https://github.com/Accenture/mercury-python/pull/21)
merge `bfca7e4`; mercury-nodejs
[PR #90](https://github.com/Accenture/mercury-nodejs/pull/90) merge `40a9f8f`.
All four at v4.12.0 - the wrappers jumped 0.1.0 -> 4.12.0 onto the engine
lock-step line (resolving the P5 npm-vs-legacy-4.3.x version question; the
publish acts themselves remain Eric-gated).

**This repo's round** (on top of the merged Phase 1/2, #299-#301):

1. **Demo relay** (lambda-example): `GET /api/hello/remote` via
   `hello.remote.relay` - stream:true endpoint forwarding its reply lane into
   the event-over-http mapped `hello.tokens`; `hello.sse` public. The
   trace-continuity check found the demo built its forward with the raw
   (untraced) EventEmitter - fixed with a trace-aware `PostOffice(headers,
   instance)` (touch stamps from/trace/span/my_cid). The Rust twin needed no
   fix (apply_current_trace is unconditional).
2. **Interop test report** (docs/test-reports/progressive-rendering-interop.md,
   all four repos + contract-provider packaging/inventory): live
   ten-combination matrix (shipped demos, zero shims), defects-found record,
   **OpenTelemetry span-lineage verification** (relay span -> wrapper span ->
   reply-lane spans, one connected tree; wrapper->engine parenting under a
   caller-supplied external span), **business-correlation-id continuity**
   (X-Correlation-Id -> my_cid tag -> my_correlation_id view -> demo echoes;
   live on both engine edges + wrapper<->wrapper), and an appendix with the
   captured end-to-end telemetry + app-log-context example (join key = spanId).
3. **Ruling recorded (Eric): /api/event serves LOCAL routes only** - no inbound
   event-over-http forwarding (routing-loop prevention; the x-event-api marker
   is the existing loop guard; the map is caller-side knowledge; hop-through =
   an explicit relay function). Surfaced by a drive that bounced off the demo
   map; documented as intended design in the report.
4. **Spec** (draft-design-specs/async-http-client-sse-streaming.md): Phase-3
   wrapper design record W1-W7 incl. W3a (business-cid parity), W7 (wrapper
   span lineage + app-log-context; stdout log-ingest agents - e.g. Dynatrace -
   as the field-grade sink per Eric, OTLP out of the wrapper fence) + staged
   follow-up: raw-frame lane parenting (engines' SSE client could inherit the
   head's span for raw token frames, Java+Rust lock-step, optional).
5. **v4.12.0 release sweep**: 32 poms, README jar refs, CHANGELOG "Version
   4.12.0, 8/30/2026" (Unreleased section promoted + demo/report item). Full
   reactor `mvn clean install` green.

**Wrapper-side rounds (recorded in the wrapper repos' own memory, headline
here):** event streaming (engines' envelope-mode contract + reply_to bus),
business-cid continuity, full span lineage emitting the engines'
distributed-trace dataset on stdout, app-log-context twin (packaged
default-log-context.yaml, update_context API). The engines' telemetry model
was the reference throughout (WorkerHandler rpc-gate, touch stamping,
EventApiService sender fill, LogContextConfig template contract).

**Durable lessons:** the engine emits trace datasets only for non-RPC
executions (WorkerHandler's rpc-tag gate) - wrapper parity means suppressing
too; PostOffice-vs-raw-EventEmitter is the trace-continuity boundary a demo
can silently cross; the hello-world/lambda-example `hello.world` route is
event-over-http-mapped in the demo config, so local drives against it bounce
to the peer (pick hello.declarative or unmapped routes for local echoes).

## Memory References

- referenced: thread-sse-consumption-streaming (closed this session),
  thread-polyglot-initiative (P5 version question resolved by v4.12.0),
  eric-release-rhythm, conv-telemetry-presentation-parity (governed the whole
  wrapper telemetry design), conv-squash-title-prefill-check (title verified
  clean), bp-agent-orchestration, bp-polyglot-functions,
  polyglot-event-over-http-design
- created: event-api-local-routes-only (tier: core),
  ot-progressive-rendering-v4120 (tier: working)
- Superseded: (none)
