# Session (2026-06-28T01:37:53.000Z)

**Agent:** Claude Code
**Lightweight:** follow-up to [[trace-thread-keyed-mono-gotcha]] — Eric asked whether Flux has the same
cross-thread trace gap as Mono. Checked: **no.** Flux doesn't set `skipResponse`; its response (the
`x-stream-id`/`x-ttl` handle) is encoded synchronously on the worker thread, and `FluxPublisher` streaming
never reads the thread-keyed trace. Added `WorkerHandlerTest.fluxResponseForwardsSpanId` (green, no prod
change) as a regression guard. Also confirmed the Mono fix live in the 2nd two-pod test: `sync.await` now
carries `parent_span_id` = the notification span; full span tree connected.

Commit `fc96895f` — test(platform-core): confirm Flux task forwards span_id (no Mono-style gap)

```
 .../platformlambda/core/system/WorkerHandlerTest.java  | 18 ++++++++++++++++++
 1 file changed, 18 insertions(+)
```

## Memory References
(none — auto-stub; enrich per AGENTS.md "After Every Session", or leave as a lite log)
