# Session (2026-07-03T20:08:26.000Z)

**Agent:** Claude Code

## Summary

PR #133 **code-review round 2** (`/tmp/pr-133-review-round2-comment.md`). Reviewer confirmed round-1 was
functionally correct and flagged **one cleanup**: the correlation-id comments in the two test-resource flows
(`kafka-sink-flow.yml`, `schema-sink-flow.yml`) still described the *old broken* `model.cid` behavior
("model.cid here is RPC-internal noise, do NOT use it").

That comment was made stale by the round-1 fix: `KafkaFlowConsumer.toFlowRequest` now stamps the inbound
Kafka cid as the `correlation_id` header (`EventScriptManager.CORRELATION_ID`), so `EventScriptManager`
seeds `FlowInstance.correlationId` → `model.cid`, and `TaskExecutor` surfaces it to every task as the
`my_correlation_id` reserved header (`PostOffice.getMyCorrelationId()`). So `model.cid` now carries the
Kafka business cid, just like an HTTP-triggered flow.

### Changes

1. **Rewrote both flow comments** to describe the corrected behavior (model.cid carries the Kafka cid via
   the header; envelope cid is what the blocking `PostOffice.request(...)` rewrites for RPC reply-matching).
   Kept the existing `input.header.cid -> header.cid` mapping (equivalent for the default header, and the
   documented path for a proprietary upstream header name).
2. **Backed the new claim with an integration assertion.** `KafkaSinkTask` / `SchemaSinkTask` now also record
   `po.getMyCorrelationId()` as `myCid`; the 3 embedded-Kafka `KafkaFlowAdapterTest` tests (byte[], JSON
   Schema, Avro) assert `myCid == sent cid`. This exercises the corrected `model.cid` path **end-to-end
   through the real EventScriptManager engine** — it would have failed before the round-1 fix (myCid would
   have been the RPC routing id). Complements the seam-level unit test
   `KafkaFlowConsumerTest#businessCorrelationIdSurvivesRpcRequestPlumbing`.

Verified: `minimalist-kafka` full module green (72 tests incl. the 3 embedded-Kafka e2e), coverage gate met.

**Status: review round 2 applied + tested. Committed & pushed. Awaiting further review.**

## Memory References
- Referenced: kafka-flow-consumer-cid-header
