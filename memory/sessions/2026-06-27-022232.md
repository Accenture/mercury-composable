# Session (2026-06-27T02:22:32.000Z)

**Agent:** Claude Code

Brought the published mkdocs docs current with the new parameters (Eric flagged that the sprint's config
surface was undocumented — it was: zero of the new keys appeared in `docs/`). Scope: config reference +
dedicated module guides (closes the "module docs" post-MVP item).

## Added
- **`docs/guides/kafka-flow-adapter.md`** — the `minimalist-kafka` library: enabling, the
  `kafka-flow-adapter.yaml` binding schema (topic/flow/group/partition, all `${ENV:default}`-substitutable),
  the externalized producer/consumer client templates (pinned vs template-owned params), consumer group,
  partition pinning, at-least-once + retry→per-topic DLQ (confirmed write, commit-gating, pre-provisioning),
  `simple.kafka.notification`, and trace continuity across Kafka.
- **`docs/guides/sync-over-async.md`** — the cross-pod Redis return-route pattern: the flow, enabling +
  `redis.*`/`sync.*` config, reliability cornerstones (Redis source of truth, final read, exactly-once
  completion, bounded growth, 408), and when-to-use vs the service mesh.
- **`configuration-reference.md`** — new `#kafka-flow-adapter` and `#sync-over-async` sections documenting
  every new key (with the note that Kafka connection/security lives in the template files, not properties).
- **Nav** (`mkdocs.yml`, Operate & integrate) + **`docs/llms.txt`** entries for both guides.

## Validation
- `python3 scripts/check-doc-canon.py` → OK (slugs, frontmatter, At-a-glance, no BOM, no retired terms).
- `uv run --with mkdocs mkdocs build --strict` → clean (exit 0, no warnings → all internal links/anchors
  resolve). mkdocs is not a repo dependency; CI does `pip install mkdocs` (theme=readthedocs, plugins=search,
  both built-in). The /tmp/mkdocs-venv had no usable mkdocs console script, so `uv run --with mkdocs` is the
  reliable local path.

## Memory References
- Created: `kafka-soa-docs` (born `tier: working`)
- Touched: `thread-redis-kafka-rpc` (marked module docs done)
- Referenced: `kafka-client-config-templates`, `kafka-flow-failure-dlq`, `kafka-partition-pinning`,
  `soa-config-driven-init`
