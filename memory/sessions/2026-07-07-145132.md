# Session (2026-07-07T14:51:32.000Z)

**Agent:** Claude Code

Round-5 work package after the second field Sonar scan (continues
[[thread-sonar-quality-gate-4.6.2]]; earlier logs: 071532/074543/082059).

## Field scan #2 results (Eric's screenshots)
Gate still FAILED but reduced to: New Code **78.9% coverage < 80%** (255 new lines to
cover) + **6 new majors**; Overall Code 3 conditions. Eric's instructions:
1. Fix MonitorService computeIfPresent (L244) — DONE `ed276f27` (with his DbDemoFunction
   S1066 fix in pg-example committed on his behalf).
2. Implement unit tests in kafka-connector + presence-monitor (kafka-presence) to lift
   New Code coverage ≥80%. Embedded kafka available in kafka-standalone (helpers/) —
   follow cloud-connector's mock-cloud harness as the example.
3. Retire placeholder tests (MsgPackTest/ServiceNameTest/PoJo) in both kafka modules.

## kafka-presence tests — DONE
Copied service-monitor's mock harness (TestBase/MockCloud/MockPubSub/MockTopicManager/
MockCloudHealth + topic-substitution.yaml) into kafka-presence test tree; test
application.properties (port 18082, cloud.connector=mock.cloud, service.monitor=true).
PresenceMonitorTest (4 tests): /info, /health (mock-cloud dependency), /livenessprobe,
plus MainApp.main() idempotency (AutoStart guards with AtomicBoolean → safe re-entry).
**MainApp 100% line-covered.**

## kafka-connector tests — DONE (embedded KRaft broker)
- pom: added `kafka-standalone` (4.6.1) as TEST dependency.
- **KEY DESIGN**: kafka-standalone's `@MainApplication` (sequence 10, in
  org.platformlambda.example.MainApp) is auto-run by AutoStart from the test classpath
  and starts the embedded broker at 127.0.0.1:9092. Test-tree `TestApp`
  (@MainApplication(sequence=20)) waits for port 9092 then `connectToCloud()`.
- **PITFALL LEARNED (crashed twice before diagnosis)**: starting EmbeddedKafka manually
  in @BeforeAll AND having kafka-standalone's MainApp on the classpath = TWO brokers;
  the second formatStorage WIPES /tmp/kafka-logs under the running broker → Kafka fatal
  Exit(1) → surefire "forked VM terminated". Let the auto-run MainApp own the broker.
- **kafka-standalone pom fix**: spring-boot-maven-plugin now uses
  `<classifier>exec</classifier>` so the MAIN artifact stays a plain library jar
  (consumable as a dependency); the executable becomes kafka-standalone-4.6.1-exec.jar.
  Without this, the boot-repackaged jar (BOOT-INF) breaks dependents in `mvn install`
  builds. ⚠ Eric must confirm the -exec jar-name change is OK for dev docs/scripts.
- Test application.properties: port 18083, cloud.connector=kafka, service.monitor=true
  (skips presence ws client), cloud.client.properties=classpath:/kafka.properties only
  (no /tmp/config override), substitution=false (real admin paths),
  cloud.services=kafka.pubsub (covers PubSubSetup 100%).
- KafkaPubSubTest (4 tests): full round trip (createTopic(2 partitions)/exists/
  partitionCount/list → publish text/map/list/bytes → subscribe partition 0 offset "0"
  → all delivered → embedded-event with RECIPIENT → duplicate-subscribe rejected →
  unsubscribe → deleteTopic), user-domain pub/sub enabled, queue API not-implemented,
  subscribe parameter validation.
- Coverage: module 0% → **68.6% lines** (PubSubSetup 100%, EventConsumer 69.6%,
  PubSubManager 69.2%, TopicManager 67.0%, KafkaConnector 64.9%; remainder is error
  arms + substitution branches + presence-ws path).

## Placeholders retired
MsgPackTest/ServiceNameTest/PoJo deleted from kafka-connector AND kafka-presence.

## Verification
**FULL offline reactor `mvn clean test`: SUCCESS, 0 failures in all 14 modules** —
proves the field sequence kafka-standalone (brokers up/down on 9092/8092) →
kafka-connector (fresh embedded broker on 9092) works in one session.
Eric's touch-up included: mini-scheduler AssignJob `seconds * 1000L` (S2184).

## Scan #3 interim (Eric's screenshot) — MAJORS CLEARED 🎉
New Code issues: 1 Bug + 80 Code Smells = **0 blocker / 0 critical / 0 MAJOR** /
57 minor / 23 info. The "6 Major Issues > 0" gate condition is resolved.

## NEXT — ON HOLD per Eric
Eric is regression-testing (core event system, kafka connector, presence monitor) and
will fix some of the 80 smells manually in the IDE. He will then ask for help with the
REMAINING smells (also: 1 Bug in new code — not yet seen). Do NOT proceed until he
signals. Coverage number from scan #3 still pending (expect ≥80% with the new kafka
tests).

## Memory References
- Referenced: [[thread-sonar-quality-gate-4.6.2]]
