# Session (2026-08-21T00:50:17.000Z)

**Agent:** Claude Code

Lightweight — agent-memory Mode B upgrade 4.35.0 -> 4.36.0 via reconcile (composable Git hook
dispatchers): both hook entrypoints verified byte-identical to the stock 4.35.0 monoliths and
no pre-existing fragment dirs, so the Below-4.36.0 semantic step resolved to fully mechanical
— dispatchers re-copied, the two managed 50- fragments installed (executable), README/init.sh
re-copied, AGENTS.md re-synced, the .githooks/*.d/* LF rule merged into .gitattributes.
The rung's renormalize also staged CRLF->LF conversions of pre-existing repo files OUTSIDE
agent-memory scope (examples/pg-example Java/SQL/README) — reset per the CRLF-trap rule; noted
as an observation for the team (those files carry CRLF against normalize-on-add settings), no
action taken.
Verified: dispatcher runs clean (pre-commit exit 0 with nothing staged), fragments executable,
reconcile re-run reports converged, memory-lint 0 errors. Adapters re-synced (gitignored).
Stamped 4.36.0.

## Memory References

(none)
