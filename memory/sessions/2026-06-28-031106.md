# Session (2026-06-28T03:11:06.000Z)

**Agent:** Claude Code

## Summary

**Upgraded the agent-memory layer in place: v4.21.0 → v4.22.4** (Mode B, run by Claude Code from the
agent-memory tool checkout at `~/sandbox/agent-memory`). Additive / non-destructive; the only overwrites
were the tool-managed protocol docs, the built-in `memory-lint` skill, and the regenerated git hook +
adapters. No project source touched.

**Ladder applied (4.21.0 → 4.22.4):**
- **Installed `MERGE.md`** — the no-code, tiered, human-gated git-conflict-resolution protocol
  (`never-pick-a-winner`) for `memory/`.
- **`.githooks/post-commit`** replaced with the per-*session* version (30-min active-session window;
  override `AGENT_MEMORY_SESSION_WINDOW_MINUTES`). This is the fix for the per-commit stubbing this repo
  reported — the old hook re-stubbed on every work commit after the log was committed.
- **`memory-lint`** re-copied — adds **check 7** (leftover git-conflict markers in live `memory/*.md` →
  ERROR; `sessions/`+`archive/` excluded), both runtimes + tests.
- **`REVIEW.md`** re-synced — safe-write safeguard (never truncate a memory file when scripting an
  archival move) + per-session log note.
- **`.agent/schema.md`** — "Concurrency & merge-friendliness" section + `status`-is-a-short-current-state-line.
- **`AGENTS.md`** — status-short + lightweight-mode-one-log-per-session.
- **`SKILLS.md`** — re-synced; **self-healed** a drift (its copy was missing the Antigravity `.agents/skills/`
  adapter section despite the 4.21.0 stamp).
- **Adapters re-synced** — 4 skills → 24 vendor adapters (gitignored); 6/6 per skill; 0 orphans.
- **`.agent/version.md`** stamped `4.21.0 → 4.22.4` (preserving `enabled_with: 4.14.1`, `mode: A`), via a
  safe read-then-write.

**Verification:** `memory-lint` → **0 errors** (10 advisory-overdue warnings = this repo's own decay
backlog, unrelated — a `REVIEW.md` pass is due). Adapter completeness 6/6 × 4 skills. AGENTS.md self-check
passed (memory-hub, not the operator file).

**Pending (human-gated, not yet done):**
- Working-tree changes are **uncommitted** — review + commit at your discretion (with a self-identifying
  co-author trailer).
- **Optional re-harvest** offered by the 4.21.0→4.22.0 rung: re-scan `docs/` (`arch-decisions/`, `guides/`,
  `draft-design-specs/`) and enrich memory from anything the older shallow discovery skipped — not run.
- This repo's own **memory review** is due (10 overdue facts; continuity 562 lines).

## Memory References
- (none — tool-layer upgrade; no project fact created/changed)
