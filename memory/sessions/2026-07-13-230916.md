# Session (2026-07-13T23:09:16.000Z)

**Agent:** Claude Code

## Release bump 4.8.3 → 4.8.4 — commit `8a420f42` on chore/release-4.8.4

Maintenance release carrying PR #176 (secondary.kafka.health + instances=5 on both
health checks + Minimalist Kafka guide rename). Sweep with `4\.8\.3(?!\d)` lookahead:
36 version files + CHANGELOG (Added: secondary.kafka.health; Changed: 5 worker
instances + guide rename) — the `httpcore5.version=5.4.3` security override untouched
(lookahead can't match it, verified in the diff). Verified: full reactor green at
4.8.4; pg-example 7/7. ~~TAG DEFERRED per the 4.8.3 precedent~~ **UPDATE (same session):
Eric corrected the flow — the 4.8.3 deferral was specific to the Snyk rejection; 4.8.4
follows NORMAL procedure (tag immediately after PR #177 merges).** He removed the
deferred-tag paragraph from the PR description accordingly.

## Release closure

PR #177 merged; **tag `v4.8.4` created on merge commit `b1265f18` and pushed**; release
text delivered. Normal-flow release (no deferral). Open thread checked off.
v4.8.4 published: https://github.com/Accenture/mercury-composable/releases/tag/v4.8.4

## Field CI follow-up (same session)

Field Jenkins build of 4.8.4 failed in `TwinKafkaBridgeTest.boot`: "secondary Kafka flow
adapter should have started" — the 20s readiness poll after AutoStart (2 embedded KRaft
brokers + registry + full app) expired on a busy executor; the primary adapter made it,
the secondary didn't = normal boot, tight deadline. Fix on `fix/ci-boot-deadline`
(commit `7fb59057`): deadlines 20s→90s for the two app-boot polls (TwinKafkaBridgeTest,
KafkaFlowAdapterTest) and 20s→60s for the two health warm-up polls — a poll returns the
moment readiness is reached, so a generous budget costs nothing on fast machines.
Suites re-verified green. LESSON (extends the settled-state family): readiness DEADLINES
on shared CI must be sized for the slowest executor, not the dev laptop.

## Memory References
- Referenced: [[release-4-8-3-shipped]] (deferred-tag flow precedent + prior sweep facts)
- Referenced: [[conv-instance-count-pattern]] (the instances=5 rationale shipping in this release)
