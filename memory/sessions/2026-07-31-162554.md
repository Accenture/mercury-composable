# Session (2026-07-31T16:25:54.000Z)

**Agent:** Claude Code

## What happened

**examples/kafka-demo now demonstrates second-level routing — code-complete on branch
`feature/kafka-demo-2nd-level-routing` (commit `1e207c18`, NOT pushed), smoke-driven
live end-to-end, awaiting Eric's PR gate.** Eric's intent: demo apps double as manual
regression templates AND as migration templates for field developers reconfiguring
their apps onto this feature (it replaces a proprietary field implementation — all
demo naming kept strictly generic per the compartmentalization rule).

**Shape (per Eric's spec — one new topic, one default flow, one specific flow, one
specific task, plus node scripts):**
- `demo.orders` binding beside the direct-routing `demo.inbound` — one
  kafka-flow-adapter.yaml shows BOTH routing styles, heavily commented as the
  developer template. Binding: `serializer: 'json'`, `ttl: '15s'`,
  `dlq-topic: demo.orders.dlq`, 4 rules exercising every grammar element.
- Specific flow `demo-order-flow` (exact `type(order)` + wildcard `type(order-*)`
  rules): OrderProcessor gets the serializer-decoded Map, returns a Map that
  simple.kafka.notification AUTO-SERIALIZES to demo.outbound — the outbound symmetry
  demonstrated in-flow.
- Specific task `task://demo.refund.processor` (body rule
  `input.body.event.kind(refund)`): direct invocation, acknowledged in the app log;
  javadoc teaches the task:// contract (verbatim headers, whole payload,
  my_correlation_id, commit-on-success) and when NOT to use it (orchestration → flow).
- Default flow `demo-catch-all-flow`: CatchAllProcessor takes Object input and handles
  BOTH body shapes (Map/List vs raw byte[]) — the production-default-handler pattern
  for the best-effort serializer contract.
- Node: new `publish-orders.js` (one keyword per routing rule — order / order-<x> /
  refund / anything-else; optional JSON payload override; prints the expected rule per
  publish; **works interactively AND piped** for scripted regression —
  `printf 'order\nrefund\n' | node publish-orders.js`); config.js + create-topics.js
  gained demo.orders. README: routing section, per-command expectations, updated
  diagram/tables. CHANGELOG Unreleased item 3.

**Live smoke drive (the exact developer path):** kafka-standalone → create-topics.js
(3 topics) → app boot (binding log: "second-level routing (3 rules + default),
serializer 'json', task ttl 15s") → all four rules driven through the real
publish-orders.js → verified in the app log with correct SPAN TOPOLOGY (flow targets:
task.executor + 2-task flow annotations; the task target: a single
demo.refund.processor span with from=kafka.flow.adapter, no flow engine) and cid/trace
continuity per record → outbound delivery confirmed through the real
listen-outbound.js (order Map arrived as auto-serialized JSON with
routedBy=input.header.type(order); raw text arrived annotated shape="raw bytes",
routedBy=default). Unit tests 8/8 (3 new classes following the DemoProcessorTest
my_*-headers pattern).

**Drive-found fix:** publish-orders.js originally raced piped stdin (close fired
before async sends completed — records silently lost). Fixed with an in-flight promise
chain awaited on close; this is what makes the script usable for scripted regression,
Eric's stated purpose.

**Also (Eric's check, same session): the `serializer` XOR `schema.enabled` mutual
exclusion is confirmed enforced** — KafkaFlowAdapter.isJsonSerializer throws
"cannot combine 'serializer' with schema.enabled - the schema registry already owns
the decode" at startup (shipped in PR #246), pinned by
KafkaFlowAdapterConfigTest.rejectsSerializerCombinedWithSchemaDecoding and documented
in the guide's field table + validation list.

Commit `1e207c18` (amended from `2f4e32cd` to fold in the CHANGELOG item):

```
 examples/kafka-demo/README.md                      |  83 +++++++++++++---
 examples/kafka-demo/node/config.js                 |   3 +-
 examples/kafka-demo/node/create-topics.js          |   2 +-
 examples/kafka-demo/node/publish-orders.js         | 109 +++++++++++++++++++++
 .../kafka/demo/tasks/CatchAllProcessor.java        |  76 ++++++++++++++
 .../accenture/kafka/demo/tasks/OrderProcessor.java |  64 ++++++++++++
 .../kafka/demo/tasks/RefundProcessor.java          |  62 ++++++++++++
 examples/kafka-demo/src/main/resources/flows.yaml  |   2 +
 .../main/resources/flows/demo-catch-all-flow.yml   |  36 +++++++
 .../src/main/resources/flows/demo-order-flow.yml   |  41 ++++++++
 .../src/main/resources/kafka-flow-adapter.yaml     |  30 +++++-
 .../kafka/demo/tasks/CatchAllProcessorTest.java    |  71 ++++++++++++++
 .../kafka/demo/tasks/OrderProcessorTest.java       |  62 ++++++++++++
 .../kafka/demo/tasks/RefundProcessorTest.java      |  50 ++++++++++
 + CHANGELOG.md (Unreleased item 3)
```

**Also (merge closeout): MERGED 2026-07-31 as PR #247, squash `929a87b9`** — CI green
(Build & Unit Tests 6m43s + memory checks), merge content verified on main, branches
cleaned up. Eric confirmed the README already covers the demo/regression procedure
("I saw the updated README. we are good").

## Memory References

- referenced: thread-kafka-2nd-level-routing (the feature this demo illustrates),
  release-4-8-1-shipped (flow-authoring conventions), conv-helpers-docker-less
  (kafka-standalone as the demo broker), event-script-over-code (task:// vs flow://
  guidance in the demo javadoc)
- updated: thread-kafka-2nd-level-routing (demo branch note added)
