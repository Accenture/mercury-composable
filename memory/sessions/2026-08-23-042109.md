# Session (2026-08-23T04:21:09.000Z)

**Agent:** Claude Code

Cross-repo coordination record: the wrapper feature round shipped on both wrapper repos —
this repo's tree is untouched; this log keeps the initiative thread current.

## What happened (in the wrapper repos)

Both wrappers gained, in one PR each (design ratified by Eric through the session,
implemented lock-step, merged same day):

- **Primitive in-process event bus** — the single dispatch pipeline (per-route FIFO
  mailboxes, faithful `instances` workers, deliver/publish only). `private` became
  faithful: local PostOffice (no endpoint) reaches private routes in-app while the wire
  keeps 403. Ratified ruling: NO spill tier / NO queue cap — back-pressure belongs to the
  engines' flows/graphs; a leaf host fails fast by deadline.
- **Actuator endpoints** `/info`, `/info/routes`, `/env`, `/health`, `/livenessprobe` —
  shapes mirror the Rust engine's actuator (the approved minimalist port of this repo's
  `ActuatorServices`); health check functions speak the engines' `type=info` /
  `type=health` contract through the bus; origin ids use this engine's reference format
  (UTC yyyyMMdd + 32-hex uuid); elapsed-time boundary quirks pinned.
- **`log.format` = text | json | compact** — verified against this repo's
  JsonAppender/CompactAppender pair: json = pretty-printed, compact = single-line JSONL.
- Sample `examples/resources/application.yml` on both wrappers (resources convention,
  -D override syntax taught in the READMEs).

Merges: python [PR #17](https://github.com/Accenture/mercury-python/pull/17) true merge
`f38ac17`; node [PR #87](https://github.com/Accenture/mercury-nodejs/pull/87) true merge
`fa7b2bf`. Trees verified identical to the gated branch heads; branches deleted both
ends. Gates: python 50/50 + ruff + basedpyright 0; node 48/48; live wire + actuator
drives on both demos. Full narratives: the wrapper repos' session logs
2026-08-23-031558 (python) and 2026-08-23-031920 (node).

## Continuity updates here

- [[polyglot-event-over-http-design]] scope fence amended (Eric's ratifications this
  session): + primitive event bus, + actuator endpoints, + three log formats — the
  "NO event bus" clause is superseded by the ratified minimalist bus; "NO orchestration"
  stands.
- [[thread-polyglot-initiative]] gained the wrapper-feature-round milestone; Remaining
  (P4 docs/demo/interop-report/ADR-0016/wrapper CI, P5 publishing gates) unchanged —
  note the wrapper repos still have NO code CI (only the agent-memory check ran on these
  PRs), strengthening the P4 CI-workflow item.

## Memory References

- referenced: thread-polyglot-initiative, polyglot-event-over-http-design,
  bp-polyglot-functions, conv-telemetry-presentation-parity, eric-release-rhythm,
  vision-mercury-composable
- updated substance: polyglot-event-over-http-design (fence amendment),
  thread-polyglot-initiative (milestone)
