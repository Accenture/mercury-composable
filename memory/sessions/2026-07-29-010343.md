# Session (2026-07-29T01:03:43.000Z)

**Agent:** Claude Code

## Summary

**Graph suspend/resume P1 (engine core) IMPLEMENTED — branch
`feature/graph-suspend-resume`, all green: 9-test e2e suite passed on the first run, 82
module tests, full reactor.** Realizes [[bp-graph-workflow-suspension]] (ratified) per
the plan in draft-design-specs/graph-suspend-resume-implementation-plan.md.

**What landed:**
- **Skills:** `graph.suspend` + `graph.resume` in minigraph-playground-engine, extending
  a new shared `GraphStateSkill` base (supersets of graph.task: `task=` names the
  pluggable store function, envelope assembly/restoration fully encapsulated — zero node
  data mapping). Suspend: reserved-alias check, model.cid required, `ttl` via
  getDurationInSeconds, envelope {cid, node(from), ttl, model minus reserved keys, seen,
  run}, synchronous store ack (failed persist fails the node), default
  `{"type":"suspended","cid":…}` response staging, sole-active-branch WARN. Resume:
  found → merge model (reserved keys survive by construction) + restore marks + new
  `resume:<alias>` directive; not-found → `next` (fresh run) or `missing=<alias>` jump;
  corrupt/unknown-node record → 500 via node error path (graph-version-drift guard).
- **Walkers (executor + traveler, kept identical):** `from` provenance threaded through
  walk/walkTo/executeSkill (delivered as skill header `from`); `suspend=true` nodes route
  to the reserved alias `suspend` on NEXT (jump-by-name, R3) with math/js rejection;
  `resume:<alias>` marks the suspension point seen+run and walks its forward links
  excluding `suspend`, with a dead-end guard; RESERVED_PARAMETERS += suspend/missing.
- **ttl ruling (Eric, follow-up): mandatory with NO default (skill-validated; a checkpoint may wait minutes or days) and deliberately NOT in RESERVED_PARAMETERS - it is a task parameter (store expiry timer), not engine-routing config; suspend/missing stay reserved. Pinned by ttlIsMandatoryWithNoDefault + fixture err4.**
- **Bug caught in design-trace before first run:** restoring the suspend node's OWN
  seen/run marks from the blob would block re-suspension at a later checkpoint (walker
  dedup) — GraphResume excludes the `suspend` alias in restoreMarks; the multi-checkpoint
  test pins it.
- **Tests (com.accenture.minigraph.suspend.GraphSuspendResumeTest, real HTTP stack, cid
  via X-Correlation-Id):** basic suspend→resume without re-execution + envelope-shape
  assert (reserved keys absent) + consume-on-resume; multi-checkpoint chain (3 runs);
  join-barrier-across-suspension (D2 proof — would 408 without restored marks);
  missing= jump; 1s-ttl expiry → fresh-run fallback; the three error guards (misnamed
  graph.suspend node, suspend=true on graph.math, missing suspend node); help-file
  convention. Store = temp-file mock `v1.file.state.store` under /tmp/suspend-resume
  (MsgPack, expiry-stamped, delete-on-read = GETDEL parity; test-scope — engine gains no
  store dependency, R4).
- Help files: `help graph-suspend.md` / `help graph-resume.md` (describe-skill surface).
- 8 graph fixtures (unit-test-suspend-1..5 + err1..3); the 5 good ones registered in
  graphs.yaml.

Also: **P2 (CompileGraph static checks + Playground surface) IMPLEMENTED same session** —
`validateSuspendResume` in CompileGraph (alias⇔skill bidirectional; suspensible-node
rules: no math/js, suspend node required, drawn checkpoint edge required — "the diagram
must show the suspension path"; suspend node task+ttl valid; resume node task + existing
`missing` target), with the six err fixtures registered in graphs.yaml so ONE fixture set
tests BOTH enforcement layers (compile rejection asserted by CompileGraphTest; runtime
guards still reached via lazy-load fallback). New compile-only fixtures err5 (no drawn
edge) + err6 (ghost missing target). Playground: `describe skill` works by help-file
convention (no registry to extend); webapp type theme gained Suspend ⏸️/Resume ▶️/
Suspensible ⏯️ icons + accents (teal/sky/fuchsia), 124 webapp tests green, bundle rebuilt
via npm run release. Module suite 85 green. Visual confirmation of the node colors rides
Eric's manual Playground pass.

Also: **P3 (Redis state store) IMPLEMENTED same session — new reactor module
`extensions/minigraph-state-redis`** (Eric's directive: single library module under
extensions; auto-registration by inclusion). `v1.redis.persist.model` (SETEX, MsgPack
bytes, native expiry, durability ack) + `v1.redis.retrieve.model` (atomic GETDEL consume
— Redis 6.2+ — empty map for absent/expired = normal fresh case); lazy shared Lettuce
connection reusing sync-over-async's `redis.*` config keys (one Redis config per app);
key namespace `graph:state:<cid>` shared across app instances (cross-pod resume);
instances=50 each + worker.instances.* envInstances knobs; module README documents
contract + custom-store path. 7 tests green vs embedded Redis (port 16380,
/tmp/graph-state-redis) THROUGH the event system incl. the auto-registration proof and
byte[] round-trip. `examples/minigraph-playground` now imports the module (lazy connect —
app boots Redis-free); playground 13 green; full reactor (30 modules) green.

Also: **P4 (tutorial + e2e + docs + ADR) IMPLEMENTED same session.** tutorial-14
(approval workflow: root → resume → record-request suspend=true → {suspend|approve} →
end) deployed in examples/minigraph-playground with graphs.yaml entry;
SuspendResumeTutorialTest drives it end-to-end against embedded Redis (port 16381,
system-property config injection per the documented gotcha — ConfigReader checks
System.getProperty first on every read, so the LAZY store connection honors it regardless
of boot order): run 1 suspends, run 2 with same X-Correlation-Id completes with run-1
state in the response; playground 15 green. Docs: NEW
guides/knowledge-graph/workflow-suspension.md (story + vocabulary + tutorial curl
walkthrough + design rules + STATE-STORE CONTRACT section + Redis module) in mkdocs nav;
skills-reference entries for graph.suspend/graph.resume; reserved-names table + note
(4 new routes; suspend alias + suspend/missing properties); CHANGELOG Added #1.
**ADR-0010 appended to docs/arch-decisions/ADR.md** (short runs + external store,
skill-encapsulated, reserved-alias routing, consume-on-retrieve; formalizes:
graph-suspend-resume-design) and the matching Key Decision fact
[[graph-suspend-resume-design]] created with the (ADR-0010) tag — the ledger update is
PROPOSED via this branch; Eric's merge gate is the approval.

Also: branch pushed; PR #238 created by Eric; doc port correction (tutorial-14 curl
examples 8600 -> 8085, the app's real rest.server.port) pushed as `17673676`.

Also: **Eric's manual-test findings round (5 items, all delivered).** (1) Business-cid
fidelity: walkers are INTERCEPTORS so PostOffice never auto-propagated the business-cid
tag onto internal skill events -> log context showed internal routing ids; both walkers
now stamp BUSINESS_CID_TAG from stateMachine model.cid (Eric's hint confirmed: the graph
state machine inherits the flow model wholesale, model.cid = the edge business id;
internal cid != business cid - routing plumbing only) on executeSkill; suspend/resume
skills annotate spans with cid; pinned by getBusinessCid assert in the counting task.
(2) tutorial-14 relocated to the ENGINE's main/resources/graph (stale target/classes
shadowing caught - mvn clean). (3) Input validation: decision-without-submission ->
HTTP 404 {type:rejected}; two new techniques: NULL-SAFE presence probe (var substitution
inside text() constants - the math engine has no null literal, empirically proven) and
DECLARATIVE RESPONSE STATUS (int(404) -> output.status honored by executionComplete; a
non-2xx rides the flow exception path which passes a map body minus 'stack' and corrects
its 'status' key - staged rejection fields must avoid the 'status' name). (4) help.md
skill list + tutorial index + new 'help tutorial 14.md'. (5) npm run release rebuilt the
engine bundle. Catalog: output.status namespace entry. Engine 85 + app 16 green; all doc
checks + strict build + reactor green.

**Remaining:** P5 Rust lock-step arc (delegated to the Rust session when Eric says go).

## Memory References

- referenced: thread-graph-suspend-resume (P1 delivered), bp-graph-workflow-suspension
  (the Blueprint gap this realizes), conv-serialization-gotchas (MsgPack envelope
  round-trip design)
