# Session (2026-09-25T18:35:40.000Z)

**Agent:** Claude Code

## Summary

**A field installation's page of nine "behaviours that return a wrong answer" reviewed, triaged with Eric one by one, and
his rulings implemented for graph.math; branch `feat/graph-math-condition-and-typed-arithmetic` `b5bea652`.** The page (with its six
fragment specifications, which quote the engine's exact messages) was measured on 4.12.7; each behaviour accepts what was
written, runs to completion and returns a wrong result — the worst run charged nearly two million dollars too much because a
boolean threshold coerced to a number in a negation and every later comparison was Number == Number. Nothing from the page
or the fragments enters this memory by name. **Eric's rulings and what they became:** (1) a misspelled function — the math
package must reject it: it did (`Attempting to call a non-function`), now by name, `Unknown function: mn`; (2) a comparison
in COMPUTE yields a boolean — by design, a doc gap, plus a new `CONDITION: var -> expr` statement (the declared boolean,
evaluated as a boolean whatever operators it carries, testable by `IF` directly); (3) a boolean coerced by arithmetic —
uniform rejection: `asNumber` no longer coerces, function arguments and a bare boolean COMPUTE result are rejected too, and
`nameOffendingSelectors` (the generalized 4.12.16 namer) maps the evaluator's error back to the selector — `Boolean operand:
model.flag (true) in '{model.flag} + 1' - a boolean is not a number; store a boolean with CONDITION or assert the type with
f:validate`; (4) default-then-overlay and (5) an unnamed plugin abort — resolved in 4.12.16 (#456), re-measure; (6) appended
lists not reseeded — by design for a `run` on the same instance; `instantiate graph` (alias `start`) creates a fresh
instance (verified in `GraphCommandService.instantiateGraph`), documented in the `run` section and the `for_each` rules;
(7) a taken IF inside a `for_each` body ends the walk — by design, documented with the arithmetic-gate advice; (8) a refusal
overwritten by `end` — the node is the terminus, documented under the invariants; (9) floating point — every arithmetic
result is now checked finite (`Arithmetic overflow in '*' (result Infinity)`, `Division by zero or arithmetic overflow in
'/'`, NaN by name) and the guide states the numeric model, ruling that exact-decimal money goes to a small `graph.task`
function with `BigDecimal` — the math package does not grow (the minimalist principle,
[[clean-knowledge-design-over-engine-coverage]]). A claim `math-boolean-operand-rejected` pins the guide's rule to the new
test (`check-doc-canon` OK). Consulted for the resolved items: [[static-decision-table-is-graph-data]] (the null-source rule)
and [[minigraph-guarded-async-completion]] (every abort carries its reason). Fact [[graph-math-typed-arithmetic]] created.
The Rust twin follows in lock-step (same evaluator shape). Code reaches main through a PR ([[eric-code-changes-via-pr]]).

## Verification

`GraphTaskTest` 17 / 17 with the three new methods over `unit-test-math-2` (a dispatcher node routing to one node per
behaviour): CONDITION on a comparison, a bare boolean and a boolean operation over a prior result; boolean rejection in
arithmetic, in a relational comparison and as a bare result, each naming `model.flag`; `Unknown function: mn`; overflow on
`1e308 * 10`; division by zero as an error and the same division computing with a non-zero divisor.
`ExpressionEngineFullTest` updated from the old semantics (division by zero as Infinity, the generic non-function message)
to the new ones, with the uniform boolean rejection across `+`, `-`, `<`, a function argument and a bare result, and booleans
still first-class in boolean contexts. Engine module **138 / 0 / 0**; `examples/minigraph-playground` 21 / 0; `mkdocs build
--strict` clean; `check-doc-canon` clean (the new claim's quote and test pin resolve).

**Follow-up in the same session — Eric's IDE inspections on the branch, fixed before the PR:** the `Evaluator`
conditional pushed inside `eval()` (`eval(test ? consequent : alternate, ctx)`); `nameOffendingSelectors` split into
`nameNullSelectors` / `nameBooleanSelectors` (cognitive complexity 21 → under the 15 allowed, same behaviour);
`GraphTaskTest`'s fourteen unchecked `Map` casts replaced by one `bodyMap()` helper that carries the single suppression
(seven method-level suppressions dropped; the `List` cast in `forEachForkJoin` keeps its own); two wording nits
(`suspendable`, "whether … or not", US spelling in a comment and the guide). Engine module re-run with compiler warnings
shown: **138 / 0 / 0**, no unchecked warning.

## Memory Review (incidental sweep)

The new log moved the completed thread `ot-subgraph-for-each-suspend` (closed 2026-09-18) to completion age 21 >
`archive_window` 20, and `memory-lint` raised `[overdue]`; swept to `memory/archive/2026-Q3.md` with `archive-fact` (its
stub record and `origin:` pointer preserved). Not a review — no other fact moved.

## Memory References

- graph-math-typed-arithmetic (created)
- clean-knowledge-design-over-engine-coverage (consulted — the minimalist ruling on BigDecimal)
- static-decision-table-is-graph-data (consulted — item 4's status)
- minigraph-guarded-async-completion (consulted — item 5's status)
- eric-code-changes-via-pr (consulted)
