# Session (2026-06-28T18:53:31.000Z)

**Agent:** Gemini CLI

## Summary
Designed and implemented the standalone Schema Registry server (`helpers/schema-registry-standalone`) to mock the Confluent Schema Registry HTTP API. This module acts as a drop-in replacement for end-to-end local testing with `KafkaAvroSerializer` and `KafkaJsonSchemaSerializer` without requiring Confluent dependencies.

- Created a memory-efficient `SchemaStore` singleton that securely manages JSON/Avro/Protobuf schema string registrations, allocating incremental IDs.
- Stored registered schemas dynamically in `/tmp/mini-schema-registry/schemas.json` to safely survive mock server restarts.
- Implemented three minimalist `@PreLoad` functions serving strictly the `/subjects/{subject}/versions` (POST) and `/schemas/ids/{id}` (GET) endpoints necessary for standard producers/consumers, via `rest-automation`.
- Supported dynamic resolution of the `schemaType` required by `KafkaJsonSchemaSerializer` without breaking the default AVRO format.
- Published `docs/guides/schema-registry-mock.md` with usage instructions and integrated it within `mkdocs.yml` and `llms.txt`.
- Built comprehensive JUnit 5 integration tests confirming Avro/JSON lifecycle, endpoint behaviors, and disk serialization. 
- Integrated the new module gracefully into the main Reactor build in `pom.xml`.

## Memory References
- Created: standalone-schema-registry-mock (tier: working)
