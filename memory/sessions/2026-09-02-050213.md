# Session (2026-09-02T06:05:00.000Z)

**Agent:** Claude Code
**Round:** The AI-grammar feedback round — a field AI agent's three-layer build feedback
(ai-enabled-repo-demo, docs/feedback/mercury-composable-docs-feedback.md) processed into
engine changes + a docs campaign. **SHIPPED both engines: Java
[PR #310](https://github.com/Accenture/mercury-composable/pull/310) squash `6ecbc938` ==
gated `ec79d719`; Rust [PR #224](https://github.com/Accenture/mercury/pull/224) merge
`206ce31a` carrying `32fda198`. Trees verified, branches deleted both ends.**

## The feedback and its grounding (verify before believing)

Seven findings, impact-ordered, evidence-backed. Grounding them in the tree changed one
materially: the reported "export silently fails" (their item 3) was actually
`Utility.bytes2file`'s deliberate **idempotent write** — identical content skips the
physical write, preserving the mtime their timing script used as evidence; their rebuilt
graph exported byte-identical JSON, so nothing needed writing and `ok:true` was truthful.
The real residue: the export's unchecked `str2file` return (an actual I/O failure would
report success) — fixed with an in-band ERROR. Their /sync placement table verified
exactly (ai-agent-guide 12 mentions, the other eight KG guides zero; the guide unlinked
from index/AGENTS/llms.txt... llms.txt actually HAD it — the agent admitted following the
title-matching page anyway, which is precisely why that page must be right on its own).

## Eric's rulings

1. **The fire-and-forget companion endpoint is RETIRED outright (ADR-0021)** — the field
   has driven /sync for multiple sprints; removal beats documented-legacy ambiguity. The
   bare URL answers 404 (re-pointing it at sync semantics rejected: silent contract change
   under old scripts). /sync unchanged.
2. **graph.js formally DEPRECATED (ADR-0022)** — backward compatibility only; the field is
   refactoring off it; AI agents must never use it. The reported silent-equality defect
   (quoted-string `===` → false, no error) is documented, not fixed — containment only per
   the standing phase-out direction. Replacement pattern documented: graph.math for
   decisions + graph.task for the rest. Converges with the Rust port (never carried it).
3. **Array mapping already existed** — `hello[0]` index + `hello[]` append (dot-bracket) —
   a pure DOC GAP in the Java flow-schema-reference (test-proven:
   for-loop-test-single-task.yml; Rust's syntax.md already documented it).

## What shipped

- **Engine (both):** async companion handler + rest.yaml mappings deleted; tests migrated
  to /sync + a 404 retirement pin; Java export write-check. Java also: the docs campaign
  (companion guide rewritten around /sync with the full in-band contract; "restart ends
  the session — export first" + "404 means the session is gone" warnings; inspect
  leaf-vs-subtree note; export idempotency note; graph.js deprecation banners + pattern;
  array mapping syntax; sync-over-async contract in configuration-reference; README →
  offline contract-provider guides + docs/guides source-of-truth note; webapp
  COMPANION_ENDPOINT_README rewritten both repos; snake.case warning; groupId note).
- **Java 25 recommendation (Eric's directive, same round):** recommended JDK/JRE is Java
  25 (current LTS); the Java 21 build target is DELIBERATE — wider compatibility; the
  toolchain (.java-version, CI) STAYS 21 until the majority of field installations run 25
  (recorded on [[stack-language-java21]]).
- ADR-0021 + ADR-0022 written (Proposed → accepted via the #310 merge; status flip
  staged on main).
- Gates: playground-engine suite; example module 21/21; ai-contract-provider 19/19; Rust
  workspace tests + clippy + fmt.

## Lessons (durable)

- **A piped gate masks failures** — `cargo test | grep | tail` exits 0 on a failing run;
  the Rust CI caught the compile error my local chain swallowed (the ot-route-pool-twin
  lesson re-learned from the other side). Verify with BARE exit codes.
- **Retiring an endpoint needs a reference sweep beyond URLs**: a test called the handler
  FUNCTION directly (`rest::post_companion_command` in graph_runtime.rs) — grep the
  symbol, not just the path.
- **Ground feedback before acting**: one of three "high severity" engine bugs dissolved
  into deliberate behavior plus a real-but-different one-line fix.
- Engine-parity observation for a future check: Java's `inspect output` prints `{}` for
  subtrees; a Rust test suggests its twin may render them — worth a parity test someday
  (noted on [[thread-docs-improvement-backlog]]).

## Deferred (recorded, not lost)

lambda-example request-response demo (the idiom lives in composable-example's
EventOverHttpRpc); graph.js equality root cause (moot under deprecation); inspect
subtree parity check → all on [[thread-docs-improvement-backlog]].

## Memory References

- Referenced: graphjs-phase-out-direction (updated — formally deprecated),
  stack-language-java21 (updated — Java 25 recommendation + toolchain hold),
  thread-doc-improvement-feedback-loop (updated — first external-source firing),
  thread-docs-improvement-backlog (updated — deferred items),
  conv-telemetry-presentation-parity, eric-release-rhythm, compilegraph-mandatory-gate,
  ot-agent-orchestration-e0
- Created: (none)
- Reactivated: (none)
- Superseded: (none)

## Memory Review (mini — single sweep, 2026-09-02)
- Archived: 3 (thread-dynamic-statement-targets, thread-graph-task-model-staging,
  thread-tutorial-14-decision → memory/archive/2026-Q3.md, faded past archive_window 20;
  surfaced by lint after this round's logs advanced the count)
