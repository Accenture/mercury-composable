# Session (2026-09-10T03:32:17.000Z)

**Agent:** Claude Code

## What happened

**Playground UI: the graph view's source is now the live session endpoint**
(branch `feature/playground-live-graph-source`; the arc Eric proposed after the
model-endpoint 404 fix). `GET /api/graph/session/{id}` is THE loading path:
initial load, SPA-return restore, and mutation auto-refresh all fetch it
directly — no `describe graph` round-trip, no temp-file link, no expiry
special-casing. `describe graph` / `export graph` stay human-operator (and
companion-agent) console commands with their snapshot + link untouched; a
console link-row click now live-refetches and shows the Graph tab, with the
URL still a raw-snapshot surface.

Webapp-side (detail in the nested layer's log
`webapp/memory/sessions/2026-09-10-033122.md`): `usePinnedGraphPath` and
`useSessionGraphRestore` deleted; `useGraphData` rides the session path with
quiet initial loads (fresh session = zero-node export, unknown = 404 → the
canvas empty state) and reveal-once tab behavior; `useAutoGraphRefresh` drops
the `graph.link` listener and the collaboration gate-arm dance (symmetric
session sync means every member re-fetches its own replica); mutation
refreshes ride the `isRefreshing` overlay instead of re-running the clearing
initial-load path. Technical Documentation rewritten where it described the
pinned-link flow. Gates: typecheck + 321 vitest green; live smoke proved
mount/mutations/away-and-back/describe/link-click/reset all on the session
endpoint with zero console errors; bundle rebuilt via `npm run release`.

Engine untouched this arc. Next: the wholesale Rust webapp mirror per the
amended sync ritual (Rust-side fact webapp-lockstep-sync-preserved-paths).

## Memory References

- conv-telemetry-presentation-parity (consulted — the webapp ships lock-step:
  Java lands first, the Rust mirror follows wholesale)
- eric-release-rhythm (consulted — branch/commit/push prep; PR creation is Eric's)
