# Session (2026-06-28T18:21:25.000Z)

**Agent:** Claude Code

## Summary

**Upgraded agent-memory v4.25.0 → v4.26.1** (Mode B, by Claude Code from the tool checkout) — a final
validation round of the metadata-refresh toolchain. Working tree left **uncommitted** for the mercury team.

**What it brings:**
- **4.26.0** — `refresh-metadata` (7th built-in): recompute every fact's `last_used`/`uses`/`tier` from the
  session reference log deterministically (REVIEW.md steps 2–3, the metadata pass agents skip;
  read-into-memory-write-once). Plus a `memory-lint` `[stale-metadata]` advisory (stored tier ≠ recomputed).
- **4.26.1** — refinement: a **pinned `- [ ]` open thread's tier is left alone** (its pinned-ness protects
  it, not the tier label), so a `working`-tagged open thread is no longer flagged or rewritten.

**Files (all scoped to this repo):** copied the new `refresh-metadata` skill; re-copied `memory-lint`
(check 9 + the refinement) + SKILL.md; re-synced `REVIEW.md` (steps 2–3 lead with `refresh-metadata`) and
`DECAY.md` (rule 4 clarified); adapters synced (7 skills → 42, gitignored); stamped `.agent/version.md` →
4.26.1 (enabled_with 4.14.1, mode A preserved).

**Validation (the point of this round):**
1. Post-upgrade `memory-lint` flagged exactly **1** real drift — `agent-memory-upgrade-v4250`
   (tier active→working). The **5 pinned threads** that the prior sanity check surfaced under v4.26.0
   (`bp-*`, `thread-*`) **no longer flag** — the v4.26.1 refinement working in the field.
2. `refresh-metadata` (real run) re-tiered the one real fact **and** refreshed pinned `thread-redis-kafka-rpc`'s
   `uses 3→6` while leaving its `tier: working` untouched — the refinement's exact intent, validated.
3. Final `memory-lint`: **0 errors, 0 warnings.** py↔node parity confirmed in this repo.

So the full metadata toolchain (`memory-lint` check 9 + `refresh-metadata` + the pinned refinement) is proven
end-to-end on a real product repo. Combined with the earlier rounds, mercury has now validated `archive-fact`,
the review-cadence advisory, `refresh-metadata`, and this refinement — cross-vendor.

## Memory References
- Created: agent-memory-upgrade-v4261
