# Session (2026-08-19T17:34:13.000Z)

**Agent:** Claude Code
**Lightweight:** docs-only correction, straight to main per the small-docs-patch precedent.

Commit `e48a6216` — docs: correct `snake.case.serialization` default (true, not false).

Eric reported the configuration reference claimed default `false` while `SimpleMapper`
defaults to `true`. Investigation confirmed the code is authoritative: the default has
been `true` since inception (a `git log -S` for a `false` default finds nothing), all 22
`application.properties` in the repo set true, event-script/syntax.md already calls
snake_case "the default", and snake_case is the cross-language wire convention (the Rust
port is snake_case at compile time via serde and does not read the key — its docs
correctly exclude it, so no Rust lock-step). Origin: the `false` was INTRODUCED by the
docs-style migration commit `fa9cc193` (likely pattern-copied from neighboring
default-false boolean keys) — born wrong, never a code change. Fixed both spots in
configuration-reference.md (per-key Default cell + the example properties block).
Migration lesson: verify Default cells against the reading code, not neighboring rows.

## Memory References

- referenced: conv-telemetry-presentation-parity (why the wire-convention default matters
  cross-engine), thread-docs-improvement-backlog
- created: (none)
