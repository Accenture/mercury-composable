# Session (2026-08-14T03:21:44.000Z)

**Agent:** Claude Code

## Summary

agent-memory **Mode B in-place upgrade v4.33.4 → v4.34.0** — the enforcing pre-commit secret
guard lands in this repo, whose config-file corpus helped tune it (all committed
json/yml/yaml/properties files were part of the 661-file probe that drove the detector to
zero false positives — including this repo's JAAS templates, `${…:demo}` defaults,
`test-secret` fixtures, and `v1.*.auth` route names, all now recognized as safe).

- **New `.githooks/pre-commit`** (rides the existing `core.hooksPath` activation — no setup):
  scans the **staged content** (index) of `memory/**.md` (full `[secret-material]` profile)
  and of config files (`.json`/`.yml`/`.yaml`/`.properties`/`.toml`/`.ini`/`.env*` —
  credential-class checks) **before a commit exists**. ⚠️ **Behavior change for everyone who
  commits here: a staged-secret finding now BLOCKS the commit by default** (the deliberate,
  scoped exception to the advisory doctrine — secrets carry irreversible after-the-fact
  cost). Escape hatches: fix the file; tag a quoted example line `lint:allow-secret-material`;
  list JSON/`.properties` files in the committed `.agent/secret-scan-ignore` (stub installed);
  bypass once with `git commit --no-verify`; or opt the guard down to warn-only via the
  `AGENT_MEMORY_SECRET_GUARD` env knob.
- **CI floor re-copied** (verified stock first): now also runs a changed-config credential
  scan on every push (`memory-lint --scan-files` — the new standalone mode in the re-copied
  built-in).
- `AGENTS.md` trigger blockquote merged (both surfaces + the enforcing-default doctrine);
  `.githooks/README.md` + `init.sh` re-copied; `.agent/version.md` stamped 4.34.0.
- **Verified live here:** 49/49 mirror tests both runtimes (byte parity); hook clean run exit
  0 with nothing staged; a staged bait file **blocked with exit 1** and was removed.

## Memory References

(none — tool upgrade only; no project facts referenced or created)
