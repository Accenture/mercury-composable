# Session (2026-09-23T05:28:33.000Z)

**Agent:** Claude Code

## Summary

Eric bumped the JaCoCo Maven plugin 0.8.14 → 0.8.15 across all 33 module poms
(commit `1e1a9d25`, authored before this session) "for Java 27 support". This
session verified the bump, established what it actually fixes, and pushed the
commit with this log.

## What was verified

- **Sweep is complete.** 33 poms declare `jacoco-maven-plugin`, 33 now read
  `0.8.15`, and `0.8.14` appears nowhere else in the tree (no build.gradle, no
  workflow, no prose). The seven poms the commit does not touch genuinely have
  no JaCoCo block: the reactor root, the three `templates/starter-*` poms,
  `benchmark/benchmark-reporter`, and the two retired Snyk placeholders
  (`system/rest-spring-3`, `examples/rest-spring-3-example` — the placeholders
  are dependency-free by design, so there is nothing to instrument).
- **0.8.15 resolves and runs.** `mvn -f system/platform-core/pom.xml test
  -Dtest=MsgPackTest` on this machine (Maven 3.9.16 running on **JDK 27**,
  Homebrew build 27, 2026-09-15): `jacoco:0.8.15:prepare-agent` attaches,
  31 tests pass, `jacoco:0.8.15:report` loads `jacoco.exec`, BUILD SUCCESS.

## What the bump actually fixes — and the failure mode worth knowing

A negative control settled it: the same module/test was re-run with the pom
temporarily reverted to `0.8.14` (restored via `git checkout` immediately
after — no tracked change).

Under JDK 27, **0.8.14 does not fail the build.** Tests pass, BUILD SUCCESS,
`jacoco:0.8.14:report` still runs. What it does is throw
`IllegalClassFormatException` → `IllegalArgumentException: Unsupported class
file major version 71` (71 = Java 27) out of the agent's bundled ASM, for
every JDK/runtime class it tries to instrument — `java/sql/Time`,
`org.apache.logging.log4j.util.StringBuilders`, `com.google.gson.GsonBuilder`
were the three hit in a 31-test run. Those classes go **uninstrumented** and
the console fills with stack traces.

So the durable point is not "the build was red". It is that an out-of-date
JaCoCo under a newer JVM degrades **silently**: coverage quietly stops covering
part of what ran, while every signal a CI gate watches stays green. The
version to track is the JDK the build **runs on**, not the release target —
`maven.compiler.release` is still 21, class files are still v65, and that is
exactly why the module's own classes instrumented fine and the problem was
invisible in the test result.

## Scope — no field or CI impact

The toolchain is unchanged and deliberately stays on Java 21
([[stack-language-java21]]): `.java-version` pins 21 and both
`.github/workflows/ci.yml` `setup-java` steps pin `'21'`, where 0.8.14 was
fine. This bump is for developers whose local JDK has moved ahead — here,
JDK 27 on PATH. JaCoCo is a `test`-phase plugin only, so nothing about a
shipped artifact changes and there is no release note to write.

## Notes

- The commit is a build-file change that landed on main directly rather than
  through a PR ([[eric-code-changes-via-pr]]). Eric authored it and directed
  the push; flagged once, not re-litigated.
- No continuity edit: no fact became false, no decision was taken, no thread
  moved. The observation above is recorded here rather than promoted to a
  fact, with continuity already carrying a standing `[continuity-bloat]`
  facts advisory (40 > 35). Promote it later if a second instance shows up.
- Method note: the A/B was run because a green build proves nothing about a
  coverage agent — the same reasoning as
  [[otel-optional-service-and-negative-control]]. Without the 0.8.14 leg this
  log would have said "verified working" and never learned that the old
  version also "works".

## Memory References

- Referenced: `stack-language-java21`, `eric-code-changes-via-pr`,
  `otel-optional-service-and-negative-control`, `conv-template-version-sweep`
- Created: (none)
- Reactivated: (none)
- Superseded: (none)
