# Session (2026-07-11T02:04:23.000Z)

**Agent:** Claude Code

Feature (Eric-designed, my design refinements confirmed): **configurable trace-id headers +
per-entry overrides** for legacy impedance matching, branch `feature/trace-cid-header-overrides`.

## Design (confirmed)
- Globals: `http.trace.id.header` (default X-Trace-Id; was HARD-CODED in HttpRouter) and
  `kafka.trace.id.header` (NEW capability, unset default — Kafka previously had traceparent
  only). Symmetric with the two existing correlation keys.
- Per-entry overrides `trace.id.header` / `correlation.id.header` in rest.yaml endpoint
  entries and kafka-flow-adapter.yaml bindings. Precedence: per-entry > global > built-in.
  Trace precedence: traceparent > configured header > UUID. Inbound-only (outbound to
  legacy = explicit header mapping; auto-stamp suppression already exists).
- Outbound: AsyncHttpClient stamps under configured HTTP name; SimpleKafkaNotification
  stamps trace-id under kafka.trace.id.header when set, ALONGSIDE traceparent.

## Key implementation facts
- **ConfigReader normalizes dotted keys into nested maps** (Eric's reminder):
  rest.yaml reads them via composite getProperty paths; adapter uses nestedText()
  walking the normalized form + flat fallback (pattern: schema.enabled).
- HTTP: RouteInfo carries the overrides; HttpRouter resolves at the capture site;
  getTraceIdHeader() static getter for AsyncHttpClient; EventEmitter's event-over-http
  X-Trace-Id left UNCHANGED (framework-internal protocol, envelope carries the trace).
- Tests: HeaderProbe mock (@PreLoad core.mock, auto-scanned) + /api/legacy/probe entry;
  Kafka legacy-test-topic binding reusing kafka-sink-flow; raw publishSync with custom
  record headers for full header control.

## Verified
platform-core 394/394, minimalist-kafka 86/86 (gates green), doc canon + grammar checks OK.
Docs: configuration-reference (2 new keys + override notes), kafka-flow-adapter binding
table, reserved-names-and-headers (configurable names + per-entry override callout),
event-envelope-reference.

STATUS: pushed `9e06cdf2`; PR text pending Eric. NOT yet in a release (4.7.2 deferred).

Commit `9e06cdf2` — feat(core): configurable trace-id headers with per-entry overrides for legacy impedance matching

```
 docs/guides/configuration-reference.md             |  6 ++-
 docs/guides/event-envelope-reference.md            |  2 +-
 docs/guides/kafka-flow-adapter.md                  |  2 +
 docs/guides/reserved-names-and-headers.md          |  8 ++++
 .../mini/kafka/KafkaConsumerBinding.java           | 33 ++++++++++++++
 .../mini/kafka/KafkaFlowAdapter.java               | 51 ++++++++++++++++++----
 .../mini/kafka/KafkaFlowConsumer.java              | 27 +++++++++---
 .../mini/kafka/SimpleKafkaNotification.java        | 18 +++++++-
 .../mini/kafka/KafkaFlowAdapterConfigTest.java     | 13 ++++++
 .../mini/kafka/KafkaFlowAdapterTest.java           | 45 +++++++++++++++++++
 .../src/test/resources/kafka-flow-adapter.yaml     |  8 ++++
 .../automation/config/RoutingEntry.java            |  7 +++
 .../automation/http/AsyncHttpClient.java           |  4 +-
 .../automation/models/RouteInfo.java               |  4 ++
 .../automation/services/HttpRouter.java            | 39 ++++++++++++-----
 .../automation/RestEndpointTest.java               | 26 +++++++++++
 .../org/platformlambda/core/mock/HeaderProbe.java  | 45 +++++++++++++++++++
 system/platform-core/src/test/resources/rest.yaml  | 11 +++++
 18 files changed, 318 insertions(+), 31 deletions(-)
```

## Memory References
(none — auto-stub; enrich per AGENTS.md "After Every Session", or leave as a lite log)
