# Session (2026-06-21T01:13:35.000Z)

**Agent:** Claude Code
**User:** Bootstrap the official GitHub repo for the documentation-rewrite effort — create a feature branch, re-apply the docs consolidation, and seed a clean agent-memory layer

## What We Did

This is the **first agent-memory session on the official Accenture GitHub repo**
(`github.com/Accenture/mercury-composable`). The documentation-rewrite effort and all
subsequent work move here as the source of truth (see user preference
`pref-github-source-of-truth`). The prior work was prototyped in a separate, private repo
whose git history is intentionally **not** carried over — only the confirmed Vision +
Blueprint and durable project facts were seeded, for a clean AI–Human commit log.

- **Branch:** created `feature/docs-ai-human-rewrite` off `main` (`9529ee39 "ignore .kiro
  AI infrastructure directory (#117)"`).
- **Docs consolidation** (commit `d8310641`): mirrored the prototype's structural change —
  `guides/` → `docs/guides/` and `arch-decisions/` → `docs/arch-decisions/` (git mv, history
  preserved), keeping the `guides/` subfolder so published `/guides/...` URLs are unchanged;
  `mkdocs docs_dir: docs`, new `docs/index.md` home, corrected `site_url`, root GitHub files
  kept at root as external nav links; 3 broken relative links fixed. Verified `mkdocs build
  --strict` exit 0; `/guides/CHAPTER-11/` etc. preserved.
- **Fresh agent-memory layer** (this commit): copied the vendor-neutral infrastructure
  (`AGENTS.md`, `DECAY.md`, `REVIEW.md`, `SKILLS.md`, `CLAUDE.md`, `.agent/schema.md`,
  `agent-skills/**`, `memory/{decay-policy.md, vision.md, smoke-test.md, archive/INDEX.md}`)
  verbatim; authored a fresh `.agent/version.md` (enabled_with 4.14.1, mode A), a cleaned
  `memory/instructions.md` (GitLab-migration header removed), and a clean-seed
  `memory/continuity.md` (project facts + confirmed Vision + 3-gap Blueprint, no prototyping
  history). Added `.gitignore` entries for per-vendor adapters / scratch / pyc.

## Decisions Made

- Official GitHub repo is the source of truth going forward; seed agent-memory clean (Vision
  + Blueprint + durable facts only), no prototyping session history. (`pref-github-source-of-truth`)
- Re-apply (not git-merge) the prototype's changes as fresh commits — the two repos have
  unrelated histories.

## Context for Next Session

- **Next: the retrieval-content rewrite** of CHAPTER-10/11 (+ companion README) — self-contained
  sections, consistent headings, stable anchors, YAML frontmatter (proven safe with mkdocs 1.6.1)
  + a sidecar `llms.txt`. Pilot → sign-off → roll out. (`bp-docs-ai-human-rewrite`)
- Branch `feature/docs-ai-human-rewrite` has 2 commits (docs consolidation; agent-memory seed).
  Push to GitHub + open a PR when ready.
- Re-verify doc builds with a local mkdocs (the prototype used a throwaway `uv` venv with
  mkdocs 1.6.1; `mkdocs build --strict` is the gate).
- Open Thread `thread-ci-docs-build`: add a `mkdocs build --strict` CI step.

## Memory References

- Created (seeded this session, all born on this repo 2026-06-20): `stack-language-java21`,
  `stack-build-maven`, `stack-integration-spring`, `stack-messaging-kafka`,
  `stack-persistence-r2dbc`, `stack-docs-mkdocs`, `stack-ci-gha`, `functions-decoupled-routes`
  (core), `typed-io-map-or-pojo` (core), `virtual-threads-rpc`, `pom-version-source-of-truth`,
  `docs-dir-layout`, `conv-add-capability`, `conv-serialization-gotchas`, `conv-canonical-example`,
  `bp-docs-ai-human-rewrite`, `bp-ai-companion-llm-backend`, `bp-graph-governance-lifecycle`,
  `thread-ci-docs-build`, `pref-github-source-of-truth`
- Referenced: `vision-mercury-composable` (core; the north star this effort serves)
