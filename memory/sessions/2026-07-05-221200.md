# Session (2026-07-05T22:12:00.000Z)

**Agent:** Claude Code
**Continuation** of 2026-07-05-213501 (same benchmark-reporter session; detail there).

## Analysis snapshot committed + two honest refinements

Saved the store comparison in one place for reference (`d0bdef46`): `benchmark/benchmark-reporter/analysis/`
= `README.md` (full picture + production recommendation) + the two self-contained report snapshots
`file-vthread.html` / `bdb-loop.html`. Module `.gitignore` changed `*.html` → `/*.html` so local run outputs
stay ignored but `analysis/` is committed.

Two refinements to the earlier read, from re-running the A/B:
1. **Probe-under-flood isolation win is reproducible** (3 runs): file mean ~0.15 ms / max <3 ms vs bdb
   mean ~1.2 ms / max 11–25 ms → ~8× mean, ~5× p99, 5–15× tail. This is the solid, mechanistic finding.
2. **Single-route tail latency is NOISY** — it flipped between runs (one run file better on RPC 100→50 max,
   next run bdb better). So the earlier "file wins the tail under spill" claim was run-specific; **corrected**
   — do not rely on single-route tail. BDB is also clearly faster on single-route *throughput*, especially
   spill (log-structured batching): RPC 100→50 bdb ~120–190k vs file ~70–75k.

**Recommendation unchanged and now documented:** default `file`+vthread for production (event-loop isolation
in multi-route services, VT-safety, portability), `bdb` a one-flag fallback; raw single-route speed favors
BDB but was never the driver. All in PR #137.

**→ Next session:** address **PR #137 code review feedback** (Eric gathering it). #137 now carries: file-FIFO
store + file-as-default (bdb fallback) + `elastic.queue.cleanup` `@KernelThreadRunner`, and the full
`benchmark-reporter` (6-scenario suite grouped normal / back-pressure / mixed; self-contained HTML; default
report → `/tmp`; `analysis/` holds the committed file-vs-bdb reference A/B). Recommendation documented:
default `file`+vthread for production (event-loop isolation, proven by the mixed-workload probe 8–15×), `bdb`
one-flag fallback. All pushed, tree clean.

## Memory References
- Referenced: thread-elastic-queue-bdb-to-file
