# Session (2026-09-24T23:14:06.000Z)

**Agent:** Claude Code

## Summary

**The consumer-side Schema Registry identity synced to twin-kafka (Eric's ruling after #458/#459: "before releasing
v4.12.17, please sync the feature to twin-kafka and Rust"); branch `feat/twin-kafka-consumer-registry-identity`
`ba379fc6`.** twin-kafka is built as the mirror of minimalist-kafka on `secondary.*` keys, so a primary-only opt-in had left
an asymmetry: `SecondaryKafkaAutoStart` shared one codec across its secondary producer and adapter. **Design decision — the
policy moved to the seam it drives:** `KafkaFlowAutoStart.resolveConsumerSchemaCodec` was package-private in
`org.platformlambda.mini.kafka`, unreachable from twin-kafka's package, so rather than duplicate it the logic became the
public, prefix-parameterized `SchemaCodec.forConsumer(config, registryUrl, keyPrefix, producerCodec)` next to the
`fromConfig` seam it calls (presence is the opt-in, blank = unset, the URL stays the feature switch; the two seam
consequences and the log lines live there now). The primary helper delegates under `schema.registry` (its constants and
`KafkaFlowAutoStartTest` unchanged); `SecondaryKafkaAutoStart` hands its adapter the codec resolved under
`secondary.schema.registry`, so `secondary.schema.registry.consumer.properties` (+ `.consumer.serde.*`,
`.consumer.cache.ttl`) is the exact twin of the primary key. Docs: twin guide paragraph + table row, configuration
reference entry, the primary guide names the twin key, the secondary template's note, llms.txt. Applied
[[clean-knowledge-design-over-engine-coverage]] (one seam, two call sites, no duplication) and the mirror principle behind
twin-kafka; extends [[kafka-consumer-registry-identity]] (addendum). The Rust twin (`SchemaCodec::for_consumer`, Increment
139) is in flight on mercury `feat/kafka-consumer-registry-identity` — its gates run after the Java suites finished, the
two never concurrently.

## Verification

`SecondaryConsumerCodecTest` 3/3 against twin-kafka's embedded registry (unset reuses the secondary producer's codec by
`assertSame`; the opt-in alone does not switch schema features on; a configured location builds a distinct codec under
`secondary.schema.registry.consumer` — its `ManagedCache` exists — that decodes what the secondary producer's codec encoded).
twin-kafka module suite **16 / 0 / 0** (13 + 3), minimalist-kafka **223 / 0 / 0** (the delegation kept `KafkaFlowAutoStartTest`
green), `mkdocs build --strict` clean. Local-build lesson: `mvn install -DskipTests` FAILS in minimalist-kafka because the
JaCoCo `check-coverage` goal runs with no data — install with `-Djacoco.skip=true` too (the first twin run compiled against
the stale released jar and could not find `forConsumer`; the reinstall fixed it).

## Merged

Eric opened PR #460 (title clean) and merged it at 23:30:43Z as squash `47d9acf8`; checks green on head `05a8cdbf` — Build &
Unit Tests (Java 21) 9m50s, Starter Templates (Gradle) 1m29s, memory ×2, docs verify (deploy skipped on a PR). Gate verified
(state MERGED, `47d9acf8` an ancestor of `origin/main`) before the local branch was deleted (the remote one already gone);
stash untouched; main at `47d9acf8`, tree clean. The Java side of the sync is complete: primary (#458), sample (#459),
twin-kafka on the shared `SchemaCodec.forConsumer` seam (#460). The Rust twin is mercury PR #327 (head `41beada5`, `test`
and `audit` in flight); v4.12.17 release prep follows both merges, on Eric's go.

## Memory References

- kafka-consumer-registry-identity (addendum: twin-kafka + the policy's new home)
- kafka-consumer-identity-release (updated: Eric ruled both syncs; version 4.12.17 named)
- clean-knowledge-design-over-engine-coverage (consulted)
- conv-ports-adopt-java-release-number (consulted — the Rust twin ships the same content at 4.12.17)
- dual-repo-gates-share-ports (consulted — Java suites first, Rust gates after)
