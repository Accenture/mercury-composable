# Session (2026-06-23T02:03:43.000Z)

**Agent:** Claude Code
**User:** Content-polishing round 2 (browser-reviewed, iterative), then a code-vs-docs drift validation;
finish by committing/pushing and opening the PR for peer review.

## What We Did

A long interactive polishing pass, all reviewed by Eric in a live `mkdocs serve` browser.

**Content polish:**
- **Merged Quickstart into Getting Started** (one Part-I doc; removed `quickstart.md`, repointed
  index/README/llms/nav). Added a **5-minute Quickstart** section + a **Knowledge-Graph "hello world"
  taste** (`minigraph-playground` → `curl /api/graph/tutorial-1`) linking to build-your-first-graph;
  At-a-glance updated. Dropped the redundant `jq` pipes.
- **Home "layered ascent" table** → 3 columns, stacked layer name (product name is the link), cells
  wrapped to fit (no horizontal scroll).
- **Kafka wording** softened (proven in production / sample coming, not "write your own"); **stages-vs-
  layers** comparison removed from user-facing prose (confuses newcomers) — the convention still holds in
  the canon, just not surfaced as a contrast.
- **Core APIs** (`architecture.md`): `PostOffice` key methods → one-per-line list; **removed
  `po.broadcast`** (its accurate meaning needs Kafka-service-mesh context).
- **Wide tables fixed site-wide via CSS** — new `docs/css/extra.css` (`extra_css` in mkdocs) wraps table
  cells + long inline `code` tokens, so reference tables stop horizontal-scrolling **without per-cell
  `<br>`**. (Use this, not hand-breaking, for future wide tables.) Also hand-wrapped the REST endpoint
  syntax table.

**Code-vs-docs drift validation** (3 reference pages audited against source via 3 parallel subagents;
code = source of truth). Docs were largely accurate; fixes applied:
- **annotations-reference:** all 11 in sync; **added `@FetchFeature`** (MiniGraph annotation,
  `com.accenture.minigraph.annotations`) — Eric confirmed MiniGraph annotations are in scope.
- **configuration-reference:** `kernel.thread.pool` "Maximum 200" → advisory (min 32, default 100, no hard
  max; ~250 practical JVM limit); `yaml.cron` default corrected to include `file:/tmp/config/cron.yaml`;
  **added `log.format`** (default `text`).
- **reserved-names-and-headers:** added `resilience.handler` (event-script) and `run.scheduled.job`
  (mini-scheduler), both registered routes missing from the table. (Connector/service-monitor routes are
  out-of-repo — not drift.)

Verified throughout: `mkdocs --strict` 0 warnings, canon + 3 grammar checks pass. **No ADR impact** —
all content/presentation/factual corrections; ADR-0004/0005 decisions unchanged.

## Context for Next Session

- **PR ready** for peer review (`feature/docs-ai-human-rewrite` → `main`); branch fully pushed (incl. the
  ADR-upgrade commit). NOTE: `gh pr create` is blocked for this Enterprise-Managed-User account
  ("Unauthorized … createPullRequest"), so the PR must be opened **manually via the GitHub web UI**.
- **Still open:** the AI-context-discovery on-ramp (writing composable functions + reasoning across the
  3 layers for greenfield/brownfield) — `thread-next-ai-context`; and the optional Layer-1 foldering
  decision — `thread-layer-reorg`.

## Memory References

- Referenced: `docs-content-canon`, `docs-no-redirects`, `adr-pattern-adopted`, `kafka-adapter-not-in-repo`,
  `docs-style-conventions`, `thread-next-ai-context`, `thread-layer-reorg`, `vision-mercury-composable`
- No new facts (round was execution of recorded decisions; table-wrapping CSS is self-documented in `extra.css`).
