# Session (2026-06-21T03:36:36.000Z)

**Agent:** Claude Code
**User:** Extend the deterministic DSL spec kit to REST automation (the third and last DSL)

## What We Did

Applied the spec pattern (`docs-dsl-spec`) to **REST automation** (`rest.yaml`), grounded in the
parser and fresh-agent-validated over two passes (committed `f55a2ec3`). This completes the trio.

- **Grounded the schema in code** (Explore over `system/platform-core`): the top-level structure,
  the `rest:` entry fields, the authoritative HTTP-method set (`RoutingEntry.VALID_METHODS` =
  GET/PUT/POST/DELETE/HEAD/PATCH + auto-added OPTIONS), binding modes (function / flow via
  `http.flow.adapter` / HTTP relay / A-B dual), and the `cors`/`headers` blocks.
- **Wrote the spec kit** (`docs/guides/rest-automation/`): `rest-grammar.md`, `rest-automation.json`,
  `ai-agent-guide.md`, and `scripts/check-rest-automation-grammar.py` (drift-checks the catalog's
  HTTP methods against `RoutingEntry.VALID_METHODS`; verified it catches injected drift). Wired into
  the nav + `docs.yml` CI.
- **Fresh-agent validation (2 passes):** Pass 1 authored a correct `rest.yaml` and answered all
  questions correctly, but flagged that my docs were *inconsistent* on boolean values (field tables
  `'true'`/`'false'` vs. the example's unquoted `tracing: true`). Standardized on unquoted `true`/
  `false` + added an explicit note. Pass 2 confirmed it consistent; residual flags were
  app-modeling/quoting-convention edges, not DSL-syntax gaps.

## Decisions Made

- All three DSLs (MiniGraph, Event Script, REST automation) now carry the deterministic spec kit
  (grammar + machine-readable catalog + AI-agent guide + CI drift test), each fresh-agent-validated.
- Each drift test keys off its authoritative code artifact: MiniGraph → shipped help + skill routes;
  Event Script → `CompileFlows.EXECUTION_TYPES`; REST → `RoutingEntry.VALID_METHODS`.

## Context for Next Session

- **The DSL-determinism initiative is complete.** Point external agents (e.g. Kiro) at the
  per-DSL `ai-agent-guide.md` + the JSON catalogs instead of hand-rolled context files.
- **Remaining in the documentation rewrite** (`bp-docs-ai-human-rewrite`): refine the spine opener
  (`docs/index.md` → the full paradigm story); retire old CHAPTER-11 behind a redirect (needs
  `mkdocs-redirects`); migrate+refresh the lower-layer chapters into Parts I–III, V–VI.
- Branch `feature/docs-ai-human-rewrite` (push). Gates: the three `scripts/check-*-grammar.py` +
  `mkdocs build --strict` (local venv `/tmp/mkdocs-venv`). Consider opening a PR for team review.

## Memory References

- Referenced/updated: `docs-dsl-spec` (extended to REST — all 3 DSLs done),
  `bp-docs-ai-human-rewrite` (progress (7)), `docs-rewrite-architecture`, `vision-mercury-composable`
