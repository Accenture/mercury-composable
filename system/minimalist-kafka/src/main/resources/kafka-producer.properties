#
# Kafka PRODUCER configuration template for the minimalist-kafka building blocks
# (used by simple.kafka.notification and the dead-letter writer).
#
# Enterprise Kafka installations vary widely - on-prem, cloud, SaaS, Confluent - and authenticate in
# different ways (SASL/PLAIN, SASL/SCRAM, OAuth2 with client-id/secret, mTLS). Rather than encode every
# variation in code, put your environment's settings here, or override this file at
#   /tmp/config/kafka-producer.properties   (typically rendered by CI/CD)
# or point kafka.producer.properties (in application.properties) at another location.
#
# Values support ${ENV_VAR:default_value} environment-variable substitution.
#
# The library PINS the serializers in code (key=String, value=byte[] - the wire contract); everything
# below is yours to configure.
#

# --- Connection ---
bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:127.0.0.1:9092}

# --- Durability (acks=all is safest; lower to 1/0 to trade durability for throughput) ---
acks=all

# --- Enterprise security: uncomment and complete for your installation ---
# security.protocol=${KAFKA_SECURITY_PROTOCOL:SASL_SSL}
# sasl.mechanism=${KAFKA_SASL_MECHANISM:OAUTHBEARER}
# OAuth2 (client-id / client-secret) is supplied through the JAAS config and a token endpoint:
# sasl.jaas.config=${KAFKA_SASL_JAAS_CONFIG:}
# sasl.oauthbearer.token.endpoint.url=${KAFKA_OAUTH_TOKEN_URL:}
# sasl.login.callback.handler.class=${KAFKA_SASL_LOGIN_CALLBACK:}
# ssl.truststore.location=${KAFKA_SSL_TRUSTSTORE_LOCATION:}
# ssl.truststore.password=${KAFKA_SSL_TRUSTSTORE_PASSWORD:}
