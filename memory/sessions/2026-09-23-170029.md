# Session (2026-09-23T17:00:29.000Z)

**Agent:** Claude Code

## Summary

Eric asked for a review of his experiment branch `fix/embedded-redis-apple-silicon` (a new reactor module
`helpers/embedded-redis-support` with a fallback chain for the embedded `redis-server` binary — "experiment only, do not
use this branch") and ruled the solution: **no new module; a simple doc update** telling a developer about the OpenSSL
prerequisite of the embedded Redis on an Apple Mac, checked for Linux and Windows too — the current codebase passes CI
and the field installations and stays as it is.

**Review of the branch (read, not used).** Its diagnosis holds and was re-verified from the jar itself: embedded-redis
1.4.3's `redis-server-6.2.6-v5-darwin-arm64` is dynamically linked against `/opt/homebrew/opt/openssl@3/lib/libssl.3.dylib`
and `libcrypto.3.dylib` (`otool -L`), so on an Apple Silicon Mac without Homebrew OpenSSL the subprocess dies before it can
print anything and the library throws `IOException("Failed to start Redis service")` caused by `Redis-server process
appears not to have started.` — the reactor build fails at `extensions/redis-connection`, the first module whose tests
start an embedded Redis (10 call sites in 8 modules incl. `helpers/redis-standalone`). Two corrections to the branch's
own log: (1) "every other bundled binary needs only system libraries" is wrong for Linux x86-64 —
`redis-server-6.2.6-v5-linux-amd64` needs `libssl.so.3`, `libcrypto.so.3` and glibc 2.34 (`llvm-objdump -p`; GitHub's
`ubuntu-latest` has both, which is why CI is green; Ubuntu 20.04, Debian 11 or RHEL 8 would not be); (2) the branch
README's `EMBEDDED_REDIS_EXECUTABLE` / `-Dembedded.redis.executable` override exists only through the module it added —
the library's default `newJarResourceProvider()` never consults them (`javap`), so a doc-only fix cannot offer an override.
Confirmed the rest: darwin-amd64 links libSystem only; linux-arm64 and linux-386 are statically linked; the Windows
5.0.14.1 exe imports only system DLLs (matches the field's Windows VDI confirmation); both macOS binaries need macOS 12+;
embedded-redis 1.4.4 (latest on Maven Central, 2026-08-23) ships byte-identical arm64 and linux-amd64 binaries (sha256
compared), so an upgrade does not help; upstream's README documents the same dependency under "SSL/TLS Troubleshooting"
(`brew install openssl@3`, `apt-get install openssl`). The branch was not merged, rebased or built on; its own log
(`2026-09-23-154604.md`) and its `conv-template-version-sweep` "44 build files" edit exist only on that branch — main
stays at 43.

**Change (commit `3105d15a`, branch `docs/embedded-redis-openssl-prerequisite` from main; PR to follow).** Doc-only, seven
files: `helpers/redis-standalone/README.md` gains a *Platform prerequisites* section (per-platform table, the symptom, the
`brew list openssl@3` / `ldconfig -p` checks, the upstream pointer) and its intro now says macOS, Linux AND Windows;
one-sentence pointers in the `extensions/sync-over-async`, `extensions/minigraph-state-redis`,
`examples/distributed-cache-example` and `examples/sync-over-async-demo` READMEs (relative links — these files are outside
the AI contract snapshot); and a note beside the first `mvn clean install` in `docs/guides/getting-started.md` plus the
whole-repository note in `docs/guides/build-test-deploy.md` (absolute GitHub links, because the snapshot's link check
covers relative links — the #421 lesson). Gates run locally from a scratch uv venv exactly as `docs.yml` does: the four
anti-drift scripts ok, `mkdocs build --strict` ok.

**Method notes.** Apple's `strings` misreads Java class files as fat Mach-O binaries (same `0xCAFEBABE` magic) — extract
constants with Python or `javap`. Verify what a binary needs from the binary (`otool -L`, `llvm-objdump -p`, `file`), not
from a prior session's one-line summary: "only system libraries" hid a real Linux requirement.

## Memory References

- clean-knowledge-design-over-engine-coverage (edited: Eric's ruling applied to the toolchain — a dev-machine prerequisite
  is documented, not engineered around)
- eric-code-changes-via-pr (consulted: a docs change reaches main through a PR)
- conv-template-version-sweep (consulted: the branch's "44 build files" edit is branch-only; main stays at 43)
