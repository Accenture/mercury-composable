# Session (2026-09-13T16:31:46.000Z)

**Agent:** Claude Code

## What happened

**The Rust lock-step arc opened, and one cross-engine naming ruling applied.**

1. **Viability investigated: the streaming return route ports to the Rust engine**
   (Eric's ask; his constraints: no Kafka module there, no embedded Redis binary for
   tests — the hand-rolled RESP double acceptable if it fully covers the use cases,
   Pub/Sub flagged as the risk; Docker intentionally not an option, VDI-class dev
   machines cannot virtualize). Findings: no-Kafka is a non-blocker (the route is
   broker-free by design, D3; Kafka only carried the Java demo's one-shot request
   leg); the Rust substrate is ALREADY in lock-step (EventStreamWriter,
   x-event-stream, stream:true SSE edge, SSE consumer); the RESP double already
   implements MULTI/EXEC, so the Rust store uses `pipe().atomic()` instead of the
   Java Lua script (same atomic-append contract, no Lua to emulate) and the double
   needs only ~225 lines (Lists, EXPIRE, Pub/Sub via mpsc+select). Emulated Pub/Sub
   is honest for THIS design because wake-ups are best-effort by contract — every
   invariant needing proof lives in the deterministic List/TTL/atomic surface. One
   real client delta: redis-rs has no Pub/Sub auto-resubscribe (Lettuce does) — the
   Rust coordinator owns a resubscribe loop + recovery drain. The prize Eric
   endorsed ("the interop is rewarding"): wire-format parity makes the rendezvous
   POLYGLOT — Rust and Java pods sharing one streaming return route.
2. **Draft port spec written in the Rust repo** — merged there via
   Accenture/mercury#263 (merge `a3f979bd`): `draft-design-specs/sync-over-async-port.md`
   + new open thread `ot-sync-over-async-port`, rulings Q1–Q5 pending (facade tasks,
   double's home, health check, crates.io naming, demo crates). Experiment plan
   R1–R4; R4 = the two-way polyglot dry-run as the wire-parity acceptance gate.
3. **Health-check route renamed `redis.health` → `soa.redis.health`** (Eric's
   ruling, both engines): the plain `redis.health` name is RESERVED for the health
   check of a planned generic Redis distributed-cache module, so both features can
   coexist against the same Redis server. Route name only — the
   `redis.health.timeout` / `redis.health.startup.grace` config keys are unchanged
   (config keys do not collide; routes do — the registry rejects duplicates), and
   the shipped v4.12.8 CHANGELOG entry stays as the historical record; the NEXT
   release's changelog carries the rename. Java: `RedisHealthCheck` @PreLoad +
   javadocs, the KafkaHealthCheck contrast note, both guides (with the reservation
   rationale spelled out) — MERGED via PR #377 (squash `e1920eea`, mergedAt
   2026-09-13T16:36:45Z API-verified, title clean); extension suite 77/77,
   minimalist-kafka compile green, doc canon + mkdocs strict green. Rust: the port
   spec's §1/§6/Q3 updated, the §6 row now normative on the prefix — MERGED via
   Accenture/mercury#264 (merge `a1a60e24`, 16:36:27Z). Continuity touched: the
   preload-lazy-config and kernel-threads facts now name the route accurately.

## Memory References

- eric-release-rhythm (consulted - branches/commits/pushes prepared in both repos;
  PR creation and merges are Eric's gates)
- conv-squash-title-prefill-check (consulted - PR titles handed off on their own
  lines in code blocks; the Rust repo merge-commits, so the guard is Java-side)
- preload-before-mainapp-lazy-config (updated - its redis.health mention renamed
  with the reservation note)
- kafka-clients-kernel-threads (updated - contrast mention renamed)
- conv-telemetry-presentation-parity (consulted - the polyglot-field reality is the
  spec's wire-parity argument; the rename applied to both engines in one ruling is
  that contract in action)
- bp-agent-orchestration (consulted - the Rust port spec cites the same streaming
  transport Q8 consumed; no thread change here)
