# Session (2026-07-20T22:27:09Z)

**Agent:** Claude Code

## Work Done

Docs-site usability overhaul on branch `docs/material-theme-mermaid-tables` (Eric's request:
wide reference tables render with mostly-invisible columns + horizontal scrolling on the
readthedocs theme; scanned PNG diagrams should become mermaid). Content structure kept —
only presentation changed. Modeled on the Rust port's mkdocs.yml (`~/sandbox/mercury`).

### 1. Theme: readthedocs → Material for MkDocs

- `mkdocs.yml` rewritten: Material theme (light/dark palettes, navigation tabs/sections,
  code copy), markdown extensions incl. `pymdownx.superfences` with the mermaid custom
  fence (Material renders mermaid natively), `admonition`, `def_list`, `pymdownx.tabbed`,
  link/nav validation block, `not_in_nav: llms.txt`. Nav preserved verbatim.
- `.github/workflows/docs.yml`: `pip install mkdocs` → `pip install mkdocs-material`
  (both verify and deploy jobs).
- `docs/css/extra.css`: readthedocs selectors (`.rst-content`, `.wy-menu-vertical`)
  replaced with Material equivalents (`.md-typeset table`) for code-token wrapping.

### 2. Diagrams: 10 PNGs → mermaid

All 9 referenced PNGs in `docs/guides/diagrams/` recreated as mermaid flowcharts in place
(getting-started ×4, methodology ×2, knowledge-graph/index, property-graph,
event-script/syntax ×2); each diagram validated with a mermaid renderer before insertion.
All 10 PNGs deleted (semantic-app.png was unreferenced).

### 3. Wide tables → per-entry sections (the Rust port's proven pattern)

Pattern: `###`/`####` heading per key + one-row mini-table for compact metadata
(Type/Default/Required) + description as full-width prose. Deep-linkable anchors, no
horizontal scrolling. Conversion done by script (scratchpad `convert_tables.py`, backtick-aware
cell splitting), output reviewed:

- `configuration-reference.md` — 127 keys (the page in Eric's screenshot)
- `flow-schema-reference.md` — 24 fields (the 4/5-column Field tables)
- `annotations-reference.md` — 18 parameters
- `event-envelope-reference.md` — 58 methods (matches the Rust port's per-method format)

Compact 2-column tables (Header|Purpose, Route|Purpose etc.) deliberately kept — they wrap
fine under Material.

Also (same session, Eric asked to convert the borderline ones too): the long-cell tables
became **definition lists** (`def_list`) rather than per-entry sections — better fit for
grouped listings:

- `event-script/syntax.md` — the 43-row built-in plugin table → `####` category headings
  (Arithmetic/Generator/Logical/Type Conversion) + def-lists; a duplicated `not` row in the
  source table was deduplicated.
- `knowledge-graph/command-reference.md` — 3 tables → def-lists: the constants closed set
  (Form|Produces), the non-constant source forms (Form|Meaning, incl. the 885-char
  `f:plugin` cell), and the `graph.math` statement grammar (Statement|Form|Purpose, form
  joined onto the term line). Verified `check-minigraph-grammar.py` reads only the JSON
  catalog + help files, so the page restructure cannot trip the anti-drift gate.

**Verification:** `mkdocs build --strict` green; one-off `validation.anchors: warn` run
clean (no broken anchors from restructuring); `check-doc-canon.py` OK. Local venv with
mkdocs-material used for builds (repo CI installs its own).

Diff: 22 files, +2019/−346 (+ 10 PNG deletions). Not committed — awaiting Eric's visual
review (`mkdocs serve`) and direction.

Also (same session): **removed all reader-facing references to the Node.js port** (Eric: the
node version is ~2 years out of sync and will be re-ported fresh — don't point readers there).
Removed: getting-started.md "autostop" note, five Java-vs-Node comparison passages in
methodology.md (incl. the TypeScript code example), and README.md's "Also available in
Node.js" section. Updated `memory/instructions.md`'s stale "Also available for Node.js" line
to reflect this (Rust port is the one to reference). Verified zero `node.js` matches remain
in docs/ + README; strict build + canon green.

Also (same session): **architecture.md "Distributed Architecture" section now summarizes
minimalist-kafka** (Eric's request). The section presents Event over HTTP, then Minimalist
Kafka as the recommended path to fully asynchronous EDA (inbound flow adapter + outbound
notification, at-least-once, DLQ, trace continuity, config-not-code), then the service mesh
with its two-problem scope (sync-over-Kafka RPC + service discovery) and a pointer to
service-mesh.md#when-to-use — consistent with the existing canon ([[kafka-mesh-opt-in]] /
ADR-0006: mesh is opt-in; prefer minimalist-kafka for plain async eventing).

## Memory References

- referenced: thread-docs-improvement-backlog (this serves the docs mission; not one of
  its listed backlog items, so the thread is unchanged)
- referenced: kafka-mesh-opt-in (the mesh-vs-minimalist-kafka recommendation now surfaced
  in architecture.md's Distributed Architecture section)
- created: docs-site-material-theme
