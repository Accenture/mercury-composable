# Session (2026-09-19T02:05:51.000Z)

**Agent:** Claude Code

## Summary

**The Rust lockstep for `for_each` suspend/resume was executed in the `mercury` repo** (Eric: "let's
start the rust lockstep … also follow agent-memory protocol to clear the same ADR that were still in
proposed phase"): branch `feat/for-each-suspend-resume-lockstep`, commit `3c98043d`, pushed — **mercury PR #284 opened
by Eric 2026-09-19**; merge is his gate ([[eric-release-rhythm]]; the twin rhythm holds in the Rust repo).
It ports what [[ot-subgraph-for-each-suspend]] handed over: the three-segment store key
`graph:{graph_id}:{cid}:{index}`, the `x-iteration-index` header from the extension skill's
`for_each` branch, the reserved `model.iteration_index`, the Redis store and file-store changes, and
the Java negative-control tests' shape (the three store scenarios, the reserved-key pin, the index
reader) — plus an end-to-end fan-out over the twin's `unit-test-sub-suspend` that this repo's suite
does not have. The Rust ledger got the same treatment as ours: its three `Proposed` ADRs (0012–0014;
0012 is the twin of our ADR-0013) accepted in place, 0012 amended with the index, `RFC.md` adopted and
packaged into the Rust AI-contract snapshot in both places it enumerates. Full record: the Rust repo's
`docs/INCREMENTS.md` Increment 118 and its session log `2026-09-19-022252`.

**Two findings for THIS repo.** (1) **A statement of the RULE, not the key, survived #418 and #420:**
the orchestrator pattern's "One record per graph per cid" bullet in
`docs/guides/knowledge-graph/workflow-suspension.md` still said a `for_each` fan-out "would overwrite
its own record" — the pre-fix behaviour stated as a design rule — and the sentence #418 added to the
self-contained rule pointed at the positional-consistency requirement "in the next rule" when the
`for_each` rule sits four bullets above. Both corrected on branch `docs/for-each-orchestrator-rule`
(`0c9166ba`, pushed; docs via PR per [[eric-code-changes-via-pr]] — **PR #425 opened and MERGED by
Eric 2026-09-19, squash `8c81a295`, one trailer**); `check-doc-canon.py` and `mkdocs build --strict`
clean. mercury #284's CI settled green (build, test, memory) the same morning; its merge is Eric's gate. The #420 sweep grepped for the key format and so missed a sentence
that states the collision without it — the same "grep for the thing you touched, not the topic"
lesson, one level up: the *thing* was the rule, not the string. (2) **The Rust twin had a fork-join
telemetry defect Java does not have:** its three fan-out skills launched batches on `tokio::spawn`ed
tasks, dropping the task-local trace bracket, so fan-out children ran untraced and lost the business
cid downstream; Java's `po.request(batch)` runs on the worker thread with the trace, so the Java
engine was never affected. Fixed in the Rust PR (`join_all` on the calling task) — recorded here only
because [[conv-ports-adopt-java-release-number]] makes Java the reference the twin was measured
against, and because the Java-parity assertion that caught it (the orchestrator test's "the working
step sees the business cid") is the kind worth re-running on every shape a feature has.

**Process.** Java leads, twin immediately after ([[conv-ports-adopt-java-release-number]]): the Rust
branch reuses the Java guide text verbatim where the guides are shared, adapts where the Rust
realization differs (`flow://` gets no index in both), and records its own increment. `uv` is
available for scratch venvs (Eric) — `mkdocs build --strict` now runs locally for both repos.

## Memory References

- ot-subgraph-for-each-suspend — edited: Rust lockstep prepared (branch + commit), the stale-rule
  finding and its Java docs branch, the Rust-only fork-join defect; stays open until both PRs merge
- conv-ports-adopt-java-release-number — consulted: Java leads, the twin follows immediately; the
  Rust engine keeps 4.12.7 until it catches up (lag, not divergence)
- conv-proposals-not-in-adr-ledger — consulted: the same treatment applied to the Rust ledger and its
  snapshot packaging (two enumeration places there, three here)
- eric-code-changes-via-pr — consulted: the docs correction went to a branch, not main
- eric-release-rhythm — consulted: PR-open and merge stay Eric's gates in both repos
- conv-declare-consulted-references — applied
