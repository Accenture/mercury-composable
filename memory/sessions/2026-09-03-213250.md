# Session (2026-09-03T21:33:00.000Z)

**Agent:** Claude Code

## What happened

Two recipe additions to `docs/guides/knowledge-graph/ai-agent-guide.md`, distilled from the real
live demo (`ai-enabled-repo-demo` branch `layer-3-live-demo`, "flawless") cross-read against the
two fresh-agent validation rounds:

1. **`output.status` surfaced in the agent-facing recipe** (step 2): a mapper can set the HTTP
   response status (`int(400) -> output.status`); non-2xx routes through the exception path
   (pointer to `workflow-suspension.md`). It was already documented — but only in
   `workflow-suspension.md` / `composing-the-layers.md`, outside the agent read-first set, and
   two independent fresh agents therefore concluded it was impossible and shipped a divergent
   `200 + rejected:true` rejection shape. A placement gap, same shape as the `/sync` finding;
   filed as feedback item 13 in the demo repo's feedback doc.
2. **New recipe step 8:** a fresh Playground session after the deploy-restart starts empty (no
   root node) even though the graph is deployed — `import graph from {name}` is required before
   `instantiate`/`run`. Every operator hits this right after the demo flow's signature restart;
   it was never stated.

Verified before documenting: the Rust engine validates `output.status` as an HTTP code in its
event-script executor — identical semantics; the same edits landed in the Rust repo.

## Memory References

- Referenced: playground-session-broker
