# Session (2026-09-25T23:06:41.581Z)

**Agent:** Claude Code
**Title:** PR #465 and mercury #332 merged — merge refs on the manifest-list fact; unreleased content noted for 4.12.19

## Summary

Merge gate passed on both repos (PR state MERGED; the merge commit reachable from `origin/main`): mercury-composable #465
squash `40ce30a7` (2026-09-25T23:03:41Z) and mercury #332 merge `d3d82a3f` (23:03:26Z). Local mains fast-forwarded, both
`feat/graph-manifest-list` branches deleted (GitHub had already removed the remote refs). The superseded
`docs/deploy-without-rebuild` branch was deleted earlier at Eric's request — its tip `46edccee` is an ancestor of the
squash, so #465 carries the recipe docs. Main CI was in progress on both merge commits at close; the Java memory push is safe
(no concurrency group), the Rust memory commit is held locally until its `rust` run completes (`cancel-in-progress`).
The content is unreleased — the 4.12.19 candidate so far.

## Memory References

- graph-manifest-list-later-wins (edited: merge refs and unreleased status)
