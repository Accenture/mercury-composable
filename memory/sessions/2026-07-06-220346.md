# Session (2026-07-06T22:03:46.000Z)

**Agent:** Claude Code

## SonarQube quality-gate remediation for 4.6.2 (branch `chore/sonar-quality-gate-4.6.2`)

Eric ran a field SonarQube scan of the 4.6.x work; Snyk passed but the Sonar **new-code quality gate
failed**: coverage 68.9% (< 80%), 9 blocker, 5 critical, 29 major (+5 minor; 50 new issues). He supplied
~70 screenshots in `/tmp/sonar`. Cataloged all issues (scratchpad `sonar-catalog.md`), then fixed.

**Strategy:** genuinely fix issues in product + core-test code; **exclude non-product tooling** (benchmark
harnesses, runnable examples/demos, standalone dev-servers) from Sonar analysis + coverage — that clears
their issues and the bulk of the coverage gap (BenchmarkApp alone was 245 uncovered lines).

- **Retired `benchmark/benchmark-client`** (module + dir + doc refs) — superseded by benchmark-reporter (Eric's request).
- **Root pom Sonar scope:** `sonar.exclusions` + `sonar.coverage.exclusions` = `benchmark/**, examples/**,
  helpers/**, **/*Benchmark.java`. NOTE: read by `mvn sonar:sonar`; mirror into sonar-scanner CLI config if the field uses that.
- **Sleep test util (Eric's suggestion):** added `Utility.sleep(ms)` (blocking-queue `poll`, interruptible,
  no Thread.sleep) + `UtilityTest`; replaced all 6 flagged Thread.sleep/TimeUnit.sleep in tests.
- **Blockers:** RedisConfig 5 constants → `_KEY` (S1845); W3cTrace `traceparent()` → `format()` (+ all callers).
- **Criticals:** refactored `KafkaFlowAdapter.buildConsumer` + `HttpRouter.copyToSecondaryTarget` (extract
  helpers, S3776); SchemaCodecTest try-with-resources (S2093).
- **Majors:** W3cTrace `parse()` empty-array (S1168, + callers/tests); 6× assertThrows hoist (S5778); 5×
  commented-code cleanup incl. pom KMS templates (S125); assertNull (S5785); dup-method delegate (S4144);
  nested-ternary extract (S3358); `record()`→`toRecord()` (S6213); unused import (S1128).
- **Left by design:** `HttpRequestEvent` public fields (S1104, minor) — the class is a public-field DTO
  throughout; not gate-blocking.

**Verified:** full reactor `clean install -DskipTests` = SUCCESS; changed-module tests green (platform-core,
minimalist-kafka 79, sync-over-async 32, opentelemetry-forwarder 22, event-script-engine, minigraph).
**Could not run SonarQube locally** — the field re-scan is the confirmation. Coverage reasoning: excluding
~740 uncovered non-product lines leaves product new-code coverage ~90% (well above 80%); residual drag if
any = BdbElasticStore 69% / ServiceQueue 68%.

Committed the code/docs as one commit on `chore/sonar-quality-gate-4.6.2` (off `main`, which already carries
the guava-failureaccess fix). **Next (Eric):** clone the branch, run the field Sonar + Snyk scan; if green,
this + the guava fix ship as **4.6.2** (version bump + CHANGELOG + tag). See [[thread-sonar-quality-gate-4.6.2]].

## Memory References
- Created: `thread-sonar-quality-gate-4.6.2` (working)
- Referenced: [[thread-guava-failureaccess-field-fix]]
