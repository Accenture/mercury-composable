# Session (2026-09-20T00:47:02.000Z)

**Agent:** Claude Code

## Summary

**Distributed cache certified Java ⇄ Rust — and a Layer 1 defect found and fixed in both engines.**
Eric: "confirm section 9 recommendation in draft design specs" (the Rust port spec — done there) and
"conduct an interop for distributed cache between Java and Rust implementation and record outcome in
the test-reports folder. The interop would use the Java helper's redis-standalone server."

**The drive.** Both `distributed-cache-example` apps — this repo's jar 4.12.12 (main code `c74ee6be`)
on 8305, the Rust one (mercury main `5ac55eaf`, PR #285) on `-Drest.server.port=8306` — against one
`helpers/redis-standalone` 4.12.12 on 6379, driven by a scratch Python script (urllib + redis-py +
msgpack): `/info` + `/health` identity; a 6 × 6 writer/reader matrix over both engines' three layers
(18 cross-engine reads); raw key inspection (`cache-demo:{id}`, plain MsgPack maps, TTL 3600 from
birth, exactly the 8 keys); 6 cross-engine deletes with the miss confirmed on a third layer;
field-by-field error-shape parity (miss bodies on L1/L2/L3 and the L3 rejection identical); a 54 s
Redis outage; recovery without an app restart. Three runs — discovery (found the defect), fix
verification, certification: **112/112 hard checks**, 1 informational (the same payload is not
byte-identical across engines: Java packs insertion order, serde_json sorts keys; both decode equal —
MsgPack map order is not the contract). Report `docs/test-reports/distributed-cache-interop.md`
(+ mkdocs nav; twin in the Rust repo) — nav-only like the suspend/resume report: no packaged guide
links it, so the AI-contract snapshot's three enumeration places stay untouched.

**Finding 1 — Layer 1 turned a cache failure into a miss, both engines, fixed.** With Redis down the
Rust L1 GET answered 404 *Profile not found* while `v1.cache.redis` failed (`broken pipe`); OUR L1
answered 500 only because its 5 s RPC timeout raced Lettuce's 5 s command timeout — the same code on a
fast-failing cache (connection refused, NOAUTH) would have misreported. Both `ProfileCacheL1` tested
the reply BODY (`byte[]`/`Value::Binary` or null), never its STATUS; a function that throws replies
with the error status and the message as a string body, so a fast failure read as "nothing cached" and
a POST would have acked `stored`. L2/L3 never had it — the flow and graph engines check task status
for the author ([[event-script-over-code]] observed in the wild). Fix: `checked()` guard in both L1
functions (GET/POST/DELETE); tests swap `v1.cache.redis` for a fail-fast stub (here
`Platform.release` + `register`, the real `RedisCache` restored in `finally` and proven by a miss;
Rust: `redis.cache.enabled=false` + a `#[preload]` stub) → L1 503 with the message, L2/L3 propagate the
503. Java branch `fix/cache-example-l1-error-propagation` (`7ccd4fa4`; `ProfileCacheTest` 6/6 via
`mvn -pl examples/distributed-cache-example -am`); Rust branch
`fix/cache-interop-l1-and-spec-confirmation` (`e9fedb55` spec + `9b10b459`). **PRs #426 (Java) and mercury #286 (Rust) opened and MERGED by Eric 2026-09-20 01:03Z** — #426 squash
`fd3916a0`, #286 merge `01710589` (after one CI follow-up there, `74e5df21`: the Rust snapshot test packages the
whole `docs/test-reports` tree, so a new report needs its `files.list` line); branches deleted after the
MERGED-state check.

**Recorded, not changed** (report *Findings*): (2) failure latency by client library — Lettuce buffers
to its command timeout (~5 s), redis-rs fails fast then retries ~4.5 s; (3) an in-function RPC timeout
is 500 here (`FutureInbox` completes exceptionally with `TimeoutException("Timeout for 5000 ms")`, the
worker renders 500) and 408 on Rust (`Result`) — both errors, neither a miss; (4) **after a >30 s
outage the Java pod's shared Lettuce connection recovers on its 30 s-capped exponential backoff**
(attempts at +0/+9/+17/+34 s, `Reconnected` at +64 s = 10.5 s after Redis returned) while `/health` is
already green — `redis.health` probes on a fresh connection, not the cache's — so an operator watching
health alone calls the pod healthy before it serves a cache call; the window closes by itself and every
request in it fails as an error. **Follow-up ruling for Eric:** reset the shared connection on
`RedisCommandTimeoutException`, lower the reconnect cap, or document the window. Rust's
`ConnectionManager` reconnected on the first command. (5) The health contract is identical (400
`DOWN` + dependency 503 `{code, text}`).

**Eric's Cargo question** (transitive deps in the Rust example): Cargo has no Maven-style transitive
classpath — a crate names only what it declares; `mercury-platform-core` must stay (the example names
`platform_core::` macros/types), `mercury-event-script` was redundant and is removed on the Rust
branch (the flow engine links transitively through knowledge-graph; the L2 suites prove it). Contrast
with [[minigraph-dev-mode-app-shape]]'s one-dependency rule and its `index.html` classpath-order trap,
which has no Rust analogue (resource roots are prepended/appended explicitly).

**Method notes.** My first outage expectation (503) was wrong — both engines answer 400 `DOWN` by
design; the fix-verification run's 10 s recovery window was too short for Lettuce's backoff — measure
time-to-recover with retries, never a single probe. Discovery ≠ certification: keep the discovery
evidence, certify on a clean rerun after the fix. Local builds: `mvn -pl <module> -am` from the reactor
root (only 4.9.2 is in `~/.m2`, so a module-only build cannot resolve the reactor artifacts).

**The mapping fix (Eric: "go ahead with the mapping fix"; "this may be a deeper issue in platform-core").**
Root cause of interop Finding 3: `EventEnvelope.setException` and `WorkerHandler.getStatusFromException`
mapped the status from the OUTERMOST exception — two slightly different tables, the envelope's with no 408
case at all — while the message came from `util.getRootCause`; so `po.request(..).get()`'s
`ExecutionException(TimeoutException)` replied 500 "Timeout for 5000 ms", and a
`CompletionException(AppException 404)` lost its 404. Fix (branch `fix/exception-status-from-cause-chain`,
`cd23c5eb`, PR-open is Eric's): ONE rule, `Utility.getStatusFromException` — walk the cause chain, the first
`AppException` (its status) / `TimeoutException` (408) / `IllegalArgumentException` (400) wins, else 500 —
used by both mappers and by `EventStreamWriter.fail`; tests at three levels (`UtilityTests`,
`EventEnvelopeTest`, `PostOfficeTest` e2e: RPC timeout via `future.get()` → 408, joined future → 404, Mono
error path → 403, checked exception → 500); full platform-core suite 503/0; the documented table in
`event-envelope-reference.md` rewritten. Live re-probe on the rebuilt example with Redis stopped: Java
Layer 1 GET/POST/DELETE answer **408 `Timeout for 5000 ms`** (Rust already did); Layers 2/3 stay 500 —
Lettuce's own `RedisCommandTimeoutException` carries no status. Report Finding 3 amended in both twins
(Rust: branch `docs/cache-interop-finding-3-amendment`, `1b21f86e`, + INCREMENTS addendum). Eric's
convention point — `AppException(408, …)` instead of the JDK `TimeoutException` in the four inboxes — left
as a separate optional decision, not taken (javadoc ×10 and three `PostOfficeTest` assertions name
`TimeoutException`; field `onFailure` handlers may too). Method notes: a zsh `local path=` inside a probe
function clobbered PATH and voided one re-probe run (personal memory); `mvn -pl` resolves the module path
against the cwd, so a full-suite run must start from the repo root.

## Doc Gaps

- (none — the cache guide states the interop contract; the report is its evidence)

## Memory References

- ot-distributed-cache — edited: the certification proof is RUN (report, 112/112); the L1 finding and
  fix added to the close record
- l1-caller-checks-reply-status — created (working)
- exception-status-from-cause-chain — created (working)
- event-script-over-code — consulted: the engines check task status for L2/L3, only imperative L1 code
  had the gap — the principle observed in the wild
- cache-separate-from-soa — consulted: the action vocabulary and opaque-bytes value contract the drive
  asserted across engines
- redis-connection-foundation — consulted: `redis.health` probes on its own connection, which is why
  health went green before the cache's Lettuce connection recovered
- minigraph-dev-mode-app-shape — consulted: the Java one-dependency rule and the `index.html` trap,
  contrasted with Cargo for Eric's question
- conv-ports-adopt-java-release-number — consulted: Rust `/info` 4.12.7 vs Java 4.12.12 recorded in the
  report as lag awaiting catch-up, not divergence
- eric-code-changes-via-pr — applied: fix + report on a branch, PR-open is Eric's
- eric-release-rhythm — applied: branch pushed, PR-open and merge stay Eric's gates
- conv-declare-consulted-references — applied
