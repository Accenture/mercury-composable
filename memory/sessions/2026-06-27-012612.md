# Session (2026-06-27T01:26:12.000Z)

**Agent:** Claude Code
**Lightweight:** committed the agent-memory 4.21.0 upgrade (commit `17909d8b`).

Eric had upgraded agent-memory 4.20.3 → 4.21.0 at the start of the session; staged + committed the tracked
upgrade files: `.agent/version.md`, `.gitignore` (ignore `.agents/`), and the `sync-adapters` builtin skill
(SKILL.md + 3 scripts) now also emitting the Google Antigravity `.agents/skills/` adapter. Generated adapters
stay gitignored.

## Memory References
(none — infra upgrade commit)
