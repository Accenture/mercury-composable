# Session (2026-07-09T00:37:56.000Z)

**Agent:** Claude Code
**Lightweight:** docs-only fix on `main` at v4.7.0.

Refreshed `extensions/sync-over-async/README.md`, stale since the commit `c8824519` extraction:
replaced the "prototype / Phase 3 remaining" status note with the shipped opt-in status
(`sync.over.async.enabled`, guide + demo pointers), added a "What ships in this module" section
(return-route engine + the `sync.prepare` / `sync.await` / `soa.reply` tasks, reference wiring in
test resources), and rewrote the Kafka-legs section — `KafkaRequestPublisher` /
`KafkaResponseConsumer` / `SyncOverAsyncKafkaIntegrationTest` no longer live in this module; the
legs are minimalist-kafka's `simple.kafka.notification` + Kafka Flow Adapter, proven end-to-end by
`RestFlowMvpTest`. Threading/virtual-thread-pinning and Building sections kept verbatim (still
accurate). Commit left to Eric (deliberate, human-initiated).

## Memory References
(none — lite log)
