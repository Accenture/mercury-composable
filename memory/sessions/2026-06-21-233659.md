# Session (2026-06-21T23:36:59.000Z)

**Agent:** Claude Code
**User:** Continue the documentation rewrite (DSL grammars done) — spine opener + retire CHAPTER-11

## What We Did

Resumed the rewrite after a Mac restart (the throwaway `/tmp/mkdocs-venv` was wiped; recreated it
with `mkdocs` + `mkdocs-redirects`). Completed two of the three remaining Blueprint items
(committed `f4038b2c`).

- **Refined the spine opener** (`docs/index.md`): reworked the home page from a functional list into
  the **paradigm story** — the layered ascent (event-driven → composable → semantic), a
  "Knowledge Graph as application" headline, and a new **"Building with an AI agent"** section
  linking the three DSL agent guides + `llms.txt`. Pointed the semantic layer at the new Part IV
  (not CHAPTER-11) and added frontmatter.
- **Retired CHAPTER-11** (the outdated glossy summary, superseded by `docs/guides/knowledge-graph/`):
  removed the file; added the **`mkdocs-redirects`** plugin with `guides/CHAPTER-11.md →
  guides/knowledge-graph/index.md` so the baked webapp/help links (`/guides/CHAPTER-11/`) keep
  working; repointed in-docs references (CHAPTER-10, APPENDIX-I, `llms.txt`); dropped it from nav;
  added `mkdocs-redirects` to the CI install step.
- **Verified** `mkdocs build --strict` exit 0; the redirect stub at `/guides/CHAPTER-11/` points to
  `../knowledge-graph/`. (The redirects plugin prints a benign MkDocs-2.0 forward-compat warning;
  the build still passes strict.)

## Decisions Made

- Spine opener doubles as the human paradigm story and the AI on-ramp (links the agent guides +
  llms.txt). CHAPTER-11 retired via redirect rather than left as a stale duplicate.
- `mkdocs-redirects` is now a docs build dependency (local + CI) — the mechanism the lower-layer
  migration will reuse for every CHAPTER-N → semantic-slug move.

## Context for Next Session

- **Last rewrite piece: migrate + refresh the lower-layer chapters into Parts I–III, V–VI.** This is
  the big one — reorganize CHAPTER-1/2/3/5/6/7/8/9 + ARCHITECTURE/METHODOLOGY/COMPOSABLE-DESIGN +
  the reference docs into the layered Part structure, with semantic slugs + `mkdocs-redirects` from
  the old `/guides/CHAPTER-N/` URLs (CHAPTER-4 is referenced by the webapp/help — preserve it), and
  fix cross-links. Suggest doing it Part-by-Part (batch + ping). APPENDIX-I is also slated for
  retirement (superseded by CONFIGURATION-REFERENCE).
- Branch `feature/docs-ai-human-rewrite` (push). Gates: the three `scripts/check-*-grammar.py` +
  `mkdocs build --strict` (local venv needs `mkdocs` + `mkdocs-redirects`). Consider a PR for review.
- Note: the build venv is ephemeral in `/tmp` (wiped on restart); the user is adding a persistent
  venv-location recommendation via agent-memory.

## Memory References

- Referenced/updated: `bp-docs-ai-human-rewrite` (progress (7) — spine + CH-11 done),
  `docs-rewrite-architecture`, `docs-dir-layout`, `vision-mercury-composable`
