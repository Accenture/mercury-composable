# Session (2026-06-27T18:23:36.000Z)

**Agent:** Claude Code

Built the minimalist-kafka **worked example** and reorganized the standalone dev servers, to make the
producer/consumer pattern easy for a developer to adopt and run. (Next planned step, per Eric: a
sync-over-async worked example.) Committed to `feature/sync-over-async` (PR #124), since it is part of the
minimalist-kafka + sync-over-async features.

## Changed
- **`helpers/` (new top-level folder for no-Docker standalone dev servers).**
  - Moved `kafka-standalone` from `connectors/adapters/kafka/` → `helpers/kafka-standalone`
    (history-preserving rename); updated root `pom.xml`, `docs/guides/service-mesh.md`,
    `memory/instructions.md`.
  - Added `redis-standalone`: one embedded Redis server (`embedded-redis` bundles a real `redis-server`
    binary), `EntryPoint`/`AutoStart` pattern like kafka-standalone, port via `redis.port` (default 6379).
- **Transient Redis storage pinned to `/tmp/soa-redis`** (cloud-native `/tmp` pattern, like
  kafka-standalone's working files). Applied to both `EmbeddedRedis` (standalone) and the sync-over-async
  `RedisTestBase` (tests disable persistence: `save ""` + `appendonly no` for a clean, isolated slate).
  Uses the `RedisServer.newRedisServer().port(p).setting("dir ...").build()` builder API.

## Added — `examples/kafka-demo` (worked example)
- Java app (pure minimalist-kafka, no REST): `kafka-flow-adapter.yaml` binds `demo.inbound` →
  `kafka-demo-flow` → `demo.processor` (self-contained function in a `tasks` package per
  `code-conventions-home`) → `simple.kafka.notification` → `demo.outbound`.
- Node helpers (kafkajs): `create-topics.js` (10 partitions each), `listen-outbound.js` (program-1),
  `publish-inbound.js` (program-2, console input), shared `config.js`. `node_modules` gitignored.
- README with the five-terminal run sequence.

## Validation (live, not just build)
- kafka-demo Java app started against a live `kafka-standalone`: flow compiled, adapter bound.
- Full round-trip with real kafkajs: published to `demo.inbound` → received processed JSON on
  `demo.outbound`, **cid preserved**, `traceId` continuous across the Kafka hop.
- `redis-standalone` jar: created `/tmp/soa-redis`, started, replied `+PONG`.
- sync-over-async suite: 32 tests green with the new Redis builder config; all Java modules build SUCCESS.

## Memory References
- Referenced: `event-script-over-code`, `code-conventions-home`, `kafka-soa-docs`,
  `functions-decoupled-routes`
- Created: `helpers-and-worked-examples`
