#
# KafkaFlowAutoStartTest fixture: the opt-in key is present but BLANK - what the ${ENV_VAR:} idiom leaves
# when the variable is unset. Blank means off, exactly like schema.registry.url: the adapter shares the
# producer's codec.
#
schema.registry.consumer.properties=
