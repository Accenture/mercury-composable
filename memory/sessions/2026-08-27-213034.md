# Session (2026-08-27T21:30:34.000Z)

**Agent:** Claude Code

**Spring maintenance round: Boot 4.1.1 + Reactor BOM 2025.0.7 upgrades, then the
SPRING BOOT 3 LANE RETIREMENT (Eric's directive).** All local on main, PR pending.

**1. Spring Boot 4.1.0 → 4.1.1 (Eric's edits, reviewed CLEAN).** The bump is the
`spring-boot-starter-parent` **parent version** in all 31 Boot-4-lane poms — which also
explains the netty-override mechanism repo-wide: every module inherits Boot's
dependencyManagement, so the per-module `netty.version` property override rides the
parent. The Boot-3 lane (rest-spring-3 + example, parent 3.5.16) was correctly untouched;
zero 4.1.0 leftovers. **Stash-diff of resolved dependencies** (capture before → pop →
capture after): platform-core moves only jspecify 1.0.1 + Jackson 3.1.5; rest-spring-4
moves the Boot family 4.1.1, Spring Framework 7.0.9, Micrometer 1.17.1 — all patch-level.
**Netty pin HELD**: 25 artifacts still 4.2.17.Final in both lanes (the field Snyk fix is
not regressed). Gates: platform-core 426/426, rest-spring-4 11/11.

**2. Reactor BOM 2025.0.6 → 2025.0.7 (Eric's ask, I swept).** 29 poms declare
`io.projectreactor:reactor-bom` (import scope); count-verified 29 files == 29
occurrences before sed; uniform diff, zero leftovers. Resolution diff: exactly
reactor-core 3.8.7 + reactor-netty core/http 1.3.7 in both lanes. Gates: platform-core
426/426, rest-spring-3 11/11, rest-spring-4 11/11 (the Boot-3 lane took ONLY the reactor
half — tested green hours before its retirement, below).

**3. Spring Boot 3 lane RETIRED (Eric: "the spring community is no longer issuing
security patches for spring boot 3... remove rest-spring-3 and rest-spring-3-example";
sharpened driver supplied at release-cut time: field Snyk REJECTS Spring Boot 3
dependencies and requires Spring Framework ≥ 7 — the deployment pipeline is BLOCKED
without this, making v4.11.12 a field-unblocking release).**
Blast radius mapped by repo-wide grep BEFORE deleting: no third module depends on the
artifact (the pair is self-contained); mentions split into build (root pom ×2 module
lines), docs (8 guide pages), strays (6 application.properties comments, 2 READMEs,
1 pom comment, system/AGENTS.md module table), memory (instructions.md + the stack
fact), and immutable history (old session logs, past CHANGELOG entries — untouched).
Executed:
- Root pom modules removed; `git rm -rf` both directories (force needed only because
  the doomed poms carried the uncommitted reactor bump).
- **Parity verified BEFORE re-pointing docs** at the Boot-4 twins: rest-spring-4-example
  has WsEchoDemo, the kafka-connector dependency, identical ports (8083/8086) and
  cloud.connector config — so service-mesh.md's 18 walkthrough mentions and
  spring-boot.md's example references swap 1:1.
- Docs: spring-boot.md rewritten Boot-4-only (front matter, at-a-glance, options list,
  library section — the Boot-3 WebFlux claim dropped, rest-spring-4 is the
  webmvc/Tomcat stack; retirement admonition added with the migration story);
  service-mesh.md, annotations-reference.md (incl. the combined-token line handled
  before the generic swap to avoid `rest-spring-4/rest-spring-4` artifacts),
  configuration-reference.md, api-overview.md, build-test-deploy.md (Dockerfile
  sample), architecture.md, actuators-and-http-client.md, llms.txt, system/AGENTS.md,
  both READMEs, all six properties comments.
- **ADR-0017 Proposed** ("Spring Boot integration targets Boot 4 only") — rationale:
  an EOL web lane is permanently unpatchable and fails the field's scanner gates;
  freezing rest-spring-3 rejected; migration = dependency swap + the app's own Boot 3→4
  upgrade; Spring stays optional-never-required (vision non-goal intact).
- **CHANGELOG Unreleased leads with the BREAKING Removed entry** (v4.11.6 precedent),
  plus Changed items 2 (Boot 4.1.1) and 3 (Reactor 2025.0.7) from the upgrade half.
- **Memory supersession:** [[stack-integration-spring]] (core) → tier: superseded,
  superseded-by: stack-integration-spring-boot4; successor fact created (core,
  supersedes link, this log as origin). instructions.md structure tree updated.
- No Rust lock-step: Spring is Java-only surface; no wrapper impact.
- Release-sweep note: the version sweep drops from 34 to 32 poms from the next release.

Gates: mkdocs strict exit 0 (no broken links after the removal); ai-contract-provider
unaffected (no packaged reference names rest-spring-3 — files.list untouched); **full
reactor `mvn clean install` (with tests) BUILD SUCCESS, exit 0, 5:32 min** — the 32-module
reactor builds green without the Boot-3 pair.

**RELEASED same day as v4.11.12 (Java only — Rust stays at v4.11.11; no Rust-ported
surface touched: Spring, netty, and reactor are Java-side dependencies).** Eric supplied
the sharpened driver at cut time (folded into ADR-0017, the CHANGELOG entry, and this
log): **field Snyk rejects Spring Boot 3 dependencies and requires Spring Framework ≥ 7
— the deployment pipeline was BLOCKED**, so on his direction the usual content-PR +
release-PR pair collapsed into ONE release branch. `release/v4.11.12`: 32-pom sweep
(72 occurrences, count-verified; the sweep surface dropped 34→32 with the removals),
CHANGELOG stamped `Version 4.11.12, 8/27/2026` leading with the BREAKING Removed entry;
the unpushed release commit was amended with the driver docs → gated `a19d1d8d`.
Gates: full reactor with tests green BOTH pre-sweep (5:32) and on the swept cut (5:21);
ai-contract-provider 19/19 re-run against the amended ADR (its packaged-docs link gate
sees ADR edits); Eric's independent local build green; CI green (Build & Unit Tests
8m31s + verify 20s; the release/* branch also triggers the verify/deploy workflow —
deploy correctly *skipping* on the PR). **MERGED as
[PR #298](https://github.com/Accenture/mercury-composable/pull/298) squash `0467dc21`
== gated `a19d1d8d`** (tree verified, squash title clean); branches deleted both ends;
main evaluates 4.11.12. Tag `v4.11.12` dereference-verified locally AND on the remote
(peels to `0467dc21`), pushed on Eric's explicit gate; **GitHub release PUBLISHED by
Eric** — notes lead with the BREAKING migration story. Field next step: point the
pipeline at v4.11.12 to clear the Snyk Boot-3/Framework-≥7 block.

## Memory References
- Created: thread-spring-round-20260827 (tier: working),
  stack-integration-spring-boot4 (tier: core)
- Superseded: stack-integration-spring → stack-integration-spring-boot4
- Referenced: thread-field-scan-remediation-20260825 (netty pin verified held),
  kafka-mesh-opt-in, conv-telemetry-presentation-parity, eric-release-rhythm,
  vision-mercury-composable, thread-ai-contract-provider
