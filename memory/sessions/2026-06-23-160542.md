# Session (2026-06-23T16:05:42.000Z)

**Agent:** Claude Code
**User:** Peer-review Finding 1 (syntax.md List-of-PoJo) — source-code validation + harmonize fix;
ADR path integrity check; agent-memory 4.16.1 upgrade verification.

## What We Did

**Source-code validation of the `*` whole-body passthrough.** Before applying the harmonize fix,
validated against source that `model.list -> *` in Event Script is a genuine, tested Layer-2 feature —
not a Layer-1-only escape. Evidence: `WildcardConversion.java` (event-script-engine test task,
`TypedLambdaFunction<List<PoJo>, List<PoJo>>` + `@PreLoad(inputPojoClass=PoJo.class)`) driven by
`wildcard-conversion-test.yml` (a proper Event Script flow); `TaskExecutor.java:1115-1119` implements
the mechanic (non-Map value stored under key `"*"`, `unwrapBodyIfWildcard()` passes it as raw event
body); `inputConversionTest()` exercises it end-to-end via HTTP. Verdict: `syntax.md` was correct;
the previous ADR-0003 fix ("Layer-1 only") was the over-statement.

**Harmonize fix applied.** Corrected the rule across four files:
- `docs/arch-decisions/ADR.md` — ADR-0003 Abstract + Rationale rewritten to distinguish **key-by-key
  data mapping** (Map/PoJo only — a List cannot be mapped field-by-field) from the **`*` whole-body
  passthrough** (bypasses key-by-key mapping; with `inputPojoClass` enables `List<PoJo>` in Event
  Script flows; Layer 1 uses the same mechanism for external JSON-list ingestion).
- `docs/guides/api-overview.md` — List-of-PoJo note rewritten to reflect the same distinction.
- `memory/instructions.md` — Core Abstractions bullet updated.
- `memory/continuity.md` — `typed-io-map-or-pojo` invariant updated; `last_used` bumped to 2026-06-23.
Also applied Finding 2 (stale `docs/notes/` refs in memory files) in the same commit.

**ADR path integrity check.** User asked why the repo departs from the agent-memory default
`docs/ADR.md`. Session log from 2026-06-22 shows the path (`docs/arch-decisions/ADR.md`) was Eric's
explicit direction at opt-in time — no deeper technical reason. Cleaned up the now-redundant
"agent-memory default `docs/ADR.md` is not used" parentheticals from `AGENTS.md` and
`memory/continuity.md` (the divergence is established; the notes had become clutter).

**agent-memory 4.16.1 upgrade committed by Eric.** Verified the upgrade:
- 4.16.0: schema.md + DECAY.md ADR path aligned to `docs/arch-decisions/ADR.md` (the new default —
  mercury-composable was already there; no file moves needed).
- 4.16.1: session filename trap documented (`currentDate` is date-only; always run
  `date -u +%Y-%m-%d-%H%M%S`); `memory-lint` gains check 5 (`check_session_filenames`).
- All 21 existing session filenames conform. `memory-lint --root .` → 0 errors, 0 warnings.

All commits pushed to PR #119 for re-review.

## Context for Next Session

- PR #119 updated with: harmonize fix (List-of-PoJo), memory cleanup, agent-memory 4.16.1 upgrade.
  Awaiting peer re-review.
- `site/` (mkdocs build artefact) was untracked; Eric removed it manually — working tree is clean.
- Still open: AI-context-discovery on-ramp (`thread-next-ai-context`); optional Layer-1 foldering
  (`thread-layer-reorg`).

## Memory References

- Corrected: `typed-io-map-or-pojo` (harmonized — key-by-key vs `*` passthrough; ADR-0003 rewritten in place)
- Referenced: `adr-pattern-adopted`, `docs-content-canon`, `vision-mercury-composable`
