{
  "name": "json-path-playground",
  "version": "1.0.0",
  "description": "A lightweight React-based JSON-Path playground for testing and debugging",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:prod": "vite build && npm run copy:dist",
    "copy:dist": "cp -r dist/* ./"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
