# Session (2026-09-04T04:59:06.000Z)

**Agent:** Claude Code

## What happened

Ran the memory smoke test in a fresh-context agent (its own method requires one; the parent
session had spent the day in this code and would have passed from working memory, learning
nothing). **11/12.**

The single ❌ was **not a content gap but a read-set regression**: Open Threads moved one-per-file
into `memory/open-threads/` at agent-memory v4.39.0, and `smoke-test.md` step 1 was never updated
— so no compliant runner could name a live workstream, in this repo or the Rust one. Fixed in
both: read set, the Q5 pointer, and the wiki-ref resolution rule (5 of 11 refs now legitimately
resolve only in `open-threads/`).

### Fixed

- `one-atom-four-roles` is cited by the core fact `event-script-over-code` as its *bound*, but
  resolves nowhere in the memory layer. The agent proposed an `archive/INDEX.md` line; **verified
  first and rejected that** — the term has no `id:` footer anywhere and was never a fact, only a
  concept named in other facts' prose, so an INDEX entry would have fabricated an archive record.
  Made the reference self-resolving in place instead, pointing at
  `docs/guides/documentation-conventions.md` + ADR-0004/0005.
- `instructions.md` Repository Structure omitted five shipped modules — `twin-kafka`,
  `minimalist-kafka`, `ai-contract-provider`, `opentelemetry-forwarder`, `minigraph-state-redis`
  — all named elsewhere *inside the read set*, so "where does minimalist-kafka live?" was
  unanswerable from the map. Completed.
- `vision.md` carried a hard-coded "v4.12.0 as of 2026-08-30" against `latest_release: v4.12.2`.
  It was re-synced at the 2026-09-01 run and drifted again within three days, so the fix was to
  **delete the version and point at `latest_release`** — remove the drift source, not re-sync it.
- Methodology note added: **re-read `continuity.md` from disk.** The auto-imported copy is a
  session-start snapshot; this run's was stale (pre-review `last_review`, and an already-archived
  fact still shown as live). The agent caught this itself and re-read.

### Rejected after verification

The agent reported the thread filename convention as self-inconsistent ("filename = the thread's
fact id" vs files like `thread-thread-polyglot-initiative.md`). Tested it: the rule holds exactly
— `thread-` + id — and the apparent double prefix is only ids that themselves begin with
`thread-`. No change made.

### Raised for Eric

Two heavily-used facts fell to `archive-candidate` in today's review: `eric-release-rhythm`
(uses 29, the *sole* User Preferences fact — if it archives, the preferences question fails
outright) and `conv-telemetry-presentation-parity` (uses 42, the most-used fact in the layer,
defining the cross-engine contract). Both faded only because the recent window was docs/config
work. Promotion to `core` is a permanent decay change, so it is his call, not mine.

**Traced root cause, worth carrying forward:** `2026-09-04-011530.md` declared
`Memory References: (none)` while its Scope section reasoned explicitly from
`conv-telemetry-presentation-parity` to decide the change was Java-only. That under-declaration is
what let the next `refresh-metadata` demote a 42-use fact. Past logs are immutable, so the fix is
forward-looking: **declare a reference when a fact is consulted to make a decision, not only when
it is edited.** That log was mine.

## Memory References

- Referenced: event-script-over-code, conv-telemetry-presentation-parity, eric-release-rhythm
