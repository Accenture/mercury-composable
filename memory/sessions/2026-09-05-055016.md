# Session (2026-09-05T05:50:16.000Z)

**Agent:** Claude Code

The white paper succession: `docs/ai-grammar-methodology.md` now carries **"Intent-Driven
Development and the Architecture of Human-AI Collaboration"**, the synthesis of Eric's
IDD thought-leadership article (Aug 2026, PDF) with "The Mercury Story" — replacing the
latter as this repo's white paper, on Eric's direction. Deck and site pointers follow.

Commit `1238c091` on `docs/idd-white-paper` (4 files: paper, deck, docs/index.md, mkdocs.yml).

## What happened

- Reviewed GitHub Copilot's draft synthesis as chief editor (Eric's role assignment) against
  both sources and the live tree. Verdict: structurally sound; its "Four Forms of Drift"
  section is an original contribution worth keeping. Factual fixes: release attribution
  (fresh-agent fixes = v4.12.2; the discovery gates = v4.12.3 — the draft lumped both into
  v4.12.2), a memory/grammar terminology inversion, byline inconsistency. Verified before
  "fixing": the per-file SHA-256 manifest claim is TRUE (SkillSnapshot computes per-file
  sha256 + snapshot_sha256) — a suspected error that wasn't.
- Restored what the draft dropped from the sources: the three-eras table, specialized
  reviewer personas, "derivable rather than guessable", the AI-maintains-the-explanatory-
  layer duty (Doc Gaps practice), the measured token benchmark (~2,500-token map → ~46k
  verified reference vs ~515k source, Rust engine 2026-09-04), the closing charge, URL'd
  references [1]–[19] with in-text anchors, the deck pointer, the twin-kafka cautionary
  tale, and the Rust-enabled-before-first-line proof point.
- **Positioning (Eric's rulings):** Mercury is an official Accenture open-source project —
  say so plainly; the old "agent-memory is not an official Accenture product / views my own"
  disclaimer is retired; agent-memory is described neutrally (vendor-neutral, Apache-2.0),
  no official claim until its planned promotion lands.
- **File name kept** (`ai-grammar-methodology.md`): the packaged contract's references/index.md
  links to the paper by ABSOLUTE site URL (the PR #317 lesson), and external links circulate —
  a rename would break both for a cosmetic gain. Title/nav/hero updated instead.
- **Deck revised in lock-step** (Eric: "this would also affect the deck presentation"):
  14 → 19 slides in the unchanged design system — retitled; new slides for the three-eras
  thesis, complementary responsibility, four forms of drift, memory-vs-grammar; proof slide
  now attributes the discovery gates to v4.12.3; closing charge slide carries the white-paper
  pointer. All 19 slides visually verified (Browser pane; the pane's data:-snapshot context
  turns the deck's hash writes into navigations — a preview quirk only, the shipped mechanics
  are unchanged from v4.12.3).
- Gates: `scripts/check-doc-canon.py` OK; `mvn test -pl system/ai-contract-provider` green
  (docs/index.md is packaged — the #317 lesson applied); mkdocs build left to the docs CI
  (not on local PATH).
- The Rust repo's white-paper edition is NOT yet updated — Eric directed "this repo";
  the Rust succession awaits his word (precedent: the story round did Rust as a follow-up).

## Memory References

- referenced: conv-telemetry-presentation-parity (Rust-edition lock-step consideration —
  deferred to Eric, docs are engine-portable in spirit but he gates the Rust round),
  eric-release-rhythm (branch prepared; PR-open gated), conv-declare-consulted-references
  (this list follows it), conv-squash-title-prefill-check (will apply at merge),
  compilegraph-mandatory-gate (paper §2/§ Compile-Before-Run claims re-checked against it),
  graphjs-phase-out-direction (confirmed the paper names no deprecated skills)
- created: (none — the succession is repo-visible in the file itself; personal-memory note
  taken for the agent-memory promotion wording rule)
- Superseded: (none)
