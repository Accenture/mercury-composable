# Session (2026-09-12T02:16:49.000Z)

**Agent:** Claude Code

## What happened

**Streaming return route designed (branch `design/streaming-return-route`,
commit `573472a6`; merged via PR #365, squash `705d2117`, title clean).** Eric raised the gap: progressive
rendering (server-side REST automation + client-side async.http.request, the
x-event-stream contract) is point-to-point — it rides the caller's reply lane,
a route on the pod holding the HTTP connection — while sync-over-async is
one-shot. His direction: when progressive events arrive over Kafka, Redis
Pub/Sub should bridge them to the UI-serving pod, which forwards until an
end-of-transmission signal that is itself an event.

Walked him through the current code first (he could not find the pub/sub
calls): all three sit in ReturnRouteCoordinator — connectPubSub/adapter/
subscribe in start(), and the easy-to-miss publish in deliver() line 167,
a plain PUBLISH on the ordinary command connection (only SUBSCRIBE needs the
dedicated pub/sub connection). The tasks contain zero Redis code by design.

Then wrote `draft-design-specs/streaming-return-route.md` (DRAFT): generalize
the return route from one response key to a per-cid segment stream —

- segments stored FIRST in a Redis Stream `stream:{cid}` (XADD, MAXLEN cap,
  TTL), preserving the module's invariant that Pub/Sub is a wake-up signal
  only, never the payload carrier (a payload dropped mid-stream has no
  final-read recovery, unlike the one-shot case);
- the existing per-pod channel and `request:{cid}` route registry are reused
  as-is — the route registry IS the pod-discovery mechanism, which is why this
  needs no mesh and Event-over-HTTP cannot substitute (local routes only);
- eof/exception are stream ENTRIES (Eric's "the end signal is also an event"),
  so the terminal can never outrun its data;
- the UI pod drains (XREAD from last-delivered id, idempotent) and forwards
  via EventStreamWriter into the shipped stream:true edge — the last mile
  needs nothing new;
- PendingStreams mirrors PendingRequests (bounded, atomic reservation) plus a
  last-delivered id per cid;
- contracts: partition the progressive topic by cid for ordering; leading Q3
  option is producer seq as the explicit stream entry id (dedupe + ordering
  enforcement in one mechanism under Kafka at-least-once redelivery);
- Q1–Q6 open (Event Script composition of stream:true with http.flow.adapter
  is Q2; trim-policy fail-in-band is Q6), E1–E4 staged (primitives →
  single-JVM e2e → cross-pod dry-run with chaos checks → LLM/graph-run token
  stream = the bp-agent-orchestration Q8 payoff).

Opened thread `ot-streaming-return-route`. Next: Eric ratifies the direction
(Q1 → D1), then E1. Java-only (sync-over-async scope), no engine change
anticipated.

## Memory References

- bp-agent-orchestration (consulted - Q8's second half, graph-run streaming,
  is the blueprint anchor and E4)
- kafka-mesh-opt-in (consulted - the design's discovery mechanism is the
  return-route registry precisely so the mesh stays opt-in)
- event-api-local-routes-only (consulted - why Event-over-HTTP cannot bridge
  the gap: reply lanes are local routes, and per-pod addressing is the mesh
  problem)
- soa-transport-neutral-cid (consulted - the streaming route inherits the
  self-contained cid contract and stays transport-neutral)
- eric-release-rhythm (consulted - spec branch prepared; PR creation and
  merge are Eric's gates)
- conv-squash-title-prefill-check (consulted - PR title handed off on its own
  line in a code block)
- ot-streaming-return-route (created, tier: working)
