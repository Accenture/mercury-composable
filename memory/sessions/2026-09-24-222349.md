# Session (2026-09-24T22:23:49.000Z)

**Agent:** Claude Code

## Summary

**A field installation's downstream branch, reviewed and reconstructed here as `feat/kafka-consumer-registry-identity`
`13eddc76` (Eric: review, reconstruct for this repo, then a new release carries it to the field).** The field found that
`KafkaFlowAutoStart` builds ONE `SchemaCodec` per JVM and hands the same instance to `simple.kafka.notification` (produce)
and the Kafka flow adapter (consume), so both directions carry one Schema Registry identity; their Confluent installation
grants CSFLE key (KEK) access per direction — separate produce and consume identity pools — so no single identity could
decrypt everything the service consumes and there was no configuration remedy. Their fix: an opt-in key
`schema.registry.consumer.properties` that builds a second codec for the adapter through the existing prefix seam
`SchemaCodec.fromConfig(config, url, prefix)` under `schema.registry.consumer` — the seam twin-kafka already runs on for a
second cluster's registry. Review against the current tree: the diagnosis matches exactly (`start()` → one codec → the
adapter), the seam is intact, so the fix applies as designed. Reconstructed faithfully (same key, prefix, helper name and
shape, presence = opt-in, package-private helper) with three additions: blank counts as unset (the `${ENV_VAR:}` idiom, as
for `schema.registry.url`); an info/warn line stating that the adapter decodes with its own identity, or that the opt-in
alone cannot switch schema features on; and the codec-ready log label `serdeOverrides=<n>` replacing `csfle=<boolean>`,
which read `csfle=true` for a bare identity override. Two consequences of the seam the field's write-up did not state, now
in the docs and the javadoc: a `schema.registry.consumer.serde.*` override reaches the deserializer's configuration and the
DEK-registry client CSFLE builds from it (where key access is decided) but NOT the codec's own schema-by-id lookups, which
keep the template's identity — a second template is the answer when the consume identity must cover those too; and the
consumer codec reads only its own prefix, so a `schema.registry.serde.*` KMS credential is repeated under it. Applied
[[clean-knowledge-design-over-engine-coverage]] (reuse the seam, add no mechanism; the consequences are declared, not
engineered around) and [[kafka-mesh-opt-in]]'s opt-in posture; code reaches main through a PR ([[eric-code-changes-via-pr]]).
The field's "fails open, silently" wording does not describe this engine: with Confluent's default `onFailure=ERROR` a decode
under the wrong identity throws, and a decode failure here is a poison message, dead-lettered — only a rule set with
`onFailure: NONE` passes ciphertext through — so the guide says what happens here.

## Verification

`KafkaFlowAutoStartTest` 5/5 — plain `ConfigReader` fixtures, so nothing leaks into `KafkaFlowAdapterTest`'s real `start()`:
unset and blank reuse the producer's codec (`assertSame`); the opt-in alone does not switch schema features on (null codec
for a blank URL); a configured location builds a distinct codec under the consumer prefix (the `ManagedCache` named
`schema.registry.consumer` exists) that decodes what the producer's codec encoded against the same embedded registry. Module
suite **223 / 0 / 0** (218 + 5) on the second full run; the first run failed 2 tests in `KafkaFlowAdapterTest` — the embedded
consumer group's assignment arrived ~35 s late in the shared JVM, the first end-to-end test timed out at 25 s and the next
one consumed its message — a timing interaction on a path that does not touch the codec (the class alone 17/17, then the
full suite clean). `mkdocs build --strict` clean. NOT verified here, because the embedded registry enforces no access
control: an identity-pool grant — the test proves the wiring; the registry's RBAC (the field's production) proves the boundary.

## Merged

Eric opened PR #458 (title clean — the handoff gave it its own line) and merged it at 22:42:04Z as squash `37e0f792`. PR checks
all green on the head `64830ec6`: Build & Unit Tests (Java 21) 9m31s, Starter Templates (Gradle) 1m19s, memory ×2, docs verify
(deploy skipped on a PR). Gate verified — state MERGED, `37e0f792` an ancestor of `origin/main` — before the local branch was
deleted (GitHub had already removed the remote one); Eric's parked stash untouched; main at `37e0f792`, tree clean. Eric's
next question — whether a sample configuration file illustrates the opt-in — is answered in the following session record.

## Sample configuration (Eric's follow-up question, same session)

Eric asked whether a sample configuration file illustrates the opt-in. None did — only the guide snippet and the library
template's comment; the one example that configures a Schema Registry for a real path is the sync-over-async demo (its
JSON-Schema topics against the standalone registry helper), so the sample went there on branch
`docs/kafka-consumer-identity-sample` `50211cb1`: a commented block after the demo's schema section explaining the opt-in
(presence; blank stays off, so `${VAR:}` is a per-environment switch; the shared URL) and both variants — A, the producer's
template plus a `schema.registry.consumer.serde.bearer.auth.identity.pool.id` override; B, a second template — plus the
repeat-the-KMS-credential rule; a new `schema-registry-consumer.properties` next to it, fully commented like the shipped
template, as the second-template form; pointers from the demo README and the guide subsection. Deliberately in the
application, not the library jar: a same-named resource in the jar would collide with an application's own copy by classpath
order ([[minigraph-dev-mode-app-shape]]'s trap, generalized). Behaviour unchanged (every line a comment; the local registry
enforces no identity pools). Verified: `mkdocs build --strict` clean, the demo module's 9 tests green, the credential-class
secret scan of both properties files clean (placeholders only).

## Not done — Eric's decisions

- **twin-kafka** has the identical shape: `SecondaryKafkaAutoStart` shares one codec across its secondary producer and adapter,
  and `secondary.schema.registry.*` has no consumer twin — the same helper with prefix `secondary.schema.registry.consumer`
  would keep the symmetry twin-kafka is built on.
- **The Rust port's** bootstrap shares one codec the same way, has the same seam (`SchemaCodec::for_registry`) and the
  identity-pool header, but no CSFLE (documented as unsupported) — a Rust twin is a config-surface parity question, not a
  decrypt one ([[conv-ports-adopt-java-release-number]]: lag is not divergence).
- **The release** (Eric: the fix propagates to the field as a new release) — [[kafka-consumer-identity-release]] opened;
  CHANGELOG from the tag range at release prep ([[conv-changelog-from-tag-range]]); PR title on its own line in the handoff
  ([[conv-squash-title-prefill-check]]).

## Memory References

- kafka-consumer-registry-identity (created; sample-location addendum)
- minigraph-dev-mode-app-shape (consulted — the classpath-collision trap)
- kafka-consumer-identity-release (created — open thread)
- clean-knowledge-design-over-engine-coverage (consulted)
- kafka-mesh-opt-in (consulted)
- eric-code-changes-via-pr (consulted)
- eric-release-rhythm (consulted)
- conv-ports-adopt-java-release-number (consulted)
- conv-changelog-from-tag-range (consulted)
- conv-squash-title-prefill-check (consulted)
