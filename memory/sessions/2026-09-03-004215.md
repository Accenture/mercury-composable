# Session (2026-09-03T00:42:15.433Z)

**Agent:** Claude Code

## What happened

Eric asked whether v4.12.2's progressive rendering can be described as using the modern
"Streamable HTTP" protocol. Grounded assessment against the tree: **no** — Streamable
HTTP is the MCP-spec named transport (JSON-RPC framing, Mcp-Session-Id, GET
server-initiated stream, Last-Event-ID resumability), and the engines implement none of
those (verified: no Last-Event-ID in platform-core main source; the SSE client parser
ignores `id:`/`retry:` by pinned test). What IS true and verified: the single unified
endpoint with per-request content negotiation (`Accept: text/event-stream` upgrades the
same connection to standards-only SSE; absent → buffered JSON — the backward-compat pin
test), and no legacy two-endpoint SSE pair ever existed. Delivered approved positioning
wording (pattern claims, "Streamable-HTTP-style"); Eric ratified the rule: no protocol
claim until an MCP wrapper/facade feature ships — recorded as
conv-no-streamable-http-claim. The literal-conformance path (a bounded MCP adapter on
the existing shape) is noted toward bp-agent-orchestration.

## Memory References

- bp-agent-orchestration (the future MCP-adapter home the rule points at)
- Created: conv-no-streamable-http-claim (tier: working)
