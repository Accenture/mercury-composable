# Session (2026-07-31T00:10:57.000Z)

**Agent:** Claude Code

## What happened

**Second-level routing for the kafka-flow-adapter: IMPLEMENTED, adversarially reviewed,
hardened, documented — code-complete on branch `feature/kafka-2nd-level-routing`
(commits `6bc6d310` feature + `b73f6ad0` review hardening + docs), awaiting Eric's
push/PR gate.** Realizes the design ratified in [[thread-kafka-2nd-level-routing]]
(session 2026-07-30-233623) exactly as spec'd, with review-round refinements folded
back into the spec.

**Implementation shape (all in the SHARED minimalist-kafka classes — twin-kafka's
secondary adapter constructs the same `KafkaFlowAdapter`, so the feature and
`topic-pattern` are automatically identical on both adapters; Eric asked, confirmed
with code evidence, twin-kafka.md updated):**
- NEW `RoutingRuleSet` — compile-once rule engine: `<selector>(<matcher>) -> <target>`
  grammar, three matcher modes (exact / wildcard-on-`*` / explicit `regex:` prefix,
  all full-string matching per the topic-pattern precedent, wildcards DOTALL),
  case-insensitive header-NAME lookup, MultiLevelMap composite body paths (Map bodies
  only; non-String value = non-match), first-match-wins, mandatory `default`.
- `KafkaFlowAdapter`: `flow` XOR `flows` (startup-validated), `serializer: 'json'`
  (rejected unless 'json'; exclusive with schema.enabled), per-binding `ttl`
  (long-math duration parse — no int wrap), all rule targets validated fail-fast
  (flow:// in compiled flows; task:// in the platform registry;
  `task://event.script.manager` explicitly rejected — flows dispatch via flow:// only).
- `KafkaFlowConsumer`: per-record target selection after schema/serializer decode;
  `toTaskRequest` mirrors TaskExecutor's bare-route contract (verbatim header copy,
  whole payload body, business cid on the engine-managed `my_cid` TAG — never a
  header; the RPC inbox owns the envelope cid field); per-target ttl (flow's own vs
  binding taskTtl default 30s, null-safe); `bestEffortJson` (object→Map, array→List,
  anything else keeps raw byte[] — the platform-core sniffing idiom).

**Adversarial review round (workflow: 4 lenses → per-finding refutation agents; 22
agents): 13 confirmed findings (5 refuted) deduplicating to 7 distinct fixes, all
applied + regression-pinned:**
1. `task://event.script.manager` passed validation then NPE'd invokeFlow's ttl
   inference → restart-resistant poll-thread crash loop. Fixed both layers: startup
   rejection + null-safe `resolveTtl`.
2. `$`-prefixed body keys leaked into MultiLevelMap's JsonPath branch, whose parser
   THROWS at record time (live-reproduced InvalidPathException) despite the spec's
   never-throws clause. Fixed both layers: compile-time rejection (composite paths
   only) + per-rule runtime catch → non-match.
3. Synchronous RuntimeException from po.request (task route released after startup →
   "Route not found") escaped deliver() to pollLoop's fatal catch. Fixed: joins the
   retry/DLQ envelope.
4. Wildcard `.*` didn't match newlines → Pattern.DOTALL.
5. `parseTaskTtl` int wrap ('50000d' → silently wrong positive) → local long-math
   duration parse (the graph-suspend ttl overflow-guard precedent).
6. schema.enabled + flows body routing was untested → new e2e binding
   (routed-schema-topic) + test.
7. Suppression-rationale comment idiom on the new test class.
   **Spec-side resolution (flag for Eric):** the "scalar delivered as parsed" clause
   was resolved in the IMPLEMENTATION's favor — a top-level JSON scalar keeps the raw
   byte[] like a parse failure (Kafka-native bytes = "unchanged"; consistent with his
   raw-bytes ruling); the spec/docs now say so explicitly.

**Evidence:** minimalist-kafka 156/156 (was 101 pre-feature; +25 RoutingRuleSetTest,
+8 KafkaFlowRoutingConsumerTest, +47-total config tests, +6 e2e incl. header/wildcard/
body-rule-to-task/schema-body-rule/byte[]-fall-through/reserved-route-rejection) with
the JaCoCo gate passing; FULL reactor `mvn clean install` 29 modules green in 4:56
(Twin Kafka suite ran against the newly installed jar). Docs: minimalist-kafka.md
`#routing` + `#routing-payload` sections, field-table rows, configuration-reference
per-binding paragraph, twin-kafka shared-surface note, llms.txt (also dropped its
stale Protobuf claim), CHANGELOG Unreleased entry; mkdocs build clean.

**No Rust lock-step constraint** — minimalist-kafka has no Rust port; the grammar
becomes part of that future port's contract.

**Also (same session, commit `538afd87`): Eric's symmetry rulings implemented.**
(1) **Outbound JSON symmetry** — `simple.kafka.notification` (and
`secondary.kafka.notification`, which inherits: the twin subclass overrides accessors
only) now accepts a Map or List body and auto-serializes it to JSON bytes via
SimpleMapper; **non-schema-registry topics only** (Eric's mid-round scoping — with a
`subject` header the body contract stays a byte[] JSON document, pinned by test);
null stays null (tombstone); other types rejected loudly. Input type changed
`TypedLambdaFunction<byte[],…>` → `<Object,…>`. (2) **List-body routing** —
`input.body` rules use dot-bracket composite paths against Map AND List bodies, incl.
top-level JSON arrays (`input.body[0].type(order) -> flow://…`). Design nicety: body
lookups now run under a synthetic `body` root, which simultaneously (a) makes a
top-level List addressable and (b) makes MultiLevelMap's throwing `$`-JsonPath
dispatch structurally unreachable — the review round's compile-time `$`-key rejection
was RETIRED (a `$`-prefixed JSON key is a legitimate literal segment now); the
runtime non-match safety net stays. Evidence: minimalist-kafka 163/163 (new
SimpleKafkaNotificationTest; list-body + outbound-Map e2e — the full symmetry circle:
Map in the producer app → JSON bytes on the wire → Map in the rule-selected consumer
flow); full reactor green again (4:55). One test-authoring note: the customized Gson
pretty-prints, so JSON-bytes assertions must be semantic round-trips, not
byte-exact.

**Also (merge closeout): MERGED 2026-07-30 as PR #246, squash `268e5ff6`** — CI green
(Build & Unit Tests 6m53s, docs verify, memory checks), merge content verified on main
(CHANGELOG Unreleased entry + routing surface present), branches cleaned up. Eric's
session-memory commit `2daaddad` rode the squash by his call ("all good"). The whole
arc — ideation review → ratified spec → grounded implementation → adversarial review →
symmetry rulings → merge — completed in two days. No release scheduled; rides the next
version.

**Also (post-review, Eric's direction): continuity.md CONDENSED** — 1153 → 619 lines,
resolving the review's [continuity-bloat] advisory. Method: every fact kept its id,
checkbox state, metadata footer (verbatim — the review owns those), outcomes, durable
lessons, ruling quotes, and PR/tag anchors; shipped-item narrative compressed to
"Full detail: origin log" pointers (session logs = the immutable ledger). 56 facts
before and after; memory-lint 0 errors / 0 warnings post-condense. Heaviest
compressions: thread-redis-kafka-rpc (85→~25 lines), thread-event-envelope-interop,
thread-graph-suspend-resume, thread-traceparent-header-config,
thread-annotation-macro-consistency, thread-kafka-2nd-level-routing, the 4.8.x/4.10.x
release facts, thread-elastic-queue-bdb-to-file. A header note records the condensing
date and convention.

## Memory Review (2026-07-31, through 2026-07-31-001057)

- Reactivated:   0
- Superseded:    0
- Archived:      1 fact → memory/archive/2026-Q3.md (log-context-on-by-default — faded,
  sslu 21 > archive_window 20; shipped in v4.10.0, docs carry the surface)
- Swept threads: 2 completed → 2026-Q3.md (thread-release-4-10-0, thread-release-4-9-1)
- Archive-verify: pass (memory-lint 0 errors; no archived id referenced within
  archive_window; no id in both continuity and archive)
- Tier changes:  14 via refresh-metadata (6 working→active, 7 active→archive-candidate,
  1 archive-candidate→active) + 4 uses-only bumps
- Invariants:    not due (12 of verify_invariants_every 40 sessions since 2026-07-27)
- Contradictions/drift: none surfaced
- Advisory left open: [continuity-bloat] 1153 lines > continuity_max_lines 1000 — the
  excess is active/dense shipped-decision facts, nothing archivable yet; flagged to Eric
  (condense shipped decisions or raise the limit in decay-policy.md)

## Memory References

- referenced: thread-kafka-2nd-level-routing (implementation delivered),
  thread-redis-kafka-rpc (adapter lineage + test conventions),
  conv-serialization-gotchas (Gson numeric semantics in serializer docs),
  release-4-8-0-shipped (twin-kafka architecture: secondary adapter reuse),
  kafka-mesh-opt-in (context), graph-suspend-resume-design (ttl overflow-guard
  precedent applied to parseTaskTtl)
- updated: thread-kafka-2nd-level-routing → implementation code-complete on branch,
  review round done, awaiting Eric's push/PR gate
