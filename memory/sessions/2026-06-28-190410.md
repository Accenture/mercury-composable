# Session (2026-06-28T19:04:10.000Z)

**Agent:** Gemini CLI

## Summary
Created the `schema-registry-demo` application in `examples/schema-registry-demo` to provide a worked example of how to interact with the minimalist Schema Registry mock. 

- Formatted it as a standard Mercury Spring Boot 3 `MainApplication`.
- Built the `SchemaRegistryClient` abstraction natively using `AsyncHttpRequest` (via `PostOffice`) to gracefully interact with the REST mock APIs for Avro and JSON Schemas.
- Established `DemoRunner.java` to showcase the registration and retrieval logic programmatically.
- Embedded a quick bash script `run-demo.sh` to allow users to play with cURL examples natively and observe JSON responses.
- Documented everything clearly in a `README.md` guiding users on mock server start-up, cURL usage, and running the `DemoRunner` module locally.
- Injected `schema-registry-demo` into the main repo `pom.xml` Reactor build loop to verify successful linkage.

## Memory References
- Referenced: standalone-schema-registry-mock
