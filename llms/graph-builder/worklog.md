# Graph Builder — Work Log

Append-only record of work performed on the graph-builder doc system and its governance artifacts. Newest entries at the **bottom**. Each entry: date, what changed (files), why, and any open items carried forward. This log is for provenance and handoff — it is not a plan ([improvement-plan.md](./improvement-plan.md)) and not policy ([./adversarial-review-checklist.md](./adversarial-review-checklist.md)).

Convention: do not edit past entries except to mark an open item resolved (with a dated note). Corrections are new entries, not rewrites.

---

### W1 — 2026-06-08 — Anti-sycophancy review checklist created
- **Files:** created `./adversarial-review-checklist.md` (v1.0).
- **What:** adversarial evaluation checklist — Gates A–F, an Output contract, an Update Protocol, and Incident Log seeded with Incident 0001.
- **Why:** a self-review found I had over-appraised earlier doc edits (graded my own advice, treated presence as quality, laundered approval through abstractions). The checklist makes that failure mode catchable.
- **Open:** none at creation.

### W2 — 2026-06-08 — Improvement plan created
- **Files:** created `llms/graph-builder/improvement-plan.md`.
- **What:** prioritized, reviewable plan (P0–P5) with verified current-state table, deletion list, sequence, calibration log, and re-verification commands.
- **Why:** the prior round of doc edits was rolled back by the user; needed a grounded plan that reads without the originating conversation. Diagnosis: the doc set optimizes internal coherence but is untethered from the engine and missing the `/build`+`/test` phases.
- **Open:** none at creation; the plan itself tracks P0–P5.

### W3 — 2026-06-08 — Checklist v1.1: Gate G + Incident 0002
- **Files:** `./adversarial-review-checklist.md` → v1.1.
- **What:** added **Gate G ("Bind the findings")** + Output-contract item 7; logged **Incident 0002**.
- **Why:** while reviewing a proposed `improvement-plan-separate.md`, found it ran Gates A–F thoroughly in self-review, then shipped its full structure with none of its own kill-findings applied. Novel mechanism (a review can be honest yet change nothing), so it earned a gate rather than a tightening.
- **Open:** none.

### W4 — 2026-06-08 — Separate plan folded in and deleted
- **Files:** edited `llms/graph-builder/improvement-plan.md`; deleted `llms/graph-builder/improvement-plan-separate.md`.
- **What:** lifted the additive parts (per-workstream outputs/exit criteria, Decision gates section, structured carried-blocker schema for P3) into the main plan; deleted the separate file to keep one planning authority.
- **Why:** the split created a second planning authority (the problem P2/precedence exists to prevent) on an unestablished multi-owner premise. Three defects were **fixed on merge** rather than imported: P0 now mandates *discovering* undocumented syntax (not only validating); P4's exit criterion asserts drift *detection* (not "a job fails"); outputs/gates now carry file paths.
- **Open:** none.

### W5 — 2026-06-08 — Governance-quality pass on the two process docs
- **Files:** `./adversarial-review-checklist.md` (header + Gate E + Update Protocol step 2); `llms/graph-builder/improvement-plan.md` (header); created `llms/graph-builder/worklog.md` (this file).
- **What (applied):**
  - Checklist: added **Owner / Authority (process-vs-content precedence) / Amendment + version policy**; added a **Proportionality** floor (full gates for ship/policy artifacts; B+C+G for low-stakes); scoped **Gate E** so "no prior position" is a valid pass instead of a manufactured revision; gave the **Update Protocol** a concrete novelty test for adding gates (must exhibit an artifact that passes all current gates yet fails); collapsed the same-day version-churn string.
  - Plan: **Status** → active; added **Owner** and **Lifecycle** (archive on P0–P5 close).
- **Why:** a governance review found the layer had unstated ownership/authority, unenforceable anti-sprawl wording, provenance/version gaps, no proportionality floor, and a stale status. Self-indictments noted: I had added Gate G one turn after writing "resist gate sprawl," and had criticized the separate plan for no owner model while my own plan also lacked one.
- **Open (carried):**
  - **Finding 1 — separation of duties.** The only control is self-review, and Incident 0002 is evidence it can fail; incident-logging also depends on the offender self-reporting. ~~Decision pending.~~ **RESOLVED 2026-06-08 (W6):** independent reviewer required for ship-class artifacts.
  - **Proportionality of the whole layer.** Two governance docs + a log now govern a workflow that has not yet produced a single graph. Recommendation: do not expand governance further until **P0** (dogfood) runs.

### W6 — 2026-06-08 — Separation of duties adopted
- **Files:** `./adversarial-review-checklist.md` → v1.2 (new "Separation of duties" section; version-policy/changelog line corrected to cover deliberate amendments, not just incidents); this log.
- **What:** ship-class artifacts (those that ship externally or govern others — including these governance docs) now require an independent review by a reviewer who neither authored nor recommended them; for an agent, a fresh instance with authoring context withheld. Self-review remains a required pre-filter but is not sufficient. Incident-logging reassigned to the catcher, not the offender.
- **Why:** resolves W5 Finding 1 — self-review was the sole control and Incident 0002 proved it can fail; the logging trigger depended on offender self-report.
- **Consequence (flagged, not yet acted on):** by this rule the checklist and improvement-plan are themselves ship-class, so my self-reviews of them do not meet the bar — they need an independent pass. This raises the governance layer's cost; weigh against the standing proportionality caution before applying it retroactively to the existing docs.
- **Open (carried):** proportionality of the whole layer — unchanged; still recommend running **P0** before further governance.

### W7 — 2026-06-08 — P0 dogfood, first run (core paths)
- **Files:** created `llms/graph-builder/evidence/dogfood-customer-360.md` (claim matrix + run log). Live graph built in engine session `ws-844990-2` (not a repo file).
- **What:** built a runnable core graph (`root → mapper-input → check-id → shape-output|validation-error → end`) one verified mutation at a time, then ran happy + negative paths. Both pass: happy → `{customer_id, status:ok, degraded:false}`; negative (missing person_id) → `{error:validation_error, message}`. P0 "Done when" met for the runnable core.
- **Why:** P0 — verify doc claims against the live engine before correcting/adding syntax. Reality-first, as the plan demands.
- **Findings (detail in the evidence file):** the doc's `!= null` "direct null check" does **not** work — a `graph.math` IF halts silently on any unresolved `{...}` reference; working null detection is `:boolean(null=true)` in a **mapper**, not as a math `MAPPING:` statement. THEN/ELSE alias jumps both work when the condition resolves. Engine stores `types[]` though input is `with type`. Null-source mappings silently skip the target. The `THEN: next` semantics were not cleanly isolated (left open).
- **Open (carried):** remaining P0 scope **not yet probed** — Fetcher/Dictionary/Provider cluster (needs a mock HTTP endpoint), `exception`, `for_each`/`concurrency`, `graph.extension`, `graph.join`, numeric constants / "no `number()`", `model.zero`. These gate full P0 completion before Gate 1.

### W8 — 2026-06-08 — P0 dogfood, round 2 (fetcher / dictionary / exception)
- **Files:** created `llms/graph-builder/evidence/stub-server.mjs` (dependency-free HTTP stub on :8099); expanded `evidence/dogfood-customer-360.md` (claims 15–22). Live graph in `ws-844990-2` grown to 13 nodes / 10 connections.
- **What:** stood up a local stub and probed the data-dictionary cluster + `exception` against real HTTP. Profile fetch succeeds end-to-end (URL templating, `response.X` extraction, result→model→output). Risk fetch forced to 500: with `exception=risk-fallback` the handler runs and the graph completes degraded; the failed fetcher records `{status, error, target}`. Without `exception`, forward traversal stops but the upstream error body is auto-copied to `output.body`.
- **Why:** P0 — execution evidence for the build-critical, previously only-asserted `exception` property and the most complex documented pattern (data dictionaries).
- **Findings (detail in evidence file):** `exception` works as designed (now execution-backed). The island/"dictionary container" node is **not** required for a single fetcher. The design's "fetcher failures abort traversal" is too strong — the upstream error body surfaces to `output.body`.
- **Open (carried):** still pending before full P0 / Gate 1 — `graph.join`, `for_each`/`concurrency`, `graph.extension`, numeric/"no `number()`", `model.zero`. Stub server still running (background task `blc3cti1l`) and graph left intact for further probing.

### W9 — 2026-06-08 — P0 dogfood, round 3 (join + for_each + concurrency)
- **Files:** expanded `evidence/dogfood-customer-360.md` (claims 23–30). Live graph in `ws-844990-2` grown to 19 nodes / 16 connections (fan-out-join with a parallel accounts branch).
- **What:** restructured into a real fan-out-join and probed join semantics + iteration. `graph.join` waits for all backward-linked predecessors and proceeds when all complete; the **deadlock hazard is real** (wiring a never-completing branch as a predecessor `.sink`s the join), confirming the design's D-02 convergence requirement. `for_each` iterates a fetcher per list item (3 IDs → 3 HTTP calls); `concurrency` produces observable parallel/out-of-order iteration; `instantiate` supports array-append seeding.
- **Why:** P0 — validate the customer-360 design's load-bearing fan-out-join topology and the iterative primitives against the live engine.
- **Findings (detail in evidence file):** join + convergence pattern work as designed and the hazard is reproducible. `for_each` output via `output[]=...->model.x[]` produces nested arrays (flatten likely needs `f:listOfMap` — document it). Array-append seeding in `instantiate` is undocumented but useful.
- **Open (carried):** remaining before full P0 / Gate 1 — `graph.extension`/`flow://`, `graph.js`, numeric/"no `number()`", `model.zero`. Stub (`blc3cti1l`) and graph left intact.

### W10 — 2026-06-08 — P0 dogfood, round 4 (js / numeric / model.zero / extension) — P0 COMPLETE
- **Files:** expanded `evidence/dogfood-customer-360.md` (claims 31–39; consolidated P1 implications). Stub torn down.
- **What:** `graph.js` executes JS (`1+1→2`) and returns node-level `next` (traversal continued) — but a malformed expression (`$.model.x.length`) halts silently like graph.math. `int()`/`double()` work; `number(7)` is rejected and drops the whole instance (confirms "no `number()`"). `model.zero` is **undefined** (bug confirmed; `int(0)` is correct). `export`/`import` by name and `session reset` all work. `graph.extension` node + error propagation confirmed, but its target resolves from a **deployed-graph registry**, not the `export`/`import` name store (`extension=customer-360-sub` → "not found" 400) — a successful sub-graph invocation isn't testable via playground export.
- **Why:** finish the remaining P0 primitives for completeness.
- **P0 status:** **complete.** Every targeted claim has an execution verdict; the only partial is `graph.extension` success-path (needs a deployed sub-graph). Consolidated doc-correction list captured in the evidence file's "Immediate doc implications" — feeds P1. Gate 1's artifact (`evidence/dogfood-customer-360.md`) now exists with mismatches recorded.
- **Open (carried):** proportionality of the governance layer (unchanged). Next per plan: **P1** apply the corrections, **P2** coherence — both now backed by execution evidence. Finding 1 (separation-of-duties owner) and Gate-0 freshness (reviewer Finding 1) still pending decisions.

### W11 — 2026-06-08 — Gate 0 added + P0 corrections applied (P1)
- **Files:** `improvement-plan.md` (new mandatory **Gate 0** freshness precondition; state table + P1 marked applied); `minigraph-syntax.md` (corrections); `companion.mjs` (reset alignment).
- **Gate 0 (reviewer Finding 1, High):** added a mandatory pre-workstream gate requiring the "Verified current state" table to be re-verified before any workstream begins; freshness is no longer end-of-doc advice. Ran it before applying corrections (state was still fresh at HEAD `6cdacc1b`).
- **P1 corrections applied to `minigraph-syntax.md`** (each backed by a P0 verdict): removed the false `!= null` "direct null check" and documented silent-halt-on-unresolved-IF + node-level `next` traversal; rewrote Null Checking to use a mapper (not an inline math statement); `model.zero`→`int(0)`; added `session reset` + array-append `instantiate` seeding; added a **Reserved Skill Properties** section (`exception`, `for_each`+`concurrency`, `extension`) with the verified nuances (error→output.body, for_each nesting + `f:listOfMap`, extension deployed-registry distinction); noted the dictionary island container is optional and that null-source mappings skip the target.
- **`companion.mjs`:** recognizes `session reset`; removed the phantom bare `reset` case.
- **Not changed:** `graph-design.md`'s join-hazard / convergence guidance — P0 *confirmed* it (claim 25), so no edit needed.
- **Note (governance):** `minigraph-syntax.md` is ship-class (governs contributors); per W6 it warrants an independent review before it "ships." These edits have had author self-review only — flag for an independent pass.
- **Open (carried):** independent review of the corrected spec (ship-class, W6); separation-of-duties owner; proportionality of the governance layer.

### W12 — 2026-06-08 — Independent review of the corrected spec + dispositions applied
- **Files:** `minigraph-syntax.md` (8 fixes), `evidence/dogfood-customer-360.md` (claims 40–41; applied-status note), this log. Resolves the W11 carried "independent review" open item.
- **What:** spawned a fresh subagent (no conversation context) to satisfy the W6 separation-of-duties rule — a genuinely independent reviewer, not author self-review. It applied the adversarial checklist to `minigraph-syntax.md`.
- **Findings it caught that author self-review missed:**
  - **`with type` is cosmetic for skill nodes** — re-tested live; omitting it stores `types: ["untyped"]` and the node still runs. Author never noticed (always wrote `with type`). Now documented (claim 41).
  - **Untested cure shipped** — `f:defaultValue` was prescribed as the null-skip remedy without being run. **Verified live this turn** (claim 40): present→source, null→fallback. Real.
  - **Overclaims introduced in W11** — `f:listOfMap` written as prescription vs evidence's "likely" (K2); `next` semantics stated definitively while evidence #14 marks them open (K3).
  - **Over-broad "verified" voice** — function table, `model.none`, statement verbs documented as fact without execution backing (K1).
- **Dispositions applied:** verified `f:defaultValue` live; added a verification legend (verified vs asserted); marked `with type` optional/cosmetic for skill nodes + the create-vs-update delimiter; restored the `f:listOfMap` "likely" hedge; marked `next` open; relabeled the function table / `model.none` / statement verbs as asserted-untested. **Overridden** (reviewer deletion-budget #6): did not delete the untested commands — relabeled instead, since they're plausibly real.
- **Meta:** second time independent/external review caught the author overclaiming (cf. Incident 0001). Separation-of-duties (W6) paid for itself on first real use — a self-review would not have found the `with type` bug.
- **Open (carried, now as debt):** execution-test the function table beyond `f:defaultValue`, `model.none`, and the statement verbs; `graph.extension` success path; separation-of-duties owner; governance proportionality.

### W13 — 2026-06-08 — Independent re-check: all findings CLOSED, ship verdict
- **What:** a second independent reviewer (fresh agent — `SendMessage` to resume the first wasn't available in this environment, so a no-context fresh instance was given the prior findings to verify closure; arguably a stronger check) re-reviewed the corrected `minigraph-syntax.md`.
- **Result:** all seven prior findings (K1–K4, F5, F6, `f:defaultValue`) marked **CLOSED** with file:line evidence; no contradictions, dead anchors, or overclaims introduced; no claim newly tagged "(verified)" that the evidence doesn't support. **Ship verdict: SHIP** — safe for other engineers to rely on.
- **Two minor, non-blocking flaws (accepted):** (a) table rows carry no inline asserted tag — they rely on the trailing blanket notes + the global legend, so a row copied out of context loses the qualifier (fragility, not a false claim); (b) the instantiate "(All verified…)" parenthetical sits near untested `map/file/classpath`. Both dispositioned by the reviewer as overridden-minor; left as-is to avoid disproportionate gold-plating.
- **One non-blocking follow-up:** a live `model.none` test would convert F6's last hedge into a verdict — already on the debt ledger.
- **Loop closed:** the W6 separation-of-duties requirement for this ship-class spec is now satisfied (independent review + independent re-check, both by no-context instances). The W11 "independent review" open item is fully discharged.

### W14 — 2026-06-08 — P2 coherence (precedence + glossary + table ownership)
- **Files:** `README.md` (Authority And Precedence + Terminology sections; working docs added to the index), `graph-design.md` (Source-Verified Primitives cross-ref note). Ran Gate 0 first — state fresh at HEAD `6cdacc1b`.
- **Authority/Precedence:** explicit conflict-resolution order — engine source/observed behavior > `minigraph-syntax.md` (syntax + canonical node-type/skill enumeration) > `requirements-gathering.md` (brief schema) > `graph-design.md` (primitive selection) > `workflow.md` (phase order); process docs (checklist/plan/worklog) advisory, not authoritative over content.
- **Terminology glossary:** resolves the `Decision` collision (node-type mapper vs decision *branch*), type vs skill vs primitive, and Provider/Dictionary/Island — incorporating the P0 finding that `with type` is cosmetic for skill nodes.
- **Primitive-table reconciliation:** `minigraph-syntax.md`'s **Node Types** declared the canonical type/skill enumeration; `graph-design.md`'s **Source-Verified Primitives** relabeled as selection guidance that defers to it — both tables now have a single owner, removing the drift risk. P2 "Done when" met.
- **Note (governance):** `README.md` is ship-class (governs contributors); per W6 these edits warrant an independent pass before they "ship." Low-risk coherence prose (mostly reinstating previously-reviewed content) — flagged for an independent review rather than auto-commissioned, to respect proportionality. Decide whether to commission.
- **Open (carried):** independent review of the P2 README/graph-design edits (ship-class, W6); separation-of-duties owner; governance proportionality; verification debt (function table, `model.none`, verbs, extension success path).

### W15 — 2026-06-08 — Independent pass on P2 edits → NO-SHIP (K1) → fixed
- **What:** fresh subagent (no authoring context) reviewed the P2 README/graph-design edits via `git diff`. Verdict: **NO-SHIP until K1 fixed**; everything structural held up.
- **Confirmed sound (kills failed):** all new links/anchors resolve (K6); the precedence order doesn't contradict the other docs; the two primitive tables genuinely agree today, so the "single owner" claim isn't hollow (K3); the Provider definition is accurate (K2).
- **K1 (blocker) — FIXED:** glossary "Node type" said "cosmetic for skill nodes" but used `Root` (a no-skill node where type is load-bearing) as an example — could mislead an author into treating `with type Root` as decorative. Reworded: cosmetic for **skill** nodes, **load-bearing** for `Root`/`End`/no-skill; examples changed to skill-node types.
- **Also applied:** K4 — tier-1 precedence now says to weight each evidence row by its own verdict (partial/untestable rows aren't conclusive); deletion-budget #3 — Island/Entity definition tightened to the `graph.island` `.sink` behavior; #2 — dropped the duplicative "headers/features" from Provider; K5 — `requirements-recalibration-assessment.md` now named as a non-authoritative rationale doc in the precedence section.
- **No change (kills failed):** K2, K3, K6.
- **Meta:** third independent review, third real catch (Incident 0001 self-grading; W12 `with type` bug + untested cure; now W15 `with type`/`Root` glossary mislead). Notably K1 is the *same `with type` subtlety* that bit the spec in W12 — it propagated into the glossary and only an independent eye caught the recurrence. Separation-of-duties continues to earn its place.
- **Open (carried):** re-check that K1/K4 fixes close the findings (optional, NO-SHIP was conditional on K1); separation-of-duties owner; governance proportionality; verification debt.

### W16 — 2026-06-08 — P2 independently re-checked: SHIP → P2 CLOSED
- **What:** fresh subagent re-checked the K1/K4 + budget fixes against the current `git diff`. **Verdict: SHIP** — K1 (blocker) and all four recommended findings **CLOSED** with file:line evidence; the previously-failed kills still hold (links/anchors resolve, precedence consistent, the two primitive tables agree so "single owner" is real).
- **Residual (accepted, non-blocking):** glossary Provider/Dictionary rows paraphrase definitions `minigraph-syntax.md` owns — a drift risk, not a contradiction (e.g. Node Types says Provider "(url, method, headers)" vs glossary "(url, method, request-param placement)"). Reviewer overrode as non-blocking; left as-is per proportionality. Carried as a minor drift note.
- **P2 status: CLOSED** — applied (W14), fixed (W15), independently cleared (W16). Full author→review→fix→re-check loop, all reviews by no-context instances.
- **Open (carried):** separation-of-duties owner; governance proportionality; verification debt; glossary paraphrase-drift (minor).

### W17 — 2026-06-08 — P3 traceability spine (convention, not machinery)
- **Files:** `requirements-gathering.md` (new "## Requirement IDs" section + `requirements` catalog in the brief template + structured `carried_blockers` schema + completion-rule point 6), `graph-design.md` (`requirement_ids` added to `source_brief`), `examples/customer-360-requirements-brief.md` (filled `requirements` catalog INV/OUT/SRC/MAP/FLOW/FAIL/NFR/TEST + structured carried_blockers as CB-01/CB-02). Ran Gate 0 first — state fresh at HEAD `6cdacc1b`.
- **What:** reinstated stable requirement IDs as a lightweight convention so `/design` traces each obligation without re-deriving it, and replaced string-only carried blockers with structured objects (`id`, `question_id`, `blocks`, `mock_used`, `replacement_required_before`, `closure_plan`).
- **Deliberately did NOT** build a checker — the spec note says "do not rebuild a string-matching doc checker," honoring the deletion-list ban (the rolled-back `check-docs.mjs` was the anti-pattern) and proportionality.
- **Partial (honest):** P3 "Done when" includes "worked examples trace bidirectionally." The brief example now carries the catalog and the design *template* has `requirement_ids` + `requirement_traceability`, but the **design-spec example was rolled back/deleted**, so the round-trip can't be *demonstrated* in examples. The convention is fully in both specs; only the worked-example round-trip is incomplete.
- **Note (governance):** `requirements-gathering.md` and `graph-design.md` are ship-class specs; per W6 these edits warrant an independent pass. Flagged, not auto-commissioned (proportionality) — decide whether to commission.
- **Open (carried):** regenerate the customer-360 design-spec example to demonstrate bidirectional traceability (debt); independent review of P3 spec edits (ship-class, W6); separation-of-duties owner; governance proportionality; verification debt.

### W18 — 2026-06-08 — Independent review of P3 → NO-SHIP → fixed
- **What:** fresh subagent reviewed the P3 edits. **Verdict: NO-SHIP** — the structured carried-blockers half was correct (field-for-field schema match, cross-refs hold), but the requirement-ID half had real defects.
- **Findings + dispositions:**
  - **K2 (must) — FIXED:** I had written that `/design`'s gate "fails if a requirement maps to no design element **and has no open question**" — an exemption that **does not exist** in the real design gate (graph-design.md). Reworded to match the actual gate (every requirement maps to ≥1 design element; unavailable deps carried as blockers). A cross-doc contradiction I introduced.
  - **K1 (must) — FIXED:** the round-trip was stated present-tense though no design-spec example demonstrates it. Softened to "should echo" + explicit note that the round-trip isn't yet demonstrated end-to-end.
  - **K5 (must) — FIXED:** the canonical example violated the spec's own "every design-relevant obligation has an ID" — missing the **profile/accounts stop-vs-degrade** distinction (added FAIL-003) and the **latency/concurrency** NFR (added NFR-002), plus a granularity clause so "every" means material obligations grouped sensibly.
  - **K4 (should) — partially fixed:** the example's "Derived from generated artifact" provenance contradicted its own "schema example" disclaimer → added an illustrative-provenance comment. FLOW-001's `assumption` label left as-is (its source_note already scopes the assumption to parallelism) — minor.
  - **K3 — overridden:** gate doesn't name the new fields, but "convention, not machinery" is the deliberate choice; acceptable once K1/K2 fixed.
- **Meta (own it):** this is the **4th** independent review and the **4th** real catch. Worse, the root cause here is specific: I reinstated P3 partly by **copying the rolled-back version's catalog/source_notes without re-verifying** — propagating pre-existing defects. That is exactly the anti-pattern the plan's Deletion list bans ("any re-added fact not re-verified… copying asserted claims back is how the trust boundary stays broken"). I violated my own documented principle. The pattern across W12/W15/W18: my first-pass quality on spec prose is mediocre and the independent reviewer is doing real load-bearing work, not ceremony.
- **Open (carried):** re-check that K1/K2/K5 fixes close the findings; regenerate the design-spec example (debt); separation-of-duties owner; governance proportionality; verification debt.

### W19 — 2026-06-08 — P3 re-check (over-engineering lens) → NO-SHIP/SIMPLIFY → simplified
- **What:** re-check requested with an explicit non-sycophancy mandate (catch over-justification / over-complication / anchoring). Verdict: **NO-SHIP + SIMPLIFY** — and it was right.
- **Confirmed:** K2 cleanly closed (gate wording matches the real gate); K1 honestly softened.
- **Caught (legitimate, acted on):**
  - **Granularity clause was a hedge** — it made "every obligation has an ID" unfalsifiable; I'd added it to dodge K5, not fix it. **Cut**; reworded the claim to "capture the obligations /design must trace; not a completeness checklist."
  - **FAIL-003 fabricated an "accounts" stop-condition** the brief never states (only profile is a stop condition) — appeasement padding. **Narrowed to profile-only.**
  - **`requirement_ids: []` (graph-design)** duplicated `requirement_traceability` keys — a parallel truth surface (Incident 0002 pattern), existing only to be diffed (which the convention disavows). **Cut.**
  - **6-field `carried_blocker` over-modeled** — `blocks`/`closure_plan` duplicate the referenced open question; `replacement_required_before` == `blocks` in every instance. **Reduced to `{question_id, mock_used}`** (the one non-duplicated datum is the mock).
- **My own judgment (did NOT take the most aggressive cut):** the reviewer recommended collapsing the catalog to bare inline `id: statement`. I kept `id`/`statement`/`source_category`/`source_note` — `source_category` is a real trust signal for `/design` (which obligations are soft assumptions vs. facts), and the schema is low-harm. Swinging all the way to inline would be anchoring to the reviewer rather than judging. Stated so the choice is visible, not silent.
- **Loop-termination note:** P3 has now had two NO-SHIP reviews; the second concluded the apparatus was over-built and I'd been accreting reactively. The fix this round was **subtraction**, not more patching — which is the correct response to "over-justified accretion." Further review rounds should not add; if the next re-check isn't clean, the answer is more cutting, not more structure.
- **Open (carried):** optional final confirm that the simplification is coherent; regenerate design-spec example (debt); separation-of-duties owner; governance proportionality; verification debt.

### W20 — 2026-06-08 — P3 final confirm: SHIP → P3 CLOSED
- **What:** tight final coherence confirm (subtract-don't-add mandate). **Verdict: SHIP**, all 5 items CLOSED, no remaining defects: `requirement_ids` removed with no dangling ref (`requirement_traceability` remains); `carried_blocker` is `{question_id, mock_used}` in both template and example and they match; granularity hedge gone (no falsifiable completeness claim); FAIL-003 profile-only (matches stop_conditions); no new contradictions; carried_blocker question_ids reference real open questions.
- **P3 status: CLOSED** — convention is in both phase specs, the brief example uses the lean catalog + lean carried_blockers, and the apparatus was cut back to what earns its place. Full loop: applied (W17) → reviewed NO-SHIP (W18) → fixed → re-reviewed NO-SHIP/over-built (W19) → simplified by subtraction → confirmed SHIP (W20).
- **One item remains as debt, NOT a P3 defect:** the brief→design **round-trip is still not demonstrated** because no filled design-spec example exists (was rolled back). Regenerating it is a separate `/design`-example task, tracked below.
- **Meta close-out:** across W12/W15/W18/W19, independent review caught real defects every time, twice including over-engineering; the durable lesson is that the reviewer is load-bearing for spec prose and the right reflex when patches pile up is to cut, not add.
- **Open (carried):** regenerate the customer-360 design-spec example to demonstrate the round-trip (debt); separation-of-duties owner; governance proportionality; verification debt (function table / `model.none` / statement verbs / `graph.extension` success path).

### W21 — 2026-06-08 — P5 drafted: canonical /build and /test specs
- **Gate 0 caught a real drift:** HEAD had moved `6cdacc1b` → `14f84348` (the user committed P0–P3 as "improvment plan written and perform P0-3. P4 & 5 pending"). Working tree clean, nothing lost; updated the plan's "Assessed against" to the new commit before proceeding. Gate 0 doing exactly its job.
- **Files:** created `build.md` and `test.md` (canonical phase specs); `workflow.md` Phase 3/4 now cite them and drop the "provisional gate" language; README index lists both as canonical phase specs.
- **What:** `/build` lowers a design into verified MiniGraph commands (companion.mjs one-mutation-at-a-time verify-after-mutation as the spine), in dependency order, with explicit checks for the things P0 proved are easy to get wrong — branch/exception target existence, the **join deadlock hazard**, build-time footguns (no `number()`, null-check-in-mapper, unresolved-IF-halts, `update` replaces props) — then a happy-path smoke run + export. `/test` runs each `/requirements` scenario via seed→run→inspect, asserts the output contract with `--expect`, confirms intermediate state transitions (not just final output), mocks unavailable sources with the stub pattern, and routes defects to the owning phase. Both carry YAML output schemas (build_log / test_report).
- **Grounding:** both specs are written from the P0 dogfood (which was effectively a manual /build + /test of customer-360), not assertion — engine-behavior claims point to `evidence/dogfood-customer-360.md` and carry the verification legend's discipline.
- **Not yet done (honest):** P5 "Done when" requires "two independent runs can follow each spec without extra guidance." That follow-along validation hasn't happened, and the specs are ship-class (govern contributors) so they warrant an independent review per W6. Marked ◐ DRAFTED, not closed.
- **Open (carried):** independent review of build.md/test.md (ship-class, W6); a follow-along validation run; regenerate design-spec example (debt); separation-of-duties owner; governance proportionality; verification debt.

### W22 — 2026-06-08 — Independent review of build.md/test.md → fixed
- **Verdicts:** build.md **NO-SHIP** (F1, F2); test.md **SHIP with minor fixes** (F5, F6). All addressed.
- **F1 (build.md) — FIXED:** the footgun list was prefixed "(all verified in P0)" but `update node` has no P0 evidence verdict (it's verified in minigraph-syntax.md, a different source). Reworded to point to the per-item tags in minigraph-syntax.md. **This is the same "(verified) that was never executed" pattern as W12/W18 — caught by review again.**
- **F2 (build.md) — FIXED:** Operating protocol / B7 / Gate implied `companion.mjs` verifies `export`. It does not — `export` isn't a mutating verb (`planVerification` returns null), so the `accepted` response is not proof; P0 confirmed export only via an `import` round-trip. All three spots now require the re-import round-trip and say companion does not verify export.
- **F4 — FIXED:** cut the self-disclaiming `smoke_test.negative_path_touched` field.
- **F6 — FIXED (both files):** weakened the header "engine behavior … is (verified) there" to "claims cite the verdict that backs them; an untagged claim is a convention."
- **F5 (test.md) — FIXED:** `test_handoff` → `test_handoff.inspection_points` (the sub-fields I listed live there; the schema has only scenarios/inspection_points/mock_data).
- **F3 — KEPT, with reason (judgment, not anchoring):** the reviewer flagged the three near-isomorphic enumerations (Operating protocol + Step checklist + Gate) as redundant and recommended collapsing. I kept all three: the Common Phase-Spec Skeleton in workflow.md mandates those exact sections, and every sibling canonical spec (requirements-gathering, graph-design) has all three. Collapsing build.md alone would break that consistency; the overlap is acceptable. Stated rather than silently kept.
- **Reviewer confirmed sound (kills failed):** join-deadlock hazard, accepted≠success, `--expect` scalar-leaf resolution, `seen` WS-only, boundary/defect routing, and the handoff field references (`source_plan.mocks`, `test_handoff`, requirements `tests[]` all exist).
- **Meta (own it, plainly):** this is the 5th independent review and the 5th real catch, and F1 is the **same recurring authoring failure** (overclaiming "(verified)"). It is not improving round-over-round, which means the fix is a *pre-write* habit, not a post-review patch. **Standing rule (already implied by the minigraph-syntax verification legend, restated here):** never tag a claim "(verified)" without a pointer to a specific evidence verdict; if something was done live but not recorded in the evidence file, it is "asserted," not "(verified)." Recording here (worklog) rather than the adversarial-review checklist's Incident Log, since that log is scoped to review/evaluation sycophancy and this is an authoring-discipline miss.
- **Open (carried):** optional re-check of the build.md fixes; the follow-along validation run (P5 "Done when"); regenerate design-spec example (debt); separation-of-duties owner; governance proportionality; verification debt.

### W23 — 2026-06-08 — build.md re-check: SHIP; both P5 specs independently cleared
- **What:** focused re-check of the build.md fixes against source. **Verdict: SHIP.** F1 CLOSED (no "(all verified in P0)" string; `rg "update node"` on the evidence file is empty, confirming the claim was correctly moved to the minigraph-syntax per-item tags); F2 CLOSED (companion `planVerification` has no `export` case → null; all three loci now require the import round-trip and say companion doesn't verify export); F4/F6 CLOSED. F3 keep-reason judged sound. No new defects; all links resolve.
- **Status:** build.md SHIP, test.md SHIP — both reviewed against source by no-context instances. The only remaining P5 item is the follow-along validation ("two independent runs can follow each spec without extra guidance").
- **Open (carried):** follow-along validation run (in progress next); regenerate design-spec example (debt); separation-of-duties owner; governance proportionality; verification debt.

### W24 — 2026-06-08 — P5 follow-along validation: PASS-WITH-GAPS → gaps fixed → P5 CLOSED
- **What:** a no-context agent built + tested a small `classify-score` graph (mapper + two `graph.math` branches + null-detect + 3 scenarios) by following **only** `build.md`/`test.md` (+ minigraph-syntax.md, companion.mjs). The graph built clean and all three scenarios (high / low / missing-score) passed. Verdict: **PASS-WITH-GAPS** — followable, with two real gaps.
- **Gap 1 — FIXED (and it was my own F2 fix's flaw):** build.md B7 said "re-import into a scratch session," but the REST driver cannot mint a session (WebSocket-only). Corrected to an in-place `session reset` + `import` round-trip done **last** (it clears+restores the live graph). My W22 fix for F2 introduced an unexecutable step — found only by *running* it, not by review.
- **Gap 2 — FIXED:** neither spec said how to seed a *missing* required input. A bare `instantiate graph` yields a live empty `input.body: {}` (not 404). Added to test.md step 1. Evidence claims 42–43 added.
- **Minor (no change):** choosing `with type` for skill nodes and relation labels — both low-friction and by-design (the Node Types / conventions tables cover them); the agent noted them only for completeness.
- **The real result:** a genuinely independent instance followed the specs end-to-end and produced a working graph + passing tests — the core of P5's "Done when." And the exercise caught that a *review-approved* fix (F2) was unexecutable in practice. Execution-validation caught what five rounds of review did not. Strongest argument yet for dogfooding over review alone.
- **P5 status: CLOSED** — specs written (W21), independently reviewed + fixed (W22), re-checked SHIP (W23), and validated by a follow-along build+test with gaps fixed (W24). A *second* confirming run would fully satisfy the literal "two independent runs" bar but is optional — the gaps the first run found are fixed and verified.
- **Open (carried):** optional second follow-along (confirm the gap-fixes enable a clean run); regenerate design-spec example (debt); separation-of-duties owner; governance proportionality; verification debt.

### W25 — 2026-06-08 — graph.extension success path VERIFIED (closes verification-debt item)
- **What:** traced the engine source to learn *why* the dogfood's extension call returned "not found", then tested the success path. `GraphExtension` forwards to the `graph-executor` flow, which loads the target via `GraphExecutor.getGraphModel` from `location.graph.deployed` (default `classpath:/graph/{id}.json`, **read-only**) — a different store from `export`/`import` (`location.graph.temp` = `/tmp/graph`). So an exported graph is importable but not extension-callable. The shipped `tutorial-*` graphs live in the deployed location and `app.env` defaults to `dev` (so `tutorial-` ids are allowed).
- **Test:** built a parent (`root → call-tut[extension=tutorial-1] → end`), ran it → `call-tut.result="hello world"`, `status=200`, `target=tutorial-1`; `output[]=result -> output.body.sub_result` produced `output.body.sub_result="hello world"`. **Extension success path works.**
- **Docs:** minigraph-syntax.md `extension` section upgraded from "(verified, with caveat)" to "(verified)" with the precise deployed-vs-temp resolution (source + execution backed); evidence claim 44 added.
- **Closes** the standing "graph.extension success path" verification-debt item. (Remaining verification debt: the asserted-but-untested function table, `model.none`, statement verbs.)
- **Note:** minigraph-syntax.md is ship-class; this is a strengthening edit (asserted→verified, source-backed) — flag for the same eventual independent pass as the other P1 edits.
- **Open (carried):** regenerate design-spec example (next); separation-of-duties owner; governance proportionality; remaining verification debt (functions / `model.none` / verbs).

### W26 — 2026-06-08 — Design-spec example regenerated; P3 round-trip demonstrated
- **Files:** created `examples/customer-360-design-spec.md`; README index lists it.
- **What:** a filled `graph_design_spec` for customer-360, matching the current graph-design.md template and grounded in the P0-validated fan-out-join topology (the `risk-complete` convergence and `exception=risk-fallback` are the verified shapes; build_handoff calls out the join-deadlock hazard and the null-detect-in-mapper rule). It also reflects the P3 schema as simplified in W19 (no `requirement_ids` field; traceability via `requirement_traceability`; lean `{question_id, mock_used}` carried blockers).
- **Round-trip demonstrated:** all **11** brief requirement IDs (INV/OUT/SRC/MAP/FLOW/FAIL-001/FAIL-002/FAIL-003/NFR-001/NFR-002/TEST-001) appear in `requirement_traceability`; a one-off check confirmed brief↔design is exact (no missing, no extra). This closes P3's last loose end (the bidirectional-example debt) — **P3 fully closed.**
- **Note (governance):** this example is a canonical artifact and the W25 minigraph-syntax `extension` edit is ship-class; both warrant the eventual independent pass alongside the other P1/P5 spec edits. Flagged, not auto-commissioned.
- **Open (carried):** independent review of the design-spec example + the W25 extension edit (ship-class); separation-of-duties owner; governance proportionality; remaining verification debt (functions / `model.none` / statement verbs).

### W27 — 2026-06-08 — Independent review of design-spec example + extension edit
- **Artifact 2 (minigraph-syntax `extension` edit): SHIP.** The reviewer corroborated the target-resolution claims directly against engine source (`GraphExecutor.getGraphModel` → `location.graph.deployed` classpath:/graph read-only; `app.env=dev` allows `tutorial-`; not-found→400) — fully source-verified. Its one reservation: it re-ran the live test and got a **uniform 404** on every inspect, so it couldn't reproduce claim 44 and flagged the "(verified)" success tag as resting on a single run. **I reproduced it cleanly (twice): `output.body.sub_result="hello world"`, `call-tut.status=200`.** The reviewer hit a transient read-path 404; claim 44 holds and the "(verified)" tag stands. No relabel (the reviewer's relabel was conditional on non-reproduction).
- **Artifact 1 (design-spec example): NO-SHIP → fixed.** Two legitimate overclaims, both my recurring failure mode:
  - **Purpose line** said the topology was "execution-validated" — but P0 ran a **3-branch** subset (profile/accounts/risk), never the 4-source graph or `fetch-preferences`. Scoped to "pattern validated in P0 (3-branch); this 4-source graph + preferences branch are designed, not instantiated."
  - **FAIL-003 "(verified)"** borrowed claim 22's (risk-fetcher) wording for a profile/accounts scenario never run. Downgraded (traceability note + failure_plan) to "inferred from claim 22, same fetcher skill path, not separately run."
- **Reviewer confirmed sound:** round-trip exact (11/11 both directions), full template conformance, `requirement_ids` not reintroduced, carried_blockers consistent with the brief, and the genuinely-backed "(verified)" tags (null-detect, named-target IF, join-deadlock, risk-complete convergence) all trace to real P0 claims.
- **Meta (blunt):** this is ~the 6th catch involving overclaiming "(verified)", and it recurred *after* I wrote the standing rule in W22 ("never tag (verified) without a pointer to a specific evidence verdict"). FAIL-003 pointed at claim 22 — a *different scenario* — which is the rule violated, not followed. The habit is not self-correcting; the only thing reliably catching it is the independent pass. That's a real limitation to keep externally guarded, not a solved problem.
- **Open (carried):** optional re-check of the two design-spec wording fixes (low value — verbatim per reviewer); separation-of-duties owner; governance proportionality; remaining verification debt (functions / `model.none` / statement verbs).

### W28 — 2026-06-08 — Verification debt cleared (function table + model.none + statement verbs)
- **Wording fixes:** the two design-spec downgrades were already applied verbatim in W27; no re-check spun up (disproportionate for two-word changes the reviewer dictated).
- **Cleared by execution (claims 45–48):** the full function table — `f:now` (ms+local), `f:parseDate`, `f:uuid`, `f:text`, `f:includes`, `f:concat`, `f:listOfMap`, `f:removeKey` (joining `f:defaultValue` from claim 40) — all produce correct output; `model.none` resolves to null; `graph.math` verbs `NEXT`, `DELAY`, `COMPUTE`, and `EXECUTE` all verified.
- **Doc correction (EXECUTE):** the doc implied `EXECUTE: another-node` runs a node. Source + execution show it **merges another evaluator's statement list into the current node** and runs them here (a merged `COMPUTE` lands under `{this}.result.*`; the source node is not traversed; it does not run a mapper). minigraph-syntax.md corrected.
- **Source-verified (claim 49):** `RESET` (clears nodes for loops — `GraphMath.resetNodes`) and `BEGIN/END` (bound `for_each` iteration) were read from source rather than executed (bounded-loop test deferred as low-value with tight-loop protection); labeled source-verified, not execution-verified.
- **Docs:** minigraph-syntax.md function-table note, `model.none`, the EXECUTE/RESET/NEXT/DELAY block, the statement verification-status note, and the `f:listOfMap` for_each note all upgraded from asserted→verified (or corrected). The verification legend's "untagged = asserted" surface is now nearly eliminated.
- **Verification debt: cleared** — everything is execution-verified except `RESET`/`BEGIN`-`END`, which are source-verified (authoritative per the precedence rule). minigraph-syntax.md edits are ship-class → flag for the eventual independent pass.
- **Open (carried):** separation-of-duties owner; governance proportionality; independent pass on the accumulated ship-class edits (W25 extension, W28 verbs/functions, design-spec example).

### W29 — 2026-06-08 — Independent pass on W28 edits; one reviewer finding was a FALSE POSITIVE
- **design-spec example: SHIP.** Reviewer confirmed both prior NO-SHIP fixes clean (Purpose scoped to 3-branch pattern; FAIL-003 "inferred from claim 22"), no residual overclaim. Done.
- **minigraph-syntax W28 edits: the reviewer returned NO-SHIP on one finding — and that finding was wrong.** It claimed the RESET bullet's "the runtime has tight-loop protection" was *fabricated* ("no loop guard exists in the engine; nodeSeen is the only control"). It had searched the **GraphMath module** (where RESET is implemented) but the guard lives in the **traversal layer**. I verified against source: `GraphLambdaFunction.highFrequency` (config `graph.node.high.frequency`, default 10) + `loopInterval` (`graph.max.loop.interval`, default 1000ms); `GraphExecutor`/`GraphTraveler.checkFrequency` (lines 187-202) increments per-node hits and, on `total > highFrequency`, returns `400 "executed too frequently"` and calls `handleErrorResponse` (aborts). The protection is real and enforcing. graph-design.md's long-standing "high-frequency loop protection" claim is corroborated.
- **Disposition (override, with proof):** did NOT apply the reviewer's recommended "delete/reverse the loop-protection claim" — doing so would have introduced a *false* statement. Instead **strengthened** the RESET bullet with the precise source-verified mechanism (>N hits in interval → 400 abort).
- **Reviewer was right on everything else:** all 9 function "(verified)" tags trace to claims 40/45 (it spot-checked `f:parseDate`→`1768453200000` = 2026-01-15 ET, plausible); EXECUTE rewrite matches source (merge, result on current node, math-only, source not traversed); `model.none`, NEXT/DELAY, BEGIN/END labels all hold; the (verified)-audit table mapped every other tag cleanly. Its confidence was capped because it couldn't re-run (session `ws-844990-2` had gone dead between my runs and its — a session-lifecycle/timeout artifact, same class as the W27 transient 404; environmental, not a doc defect).
- **Meta (the useful counter-point):** this is the first independent finding I've *rejected* — and rejecting it was correct, proven by reading the authoritative source. Lesson cuts both ways: review is load-bearing but fallible; the engine source is the tiebreaker, and verifying against it (precedence rule #1) is what resolved the conflict. It also means a reviewer's incomplete-search false-positive is a real failure mode to guard against, not just author overclaiming.
- **Status:** both artifacts now SHIP (design-spec confirmed; minigraph-syntax — the lone NO-SHIP was a false positive, RESET bullet strengthened). Verification debt remains cleared.
- **Open (carried):** separation-of-duties owner; governance proportionality.

### W30 — 2026-06-08 — Proportionality verdict + steady-state cadence (last governance note)
- **Verdict:** the governance layer did its job — but it was sized to the *author's* overclaim rate (a compensating control), not the artifact's intrinsic risk, and it is now heavier than the validated 8-file doc set warrants. Tally: ~12 independent passes (≈7 first-reviews caught real defects; ≈4 re-checks found nothing new; 1 false positive). Dogfooding was the highest-ROI practice; the *first* independent review per ship-class change was second; the re-check cycle and per-edit logging were the over-spend.
- **Steady-state cadence (maintenance mode):**
  - **Keep:** dogfood any new engine claim; one independent review for genuinely ship-class changes; the verification legend (`(verified)` ⇒ traced to an execution verdict).
  - **Drop:** routine re-checks (re-verify only after *substantial* rework); per-sub-fix worklog entries (log per change, not per edit); Gate-0 ceremony unless multiple parties are editing concurrently.
  - **Stop adding governance.** This is the last process addition; further effort should be *using* the docs, not building rules. (This note honors that by being the only change.)
- **Handoff:** next action is a brand-new, no-context agent guiding the full workflow. Entry point: [README.md](./README.md) → [workflow.md](./workflow.md) → the four phase specs (`requirements-gathering` → `graph-design` → `build` → `test`), with `minigraph-syntax.md` + `companion.mjs` as references. The `/build` + `/test` specs were validated as no-context-followable in W24; the engine quirks a fresh agent will hit are all in the evidence file + the verification legend.

### W31 — 2026-06-09 — Agent distribution plan drafted (new phase, for review)
- **Files:** created `llms/graph-builder/distribution-plan.md`; added a row to the README support-doc index.
- **What:** a dated D0–D5 plan for a **new phase** — packaging the validated four phases as installable commands for Claude Code (reference), GitHub Copilot, GPT Codex, and Amazon Kiro. Locked decisions captured: hybrid one-body→generated-wrappers; distilled-operating-prompt body that links the canonical spec; Claude-first then template; fixed namespaced prefix; engine deps documented as prerequisites (not wired); npx-from-git Node CLI in a new in-repo dir, no npm publish; and a hard Gate 0 requiring each agent's command format be verified+cited before any wrapper is generated for it.
- **Why:** the workflow is validated but installed nowhere; the maintainer wants it runnable on any agent. Decisions were elicited over two question rounds on 2026-06-09.
- **Not governance:** W30 closed the governance build-out; this is product/distribution work and the plan explicitly does not reopen it. Key authoring risk flagged in the plan: the distilled prompts must stay **subordinate** to the canonical specs (link + defer), not become a parallel authority — the P2 / Incident 0002 trap.
- **Status:** plan **drafted for maintainer review; not executed.** No code written yet (decision: dated plan + gates first).
- **Open (carried):** maintainer review/approval of the plan; lock the open items (prefix string, where referenced specs live in a target repo, installer dir name); then begin D0(Claude)+D1. Separation-of-duties owner; governance proportionality (both unchanged from W30).

### W32 — 2026-06-09 — Distribution plan independently reviewed → NO-SHIP → revised
- **Files:** `distribution-plan.md` (decisions 2/4/5 reworded; Source-of-truth tier 3 + D1 + D2 + Gate 3 reworked; new "Review dispositions" section; status updated); this log.
- **What:** spawned a fresh no-context subagent to review the v1 plan against `./adversarial-review-checklist.md` (Separation-of-duties: I authored the plan, so an independent pass was required for Gate A to be literally true). **Verdict: NO-SHIP-as-an-execute-plan** (fine as a draft to revise), 5 findings.
- **Dispositions (also recorded in the plan's Review dispositions table):**
  - **K2 (parallel authority) — APPLIED, the load-bearing fix.** The reviewer correctly sharpened the risk I'd flagged pre-draft: a "distilled prompt" that restates a spec's step sequence + gate is a second authority with no drift check (the Incident 0002 / W19 pattern), and my "subordinate" claim was prose, not mechanism. Fix (maintainer chose it): the wrapper's step/gate content is now **extracted from the canonical spec at generate-time**, not hand-authored — a projection that cannot drift — and Gate 3 is now a byte-for-byte regenerate-and-diff check with a named artifact (`--check` mode). Subordination is structural.
  - **K4 (unverified `/build` `/test` collision claim) — APPLIED.** Reframed decision 4: namespacing is a chosen convention; any specific collision is a D0 evidence item, not an asserted fact. (The plan had violated its own Gate 0 / "verified not asserted.")
  - **K5 (engine-deps framed workflow-wide) — APPLIED.** Decision 5 + D3 now state engine dependence per command; `/requirements` + `/design` need no engine.
  - **K1 (Gate B: generator+CLI vs. 16 committed files) — OVERRIDDEN (maintainer call).** Kept the generator/CLI for cross-agent body consistency; the residual risk (under-pays for a 4-agent matrix) is accepted, and D4 is the checkpoint that tests it before D5 replicates.
  - **K3 (proportionality vs. W30) — OVERRIDDEN, with reason.** The apparatus shape was maintainer-requested (mirror `improvement-plan.md`), so it is requested scope, not author over-spend; the plan keeps W30's highest-ROI practice (D4 dogfood) and stays a single planning authority.
- **Meta:** the independent pass again caught a real authoring defect (K2) that self-review stated but did not *mechanize* — consistent with the W12–W27 pattern that review is load-bearing for exactly the "I asserted subordination/verification" failure mode. Two findings (K1, K3) were overridden because the reviewer lacked the maintainer-decision context a no-context instance can't have — a known limit of context-withheld review, dispositioned explicitly rather than silently.
- **Status:** plan **revised; still drafted-not-executed.** Next: lock the three open items, then D0(Claude)+D1.
- **Open (carried):** lock open items (prefix string, target-repo spec location, installer dir name); maintainer go/no-go to begin D0+D1; separation-of-duties owner; governance proportionality.

### W33 — 2026-06-09 — Distribution plan re-reviewed → NO-SHIP → two phase specs normalized + plan fixed
- **Files:** `requirements-gathering.md` (+`## Step checklist`, R11 gate promoted to a top-level `## Gate`), `graph-design.md` (+`## Step checklist`), `distribution-plan.md` (tier 3 / D1 / D2 / Gate 3 / D4 / K1+K3 override reasons reworded; second-review disposition table added; status → "reviewed twice"), this log.
- **What:** a *second* fresh no-context subagent re-reviewed the revised plan against the adversarial checklist. **Verdict: NO-SHIP**, and its central finding was correct: the W32 K2 fix ("extract the step sequence + gate from the spec") assumed all four phase specs carry uniform `## Step checklist` + `## Gate` sections — but **only `build.md`/`test.md` did**. `requirements-gathering.md` had neither (steps were `## Step R1–R11`, gate buried in R11 as `### Gate A–D`); `graph-design.md` had `## Gate` but no `## Step checklist` (steps were `### D1–D11`). I verified by Node (the shell's `grep` is shadowed/unreliable).
- **Fix (maintainer chose "normalize the specs"):** added a terse `## Step checklist` to both specs and promoted `/requirements`' buried gate to a top-level `## Gate`. Independently justified — [workflow.md](./workflow.md)'s "Common Phase-Spec Skeleton" already mandates both sections, so the two older specs were **non-conformant**; this is conformance, not invention. The "extract from spec" mechanism is now factually supported (all four specs uniform, verified). Extraction reworded to **verbatim** lift of the named headings, so the wrapper body equals the spec text by construction.
- **Other findings applied:** Gate 3 split into (a) idempotency-diff and (b) extraction-fidelity (dropped the overclaim that "a diff proves no contradiction"; D4 is the fidelity gate); K1 override reasoning corrected (consistency comes from single-sourcing, not the generator — the generator's unique value is format-porting + regen, with D4 as kill-switch); K3 override reworded from provenance ("it was requested") to a worth basis; open item 4 (schema inline-vs-link) now a named D4 output instead of a deferral to nowhere.
- **Meta (own it):** I committed the repo's recurring "asserted, not verified" failure (cf. W12/W18/W22/W27) — *inside the fix for the prior review's parallel-authority finding*, one entry after the plan cited "verified not asserted" as a strength. The independent re-check caught it; a grep settled it (precedence #1: observed reality over prose). Strongest evidence yet that the independent pass is load-bearing for exactly this failure mode, and that re-checking after substantial rework (W30's rule) was the right call.
- **Note (governance):** the two spec edits are **ship-class** (they govern contributors); per W6 they warrant an independent pass before they "ship." Conformance-only structural edits (added a checklist + promoted a heading; no content/gate-criteria changes) — flagged, not auto-commissioned, per proportionality.
- **Status:** plan revised; still drafted-not-executed.
- **Open (carried):** independent pass on the W33 spec normalizations (ship-class); lock open items (prefix string, target-repo spec location, installer dir name); maintainer go/no-go to begin D0+D1; separation-of-duties owner; governance proportionality.

### W34 — 2026-06-09 — Third review (plan + W33 spec edits) → 2 findings verified FALSE, rest applied
- **Files:** `distribution-plan.md` (extraction rule named in tier 3 / D1; Gate 3(b) upgraded to a string-equality check; decision 2 + distilled/verbatim wording reconciled; K3 circular sentence cut; Meta paragraphs + confidence note removed; third-review disposition table added; status → "reviewed three times"); this log. No spec edits this round.
- **What:** a third fresh no-context subagent reviewed both the plan and the W33 spec normalizations; **NO-SHIP on both.** Its two central findings were testable claims about spec structure, so I tested them rather than accept the verdict (precedence #1; the W29 false-positive precedent):
  - **Kill A (no single extraction rule works) — FALSE.** Ran a heading-scoped extractor (*content under a heading until the next heading of level ≤ its own*) over all four specs: it cleanly lifts both `## Step checklist` and `## Gate` from each (requirements gate = 60 lines incl. `### Gate A–D`; others clean). The reviewer mistook the checklist lift correctly stopping at `## Step R1` for a defect — but checklist and gate are *separate* extractions. Legitimate kernel: the rule was unstated in the plan; now named + the run recorded.
  - **Kill B (the new `## Step checklist` is a within-spec parallel surface violating Gate D) — FALSE.** The Operating-protocol + Step-checklist pairing is the workflow.md Common Phase-Spec Skeleton, already carried by build.md/test.md; W33 brought requirements/design into conformance. Requirements' Gate D bars a divergent *gate* checklist vs the step quality-checks — not a step index.
  - **Kills C/D/E/F — APPLIED.** C: "verbatim" vs "distilled/lean" reconciled (the `## Step checklist` is the author's distillation; the generator lifts it verbatim; dropped blanket "lean"). D (an upgrade): verbatim lift makes extraction-fidelity a mechanical string-equality check, no longer deferred to D4. E: cut the Meta/confidence prose from the plan (it lives here). F: cut K3's circular "prevents the failure modes this review chain exercised" clause.
- **Spec edits verdict:** the reviewer's spec-edits NO-SHIP rested entirely on Kill A + Kill B, both verified false; the R11→`## Gate` promotion it already judged clean. **The W33 spec normalizations stand (effectively SHIP)** — this third pass doubles as the independent ship-class check W33 flagged as owed.
- **Meta:** two verified-false findings in one pass (after W32's one true catch and W33's one true catch) is the signal that review has reached wording, not substance. Recording explicitly: per W30's diminishing-returns rule, **further adversarial review rounds on this plan are not expected to pay** — the productive next step is locking the open items and executing D0+D1, where real evidence (agent formats, a dogfood run) replaces argument. Also a fair counter-lesson to W12–W27: review is load-bearing but fallible in both directions; the tiebreaker is running the thing.
- **Status:** plan revised; still drafted-not-executed.
- **Open (carried):** lock open items (prefix string, target-repo spec location, installer dir name); maintainer go/no-go to begin D0+D1; separation-of-duties owner; governance proportionality.

### W35 — 2026-06-09 — Three open items locked; plan ready to execute
- **Files:** `distribution-plan.md` (open-items section split into "Locked items" + remaining "Open items"; decisions table item 4, D2, D3 updated); this log.
- **Locked (maintainer, with recommendations):**
  - **Command prefix `graph`** — `/graph-requirements`, `/graph-design`, `/graph-build`, `/graph-test` (`/graph:…` where the agent supports `:` namespacing). Meaningful, short, portable across agents.
  - **Target spec location `.graph-builder/` at repo root** — CLI copies one shared spec set there; all agents' wrappers reference it. Doesn't touch the user's `docs/`; version-pinned at install.
  - **Installer home `llms/graph-builder/installer/`** — co-located with the specs, consistent with the existing `companion.mjs` / `evidence/stub-server.mjs` precedent that graph-builder tooling lives under `llms/graph-builder/`.
- **Flagged D3 execution detail:** `npx <github-repo>` resolves a bin from a root `package.json` this Java repo lacks — D3 picks between a thin root `package.json` pointing at the installer bin vs. a `node …/installer/bin/cli.mjs` post-clone call. Doesn't affect the locked locations.
- **Remaining open (by design, decided during execution):** schema inline-vs-link (at D4), cleanest-to-verify agent order (at D0).
- **Status:** plan is **execution-ready.** Next action: begin **D0 (Claude format verification) + D1 (extraction rule + framing templates)**.
- **Open (carried):** maintainer go/no-go to begin D0+D1; separation-of-duties owner; governance proportionality.

### W36 — 2026-06-09 — D0–D2 executed: Claude format verified, generator working + verified
- **Files:** created `evidence/agent-command-formats.md` (D0 Claude row, cited); `installer/` package — `package.json`, `src/extract.mjs`, `phases.mjs`, `src/targets/claude.mjs`, `src/generate.mjs`, `.gitignore`.
- **D0 (Claude format, verified+cited):** confirmed via the `claude-code-guide` agent against official docs (`code.claude.com/docs/en/skills.md`, `.../commands.md`). Key findings: custom commands are now **Skills** (`.claude/skills/<name>/SKILL.md`); **subdirectories do NOT namespace the invocation**, so the `graph` prefix is baked into the name (`/graph-requirements`, not `/graph:requirements`) — the hyphen branch the locked decision anticipated; no collision with built-ins. Recorded with a D4 caveat to live-verify the no-subdir-namespacing claim on install.
- **D1:** `extract.mjs` implements the heading-scoped rule (content until the next heading of level ≤ its own); `phases.mjs` holds the four hand-authored framing configs (purpose / invocation / spec / engine-need). Framing is the *only* hand-authored wrapper content.
- **D2:** generator + Claude adapter emit four `SKILL.md`. Ran it — **all four contain their spec's `## Step checklist` + `## Gate` verbatim** (the fidelity assertion passes). Eyeballing the output caught a real seam (lifted text says `/design`; the installed command is `/graph-design`) → added a one-line **name-mapping note** to the framing instead of rewriting the lift, preserving the verbatim guarantee. (Verify-by-running caught what the boolean alone wouldn't have surfaced — the recurring lesson.)
- **Not yet:** D3 (CLI — copy specs to `.graph-builder/`, write skills, `--check`; per-command prerequisites README) and D4 (dogfood the installed commands). D3 carries the flagged npx/root-`package.json` detail.
- **Status:** D0–D2 done, generator verified by execution; previews under `installer/.preview/` (gitignored).
- **Open (carried):** D3; D4 (writes into a live `.claude/skills/`, so checkpoint before it); D4 live-verify of Claude namespacing; remaining agents (D5); separation-of-duties owner.

### W37 — 2026-06-09 — D3 + D4: CLI built and dogfooded (install + integrity check + drift detection)
- **Files:** `installer/src/install.mjs`, `installer/bin/cli.mjs`, `installer/README.md`; `distribution-plan.md` (decision 6 reworded + new decision 7; locked item 3 + D3 updated); `evidence/agent-command-formats.md` (project-scoped note).
- **Maintainer decisions recorded (per request):** (6) **no npm publish / no npx** — CLI is runnable via `node llms/graph-builder/installer/bin/cli.mjs`, no root `package.json`; (7) **per-project install only**, never `~/.claude`.
- **D3:** `install <agent> [--to <dir>] [--dry-run]` copies the canonical spec set (excluding the planning/process docs + `installer/`) into `<project>/.graph-builder/` and writes the four `SKILL.md` into `<project>/.claude/skills/`; `check` regenerates and diffs (Gate 3a). README states per-command engine prerequisites (decision 5).
- **D4 (mechanical dogfood — PASS):** scratch install wrote the 4 skills + spec set correctly; planning docs confirmed excluded (no leak); `check` = OK / exit 0 when clean; injecting a hand-edit into a wrapper made `check` report `drift` / exit 1 — **the integrity gate detects injected drift** (assert-the-catch, matching the plan's P4 ethos). Generator fidelity (verbatim Step checklist + Gate) was proven in W36.
- **Remaining D4 caveat:** the **live invoke** (`/graph-requirements` in a real Claude session) is not yet confirmed — needs the skills in a real project `.claude/skills/` + a fresh session. This is where the D0-recorded no-subdir-namespacing claim gets its execution check. Mechanics are fully proven; only the live load is unverified.
- **Minor known item:** the copied `.graph-builder/README.md` carries a few dangling links to the excluded planning docs (improvement-plan/worklog/distribution-plan) — cosmetic; the precedence rule it carries is intact. Polish later (strip those rows on copy) if it matters.
- **Status:** **D0–D4 done for Claude** (mechanical); the Claude reference is installable and verified by execution. Gate 1 ("Claude reference proven") is met except the live-invoke confirmation.
- **Open (carried):** live-invoke confirmation (user installs into a real project + new session); D5 (Copilot / Codex / Kiro); copied-README dangling-links polish; separation-of-duties owner.

### W38 — 2026-06-09 — Workflow meta-audit (first full real run: VoltaicMesh) — analysis, not applied
- **Trigger:** maintainer ran the full four-phase pipeline once on a real graph (VoltaicMesh Dispatch Evaluator) and wrote a retrospective (`.graph-builder/retrospective-voltaicmesh-session.md`). A conceptual defect (DEF-1: branch predicate keyed on `req>head` instead of "allocation reached the cap") survived all four gates and was caught only by external review.
- **Method:** I drafted a per-phase weakness analysis, then — to avoid a persuasive self-review — sent it to a fresh no-context subagent to attack, and verified its spec claims against the actual text myself (precedence #1). The subagent gutted my draft, correctly.
- **Honest result (after collapse):**
  - **Root (one meta-issue):** *single laundered truth surface.* Intent is recorded once — in the brief — and every gate checks conformance to it. A misconception in the brief is invisible at every gate because nothing independent ever contradicts it. (= the retro's thesis, verified.)
  - **Two real, spec-grounded per-phase weaknesses:** **`/requirements`** is a self-graded gate whose Gate D actively invites filing a conceptual contradiction (the ms-vs-epoch unit clash) as cheap `build/deploy` debt (confirmed: status author-set; Gate D line ~459). **`/test`** authors its oracle from the contaminated brief (proves code agrees with brief, not reality), has no boundary/interaction scenario category, and routes "spec was *silent*"→requirements but has **no** "spec was *wrong* and the test agrees" route (test.md line 81).
  - **`/design` and `/build` are NOT distinct weaknesses** — verified against spec text: `build.md` already states its PASS scope (lines 12/64/104, "do not claim correctness here"); `graph-design.md`'s "does not rediscover requirements" bars re-*gathering*, not predicate-checking (D5 already puts decisions in design's hands). DEF-1 surviving there was **operator behavior, not a spec gap.** (My first draft mischaracterized both — asserting from memory instead of re-reading, on a doc I wrote.)
- **Minimal fixes, proportionality-ranked (do NOT re-grow governance — W30):**
  - *Free/structural:* (a) `/requirements` — a conceptual contradiction (unit clash, status with no testable meaning, sourceless external assumption) **cannot be author-classified as build/deploy debt; it blocks requirements**; (b) `/test` — add the "spec-was-wrong → `/requirements`" route; (c) **define "intent" workflow-wide as a reading taken independently of the brief** — else every "check against intent" silently re-reads the contaminated surface.
  - *One ship-class review cycle (highest leverage; the only fix that structurally escapes the chain):* **independent test-oracle authorship** — a fresh instance derives expected outputs from intent without sight of the code; that disagreement *is* the bug. Would have caught DEF-1.
  - *Cut (W30 violations / discipline-as-spec):* design predicate dry-run ceremony; mandatory capability-probe-in-design.
- **Two meta-issues the analysis initially missed:** **no post-deploy net** (DEF-1 caught by a human *after* ship; nothing catches a conceptual error in the world); **operator variance is the only conceptual floor** (the one control that worked was a review the operator *chose* to run).
- **Over-rigidity answer (maintainer's question):** the unit is *the brief's claims*, not "boolean predicates" — so transform/fetch graphs aren't burdened (independently re-check their mappings/contracts; no predicates to dry-run). My predicate-centric "one rule" was itself over-fit; dropped.
- **Status:** analysis + recommendations only. **Not applied to the specs** (maintainer's call) and **not governance build-out** (no framework added — this note is the only artifact, per spirit). The highest-leverage single change is the independent test oracle; the cheapest real win is the requirements bucket-rule.
- **Meta:** my first draft inflated one root into four "per-phase" issues and mischaracterized two specs from memory — caught by the adversarial subagent + my own re-verification. The documented pattern (W12–W34), once more: review is load-bearing for the "I asserted it" failure, and the tiebreaker is the source.

### W39 — 2026-06-10 — Verified (and refuted) the retrospective's graph.js `/build` claims; folded the corrected behavior
- **Trigger:** maintainer asked to verify the retrospective's `graph.js`-in-`/build` claims and, *if valid*, fold them into `minigraph-syntax.md`, then update docs per protocol. The claims: "multi-line `'''` COMPUTE fails / multi-line IIFE non-buildable" and "`{var}` refs swallowed inside braces" (`retrospective-voltaicmesh-session.md` §IV `/graph-build`, §III table).
- **Method:** source first (`GraphJs.compute` → `substituteVarIfAny`/`replaceWithParameter`, `GraphLambdaFunction.java:208-261`; `Utility.extractSegments`/`findEndBracket`, `Utility.java:1316-1356`), which already predicted the claims were overstated — so I ran five live experiments on a minimal `root → enrich(graph.js) → end` graph (session ws-258924-4, seed `model.x=int(5)`) rather than trust either the retrospective or my own source-reading.
- **Result — both claims REFUTED as written** (evidence rows 50-54):
  - Multi-line `'''` COMPUTE / IIFE **works** → `w = 15`. COMPUTE is evaluated before the `\n`→space flatten (`GraphJs.java:161-165`), so newlines reach the JS engine.
  - `{var}` is **not** generally swallowed: substitutes inline (`y=6`), inside a single-line IIFE (`z=10`), and *nested* inside an object literal (`JSON.stringify({tag:{model.x}})` → `{"tag":5}`) because `extractSegments` scans innermost-first.
  - The **one real, narrow rule** (`GraphLambdaFunction.java:238`, confirmed by run): a `{…}` group whose inner text has `:`/newline/tab/CR is left verbatim as JS/JSON; the only genuine swallow is a lone `{path}` sharing braces with a colon/newline (`{model.x : 0}`), which then **halts the node silently** (result absent / 404).
- **Per protocol — "if valid, fold":** what was valid to fold is the *corrected* behavior, not the claims. Added a `## graph.js COMPUTE {var} substitution` round 8 (rows 50-54) to `evidence/dogfood-customer-360.md`; added a verified COMPUTE substitution subsection (multi-line works + the innermost-first rule + the colon/newline footgun) to `minigraph-syntax.md`. Did **not** add the false "multi-line fails" claim; the syntax doc now states the opposite, with evidence.
- **No-divergent-truth sync:** annotated `retrospective-voltaicmesh-session.md` with a dated correction note at the two claim sites (record preserved, not rewritten — same discipline the retro itself used in its §VI).
- **Meta:** the retrospective generalized a real but narrow footgun into two broad rules — the n=1 over-fit the HANDOFF names. Caught by source + execution, not by re-reading the retrospective. The symmetric lesson (W29/W34): distrust a confident *record* exactly as much as a confident author or reviewer — verify against the engine.
- **Open (carried):** unchanged from W37/W38 (live-invoke confirmation; D5; copied-README dangling links; W38 workflow fixes; separation-of-duties owner). The scratch test graph remains in session ws-258924-4 (ephemeral; `session reset` clears it).

### W40 — 2026-06-10 — Created `/graph-verify` (optional adversarial-verification step); corrected W39's false `graph.js` claim
> ⚠ **Superseded in part by [W41]: the `graph.js` "multi-line COMPUTE fails" correction recorded in this entry is itself FALSE** (it misread an inspect scalar-leaf `404` as a node halt). Multi-line COMPUTE works. The `/graph-verify` creation in this entry stands; only its `graph.js` claim is retracted. Read W41 for the verified behavior. (Entry preserved per the append-only convention.)
- **Files:** created `verify.md` in **both** trees (`.graph-builder/verify.md`, `llms/graph-builder/verify.md`, identical). Corrected the false "multi-line COMPUTE works" claim in **both** trees' `minigraph-syntax.md` (line ~248) and `evidence/dogfood-customer-360.md` (row 52 + Net + appended re-verification note). Annotated `.graph-builder/retrospective-voltaicmesh-session.md` (§III table, §IV `/graph-build` bullet, W39 "Correction trail") and `.graph-builder/builds/voltaicmesh-dispatch-evaluator-build-log.md` (D-B3 `w39_correction`).
- **Correction to W39 (per convention — corrections are new entries, not rewrites of W39):** W39 recorded "**both** retrospective claims REFUTED — multi-line `'''` COMPUTE/IIFE **works** → `w=15`" and folded that into `minigraph-syntax.md`. **That fold was false.** Re-verified in the same rig (session `ws-258924-4`, seed `model.x=int(5)`) with a single-line-vs-multi-line **control on the byte-identical IIFE**: single-line → `15`, **multi-line `'''` → `404` / silent halt**; the statement persists with its newlines intact (`["(function(){\n return {model.x} * 3;\n})()"]`), so it is an execution failure, not a parser-mangle. Net: of the retrospective's two claims, **only one** was refuted — "`{var}` swallowed inside any brace" is wrong (substitution is innermost-first; nested refs resolve), but "**multi-line COMPUTE fails**" is **CONFIRMED**. The "15" in W39 was the single-line control mis-attributed to the multi-line run. The two correct, narrow rules now in `minigraph-syntax.md`: (a) keep each `{path}` in clean braces (nesting is fine); (b) write every COMPUTE on a single physical line.
- **What `/graph-verify` is:** an **optional, cross-cutting** step (not a 5th pipeline stage) runnable at any time against any artifact from any stage. On invocation it **spawns a fresh-context subagent with authoring/advocacy context withheld** to **reproduce the artifact's load-bearing claims against ground truth** (live engine + source + intent read independently) — never against the record. Motto: *verify against the engine, and verify the verifications.* Counters: laundered truth, oracle corruption, n=1 over-generalization (in either direction), boundary/interaction errors, unreproduced `(verified)` tags, and conformant-but-poor (over-built / wrong-problem).
- **Arguments documented as features (not only in the spec body or the future skill wrapper):** added `/graph-verify <target> [--report [path]]` to the canonical phase catalog — `workflow.md` (new "Optional Cross-Cutting Step" section + a line in the lifecycle diagram) and the `README.md` Canonical Documents table — in **both** trees. This keeps the argument surface discoverable in the durable doc set rather than buried in the installer/`SKILL.md` (which is generated). `workflow.md`'s section states plainly that the arguments are first-class features documented there and in `verify.md`.
- **Revised same session after maintainer review:** the **primary deliverable is now an honest, human-facing QUALITY verdict in prose** — *is this good and trustworthy?*, not just *does it conform?* The **structured report artifact is optional** via `--report [path]` (invocation `/graph-verify <target> [--report [path]]`); by default no artifact is written. Rationale (maintainer): a schema filled to look thorough is itself the review-as-theater this step defeats; conformance is what the per-phase gates already prove cheaply, so this step's value is the harder quality judgment a gate can't give. Added a *verdict* section (bottom-line-first, wrong-before-missing, property-with-cost not praise, confidence + what-would-change-it) and dropped the "PASS/blocked" framing — a true negative verdict on a bad artifact is a *successful* verification, not a failure.
- **Why now / lineage:** operationalizes the **one control W38 found "structurally escapes the chain"** (independent authorship/review) into a runnable step — the control that caught all three real defects (DEF-1, K1, the multi-line claim). Deliberately **does not** re-grow the ceremony W38 cut (no mandatory per-predicate dry-run, no forced capability-probe; W30). Unit of review is *the artifact's claims*, so transform/fetch graphs aren't burdened. `verify.md`'s closing note makes the proportionality explicit: scalpel, not turnstile; optional by design.
- **Meta:** W39 is itself the cleanest case for why this step exists — a confident, source-cited, `(verified)`-tagged record (incorporated from an external contributor) that was false, caught only by re-running. Three confident records in a row (retro → its correction → W39) were each refuted by execution; the new step makes "re-run before you trust" a thing you can invoke rather than a thing you must remember.
- **`SKILL.md` wrapper created (now slash-invocable):** hand-authored `.claude/skills/graph-verify/SKILL.md` matching the family format — frontmatter `name: graph-verify`, `argument-hint: "<target> [--report [path]]"`, `disable-model-invocation: true` (user-invoked, like the other four). Body lifts the invocation/arguments, V1–V6 protocol, the verdict requirements, and completion criteria from `verify.md` (pointer marks `verify.md` authoritative — wrapper is not a second source of truth). Adapted shape: `verify.md` deliberately doesn't use the rigid Step-checklist/Gate skeleton, so the wrapper carries *Operating protocol* + *The verdict* + *Completion* instead.
- **Open (carried — W41 update:** the `graph.js` correction in this entry is itself wrong; see W41**):** the wrapper is **hand-authored, not yet emitted by the installer generator** — reconcile so `installer` `check`/drift doesn't flag it and a fresh `install` produces it (the generator currently knows only the four pipeline phases; `verify.md` is the first optional cross-cutting spec, and its non-skeleton shape means the generator's verbatim Step-checklist+Gate lift won't apply as-is). W38 workflow fixes still pending (independent test-oracle; requirements bucket-rule; "spec-was-wrong"→requirements route); separation-of-duties owner. Pre-existing `.graph-builder/README.md` link to `./adversarial-review-checklist.md` dangles from that tree (checklist lives at `./adversarial-review-checklist.md`); `verify.md` and the new wrapper follow the same established convention — fold into the W37 "copied-README dangling links" cleanup.

### W41 — 2026-06-10 — W40's `graph.js` correction was itself false; multi-line COMPUTE works (re-verified two ways + source); first real `/graph-verify` use
- **Trigger:** maintainer asked for an accuracy/quality audit of the W40 edits, inviting `/graph-verify` if useful. W40 had "corrected" W39 to claim a multi-line `'''` COMPUTE **fails** (`404`/silent halt) and rewrote `minigraph-syntax.md` to "a COMPUTE must be a single physical line."
- **Finding — W40 is a false correction; W39 was right.** Re-ran the contested case (live session `ws-646809-2`, seed `model.x=int(5)`), byte-identical IIFE, **distinct result keys** single-line (`sgl`) vs multi-line (`mln`), re-instantiated and run twice each: **both return `15`.** The stored multi-line statement retains its newlines (`graph` dump: `"...(function(){\n return {model.x} * 3;\n})()"`), so it is genuinely multi-line. W40's `404` was the **inspect scalar-leaf artifact**: `inspect enrich.result.mln` (a number) returns `404` *by design* — `rest/InspectStateMachine.java:47-51` serves a value only if it is a `Map`/`List`, else throws `404 "Not found"` — while the container `inspect enrich.result` holds `{mln:15}`. W40 read the leaf-404 as a silent halt — the exact trap the retrospective §IV(b) flagged and recommended documenting (never done until now).
- **Independent confirmation (`/graph-verify`, first real use):** executed the `verify.md` V2 protocol — spawned a **fresh-context subagent with the verdict and the W39/W40 history withheld**, given engine + source + the quoted claim. It reproduced `{mln:15}` for the multi-line case by its own runs, found an extra control (`enrich.result.out`→404 leaf vs `enrich.result`→`{out:2}` on a trivial `1+1` graph), and independently traced the 404 to `InspectStateMachine.java`. Verdict matched: multi-line works; the single-line restriction is false. Symmetric skepticism worked as designed — the confident *correction* was distrusted as much as the original claim.
- **Corrections applied (per protocol — appended/annotated, not silent rewrites):**
  - `minigraph-syntax.md` (both trees): COMPUTE section now states **multi-line works**, cited to **source** (`GraphJs.java:161-195` — flatten runs after `compute()` on a copy) per maintainer preference (no example-run citations in the syntax doc); substitution rule re-cited to source (`GraphLambdaFunction.java:208-261/238`, `Utility.java:1316-1356`); **added the inspect scalar-leaf-404 gotcha** (`InspectStateMachine.java:47-51`) to the run/inspect section — read the container, not the leaf.
  - `evidence/dogfood-customer-360.md` (both trees): row 52 + Net corrected to "multi-line WORKS — both retrospective claims REFUTED"; added a W41 correction trail (the three records; W40's leaf-404 misread).
  - `verify.md` (both trees): the founding example (§Purpose) reframed from the false "multi-line works was false" to the true "the W40 *correction* was false (scalar-leaf misread), caught by container re-run + independent reviewer" — a stronger symmetric-skepticism example.
  - `.graph-builder/retrospective-voltaicmesh-session.md`: §III table + §IV `/graph-build` (b) rule + W39 correction-trail annotation updated to the verified truth (multi-line works; only-footgun = colon/newline brace).
  - `.graph-builder/builds/voltaicmesh-dispatch-evaluator-build-log.md`: D-B3 gains a `w41_correction` — the single-line deviation was a fine authoring choice but **not** forced by any multi-line limitation (there is none).
  - W40 entry flagged in place (open-items line) pointing here; W40 not rewritten (append-only convention).
- **Meta:** the chain is retro → W39 → W40, and the **last** record was the wrong one — the case where a confident self-correction, source-cited and `(verified)`-tagged, went unre-run. W40's own `verify.md` names this trigger (line 61: "a claim + its own 'correction'") and W40 didn't run it on itself; W41 did, and it caught it in seconds. The discipline holds recursively: verify against the engine, and verify the verifications — including corrections.
- **Open (carried):** unchanged (live-invoke; D5; `/graph-verify` wrapper not generator-emitted → drift; W38 workflow fixes; separation-of-duties owner; copied-README dangling links). The scalar-leaf-404 gotcha is now documented, closing the long-standing retrospective §IV(b) recommendation.

### W42 — 2026-06-10 — Installer support for `/graph-verify` (+ a general mechanism for optional commands); reviewed before building
- **Trigger:** maintainer asked whether the installer ships `/graph-verify` — verified at source it did **not** (the generator's `PHASES` knew only the four; `verify.md` was *copied* to `.graph-builder/` but no wrapper was generated, and `check` was blind to the hand-authored one). Task: add it, with the least primitives, and in a way that admits *future* optional commands. Maintainer required a scope-first pass and an independent review before coding.
- **Scope + independent review (before any edit):** scoped a minimal "per-command list of headings to lift" plan, then spawned a fresh-context reviewer (verdict/authorship withheld, pointed at the code + `distribution-plan.md` + the checklist). It returned **sound-with-specific-changes** and caught a real defect my plan missed: a free-form per-command `sections` list **removes the Gate 3(b) teeth** ("a single fixed rule over uniform headings leaves no room for a wrong choice"), and my proposed 3-section lift was itself the wrong choice — it dropped `## Core principles` / `## What it counters` / `## When (not) to run`, the sections that *define* terms the lifted `## Operating protocol` only references. I.e. the plan would have generated a `/graph-verify` wrapper that **silently misrepresents its spec** — the exact Incident 0002 / Kill #2 failure the single-source mechanism exists to prevent, on the command whose whole job is catching that. Verified the reviewer's claims against the spec + governing doc before accepting (not taken on faith — W29/W34 discipline); they held.
- **Governance decision (maintainer):** of three models — (A) spec self-designates its body via markers, (B) an optional-command skeleton mirroring the phases, (C) committed static wrapper — chose **A**. Most faithful (the spec owns what's operative, co-located and reviewable), handles heterogeneous/non-contiguous optional commands, and keeps a checkable fidelity gate; B was rejected as too rigid (would force the just-corrected `verify.md` into a 2-section skeleton), C as reintroducing the hand-authored-vs-spec drift the generator exists to prevent.
- **Built (the marker primitive):** a spec marks each operative section with `<!-- wrapper-body -->` on the line above its heading; the generator lifts exactly those, **verbatim**, via the *existing* heading-scoped rule (selection changes, the lift does not — "which, not what" preserved). Two governance regimes now coexist cleanly: **phases** keep the fixed `## Step checklist` + `## Gate` rule (governed by workflow.md's uniform skeleton); **optional commands** self-designate (governed by in-spec markers).
- **Files:** `verify.md` (7 markers on the operative sections; both trees synced) · `installer/src/extract.mjs` (`extractMarkedBody` + `WRAPPER_MARKER`; strips markers before lifting) · `installer/phases.mjs` (`EXTRAS` — verify *framing only*, no section list; `prerequisites` field) · `installer/src/generate.mjs` (iterate `[...PHASES, ...EXTRAS]`; fidelity self-test generalized to "every designated section present verbatim") · `installer/src/targets/claude.mjs` (declarative `numbered` flag instead of an overloaded `optional`; `prerequisites` override; emits the lifted section list) · `installer/bin/cli.mjs` (Commands summary derived from what's written → lists `/graph-verify` automatically). **`install.mjs` untouched** — `install`/`check` already iterate whatever `generate()` returns.
- **Verified by running (not asserted):** generator self-test green for all 5 wrappers (phases 2 sections, verify 7); exit 0. **Eyeballing caught a real bug the boolean missed** — the marker leaked into the lifted text (it sits at the tail of the preceding section, before the next heading); fixed by lifting from a marker-stripped copy, re-confirmed 0 leaks. Scratch `install` writes all 5 + copies `verify.md`; `check` = ok for all 5 (the drift-coverage gap is closed). Repo's own 5 wrappers regenerated to match the adapter change; `check --to .` green.
- **Closes:** the carried open item "`/graph-verify` wrapper not generator-emitted → drift." A fresh `install` now produces it and `check` covers it.
- **Adopted from review / deferred:** fixed the fidelity gap and the `optional`-overload; **deferred** the reviewer's "split framing into more declarative fields" until a *second* optional command exists (kept the single `numbered` lever; `prerequisites` already demonstrates the per-field pattern — YAGNI, and W30 anti-sprawl).
- **Not done (deliberately):** `distribution-plan.md` not yet updated to record the marker mechanism / the optional-command tier (maintainer reviewing the code first). D5 (other agents) unchanged — each still needs its own format-verified adapter; the marker mechanism is agent-agnostic and will carry over.
- **Open (carried):** live-invoke confirmation; D5; W38 workflow fixes; separation-of-duties owner; copied-README dangling links; `distribution-plan.md` write-up of the optional-command mechanism (above).

### W43 — 2026-06-10 — End-to-end dogfood on a new use case (document ingestion); surfaced the design→build gap; hardened the tool + docs
- **Trigger:** maintainer asked to exercise the full `/graph-requirements → /graph-design → /graph-build` pipeline on a use case deliberately different from Customer 360 and complex enough to stress the framework. Chose **document ingestion & enrichment** (paginated manifest → per-document fetch/enrich/index → aggregated report) to exercise loops, per-item iteration, multi-way routing, and per-item failure isolation. Scope: this repo's `llms/graph-builder/` tree only (plus installing the five `/graph-*` thin-pointer commands under `.claude/skills/`); the legacy `.graph-builder/` tree untouched.
- **Phases run:**
  - `/graph-requirements` → `examples/document-ingestion-requirements-brief.md`. `/graph-verify` on the brief (fresh-context reviewer, engine + source) **refuted** the load-bearing claim that a `for_each` fetcher isolates per-item failures — source shows all iterations share one `.status`/`.error` (`GraphApiFetcher.java:280-282`) and a single un-handled failure aborts the graph (`GraphTraveler.java:130-135`). Re-filed as a **design-blocking** open question; gate downgraded.
  - `/graph-design` → `examples/document-ingestion-design-spec.md`. Resolved isolation by **architecture** (contain failure inside a per-doc unit that returns `status:"failed"` as data, never an error status).
  - `/graph-build` (live engine, session `ws-970252-2`, stub `examples/doc-ingest-stub.mjs` on :8099) → **central finding: `graph.extension` targets aren't runtime-deployable via the Companion API** (`GraphExecutor.getGraphModel` reads the read-only deployed classpath; cf. minigraph-syntax.md:313 + customer-360 rows 39/44). Maintainer directed a pivot to **island/fetcher composition per tutorial-3/6** (design revision v2). Built ~25 nodes; **verified live: the bounded RESET pagination loop terminates, and the per-document sequential RESET loop isolates a failing doc** (`d-fail` → `status:"failed"`, batch continued, `output.body.documents` = 5). Enrich/index/routing/aggregation + index-retry loop **not** built.
- **Meta-retrospective (`build-phase-findings.md`):** why build surfaced what design didn't. Root cause isn't "design ignores build constraints" per se but that **pattern knowledge and constraint knowledge are stored separately from runnable proof** — source-fluency without idiomatic fluency; the extension deploy-constraint lived in the syntax/evidence docs not the design primitives table; `/graph-verify` ran on requirements, never on the design that introduced the risky primitive. Anti-anchoring rule distilled: record the *mechanism + failure mode*, proven by the smallest domain-neutral graph, not a use-case recipe.
- **Action plan + independent review (`build-phase-actions.md`, A1–A5):** fresh-context adversarial review (checklist, authorship withheld) cut/scoped the plan — corrected A1's failure mode + deleted an undeliverable fallback, cut a redundant A2 row, cut A4's per-primitive "buildability" box as ritual, scoped A5 from a 6-card library to one card. Dispositions applied.
- **Shipped (verified live; sessions `ws-970252-2` build + `ws-697490-4` probes):**
  - **A1 — `companion.mjs` verifies properties applied, not just node presence.** A malformed property line is "accepted" but aborts the whole property block, leaving a hollow node (`properties:{}`) that presence-check passed. Added `parseSentProperties` + `checkProps` (normalize to engine's stored form: `'''` → inner lines `\n`-joined+trimmed; single lines verbatim) in `verifyMutation` — hard-fail on dropped key/entry, warn on present-but-non-verbatim. Tests: broken node now exits non-zero; valid passes "N properties applied"; no-property nodes + `connect` regress clean.
  - **A3 — four scenario-stripped footgun callouts in `minigraph-syntax.md`:** `:boolean(null=true)` asymmetry (coerce with `f:defaultValue`); loop must RESET its own re-entry head; `next` fans out to **all** natural edges; `int()`/`text()` in an IF/COMPUTE aborts (`Attempting to call a non-function`; use bare literals). Corrected line 432's "presence proves it took effect" overclaim.
  - **A4 — design→build gap closed in `graph-design.md`:** **verify-the-design norm** in D11 (run `/graph-verify` on the design, not only requirements, when it introduces a primitive the brief didn't name) + cross-linked the `graph.extension` runtime-deploy constraint from the primitives table. No per-primitive gate box (review cut it as ritual; existing gate already fails un-surfaced build blockers).
  - **A2 — reversed.** Initially wrote a standalone `evidence/dogfood-doc-ingest.md`; maintainer flagged it redundant with the A3 callouts and an unmaintainable file-per-change precedent. **Deleted it** — generalizable rules live in `minigraph-syntax.md` (A3); provenance lives here. Verified facts: RESET pagination loop terminates; loop-head self-RESET; per-item isolation via exception-as-data; `next` fan-out; `int()`-in-IF abort; `:boolean(null=true)` asymmetry; `'''`-statement normalized storage; malformed-line drops the whole property block (motivated A1). The A3 callouts cite `(verified, session ws-697490-4)` inline; this entry is their provenance (one log, not a file per change).
- **Open (carried):** ~~**A5** not done~~ → **A5 DONE (same day):** `patterns/per-item-isolation.md` (verified, session `ws-969255-6`) + `patterns/README.md` intent→exemplar index + a `graph-design.md` cross-link. **All of A1–A5 are now complete.** Prior carried items unchanged (D5; W38 workflow fixes; separation-of-duties owner; copied-README dangling links; `distribution-plan.md` write-up).
- **Closing scrub (same day, maintainer-directed):** the half-built doc-ingest graph was **abandoned** (the build was a vehicle to surface the enhancements, not a deliverable). Cleared: live graph (session `ws-970252-2` reset), stub process (:8099 killed). **Deleted:** `examples/document-ingestion-requirements-brief.md`, `examples/document-ingestion-design-spec.md`, `examples/doc-ingest-stub.mjs` (the per-item-isolation card was made self-contained first — it now carries its own inline stub, no external dependency). The doc-ingest **smoke run** (enrich/index/routing/aggregation + index-retry) is therefore **dropped, not carried** — a fresh run starts from the enhanced docs/tool, not this graph.
- **Findings/actions docs folded in then deleted (maintainer-directed):** `build-phase-findings.md` and `build-phase-actions.md` were the retrospective + the A1–A5 plan; their substance is now in this entry, so they were deleted. (They were untracked/new this session, so deletion is permanent — folding into this log first was the deliberate preservation step, not git.) The durable lessons they carried, beyond the A1–A5 changes above:
  - **Root cause of the design→build gap:** pattern knowledge and constraint knowledge were stored *separately from runnable proof, and from each other* — the agent had source-fluency (could read the engine) but not idiomatic fluency (didn't reach for the tutorials' island/fetcher idiom), and the one fatal constraint (`graph.extension` not runtime-deployable) lived in the syntax/evidence docs, not the design primitives table. Both symptoms ("design ignores build constraints" and "not fluent in patterns") share that one root.
  - **Anti-anchoring rule for future docs:** record the *mechanism + its failure mode*, proven by the smallest domain-neutral graph that exhibits it; if an agent in an unrelated domain can't reuse the note verbatim, it's anchored too tightly. (This is why generalizable rules went to `minigraph-syntax.md` and the isolation *pattern* to `patterns/`, not to a use-case writeup.)
  - **Not-yet-built ideas worth carrying** (were A4/A5-adjacent, deliberately deferred — W30 anti-sprawl): (1) a **probe catalog** — domain-neutral cheap live experiments that let an agent *earn* fluency on demand (generalizes the ad-hoc `/build` "Step 0" capability-probe into a norm); (2) a fuller **capability/constraint manifest** per primitive (runtime/deploy/encoding constraints visible at selection time — the `graph.extension` cross-link is the first instance, not the whole manifest); (3) **expand `patterns/`** only as genuine tutorial-coverage gaps appear (not a re-authoring of what tutorials prove).
