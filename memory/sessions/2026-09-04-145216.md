# Session (2026-09-04T14:52:16.000Z)

**Agent:** Claude Code

## What happened

Wrote the milestone story deliverables Eric asked for at the close of the ai-enabled-repo-demo
round: **the Mercury white paper and a presentation deck**, placed front and center on the docs
site per his direction ("just like the agent-memory mkdocs site"). Branch `docs/ai-grammar-story`;
PR gated by Eric as usual.

- **White paper** — `docs/ai-grammar-methodology.md` ("The Mercury Story — Origin, Evolution, and
  the AI Grammar Methodology"). Origin (2018, never couple functions; actor lineage; virtual
  threads removed the ergonomics tax) → the ascent (code → configuration → knowledge) → the turn
  (change the artifact the AI authors; governed nondeterminism) → the AI grammar defined
  (sufficient / verified / cheap) and inventoried → the benchmark with the measured numbers
  (~2.5k-token map → ~46k of verified reference vs ~515k of source) → **the methodology** (Eric's
  three steps, with the recursion: building blocks compile their own grammar, so grammar composes
  the way dependencies compose; twin-kafka as the in-repo proof and the week's cautionary tale) →
  industry-practice grounding (llms.txt, docs-as-code→contract, ADRs, golden-vector contract
  testing, agent-instruction files, HITL governance, evaluation culture) → Blueprint outlook.
  The distilled thesis: *to an AI partner, undocumented capability is absent capability.*
- **Deck** — `docs/presentations/ai-grammar-story.html`, 14 slides, self-contained single-file
  HTML (Eric's call: HTML, not pptx, so mkdocs serves it verbatim). Keyboard/click/hash
  navigation, per-slide deep links, print flips to a light handout. Two hand-authored SVG
  diagrams (the three-layer ascent; the grammar-composition recursion). Committed dark violet
  look with amber reserved for measured numbers; Fraunces/Instrument Sans. Published as an
  artifact for immediate review; the site copy is the durable home.
- **Home page** — `docs/index.md` gains "The story and the method" feature block right under the
  newcomer pointer; white paper added to nav under Orientation. Canon check green.

Craft notes: the one-look review caught a real defect — the recursion diagram's amber label
collided with the "user application" box — fixed in the single edit pass; an apparent
"all slides black" scare was only the entry animation's first frame, confirmed with one wait.

Content discipline: no client-identifiable material; no Streamable-HTTP protocol claim
(deliberately absent); graph.js never cited as current; canon vocabulary throughout (layers =
the three paradigm layers; AKG/MiniGraph/paradigm naming; one atom, four roles).

### Review round (same session)

Eric reviewed both and approved, with one wording fix (exercise → exercises — plural is accurate:
rehearsals + two validation rounds + the live demo; commit `87f2e9e7`; the blind replace was
caught making the sources footnote ungrammatical and repaired). He then asked for **industry and
academic sources** behind the best-practice claims. Nineteen references added (commit `b3e54721`),
**every one verified by web search before citing** — venue, year, identifier; one arXiv id I had
written from memory (SWE-bench) was checked before it shipped. Load-bearing additions: the
governance problem now cites Pearce (IEEE S&P 2022), Perry (ACM CCS 2023) and DORA 2024–25
(AI adoption ↔ reduced delivery stability); the context problem cites Lost in the Middle
(TACL 2024); the founding rule is placed in its lineage (Parnas 1972, Hewitt 1973); each §6
practice carries its canonical source; and a new §6 bullet ties the AI grammar to the industry's
2025 spec-driven-development turn (GitHub Spec Kit). Deck slide 13 gained a compact source line.

### Site preview + hero round (same session)

Rendered the site locally for Eric's preview (scratchpad venv, `mkdocs build --strict` green,
served through the preview pane; note: `site_url` carries a path, so mkdocs serve mounts at
`/mercury-composable/` — deep links need the prefix, faithful to gh-pages). Verified the home
page, the white paper page, and the deck through the site. Eric then pointed at the agent-memory
home page as the model; `docs/index.md` reshaped to the same hero treatment — Get started
(primary) + Read the white paper + View the deck buttons (`.md-button` via attr_list), replacing
the bulleted feature section (commit `9f91d9b4`). Nothing preview-related is
committed: venv in scratchpad, `site/` and `.claude/launch.json` gitignored.

### The contract gate fired (same session) — and proved the paper's point

PR #317's first CI run FAILED in `Build & Unit Tests`: the `ai-contract-provider` **packages
`docs/index.md` into the mercury-platform skill** (pom copy → `skill/references/index.md`,
inventory pinned by `files.list` + `SkillSnapshotTest`) and validates every relative link against
the packaged set — the new hero buttons' relative links to the white paper and deck broke it.
`Broken relative link in references/index.md -> ai-grammar-methodology.md`. The white paper's own
docs-as-contract thesis, enforcing itself against the PR that states it.

Fix (commit on the branch): the two hero links went **absolute site URLs** — deliberately NOT
packaging the story into the skill, which would spend agent-contract tokens on human-facing
narrative against the paper's own benchmark; the skill stays curated, and inside the offline
skill an absolute link to the site is the right behavior. `Get started` stays relative because
`guides/**` IS packaged. Re-verified all three gates locally before pushing.

**Durable lesson: a "docs-only" change in this repo is not Java-free.** `docs/index.md`,
`docs/guides/**`, `arch-decisions/ADR.md` and two test reports are build inputs to
`system/ai-contract-provider`, whose tests validate packaged-set link integrity. The validation
command for any PR touching those paths includes `mvn test -pl system/ai-contract-provider` —
mkdocs `--strict` and the canon check do not cover the packaged-skill view of the same files.

### Model-signature clarification (Eric's question, same session)

The `Co-Authored-By` trailer is **self-reported text**, not a platform stamp: the harness names the
current model each request and the agent types the trailer. This session ran Opus 4.8 during
yesterday's stretch (trailers correct then), was switched back to Fable 5 this morning — and the
agent kept copying the session's established "Opus 4.8" trailer instead of re-reading the live
directive, so today's five story-branch commits are Fable 5's work signed Opus 4.8. Eric reviewed
the explanation and is fine with the signatures as they stand; no history rewrite. Practice going
forward: **read the trailer name from the current directive at each commit, never from the
session's earlier commits** — and note the repo's dedup rule (one trailer per collaborator) is
about duplicates within a commit/PR, not a license to freeze a stale name.

## Memory References

- Referenced: vision-mercury-composable, compilegraph-mandatory-gate, event-script-over-code,
  functions-decoupled-routes, virtual-threads-rpc, conv-no-streamable-http-claim,
  graphjs-phase-out-direction, polyglot-event-over-http-design, playground-session-broker,
  bp-agent-orchestration, eric-release-rhythm, conv-telemetry-presentation-parity
