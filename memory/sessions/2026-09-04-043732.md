# Session (2026-09-04T04:37:32.000Z)

**Agent:** Claude Code

## What happened

Ran the `REVIEW.md` memory review ritual on Eric's request. The cadence trigger had fired exactly
(10 sessions since `last_review`, `review_every: 10`), and the invariant cadence was well past due
(48 sessions since `2026-08-21-005515.md`, `verify_invariants_every: 40`).

Steps 2–3 (metadata) and 4–5 (archival) were run with the deterministic skills rather than by hand
— `refresh-metadata` recomputed 13 footers, `archive-fact` performed the moves. Step 6 verified
with `memory-lint`: no over-archival, and no id living in both `continuity.md` and the archive.

Result: continuity 345 → 327 lines, open threads 23 → 19, and `memory-lint` from 16 warnings to
**0 errors / 0 warnings**.

## Memory Review

**Metadata refreshed (13):** `polyglot-event-over-http-design`, `compilegraph-mandatory-gate`,
`conv-telemetry-presentation-parity`, `graphjs-phase-out-direction`,
`conv-squash-title-prefill-check`, `snyk-retired-manifest-placeholders`, `eric-release-rhythm`,
`ot-agent-orchestration-e0`, `ot-json-plugin`, `thread-polyglot-initiative` (all active →
archive-candidate); `playground-session-broker` (working → active, uses 1→4);
`conv-no-streamable-http-claim` (working → active, uses 1→2); `bp-agent-orchestration` (uses 9→10).

**Archived — faded, not referenced in over `archive_window` (20) sessions (5):**
`graph-suspend-resume-design` (fact, sslu 26 — the ADR-0010 suspend/resume design; retrievable via
`archive/INDEX.md`), and four completed `- [x]` threads already condensed to stubs:
`ot-progressive-rendering-v4120` (sslu 21), `thread-ai-contract-provider` (29),
`thread-field-scan-remediation-20260825` (27), `thread-sonar-4-11-x-field-round-3` (28).
All → `memory/archive/2026-Q3.md`.

**Reactivated:** none. **Superseded:** none.

**Contradiction / altitude scan:** no conflicts found. The Blueprint items still serve
`vision-mercury-composable`; the `(blueprint)` threads all trace upward cleanly.

**Invariant re-verification: DONE — all 15 confirmed by Eric, plus the Vision.** Raised
`ot-reverify-invariants-20260904`, then walked every fact one-by-one against the live tree
(Eric's request) rather than listing them: 3 Architectural Invariants, 5 Stack & Tools, 7 core
Key Decisions/Conventions. No supersessions — nothing drifted since 2026-08-21. The only fact
touched by shipped work was `stack-messaging-kafka`, and PR #315 added Kafka config keys
additively without changing its substance. Thread closed; `last_invariant_check` stamped to
2026-09-04 | 2026-09-04-043732.md.

Evidence that mattered, for the next walkthrough: `virtual-threads-rpc` looked thin on a
`startVirtualThread` grep (one call site) — the real dispatch is
`Executors.newVirtualThreadPerTaskExecutor()` (Platform.java:71) plus a per-route
`Thread.ofVirtual()` (ServiceQueue.java:121). **Probe the mechanism, not the idiom.**
`event-script-over-code` was verified at the exact line the fact warns about —
`Boolean.TRUE.equals(decisionValue) ? 1 : 2` (TaskExecutor.java:724), so `true`=1=first still
matches the engine rather than the derived docs that once had it inverted.

**Stamped:** `last_review` → 2026-09-04 | through `2026-09-04-040305.md`.

## Memory References

- Created: ot-reverify-invariants-20260904
- Referenced: vision-mercury-composable
