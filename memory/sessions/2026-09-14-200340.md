# Session (2026-09-14T20:03:40.000Z)

**Agent:** Claude Code

## Drafted the distributed-cache design spec

Wrote `draft-design-specs/distributed-cache.md` at Eric's request — a generic Redis-backed L2
cache as a SEPARATE composable module that reuses sync-over-async's Redis client layer, keeping
sync-over-async small ([[cache-separate-from-soa]]). Design-first: the spec ends with open
questions Q1–Q8 for Eric to rule (module home/name, extract the shared client layer now,
value type = opaque bytes, route name, v1 op set, key namespacing, consumer helper, Rust
follow-up), matching the minimalist-kafka Q-series rhythm.

- Module boundary: the action function (PUT/GET/MGET/DELETE/PUT_IF_NOT_PRESENT + PING) + config
  + `redis.health` (the reserved route) IN; the shared RedisBackend/RedisConfig/factory/health
  extracted to a prefix-parameterised `redis-connection` foundation; the rendezvous protocol
  stays OUT (the cache never imports sync-over-async).
- The field's two consumption patterns are both "a composable function": L1 PostOffice, L2
  Event Script task + output mapping, L3 graph.task.
- Cluster-safe op mapping documented: atomic SET NX EX for put-if-absent; Lettuce cluster
  scatter-gathers cross-slot MGET for free.
- Compartmentalized: generic "a field installation asked" framing; no client/secrets-manager
  names in the spec.
- No continuity fact change (the decision predates this); no open thread yet (proposal awaiting
  Eric's rulings). Spec is a journal artifact (draft-design-specs/README).

## Memory References
- cache-separate-from-soa — consulted (the module boundary the spec elaborates)
- soa-redis-cluster-support — consulted (the Redis client layer to be shared + prefix namespacing)
- vision-mercury-composable — consulted (lean, decoupled modules)
