# Session (2026-08-25T21:37:03.000Z)

**Agent:** Claude Code

**AI agent orchestration — strategy round; blueprint OPENED (Eric's explicit ask).**
Eric, from his AI workshop, raised the idea: "graph engineering" is being discussed as
the next iteration of the "agent loop" — can the Active Knowledge Graph be the vehicle,
i.e. an AI agent orchestrator? ("all we need is a wrapper composable function (or graph
task) to send a prompt to an agent" — then "plus wrapper to request MCP service, etc.")
Assessment delivered; direction RATIFIED in-conversation ("good idea — please draft the
design concept doc and create one blueprint thread. We can do some experiments after the
workshop.").

**Thesis captured:** MiniGraph already IS a graph-engineering runtime — the capability
mapping is near one-to-one (explicit graph + CompileGraph gate; decision routing on LLM
verdicts; wait-loop RESET = the visible agent loop; suspend/resume = production-proven
human-in-the-loop; composable functions = the tool substrate; virtual threads = slow
LLM I/O economics; x-ttl deadline propagation; mock-by-config = token-free dry-runs).
The only missing piece is the AI node type, and the just-shipped polyglot wrappers make
it a user-land function — zero engine change. Enterprise pitch: agent orchestration is
just another workload for the same governed runtime ("governed nondeterminism", never
determinism claims). MCP leg: official MCP SDKs are Python/TypeScript = the two shipped
wrappers; preferred shape route-per-tool with an explicit allowlist (the allowlist is
the certification artifact); reverse direction = Mercury as MCP server via a thin facade
over ai-contract-provider discovery + the Event API. Engine core stays LLM/vendor-free
(kafka-mesh-opt-in posture); wrapper scope fence intact — adapters are functions ON the
wrappers, so no Rust lock-step until/unless a built-in skill emerges.

**Deliverables:**
- Concept doc: `draft-design-specs/ai-agent-orchestration.md` (gitignored, like prior
  specs) — thesis, capability-mapping table with honest-evidence discipline (flows→python
  proven live 2026-08-22; graph.task→wrapper pinned v4.11.11 but NO live drive yet; no
  LLM/MCP call has run through Mercury — experiments produce the evidence), architecture
  (three adapter families: llm.chat / MCP route-per-tool / agent-as-node; + Mercury as
  MCP server), governance posture, open questions Q1–Q8 (dynamic dispatch, llm.chat
  contract, MCP registration timing, audit/PII, context economics, adapter home,
  MCP-server facade, streaming-to-edge), experiment plan E0–E4 (E0 = llm.chat in the
  mercury-python demo app + a bounded-agency demo graph — hours, and incidentally the
  first live graph.task→wrapper drive).
- Blueprint opened: `bp-agent-orchestration` (human-gated open — Eric's explicit
  direction). Distinct from bp-ai-companion-llm-backend (design-time AI co-authoring)
  — this is run-time AI orchestration; the two compound.

Next step (post-workshop, Eric's go): run E0.

## Memory References
- Created: bp-agent-orchestration (tier: working)
- Referenced: vision-mercury-composable, bp-ai-companion-llm-backend,
  bp-graph-governance-lifecycle, bp-polyglot-functions,
  polyglot-event-over-http-design, graph-suspend-resume-design,
  compilegraph-mandatory-gate, kafka-mesh-opt-in, graphjs-phase-out-direction,
  conv-telemetry-presentation-parity, thread-ai-contract-provider,
  thread-polyglot-initiative, thread-tutorial-14-decision,
  thread-dynamic-statement-targets, thread-graph-task-model-staging,
  virtual-threads-rpc, functions-decoupled-routes
