# Session (2026-06-22T19:52:02.000Z)

**Agent:** Claude Code
**User:** Milestone batch — finish Layer-2 (overview at the section index), apply polish, remove all redirects.

## What We Did (milestone — committed + pushed)

Continuation of the layer-standardization reorg, plus a clean-rewrite decision.

- **Fixed a routing bug** (earlier in the session, committed separately as `4ac1ef21`): 8 `mkdocs-redirects`
  entries were pure *case* renames whose stubs overwrote the real page on case-insensitive filesystems
  (macOS / `mkdocs serve`). Dropped them; added a `check-doc-canon.py` guard.
- **Polish:** Layer-2 sidebar parity + cross-layer "ascent" See-also links on the Layer 1 & 2 overviews.
- **Layer-2 overview → section index (Option X):** promoted the overview to `event-script/index.md`
  (H1 "Composable Orchestration"), moved the deep syntax to `event-script/syntax.md`, and refactored
  ~30 inbound links + README + llms.txt + nav. Now every layer's overview sits at the section root
  (`/guides/event-script/`, `/guides/knowledge-graph/`), matching Layer 3. Eric verified in a browser.
- **Removed ALL redirects** (`docs-no-redirects`): clean rewrite, no backward-compat. Dropped the
  `redirects` plugin from `mkdocs.yml` + `mkdocs-redirects` from CI; updated the canon page to say so.
  Old URLs now 404 by design.

Verified throughout: `mkdocs --strict` 0 warnings, canon + 3 grammar checks pass, no redirect stubs,
deep-link anchors (`#tasks-and-data-mapping`) still resolve on `syntax.md`.

## Decisions Made

- **`docs-no-redirects`** — clean rewrite; navigation is the source of truth; no URL backward-compat.
  Reverses the redirect-as-safety-net aspect of `docs-style-conventions` / `docs-content-canon`.

## Context for Next Session

- **Open (Eric's call):** Layer 1's overview is a flat page (`event-driven-foundation.md`), not a section
  folder. Fold Layer 1 into `guides/event-driven/` (index.md + function-execution + rest-automation) for
  full parallelism with Layers 2 & 3? Bigger refactor (CHAPTER-2/3 links). See `thread-layer-reorg`.
- PR still on hold until the full rewrite lands.

## Memory References

- Referenced: `docs-content-canon`, `docs-style-conventions`, `thread-layer-reorg`, `stack-docs-mkdocs`
- Created: `docs-no-redirects` (working)
- Superseded: redirect aspect of `docs-style-conventions` / `docs-content-canon` → `docs-no-redirects`
