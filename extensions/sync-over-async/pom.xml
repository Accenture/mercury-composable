<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.platformlambda</groupId>
    <artifactId>sync-over-async</artifactId>
    <version>4.12.11</version>
    <packaging>jar</packaging>
    <name>Sync-over-async (request/reply and streaming for async backends)</name>
    <description>
        A self-initializing extension that exposes synchronous REST semantics over asynchronous
        processing across horizontally scaled pods, and generalizes the same rendezvous to a streaming
        return route for cross-pod progressive rendering. Redis carries the return route: segments are
        appended to a per-correlation-id rendezvous queue (a Redis List, stored before any signal) and a
        per-pod Pub/Sub channel wakes the originating pod, which drains the queue destructively. A
        one-shot response is the degenerate stream; transport-neutral by composition (platform-core +
        Lettuce only).
    </description>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
    </parent>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>21</java.version>
        <netty.version>4.2.17.Final</netty.version>
        <jackson-2-bom.version>2.22.2</jackson-2-bom.version>
        <jackson-bom.version>3.2.2</jackson-bom.version>
        <log4j2.version>2.26.1</log4j2.version>
        <!-- Security override for the transitive httpcore5/httpcore5-h2 (Snyk HIGH, CWE-770) pulled
             in via minimalist-kafka's Confluent registry client - a test-classpath path here now that
             minimalist-kafka is test scope, kept patched all the same. Spring Boot's dependencyManagement
             pins the vulnerable 5.4.2 (management beats nearest-wins), so each module resolving the
             Confluent chain must override the property itself - see minimalist-kafka's pom. -->
        <httpcore5.version>5.4.3</httpcore5.version>
        <!-- Embedded broker (kafka_2.13) version for tests. Matches the kafka-clients version pinned by
             minimalist-kafka so there is no client/broker protocol skew. -->
        <kafka.version>4.3.1</kafka.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.projectreactor</groupId>
                <artifactId>reactor-bom</artifactId>
                <version>2025.0.7</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Converge Guava to platform-core's version: Confluent's serdes (via the test-scoped
                 minimalist-kafka) otherwise win with an older 32.0.1 on the test classpath, leaving
                 the tests on a different Guava (and failureaccess) than the runtime. -->
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>33.5.0-jre</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo1.maven.org/maven2/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- The facade is transport-neutral: production code speaks only the module's self-contained
             correlation-id key (SyncRuntime.CID) and the platform-core function contract, so the
             application declares its own Kafka library next to this extension (minimalist-kafka;
             twin-kafka for a second cluster). platform-core supplies the function runtime here. -->
        <dependency>
            <groupId>org.platformlambda</groupId>
            <artifactId>platform-core</artifactId>
            <version>4.12.11</version>
        </dependency>

        <!-- The shared Redis client layer (standalone/cluster seam, config, auth, health probe). This
             module is a consumer of the foundation: it programs against RedisBackend<String> and binds the
             health probe to soa.redis.health. lettuce-core is still declared directly below because the
             return-route engine uses the Lettuce API (RedisClient, Pub/Sub, StringCodec) beyond the seam. -->
        <dependency>
            <groupId>org.platformlambda</groupId>
            <artifactId>redis-connection</artifactId>
            <version>4.12.11</version>
        </dependency>

        <!-- Kafka building blocks for the end-to-end regression only (notification function + flow
             adapter, bringing event-script-engine and the Kafka client onto the test classpath).
             Test scope keeps the published artifact free of any transport dependency. -->
        <dependency>
            <groupId>org.platformlambda</groupId>
            <artifactId>minimalist-kafka</artifactId>
            <version>4.12.11</version>
            <scope>test</scope>
        </dependency>

        <!-- Guava's failureaccess companion is required at runtime by Guava itself (arriving with
             platform-core; Confluent's serdes exercise the same Guava LocalCache path on the test
             classpath). Declared explicitly so it is a first-class classpath entry and cannot be
             dropped by strict/curated dependency resolution in the field. -->
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>failureaccess</artifactId>
            <version>1.0.3</version>
        </dependency>

        <!-- Reactor-native, battle-tested Redis client with robust Pub/Sub + auto-reconnect.
             Version is managed by the spring-boot parent BOM. -->
        <dependency>
            <groupId>io.lettuce</groupId>
            <artifactId>lettuce-core</artifactId>
        </dependency>

        <!-- Embedded Redis (real redis-server binary, incl. arm64 macOS) for unit tests -->
        <dependency>
            <groupId>com.github.codemonstur</groupId>
            <artifactId>embedded-redis</artifactId>
            <version>1.4.3</version>
            <scope>test</scope>
        </dependency>

        <!-- Embedded Kafka (real KRaft broker) for integration tests. Reuses the kafka-standalone
             pattern; same exclusions to keep the transitive footprint lean. -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka_2.13</artifactId>
            <version>${kafka.version}</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>at.yawk.lz4</groupId>
                    <artifactId>lz4-java</artifactId>
                </exclusion>
                <!-- CVE-2026-90559 (out-of-bounds write in Snappy.uncompress(ByteBuffer, ByteBuffer),
                     CVSS 8.7): NVD records snappy-java as affected "through 1.1.10.8", which is the
                     NEWEST release (2025-07-19) - there is no version to upgrade to, so the codec is
                     excluded like lz4 above rather than pinned. Mercury configures no compression.type,
                     so nothing here produces snappy-compressed records; an application that produces
                     or consumes snappy-compressed records must declare snappy-java itself. Drop this
                     exclusion once a fixed snappy-java is released. -->
                <exclusion>
                    <groupId>org.xerial.snappy</groupId>
                    <artifactId>snappy-java</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-collections</groupId>
                    <artifactId>commons-collections</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-io</groupId>
                    <artifactId>commons-io</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-beanutils</groupId>
                    <artifactId>commons-beanutils</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-digester</groupId>
                    <artifactId>commons-digester</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.junit</groupId>
            <artifactId>junit-bom</artifactId>
            <version>6.0.3</version>
            <type>pom</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
                <executions>
                    <execution>
                        <id>default-compile</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>default-testCompile</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>java-compile</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <compilerArgs>
                                <arg>-proc:full</arg>
                                <arg>-Xlint:deprecation</arg>
                            </compilerArgs>
                        </configuration>
                    </execution>
                    <execution>
                        <id>java-test-compile</id>
                        <phase>test-compile</phase>
                        <goals>
                            <goal>testCompile</goal>
                        </goals>
                        <configuration>
                            <compilerArgs>
                                <arg>-proc:full</arg>
                                <arg>-Xlint:deprecation</arg>
                            </compilerArgs>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.2</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-install-plugin</artifactId>
                <version>3.1.4</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.4</version>
                <configuration>
                    <!-- One JVM per test class: two classes boot the whole application (AutoStart with
                         REST automation on a fixed port) - RestFlowMvpTest and StreamingRestE2eTest -
                         and the platform is a per-JVM singleton, so they must not share a fork. -->
                    <reuseForks>false</reuseForks>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.14</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>check-coverage</id>
                        <phase>test</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.85</minimum>
                                        </limit>
                                        <limit>
                                            <counter>INSTRUCTION</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.85</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
