# Session (2026-09-14T05:28:09.000Z)

**Agent:** Claude Code

## What happened

**The field CI's second blocker cleared — two Snyk Code (SAST) hardcoded-
password findings** (CWE-798, policy-escalated from low to High, score 204),
both in `RedisHealthCheckAuthTest.java`. Branch
`fix/snyk-code-hardcoded-test-credentials`.

The two literals were test fixtures for the late-credential semantics (the
embedded requirepass Redis): the server's fixture credential and the
deliberately-wrong companion in the WRONGPASS scenario (values not repeated
here - the guard below is right that logs need no credential shapes). The fix removes the CLASS, not just
the findings: the fixture credential is now **generated per test run**
(`Utility.getInstance().getUuid()`) — the embedded server is started with
that value, so it is authoritative by construction — and the wrong credential
is **derived by suffix** (`PASSWORD + "-stale"`), guaranteed unequal and
still not a literal. No credential literal remains anywhere in the repository
(repo-wide sweep confirmed these were the only two). Auth test 2/2 with the
NOAUTH and WRONGPASS classifications visibly firing; extension suite green.

Twin note: the Rust port's `redis_health_check.rs` carries the same ported
fixture literal — fixed in the Rust repo in the same round (its own branch).

## Memory References

- field-sonar-gate-covers-overall-code (consulted - the field's Snyk Code
  policy escalates low-severity findings on the WHOLE codebase, test trees
  included; fixture credentials must be generated, never literal)
- eric-release-rhythm (consulted - branch prepared and pushed; PR creation
  and merge are Eric's gates; the field redeploy follows his merge)
- conv-squash-title-prefill-check (consulted - PR title on its own line)
