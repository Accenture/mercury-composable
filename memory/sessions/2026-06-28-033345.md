# Session (2026-06-28T03:33:45.000Z)

**Agent:** Claude Code

## Summary

**Upgraded agent-memory v4.22.4 → v4.23.0** (Mode B, single rung, by Claude Code from the tool checkout).
Stacked onto the still-uncommitted →4.22.4 upgrade at the maintainer's direction — both land in one commit.

- **Installed the `harvest-knowledge` built-in skill** (`agent-skills/harvest-knowledge/SKILL.md`,
  `provenance: agent-memory-builtin`, no-code/agent-run) — the on-demand, recurring counterpart to the
  enable-time curious harvest. Re-scans this repo's human-authored docs and folds newly-durable facts into
  `memory/` additively (map-don't-mirror; conflicts → `Contradiction`). Not a vendor `/init`.
- Re-synced adapters: now **5 skills → 30 adapters** (gitignored); `harvest-knowledge` 6/6.
- Stamped `.agent/version.md` → **4.23.0** (enabled_with 4.14.1, mode A preserved), safe read-then-write.

**Why this is a good fit for mercury:** it's a living-docs repo (`docs/arch-decisions/`, `guides/`,
`draft-design-specs/`) — exactly the case `harvest-knowledge` exists for. The skill is now available
(`/harvest-knowledge` or by description) to fold new ADRs / design specs into memory on demand.

**Pending:** working-tree changes (4.22.4 + 4.23.0) remain **uncommitted** — review + commit at your
discretion. This repo's own **memory review** is still due (its decay backlog).

## Memory References
- (none — tool-layer upgrade; no project fact created/changed)
