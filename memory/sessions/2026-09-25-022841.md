# Session (2026-09-25T02:28:41.000Z)

**Agent:** Claude Code

## Summary

**The invariant re-verification walk (the gate [[reverify-invariants-20260925]] raised by the 2026-09-25 review; cadence 40).**
Twenty never-decay facts listed one by one with the claim, live-tree evidence and a read; Eric's ruling: **"the rest holds"**
for 19, and **`instant-serialization` demoted from core** — "an implementation detail that is not influencing key decisions
going forward". Evidence walked: `.java-version` 21 and both CI jobs on 21 with Java 25 recommended in Getting Started
(`stack-language-java21`); 35 reactor modules and three template Gradle builds (`stack-build-maven`); only `rest-spring-4` in
the reactor on Spring Boot 4.1.1, `rest-spring-3` a `packaging=pom` placeholder with relocation (`stack-integration-spring-boot4`);
the six Kafka directories and msgpack in platform-core (`stack-messaging-kafka`); three workflows (`stack-ci-gha`);
`@PreLoad(route)` + `PostOffice.request` + ADR-0001 (`functions-decoupled-routes`); `inputPojoClass` on `PreLoad`, the `ALL = "*"`
passthrough in `TaskExecutor`, ADR-0003 (`typed-io-map-or-pojo`); `Thread.ofVirtual()` in `Platform` and `ServiceQueue`, ADR-0024
(`virtual-threads-rpc`); `EventEmitter` keyed on `threadId()`, `WorkerHandler.stopTracing`/`applyTraceContext`, both regression
tests (`trace-thread-keyed-mono-gotcha`); the starters' `cloud.connector=none` and ADR-0006 (`kafka-mesh-opt-in` — the in-code
default not shown by grep, confirmed by Eric); `handleDecisionTask` and ADR-0007 (`event-script-over-code`); the 404 path in
`EventApiService` and the `x-event-api` marker in three classes (`event-api-local-routes-only`); the pattern in
`instructions.md` (`conv-add-capability`); `str2int`/`str2long` in `Utility` (`conv-serialization-gotchas`); the protocol's
edit-or-closure rule (`conv-declare-consulted-references`); `RFC.md` with `RFC-NNNN` packaged beside `ADR.md`
(`conv-proposals-not-in-adr-ledger`); tonight's four PRs and five direct memory commits (`eric-release-rhythm`,
`eric-code-changes-via-pr`); the two-track Vision text in place with Track 1 shipping a field round this week and
`bp-agent-orchestration` the sole Blueprint gap (`vision-mercury-composable`). **Mechanics of the demotion:** the tier is a
human call the arithmetic then completes — `instant-serialization` has no session reference since 2026-06-27, so once it is
not `core` it is already past `archive_window` and archives as faded in this same review (retrievable via the archive INDEX;
the behaviour it records — `Instant` first-class in all three serialization paths, millisecond ISO-8601 — is code and
tests, not a decision anyone consults). Core 19 → 18; `last_invariant_check` stamped COMPLETE; the gate thread closed;
`vision.md`'s header notes the re-confirmation.

## Memory Review

- Archived (faded, after Eric's demotion): instant-serialization — `tier: core` → `archive-candidate` with the demotion note,
  then `archive-fact --reason faded` (unreferenced since 2026-06-27).
- Invariant re-verification: COMPLETE 2026-09-25; 18 core facts + the Vision confirmed; next due at +40 sessions.

## Memory References

- reverify-invariants-20260925 (closed — walked; every listed fact dispositioned)
- functions-decoupled-routes (re-confirmed by Eric at the invariant walk)
- typed-io-map-or-pojo (re-confirmed by Eric at the invariant walk)
- virtual-threads-rpc (re-confirmed by Eric at the invariant walk)
- stack-language-java21 (re-confirmed by Eric at the invariant walk)
- stack-build-maven (re-confirmed by Eric at the invariant walk)
- stack-integration-spring-boot4 (re-confirmed by Eric at the invariant walk)
- stack-messaging-kafka (re-confirmed by Eric at the invariant walk)
- stack-ci-gha (re-confirmed by Eric at the invariant walk)
- trace-thread-keyed-mono-gotcha (re-confirmed by Eric at the invariant walk)
- kafka-mesh-opt-in (re-confirmed by Eric at the invariant walk)
- event-script-over-code (re-confirmed by Eric at the invariant walk)
- event-api-local-routes-only (re-confirmed by Eric at the invariant walk)
- conv-add-capability (re-confirmed by Eric at the invariant walk)
- conv-serialization-gotchas (re-confirmed by Eric at the invariant walk)
- conv-declare-consulted-references (re-confirmed by Eric at the invariant walk)
- conv-proposals-not-in-adr-ledger (re-confirmed by Eric at the invariant walk)
- eric-release-rhythm (re-confirmed by Eric at the invariant walk)
- eric-code-changes-via-pr (re-confirmed by Eric at the invariant walk)
- vision-mercury-composable (re-confirmed by Eric at the invariant walk)
