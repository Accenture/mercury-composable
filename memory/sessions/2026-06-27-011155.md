# Session (2026-06-27T01:11:55.000Z)

**Agent:** Claude Code
**Lightweight:** follow-on to the Kafka-client-config-templates work — make the consumer group id configurable.

Eric: enterprise DevSecOps teams create topics, ACLs, and consumer/producer groups **administratively**, so
the library must use the **exact** group id given — a derived suffix is not generic enough. Decision: put the
group id in `kafka-flow-adapter.yaml` per topic→flow binding (explicit, visible to the operator, unambiguous):

```
consumer:
  - topic: 'mini-test-topic'
    flow: 'kafka-sink-flow'
    consumer_group: 'my-consumer-group'
```

### Change
- `KafkaFlowAdapter`: new optional `consumer_group` per binding, **used verbatim** as the Kafka
  `group.id`; when omitted defaults to `kafka-flow-adapter.<topic>` (dev convenience). Extracted
  `resolveGroupId(entry, topic)` (package-private, testable); `newConsumer(groupId)` no longer derives the
  group from the topic. Updated the class Javadoc example.
- `kafka-consumer.properties` template: removed `group.id` from the pinned list in the header comment and
  noted it is set per binding via `consumer_group` (the template intentionally has no `group.id`).
- Test `kafka-flow-adapter.yaml` (minimalist-kafka): added `consumer_group: 'mini-test-group'` to exercise
  the explicit path end-to-end; sync-over-async's adapter yaml stays on the default path.

### Validation
- minimalist-kafka: **21 tests** (added 3 `resolveGroupId` cases: explicit-verbatim, default-per-topic,
  blank→default), coverage met; the embedded-broker integration test runs with the explicit group.
- sync-over-async: **29 tests**, coverage met (default group path through the full round-trip).

## Memory References
- Updated: `kafka-client-config-templates` (group.id is no longer code-pinned; now per-binding `consumer_group`, used exactly)
