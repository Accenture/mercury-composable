# Session (2026-09-25T23:28:07.760Z)

**Agent:** Claude Code
**Title:** Release 4.12.19 prepared — build-file sweep, CHANGELOG from the tag range, readiness build (lock-step with Rust)

## Summary

Eric asked for v4.12.19 "for field deployment"; prepared on both engines in lock-step (the ports adopt the Java number).
Branch `release/4.12.19` from main `9819cbd3`. Tag range `v4.12.18..HEAD`: one content PR — #465 squash `40ce30a7`
(`graph.model.automation` accepts a comma-separated list of manifests, the later manifest wins; the rapid-prototyping deploy
recipe) — memory-only commits excluded. **Sweep BUILD FILES ONLY**, re-derived: 43 files / 98 occurrences 4.12.18 → 4.12.19
(`--include=pom.xml --include=build.gradle`, `target/` excluded), including the two Snyk placeholder poms
(`system/rest-spring-3`, `examples/rest-spring-3-example`) and the three templates' `mercuryVersion`; 0 build-file
occurrences of 4.12.18 remain and no prose carries the version. **CHANGELOG** "## Version 4.12.19, 9/25/2026" — Added 1 (#465),
Documentation 2 (#465), each with its upgrade action (additive; read if a graph id is listed in two manifests).
**Readiness** `mvn clean install` on the swept tree: 220 suites, 1520 tests, 0 failures, 0 errors, 3 skipped (surefire XML;
+3 = the CompileGraphTest cases of #465); artifacts installed at 4.12.19, `minigraph-playground-4.12.19.jar` built.
**Rust twin:** `release/4.12.19` from `d2adb38c` — 13 Cargo.toml / 24 occurrences, `cargo update --workspace` → 24 lock
entries, 12 publishable crates at 4.12.19, CHANGELOG entry (Increment 142, #332); fmt, clippy, audit, claims and links clean,
`cargo test --workspace` run after this build (the suites share ports). Not merged, not tagged, not published — each step
waits for Eric.

## Incidental sweep

memory-lint flagged two `[overdue]` facts once this log entered the window (sslu 21 > `archive_window` 20):
`conv-reentrantlock-not-synchronized` (the rule is still live while the toolchain targets Java 21 — it stands in the code
comments and the archive keeps it retrievable; Eric may pin it `core` if it should stay in working memory) and
`kafka-consumer-leave-group-on-shutdown` (shipped in 4.12.15, PRs #440/#441). Both archived with `archive-fact`. Not memory
references of this session.

## Memory References

- conv-changelog-from-tag-range (consulted — the entry comes from `git log v4.12.18..HEAD`, counts re-derived)
- conv-template-version-sweep (consulted — build files only, `--include=build.gradle`, counts re-derived)
- snyk-retired-manifest-placeholders (consulted — the two non-reactor placeholder poms are in the sweep)
- conv-ports-adopt-java-release-number (consulted — the Rust port takes 4.12.19, the Java number)
- eric-release-rhythm (consulted — PR-open, merge, tag and publish each wait for Eric)
- graph-manifest-list-later-wins (consulted — the release content)
