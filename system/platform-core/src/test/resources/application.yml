hello:
  world: great

this.is.a.test: 'message'

oh:
  hi.great:
    # the second element is intentionally to be missing. It will return null.
    - 1
    -
    - nice:
        way: 'great'
        another:
          - good
    - 456

system.path: '${PATH}'

instances.hello.world: 15

mime.types:
  ico: 'image/vnd.microsoft.icon'
  json: 'application/json'

custom.content.types:
  - 'application/vnd.my.org+json -> application/json'

modules.autostart:
  - 'demo.main.app'
