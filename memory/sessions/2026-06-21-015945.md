# Session (2026-06-21T01:59:45.000Z)

**Agent:** Claude Code
**User:** Decide the documentation-rewrite strategy, then build and get sign-off on the first exemplar

## What We Did

Turned the documentation effort from "pilot two chapters" into a **documentation architecture
decision**, then produced and got sign-off on the template.

- **Surveyed the current docs** (Explore over all 23 guides): Chapters 1–9 are solid and current
  (Platform Core + Event Script); the Knowledge Graph layer is the real gap — CHAPTER-11 is a glossy
  executive summary (no code, no execution API, no integration, no lifecycle); no narrative spine;
  no AI-discovery layer; APPENDIX-I is dead (superseded by CONFIGURATION-REFERENCE).
- **Approved the Design** (`docs-rewrite-architecture`): a structural, layered re-architecture
  (Parts I–VI ascending the layers) centering "Knowledge Graph as application"; dual human-narrative
  + AI-discovery design; **semantic slug URLs + mkdocs redirects** for old `/guides/CHAPTER-N/` links;
  build-alongside → per-layer sign-off → retire. Eric Law confirmed all 3 forks; first iteration =
  spine + Part IV.
- **Grounded Part IV in live code** (Explore over `system/minigraph-playground-engine`): corrected
  CHAPTER-11's model — event-driven async execution (`{flowInstanceId}@{nodeName}` correlation),
  `MultiLevelMap` state machine, skills return `next`/node-name/`.sink`, active loop detection, the
  real node model + 7 skills, layer integration (`graph.extension` → sub-graph or `flow://`), and an
  honest built-vs-aspirational split (governance lifecycle is roadmap, not code).
- **Wrote the first exemplar** — `docs/guides/knowledge-graph/index.md` ("Knowledge Graph as
  Application") establishing the template: YAML frontmatter, "At a glance" block, narrative voice,
  code-true substance, stable explicit anchors, "See also". Plus **`docs/llms.txt`** (the AI map,
  organized by layer) and a nav entry. Verified `mkdocs build --strict` exit 0; frontmatter stripped;
  `/guides/knowledge-graph/` + `/llms.txt` render.
- **Eric reviewed and accepted the template** ("assertive and honest. Agree with your draft.").

## Decisions Made

- Documentation rewrite = layered structural re-architecture; see `docs-rewrite-architecture`.
- The doc template (frontmatter schema + At-a-glance + narrative + stable anchors + honest
  maturity section + `llms.txt`) is the accepted standard for all new/rewritten chapters.

## Context for Next Session

- **Next: the deeper Part IV chapters**, all grounded in the engine survey: a *build-a-graph*
  walkthrough; the *7-skills reference* with worked examples from the shipped help tutorials
  (`system/minigraph-playground-engine/src/main/resources/help/`); a *layer-integration* how-to;
  and a *Playground & AI companion* guide. Then refine the spine opener (`docs/index.md`), then
  retire old CHAPTER-11 behind a redirect (needs the `mkdocs-redirects` plugin — new docs build dep).
- Build gate: `mkdocs build --strict` via a local venv (throwaway `/tmp/mkdocs-venv`, mkdocs 1.6.1).
- Branch `feature/docs-ai-human-rewrite`; keep committing clean per-milestone commits.

## Memory References

- Created: `docs-rewrite-architecture` (the Design)
- Referenced: `bp-docs-ai-human-rewrite` (progress updated — pattern accepted, Part IV opener landed),
  `docs-dir-layout`, `vision-mercury-composable` (the north star this serves),
  `functions-decoupled-routes`, `typed-io-map-or-pojo`
