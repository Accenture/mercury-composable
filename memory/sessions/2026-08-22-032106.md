# Session (2026-08-22T03:21:06.000Z)

**Agent:** Claude Code

## Summary

**v4.11.10 SHIPPED — both engines in lock-step (2026-08-21 local); GitHub release
publishing pending Eric.** Java: release
[PR #291](https://github.com/Accenture/mercury-composable/pull/291) squash `5cb65f04`
(tree verified identical to the gated `689adf5e`, squash title clean), CI green (Build &
Unit Tests + recheck), **34-pom sweep** 4.11.9→4.11.10 (33→34 last release: ai-contract-provider),
full reactor build as the gate (BUILD SUCCESS), tag `v4.11.10` dereference-verified on the
squash, branches deleted. Contents: the `system/ai-contract-provider` AI discovery app
(ADR-0015), the Event Script `f:setConfig` plugin, the two OTLP forwarder span-loss fixes,
and the rest.yaml flow-binding docs fix.

Rust: same-cut release PR #211 merge `b77f17e8` carrying `1beff96d` (tree verified), gate
63/317 + clippy 0 + fmt clean, tag on the merge, dereference-verified. **En route, Eric
caught one inconsistency: the Rust port lived under `examples/` — moved to
`system/ai-contract-provider` (mercury PR #210, pure relocation, 26 renames at 100%)
BEFORE the Rust release cut, completing the cross-repo `system/` convention.** The release
sequencing followed house style: content merges first, the release PR stays mechanical
(the Rust release branch was discarded and recreated on top of the move).

Also fixed on the Rust side (Eric's catch): the stale `memory/archive/INDEX.md`
placeholder ("*nothing archived yet*") removed — entries had long existed below it.

## Memory References

- referenced: thread-config-plugin + thread-ai-contract-provider (release contents),
  thread-otlp-export-retry (release contents), conv-squash-title-prefill-check (applied at
  the merge), eric-release-rhythm
- created: thread-release-4-11-10 (tier: working)
