# Session (2026-06-26T03:34:32.000Z)

**Agent:** Claude Code
**Lightweight:** doc follow-up to the 4.5.0 release / minimalist-kafka extraction.

Aligned the published docs with reality: `docs/guides/architecture.md` had claimed only the HTTP flow
adapter ships in-repo, with a Kafka flow adapter "planned for a future iteration." That iteration is
done — reworded to: HTTP adapter built into `event-script-engine` (always available); the minimalist
Kafka flow adapter (inbound) + notification function (outbound) now ship as the opt-in
`system/minimalist-kafka` library (enable via `yaml.kafka.flow.adapter`). Swept the rest of `docs/` —
only architecture.md carried the roadmap claim (methodology.md describes the adapter as a concept, which
stays). doc-canon checker green. Matches the already-updated `kafka-adapter-not-in-repo` memory fact.

Commit `d4fc0425` — docs(architecture): Kafka flow adapter now ships as the opt-in minimalist-kafka library

```
 docs/guides/architecture.md | 8 +++++---
 1 file changed, 5 insertions(+), 3 deletions(-)
```

## Memory References
(none — auto-stub; enrich per AGENTS.md "After Every Session", or leave as a lite log)
