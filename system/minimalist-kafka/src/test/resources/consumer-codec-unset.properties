#
# KafkaFlowAutoStartTest fixture: the opt-in key schema.registry.consumer.properties is deliberately ABSENT,
# so the flow adapter shares the producer's codec - the unchanged default. Also the config the test's
# producer codec is built from (the default template, all commented out, under the embedded registry).
#
schema.registry.cache.ttl=1h
