# Session (2026-08-01T23:09:46.000Z)

**Agent:** Claude Code

## What happened

**v4.11.1 SHIPPED (Java).** Eric completed manual testing of the second-level routing
feature and called the release; prepared and shipped same-session:
[PR #252](https://github.com/Accenture/mercury-composable/pull/252), squash `410e03bb`,
CI green (Build & Unit Tests 8m9s), tag `v4.11.1` pushed onto the VERIFIED squash
commit per the standing tag rules.

**Release mechanics:** 33-pom sweep 4.11.0 → 4.11.1 with the digit-lookahead guard (no
substring hazards existed this time); CHANGELOG cut to "Version 4.11.1, 8/1/2026" (4
Fixed + 7 Added); no version references outside the poms (README/docs are
version-free). Full reactor BUILD SUCCESS at the new version (5:28) BEFORE the PR.
One catch during prep: the broad `git add` staged the local `.interop-mercury` dev
symlink — removed by amend, and the symlink added to .gitignore so it cannot recur.

**Content consolidated since 4.11.0:** second-level routing + JSON serde symmetry
(#246) with the kafka-demo template (#247, #249); Redis GETDEL version-aware consume
(#248); task-level ttl override + model-metadata immutability + the deadline cleanup
round (#250); IDE/Sonar polish (#251).

**Sequencing precedent: first Java-ahead-of-Rust release since the 4.8.x line.** The
Rust port stays at 4.11.0 until the two lock-step halves land (GETDEL compat +
task-ttl/deadline arc; handoffs final in /tmp). Deliberate: second-level routing is
Java-only by design (no minimalist-kafka Rust port — the grammar is the future port's
contract), and Eric wanted the field-tested routing feature plus the two field fixes on
a released version now. Rust 4.11.1 ships when the lock-step session runs.

## Memory References

- referenced: thread-kafka-2nd-level-routing (release driver — field-tested, closed by
  this ship), thread-redis-getdel-compat (Java half released), thread-task-ttl-override
  (Java half released), release-4-8-1-shipped (sweep-gotcha lesson applied),
  thread-release-4-11-0 (tag rules followed)
- created: thread-release-4-11-1 (see continuity)
