# Session (2026-06-27T00:12:37.000Z)

**Agent:** Claude Code

## Summary

Eric shared a standing Mercury practice: **list a module's configurable parameters explicitly in the test
`application.properties`, even when the code supplies defaults**, so the test config documents the
configurable surface for the reader. Recorded as a preference (`pref-explicit-test-config`) and applied to
both modules touched this sprint:

- `system/minimalist-kafka` test config — added `kafka.flow.max.retries=3`, `kafka.flow.retry.backoff.ms=500`,
  `kafka.flow.dlq.suffix=.dlq` (were relying on code defaults).
- `extensions/sync-over-async` test config — added the `redis.*` startup params
  (`redis.host`/`redis.port`/`redis.password=${REDIS_PASSWORD:}`/`redis.ssl`/`redis.database`/`redis.timeout.ms`)
  and the `sync.*` engine tunables, alongside the existing `sync.over.async.enabled=true`.

All listed values match the code defaults, so behaviour is unchanged; the test still overrides `redis.port`
(and `kafka.bootstrap.servers`) via a system property at runtime — `ConfigReader` resolves system properties
before the file, so the file value documents the default while the embedded broker/Redis wins at runtime.

### Validation
- `mvn test -f system/minimalist-kafka` → 16 tests pass, coverage met.
- `mvn test -f extensions/sync-over-async` → 29 tests pass, coverage met (the `${REDIS_PASSWORD:}` substitution
  resolves to empty → no-auth, and the embedded-Redis MVP round-trip still passes).
- `memory-lint` run after the continuity edits.

## Memory References
- Created: `pref-explicit-test-config` (born `tier: working`)
- Referenced: `soa-config-driven-init`, `kafka-flow-failure-dlq`
