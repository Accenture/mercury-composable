# Session (2026-08-11T17:55:33.415Z)

**Agent:** Codex

## Summary

Added JSON-Path Help under `ui-loop-engineer` run `ui-jsonpath-help-20260811-08` by extending the
existing shared Help shell with a config-selected content profile. JSON-Path now exposes the same
top-right `?` button, first-load `Ctrl + backtick` hint, global shortcut, resizable/maximizable Help
split, close behavior, and persisted open/topic state as Minigraph. Its content is deliberately
isolated to one starter Overview section, and its bare `help` command is handled locally without
capturing MiniGraph-only topics such as `help create`. Minigraph's existing content and defaults remain
unchanged.

README and technical documentation now describe the shared shell, profile isolation, JSON-Path quick
start, command behavior, and the actual localStorage/sessionStorage ownership. The Vite production
bundle was released into the Java-served public directory.

## Verification and review

- Focused profile/content/resolver/HelpBrowser/event-bus tests passed before the full regression run.
- Full webapp Vitest passed after review fixes: 29 files, 238/238 tests.
- `tsc --noEmit` passed.
- `npm run release` passed; Vite transformed 644 modules and emitted only the existing >500 kB
  main-chunk warning before deploying to Java public resources.
- `dist/` and the Java `public/` directory have exact parity; production output contains the
  JSON-Path Overview/profile/storage markers, and `git diff --check` passed.
- Fresh correctness review found no actionable issue. Fresh delivery review identified missing
  Playground-level acceptance coverage and two shortcut-documentation errors; a focused disconnected
  JSON-Path Playground test and documentation corrections closed both findings, and the reviewer
  confirmed no remaining actionable issue.
- Browser automation is unavailable in this workspace, so a pixel-level visual check remains manual.

## Memory References

- created: webapp-playground-help-profiles
