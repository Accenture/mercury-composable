# Session (2026-08-01T00:15:28.000Z)

**Agent:** Claude Code

## What happened

**kafka-demo feedback round from Eric's manual regression — code-complete on branch
`fix/kafka-demo-feedback` (commit `3318a24d`, NOT pushed), verified live, awaiting
Eric's PR gate.** Eric's manual test of the second-level routing demo produced two
feedback items (file on his Desktop) + a mid-round hint; the round fixed four things:

1. **DLQ topics pre-created** — his run hit `UNKNOWN_TOPIC_OR_PARTITION` for
   demo.orders.dlq (a dead-letter write to an uncreated topic; my smoke drive never
   dead-lettered so never saw it). create-topics.js now makes demo.inbound.dlq +
   demo.orders.dlq (DLQ topics must be pre-provisioned), turning the failure path into
   a first-class demo scenario instead of a warning.
2. **`refund <json>` semantics fixed** — his suggested example
   `refund {"order_id":"order-123"}` exposed a UX trap: custom json REPLACED the whole
   canned payload, silently missing the body rule (falls to default). Now the json is
   the refund DETAILS and the script adds the `{"event":{"kind":"refund"}}` envelope
   the rule matches on — his exact command routes to the task (verified live:
   RefundProcessor log with `{event={kind=refund}, order_id=order-123}`).
3. **`order <plain text>` = the canonical failure-path trigger** (Eric's hint: he used
   "order hello" to trigger the DLQ) — previously plain text after order was silently
   DISCARDED in favor of the canned payload. Now the rest of the line is the payload
   VERBATIM: non-JSON keeps raw byte[] under serializer 'json', the Map-typed order
   processor can't digest it → 3 retries → dead-letter to demo.orders.dlq. The
   publisher announces the expected outcome at publish time. Verified live: the exact
   sequence in the app log, clean delivery to the pre-created topic, no data loss.
   **Eric's assessment: this DLQ test is exactly right — it passes a byte array into a
   composable function that expects a Map, proving the function throws back to the
   kafka flow adapter** (his original parse-failure ruling — "no special handling in
   the adapter loop" — demonstrated live by the demo).
4. **Instructions reformatted to Eric's numbered command/example style** in the script
   banner and README (rule labels kept accurate: exact header / wildcard header / body
   rule / default — his draft had labeled the refund rule "exact"), plus the failure
   path documented as scenario 5 and a new README `#failure-path` section.
5. **New `listen-dlq.js` (program-4, Eric's request)** — subscribes to both DLQ topics
   and prints intercepted dead letters: cid, `dlq.origin.topic`, `dlq.error`, and the
   preserved body. Verified live: `order hello` → the listener printed the record with
   origin demo.orders, error "IllegalStateException: flow 'demo-order-flow' returned
   status 500", body "hello" verbatim. Instruction 5 and the README failure-path
   section now point at it. (Commit amended to `82b95154`.)

**Also (Eric, real-time from his team meeting): the composable content-based
partitioning pattern (tiny selector function → explicit `partition` header) was
APPROVED for field use** — the docs landed earlier (`8ed23f34`) now serve as the
field's reference for it.

## Memory References

- referenced: thread-kafka-2nd-level-routing (the demo being polished; field adoption
  of the partitioning answer recorded), conv-serialization-gotchas (best-effort
  serializer contract driving the failure-path design)
