# Session (2026-09-18T23:26:41.120Z)

**Agent:** Claude Code

Lightweight: Mode B upgrade 4.41.1 → 4.41.2 (PATCH — from this team's two 2026-09-18 reports: (1) the seeded
continuity.md header's example footer, shown inside an inline code span, parsed as a live `kebab-id` fact in
every enabled repo — active, unpinned, counted toward `[continuity-bloat]`, raising nothing itself; a
backtick-wrapped footer is now documentation to memory-lint, the `[undeclared-reference]` mapper and
archive-fact; (2) the ADR ledger records decisions only — an entry is written on acceptance, proposals live in
the repo's register, e.g. `docs/arch-decisions/RFC.md` with `RFC-NNNN` ids, never as a `Proposed` ADR).
Reconcile re-copied DECAY.md, .agent/schema.md and the memory-lint and archive-fact built-ins. Semantic step:
protocol — re-copied (target protocol was byte-identical to the 4.41.1 template). Rung step 3: header example already a placeholder. Rung step 4: ledger holds 0 Proposed ADR(s) — a one-time human cleanup (accept the shipped ones in place, move the rest to a register as withdrawn). Adapters re-synced; stamped 4.41.2; every
re-copied file byte-identical to its MANIFEST source. Ran in a throwaway worktree of origin/main (`--forge
github`). memory-lint: 0 errors, 3 warning(s); live facts 58 → 58. Upstream release:
https://github.com/Accenture/mercury-go/releases/tag/v4.41.2 .

## Memory References

(none)
