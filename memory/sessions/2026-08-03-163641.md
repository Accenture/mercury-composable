# Session (2026-08-03T16:36:41.000Z)

**Agent:** Claude Code

## What happened

**Fixed the two piped-mode quirks in the kafka-demo node publish helpers** (the residual
observation from the v4.11.2 regression drive — Eric asked for the fix). Branch
`fix/kafka-demo-node-pipe-mode`, commit `7c88d695`, two files, awaiting the PR gate.

- **publish-inbound.js — real bug, fixed:** the async `line` handler raced the `close`
  handler's `producer.disconnect()` on piped EOF, dropping the message. Now chains sends
  on an `inflight` promise awaited at close — the exact pattern publish-orders.js already
  used. Works interactively AND piped.
- **Both publishers:** the readline prompt is now shown only when stdin is a TTY. Piped
  input previously accumulated `> ` prompt characters that glued onto the first
  confirmation line.

**Diagnosis correction (supersedes the observation recorded in session
2026-08-03-155225):** publish-orders.js never had an off-by-one — cid/traceId are locals
inside `publishOne`, so each confirmation always paired correctly with the wire. The
"shifted pairing" I reported during the v4.11.2 regression was my own capture artifact:
prompt characters glued onto the FIRST confirmation line and my `grep -v "^>"` filter
swallowed it, shifting the apparent pairing by one. **Durable lesson: when a console
tool's output looks wrong under a filter pipeline, verify against the wire/receiver
ground truth before diagnosing the tool** — the Java-side receipts were what exposed
this.

**Verification (live, against the 4.3.1 kafka-standalone broker + kafka-demo app):**
a piped inbound message publishes without the stdin-hold workaround and arrives with the
identical cid/traceId; the four piped routing commands (exact, wildcard, body-rule,
default) print clean confirmations pairing one-to-one with the Java-side receipts.
Interactive behavior unchanged (the TTY branch keeps the original prompt flow).

## Memory References

- referenced: thread-release-4-11-2 (its residual observation is what this fixes —
  substance updated in continuity with the corrected diagnosis),
  thread-kafka-2nd-level-routing (the demo these helpers drive),
  conv-helpers-docker-less (kafka-standalone as the live test broker)
- Superseded: (none — the misdiagnosis was inside thread-release-4-11-2's text, corrected
  in place; past session log 2026-08-03-155225 left untouched per append-only rule)
