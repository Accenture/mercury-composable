# Session (2026-09-10T17:36:49.000Z)

**Agent:** Claude Code

## What happened

**v4.12.6 released.** Eric merged the release PR #351 (squash `0bcfadb2`,
title clean), tagged and published the GitHub release (17:30Z; Rust twin
17:31Z), and published the seven crates. Verified: GitHub releases live on
both repos; all seven mercury-* crates at 4.12.6 on the crates.io API
(publish order: the three dependency-free macro crates, then platform-core,
event-script, knowledge-graph, minigraph-state-redis). Continuity
`latest_release` updated. Release CI was green on both PRs; the local
full-reactor verification was green on the serialized rerun (the first run
red-flagged only under self-inflicted contention — both engines' full suites
running simultaneously, the documented laptop pattern).

Also landed inside the release content: the parallel session's agent-memory
attribution locators (Java PR #350 / Rust PR #253 — agent-memory promoted
into the Mercury family at github.com/Accenture/mercury-go; docs-only).

Next arc queued (Eric's backlog ruling): the simple-plugin allowlist gate
hardening — [[ot-simple-plugin-gate-body-scan]].

## Memory References

- eric-release-rhythm (consulted — merge/tag/publish were each Eric's
  explicit steps; the EMU policy also blocks API PR creation, so PR-open is
  his step by constraint as well as preference)
- conv-squash-title-prefill-check (consulted — landed squash title verified
  clean)
- ot-simple-plugin-gate-body-scan (consulted — confirmed queued as the
  post-release arc)
