# Session (2026-06-27T23:35:44.000Z)

**Agent:** Claude Code
**Lightweight:** follow-up to [[2026-06-27-182336]]. Eric's manual kafka-demo run showed `demo.processor`
with no `parent_span_id` — diagnosed (correctly) as: the publisher sent no `traceparent`, so the flow
started a fresh trace and the first task rooted it (`TaskExecutor:170` seeds the first task's parent from
the triggering function's span; `KafkaFlowConsumer:169` adopts an inbound `traceparent`). Made trace
continuity visible: `publish-inbound.js` now sends a W3C `traceparent`; `listen-outbound.js` logs the
received trace-id. Verified live end-to-end (sent traceId == Java telemetry == listener traceId; new span
per hop; `demo.processor` now parents to the publisher span). On PR #124.

Commit `a2bae4c1` — feat(kafka-demo): demonstrate trace continuity across the Kafka hops

```
 examples/kafka-demo/README.md               | 19 +++++++++++--------
 examples/kafka-demo/node/listen-outbound.js |  9 +++++++--
 examples/kafka-demo/node/publish-inbound.js | 14 +++++++++++---
 3 files changed, 29 insertions(+), 13 deletions(-)
```

## Memory References
(none — auto-stub; enrich per AGENTS.md "After Every Session", or leave as a lite log)
