# Session (2026-06-28T03:59:54.000Z)

**Agent:** Claude Code

On-demand **knowledge harvest** (`harvest-knowledge` skill, first run since the docs grew — the v4.23.0
upgrade had offered a re-harvest but not run it). Re-scanned the human-authored doc trees and cross-checked
durable facts against existing memory. **Outcome: no new facts folded — memory already reflects the docs
(additive/no-duplicate run).**

## Swept
- `docs/guides/` (all subtrees: event-driven, event-script, knowledge-graph, rest-automation), the ADR
  ledger (`docs/arch-decisions/ADR.md`), repo-root knowledge docs, and `draft-design-specs/` (the
  Redis-assisted Kafka request/reply design spec — note: **untracked**, local-only).
- Prioritized what changed since enable (2026-06-20): docs reorg, AI developer guides, the 7 ADRs,
  W3C/OTLP telemetry (#122), Kafka flow adapter + sync-over-async guides, code-conventions, worked examples.

## Coverage verified (representative cross-checks — all already in memory)
- Architecture/decisions → ADR-0001…0007 facts (`functions-decoupled-routes`, `virtual-threads-rpc`,
  `typed-io-map-or-pojo`, `docs-content-canon`, `kafka-mesh-opt-in`, `event-script-over-code`).
- W3C/OpenTelemetry tracing + OTLP forwarder (#122) → `otel-w3c-tracing` (detailed).
- Sync-over-async **reliability invariant** from the design spec §11 (Redis SETEX = source of truth,
  Pub/Sub = wake-up only, final Redis read before timeout, idempotent completion) → already captured in
  `thread-redis-kafka-rpc` (continuity.md:516-517).
- Code conventions / worked examples / minimalist-examples / Instant serialization / Mono-trace gotcha →
  `code-conventions-home`, `helpers-and-worked-examples`, `minimalist-examples`, `instant-serialization`,
  `trace-thread-keyed-mono-gotcha`.

## Mapped-not-mirrored (deliberate, per the skill)
- Layer-3 **Active Knowledge Graph** depth (node execution contract `{node}.status`/`{node}.error` →
  routing, `graph.executor`, built-in skills) is represented at the **vocabulary/layer altitude** via
  `docs-content-canon`; the canonical `docs/guides/knowledge-graph/` is the source of truth. Not
  deep-distilled into finer facts (it is stable and pre-dates this harvest window). A focused KG harvest
  can be run on request.

## Memory References
- Created / changed: **none** — coverage verified; no contradictions or supersessions found.
