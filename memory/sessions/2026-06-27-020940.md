# Session (2026-06-27T02:09:40.000Z)

**Agent:** Claude Code
**Lightweight:** doc-only — surface the optional `partition` field in the test `kafka-flow-adapter.yaml` files.

Per `pref-explicit-test-config`, added commented `partition:` examples (literal `0` and the per-pod
`${POD_PARTITION}` env form) to both adapter configs so the opt-in partition-pinning knob is visible to the
reader: `system/minimalist-kafka/src/test/resources/kafka-flow-adapter.yaml` and
`extensions/sync-over-async/src/test/resources/kafka-flow-adapter.yaml` (the latter also lists the optional
`group:` field in its header comment). Comment-only; minimalist-kafka 27 + sync-over-async 29 still green.

## Memory References
(none — test-config documentation; feature fact `kafka-partition-pinning` unchanged)
