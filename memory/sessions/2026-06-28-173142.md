# Session (2026-06-28T17:31:42.000Z)

**Agent:** Claude Code

## Summary

**Upgraded agent-memory v4.23.1 → v4.25.0** (Mode B, single sitting, by Claude Code from the tool
checkout). Three rungs applied in place; working tree left **uncommitted** for the mercury team (it also
still carries the prior, uncommitted v4.23.1 work — the team commits the whole 4.23.1→4.25.0 set together).

**Rungs:**
- **4.23.2** — `AGENTS.md` gains the "Long session? Keep state externalized so compaction is safe" block
  (compact at a seam, after a memory write; objective signal is context-window utilization, not time/"fog").
- **4.24.0** — decay-policy retuned + a new `memory-lint` advisory. `continuity_max_facts: 30` added (the
  count-based primary lean signal); `continuity_max_lines` 300→600; `verify_invariants_every` 20→40 (all
  stock here — no custom values clobbered). `memory-lint` now emits `[review-overdue]` + `[continuity-bloat]`.
- **4.25.0** — `archive-fact`, a **6th built-in**: a deterministic, safe archive-move (reads continuity into
  memory, writes once — the truncate-before-read trap is structurally impossible). Python+Node at parity.

**Files (all scoped to this repo):** re-synced `AGENTS.md` (2 additive edits), `REVIEW.md` (overwrite —
generic), `.agent/schema.md` (windows list); merged `memory/decay-policy.md` additively; copied the
`memory-lint` skill (4 scripts + SKILL.md) and the new `archive-fact` skill; re-synced adapters (6 skills →
36, gitignored); stamped `.agent/version.md` → 4.25.0 (enabled_with 4.14.1, mode A preserved).

**Live validation (notable):** running `memory-lint` post-upgrade, the new v4.24.0 advisories **fired on
this repo for the first time anywhere** — `[review-overdue] 21 sessions since last review ≥ 10` and
`[continuity-bloat] 41 facts > 30` — plus 11 per-fact `[overdue]` advisories. **0 errors.** This is exactly
the overdue state v4.24.0 was designed to surface (this repo's drift was the original motivating evidence).

**A review is now due.** Recommend the mercury team run the `REVIEW.md` ritual — it can use the new
`archive-fact` helper to perform the archive-moves safely. **Not run here:** the faded facts (r2dbc, kafka
adapter, docs canon, …) are mercury's domain content; deciding what's truly faded needs the team's context.
agent-memory flags; it never picks (`never-pick-a-winner`).

## Memory References
- Created: agent-memory-upgrade-v4250
