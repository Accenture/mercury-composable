# Files waived from the agent-memory pre-commit secret guard (config files only - the
# guard never waives memory/). One path per line, repo-relative. Waive only files whose
# flagged values are deliberate non-secrets, and say why.

# Test fixture: llm.api.key=emulated-test-key is a dummy value so the emulated-LLM
# round-trip proceeds; the stub provider never reads it. Real keys stay in the
# environment (${GEMINI_API_KEY:} in the main profile properties).
examples/sync-over-async-demo/src/test/resources/application.properties
