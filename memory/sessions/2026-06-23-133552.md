# Session (2026-06-23T13:35:52.000Z)

**Agent:** Claude Code
**User:** Peer-review feedback on PR #119 — correct the over-stated "List of PoJo is not supported"
claim; also delete the design-notes notepad as a drift source.

## What We Did

**List-of-PoJo accuracy fix.** A colleague flagged that "List of PoJo is not supported" is over-stated.
Verified against code (`@PreLoad` javadoc: "Event Script's Input/Output Data Mapping does not support
list of PoJo … [inputPojoClass] converts the list of Maps back to a list of PoJo"). Corrected the
**canonical statement**: a List of PoJo is **not a data-mapping contract** in Event Script (Layer 2) or
the Knowledge Graph (Layer 3); **Layer 1 (Platform Core) accepts a `List<PoJo>` input via
`@PreLoad(inputPojoClass=…)`** to ingest an incoming JSON-list payload from an external source (function
extracts the key-values). Outgoing list payloads need no special handling (Layer-2 `AsyncHttpClient`,
Layer-3 API-fetcher do their own data mapping). Applied to:
- **ADR-0003** — Abstract + Consequences reworded **in place** (status stays `Accepted` — a clarification,
  the decision is unchanged, **not** a supersession).
- **Core invariant `typed-io-map-or-pojo`** (`continuity.md`) and **`memory/instructions.md`** — scoped.
- **`api-overview.md`** — small enhancement (names the incoming-external-JSON-list use case).
- (`annotations-reference.md` was already accurate; untouched.)

**Deleted `docs/notes/design-notes.md`** (Eric — it was the author's notepad, a design-drift source now
that the ADR ledger holds the durable rationale). Checked first: every section is already covered by the
canonical docs/ADRs (engine history → ADR-0002, serialization → ADR-0003, "halves code" → ADR-0001) and
it carried the same over-stated PoJo line — nothing unique lost. Cleaned all 5 references (nav, index.md,
llms.txt, the ADR preamble, and the `(per the design notes)` aside in ADR-0001 rationale).

Verified: canon + 3 grammar checks pass; `mkdocs --strict` 0 warnings; no residual `design-notes` refs.
Committed + pushed → updates **PR #119** for re-review.

## Context for Next Session

- PR #119 updated with the correction + notepad removal; awaiting peer re-review.
- Still open: AI-context-discovery on-ramp (`thread-next-ai-context`); optional Layer-1 foldering (`thread-layer-reorg`).

## Memory References

- Corrected (substance): `typed-io-map-or-pojo` (core invariant — scoped, still holds; ADR-0003 reworded in place)
- Referenced: `adr-pattern-adopted`, `docs-content-canon`, `kafka-adapter-not-in-repo`, `vision-mercury-composable`
- No supersession (ADR-0003 stays Accepted); no new facts.
